- name: Force Commit 
  run: |
    git config --global user.name "GitHub Actions"
    git config --global user.email "actions@github.com"
    echo "# Autogenerated line" >> force_change.txt
    git add .
    git commit -m "Force commit to test workflow" --allow-empty
    git push
